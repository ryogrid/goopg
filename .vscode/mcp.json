{
  "servers": {
    "serena": {
      "type": "stdio",
      "command": "/home/ryo/.local/bin/uv",
      "args": [
        "run",
        "--directory",
        "${workspaceFolder}/serena",
        "serena",
        "start-mcp-server",
        "--context=claude-code",
        "--project",
        "${workspaceFolder}"
      ]
    }
  }
}
