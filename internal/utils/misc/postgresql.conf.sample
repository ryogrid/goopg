# -----------------------------
# PostgreSQL configuration file (goopg edition)
# -----------------------------
#
# This file consists of "name = value" lines (the "=" is optional);
# whitespace may be used, and comments are introduced with "#" anywhere
# on a line. The complete list of parameter names and allowed values can
# be found in goopg's `internal/config/defaults.go`, which is the
# authoritative source for every GUC the server knows about.
#
# All settings are commented out by default.  Uncommenting an entry sets
# it to the value shown, which equals the server's compiled-in default.
#
# This template is hand-maintained against `BuildDefaultRegistry()` in
# `internal/config/defaults.go`.  The unit test
# `TestSampleConfigCoversRegistry` enforces that the two stay in lockstep:
# adding or removing a GUC in the registry MUST be accompanied by adding
# or removing the matching line in this file (see the "GUC sample-file
# discipline" section in `.ralph/AGENT.md`).
#
# Memory units: B / kB / MB / GB / TB.
# Time   units: us / ms / s / min / h / d.


#------------------------------------------------------------------------------
# CONNECTIONS AND AUTHENTICATION
#------------------------------------------------------------------------------

# - Connection Settings -

#listen_addresses = 'localhost'         # comma-separated list of addresses;
                                        # defaults to 'localhost'; use '*' for all
                                        # (change requires restart)
#port = 5432                            # 1-65535
                                        # (change requires restart)
#max_connections = 100                  # 1-262143
                                        # (change requires restart)

# - Authentication -

#scram_iterations = 4096                # 1-1073741824
#password_encryption = 'scram-sha-256'  # md5 | scram-sha-256; accepted,
                                        # verifier form follows pg_hba


#------------------------------------------------------------------------------
# RESOURCE USAGE (except WAL)
#------------------------------------------------------------------------------

# - Memory -

#shared_buffers = 128MB                 # min 128kB
                                        # (change requires restart)
#dynamic_shared_memory_type = posix     # posix | sysv | mmap; accepted,
                                        # goopg allocates no DSM segments
#transaction_buffers = 0                # CLOG SLRU page-cache buffers;
                                        # 0 = auto from shared_buffers
                                        # (change requires restart)
#work_mem = 512MB                       # min 64kB
#maintenance_work_mem = 64MB            # min 64kB; accepted, VACUUM/CREATE
                                        # INDEX do not yet size off this GUC

# - Background Writer -

#bgwriter_delay = 200                   # 10-10000 ms between rounds
                                        # (change requires restart)
#bgwriter_lru_maxpages = 100            # 0-1000 max buffers written/round;
                                        # 0 disables background writing
                                        # (change requires restart)
#bgwriter_flush_after = 64              # 0-256 pages; 0 disables writeback
                                        # (SIGHUP to apply)

# - Asynchronous Behavior -

#max_worker_processes = 8               # 0-262143
                                        # (change requires restart)
#io_method = worker                     # sync | worker | io_uring
                                        # (change requires restart)
#io_workers = 3                         # 1-1024 worker goroutines for io_method=worker
                                        # (change requires restart)
#io_max_concurrency = 0                 # 0-1024; 0 = let the method decide
                                        # (change requires restart)
#backend_flush_after = 0                # 0-256 pages; 0 disables writeback (default)
#max_parallel_workers_per_gather = 4    # 0-1024; taken into account by SET-only; goopg default 4 (PG ships 2)
                                        # planner currently ignores it
#max_parallel_workers = 8               # 0-1024; cluster-wide cap on active
                                        # parallel workers
#min_parallel_table_scan_size = 8MB     # 0-715827882 blocks
#min_parallel_index_scan_size = 512kB   # 0-715827882 blocks


#------------------------------------------------------------------------------
# WRITE-AHEAD LOG
#------------------------------------------------------------------------------

# - Settings -

#wal_level = replica                    # minimal | replica | logical
                                        # (change requires restart)
#wal_buffers = 16777216                 # 0-1073741824 bytes; 0 disables
                                        # the in-memory WAL buffer
                                        # (change requires restart)
#wal_writer_delay = 200                 # 1-10000 ms between WAL-writer rounds
                                        # (change requires restart)
#wal_writer_flush_after = 1048576       # 0-1073741824 bytes; 0 fsyncs every round
                                        # (change requires restart)
#commit_delay = 0                       # 0-100000 microseconds
#commit_siblings = 5                    # 0-1000 range; min concurrent flushers
                                        # before commit_delay applies
#wal_init_zero = on                     # zero-fill new WAL segments
                                        # (change requires restart)
#wal_sync_method = fdatasync            # fsync, fdatasync, open_sync, open_datasync
                                        # (change requires reload)
#wal_sender_memory_buffer = 16777216    # 0-1073741824 bytes in-memory WAL ring
                                        # (change requires restart)
#full_page_writes = on                  # log full pages after first mod since
                                        # last checkpoint (SIGHUP to apply)
#fsync = on                             # off skips all durability syncs;
                                        # only ever off for throwaway test servers
#synchronous_commit = on                # off | local | remote_write |
                                        # on (= remote_flush) | remote_apply

# - Checkpoints -

#checkpoint_timeout = 300               # 30-86400 s between automatic checkpoints
                                        # (SIGHUP to apply)
#checkpoint_completion_target = 0.9     # 0.0-1.0 fraction of checkpoint_timeout
                                        # (SIGHUP to apply)
#max_wal_size = 1024                    # 2-2147483647 MB
                                        # (SIGHUP to apply)
#min_wal_size = 80                      # 2-2147483647 MB
                                        # (SIGHUP to apply)
#checkpoint_flush_after = 32            # 0-256 pages; 0 disables writeback
                                        # (SIGHUP to apply)

# - Archive Recovery -

#recovery_target_timeline = 'latest'    # 'latest' | 'current' | timeline OID
                                        # (change requires restart)


#------------------------------------------------------------------------------
# REPLICATION
#------------------------------------------------------------------------------

# - Sending Servers -

#max_wal_senders = 10                   # 0-262143; concurrent walsender procs
                                        # (change requires restart)
#max_replication_slots = 10             # 0-262143
                                        # (change requires restart)
#wal_sender_timeout = 60s               # 0-1073741824 ms; 0 disables timeout
                                        # (SIGHUP to apply)
#max_slot_wal_keep_size = -1            # -1 to 2147483647 MB; -1 = no limit
                                        # (SIGHUP to apply)
#synchronous_standby_names = ''         # standby selection grammar;
                                        # '' = async (no wait)
                                        # (SIGHUP to apply)
#summarize_wal = off                    # toggles the WAL-summarizer worker
                                        # (SIGHUP to apply)

# - Standby Servers -

#primary_conninfo = ''                  # libpq-style connection string
                                        # (SIGHUP to apply)
#primary_slot_name = ''                 # replication slot to use on primary
                                        # (SIGHUP to apply)
#restore_command = ''                    # shell command to fetch WAL from archive
                                        # (change requires restart)
#wal_receiver_status_interval = 10      # 0-2147483 s; 0 disables status updates
                                        # (SIGHUP to apply)
#hot_standby = on                       # allow read-only queries during recovery
                                        # (change requires restart)

# - Subscribers -

#max_logical_replication_workers = 4    # 0-262143; accepted, goopg runs no
                                        # logical-replication apply workers
                                        # (change requires restart)


#------------------------------------------------------------------------------
# QUERY TUNING
#------------------------------------------------------------------------------

# - Planner Method Configuration -

#enable_seqscan = on
#enable_indexscan = on
#enable_indexonlyscan = on
#enable_bitmapscan = on
#enable_hashjoin = on
#enable_mergejoin = on
#enable_nestloop = on
#enable_sort = on
#enable_hashagg = on
#enable_material = on
#enable_partition_pruning = on
#enable_partitionwise_join = on
#enable_partitionwise_aggregate = on
#enable_parallel_hash = on
#enable_parallel_append = on
#enable_gathermerge = on
#enable_incremental_sort = on
#enable_async_append = on
#enable_memoize = on
#enable_presorted_aggregate = on
#enable_distinct_reordering = on
#enable_group_by_reordering = on
#enable_self_join_elimination = on
#enable_tidscan = on
#enable_nestloop_index = on             # goopg-specific: index nested-loop join
#enable_opportunistic_prune = on        # goopg-specific: inline HOT page-prune
#parallel_leader_participation = on

# - Planner Cost Constants -

#seq_page_cost = 1.0                    # 0-1e9
#random_page_cost = 4.0                 # 0-1e9
#cpu_tuple_cost = 0.01                  # 0-1e9
#cpu_index_tuple_cost = 0.005          # 0-1e9
#cpu_operator_cost = 0.0025            # 0-1e9
#effective_cache_size = 4GB             # 1-1099511627776 kB
#jit_above_cost = 100000                # perform JIT compilation when query cost is above this; -1 disables
#jit_optimize_above_cost = 500000       # optimize JIT-compiled functions when query cost is above this; -1 disables
#jit_inline_above_cost = 500000         # perform JIT inlining when query cost is above this; -1 disables
#parallel_setup_cost = 1000             # cost of starting up worker processes for parallel query
#parallel_tuple_cost = 0.1             # cost of passing each tuple from worker to master backend
#from_collapse_limit = 8               # 1-2147483647
#join_collapse_limit = 8               # 1-2147483647; 1 disables collapsing of explicit JOIN clauses
#hash_mem_multiplier = 2.0             # 1-1000

# - Genetic Query Optimizer -
# (goopg's planner never runs GEQO; these are accepted no-op compatibility stubs)

#geqo = on
#geqo_threshold = 12                    # 2-2147483647
#geqo_effort = 5                        # range 1-10
#geqo_pool_size = 0                     # selects default based on effort
#geqo_generations = 0                   # selects default based on effort
#geqo_selection_bias = 2                # range 1.5-2.0
#geqo_seed = 0                          # range 0.0-1.0

# - Other Planner Options -

#constraint_exclusion = partition       # on, off, or partition
#cursor_tuple_fraction = 0.1            # range 0.0-1.0
#recursive_worktable_factor = 10        # range 0.001-1000000.0


#------------------------------------------------------------------------------
# REPORTING AND LOGGING
#------------------------------------------------------------------------------

# - When To Log -

#log_statement = 'none'                 # none | ddl | mod | all
#log_min_duration_statement = -1        # -1 disables; otherwise ms threshold

# - What To Log -

#log_line_prefix = '%m [%p] '           # supported escapes: %m %p %u %d %a %x %%
#log_timezone = 'GMT'                   # accepted; goopg log timestamps are
                                        # not zone-formatted
#log_file_mode = 0600                   # 0000-0777 octal; accepted, goopg
                                        # does not write log files itself

# - Process Title / I/O Timing -

#track_activities = on                  # collect info in pg_stat_activity
#track_counts = on                      # collect per-relation row statistics
#track_functions = none                 # none | pl | all
#track_io_timing = off                  # collect per-I/O wait timing
#stats_fetch_consistency = cache        # none | cache | snapshot
#jit = off                              # allow JIT compilation
#jit_debugging_support = off            # register JIT-compiled functions with debugger
#jit_dump_bitcode = off                 # write out LLVM bitcode to facilitate JIT debugging
#jit_expressions = on                   # allow JIT compilation of expressions
#jit_profiling_support = off            # register JIT-compiled functions with perf profiler
#jit_tuple_deforming = on               # allow JIT compilation of tuple deforming
#jit_provider = 'llvmjit'               # JIT provider to use
#compute_query_id = off                 # compute query identifiers
#plan_cache_mode = auto                 # plan cache mode
#check_function_bodies = on             # validate function bodies during creation
#extra_float_digits = 1                 # float output precision adjustment
#bytea_output = hex                     # bytea output format (hex or escape)
#max_parallel_maintenance_workers = 2   # max parallel workers for maintenance


#------------------------------------------------------------------------------
# AUTOVACUUM
#------------------------------------------------------------------------------

#vacuum_cost_delay = 0                  # 0-100 ms; 0 disables cost-based delay
#vacuum_cost_limit = 200                # 1-10000
#vacuum_cost_page_hit = 1               # 0-10000
#vacuum_cost_page_miss = 2              # 0-10000
#vacuum_cost_page_dirty = 20            # 0-10000
#vacuum_truncate = on                   # accepted; goopg's VACUUM never
                                        # truncates trailing empty pages yet
#vacuum_freeze_table_age = 150000000    # 0-2000000000 xids
#vacuum_failsafe_age = 1600000000       # 0-2100000000 xids
#vacuum_multixact_freeze_table_age = 150000000  # 0-2000000000 multixacts
#vacuum_multixact_freeze_max_age = 400000000    # 1000000-5000000000 multixacts
#vacuum_multixact_failsafe_age = 1500000000     # 0-2100000000 multixacts
#autovacuum_worker_slots = 16           # 1-262143; accepted, goopg has no
                                        # autovacuum worker pool
                                        # (change requires restart)
#autovacuum_freeze_max_age = 200000000  # 100000-2000000000 xids
                                        # (change requires restart)

#------------------------------------------------------------------------------
# AUTOVACUUM PARAMETERS (parity bundle: consumed by the launcher where noted)
#------------------------------------------------------------------------------

#autovacuum = on                        # enables the autovacuum launcher
#autovacuum_naptime = 60                # 1-2147483 s between launcher sweeps
#autovacuum_vacuum_threshold = 50       # min dead tuples before vacuum
#autovacuum_vacuum_scale_factor = 0.2   # fraction of table size added to threshold
#autovacuum_vacuum_insert_threshold = 1000      # min inserted rows before vacuum
#autovacuum_vacuum_insert_scale_factor = 0.2    # fraction of table size added
#autovacuum_analyze_threshold = 50      # min row modifications before analyze
#autovacuum_analyze_scale_factor = 0.1  # fraction of table size added to threshold
#autovacuum_vacuum_max_threshold = 200000000    # cap on the effective vacuum threshold
#autovacuum_vacuum_cost_delay = 2       # -1-100 ms (overrides vacuum_cost_delay)
#autovacuum_vacuum_cost_limit = -1      # -1 means use vacuum_cost_limit
#log_autovacuum_min_duration = 600000   # -1-2147483647 ms; -1 disables logging

#------------------------------------------------------------------------------
# AUTOVACUUM REGISTERED-INERT (accepted for operator parity, not yet enforced)
#------------------------------------------------------------------------------

#autovacuum_max_workers = 3             # 1-262143; goopg runs a single worker
                                        # (change requires restart)
#autovacuum_work_mem = -1               # KB; -1 uses maintenance_work_mem
                                        # (change requires restart)
#autovacuum_multixact_freeze_max_age = 4000000000 # 1000000-5000000000 multixacts
                                        # (change requires restart)


#------------------------------------------------------------------------------
# CLIENT CONNECTION DEFAULTS
#------------------------------------------------------------------------------

# - Statement Behavior -

#search_path = '"$user", public'        # schema search path
#default_statistics_target = 100        # 1-10000
#statement_timeout = 0                  # 0-2147483647 ms; 0 disables
#lock_timeout = 0                       # 0-2147483647 ms; 0 disables
#idle_in_transaction_session_timeout = 0  # 0-2147483647 ms; 0 disables
#transaction_timeout = 0                 # 0-2147483647 ms; 0 disables
#synchronize_seqscans = on              # accepted; goopg has no synchronized scans
#row_security = on                      # accepted; goopg has no row-level security
#xmloption = 'content'                  # content | document; accepted, no document-mode parsing
#client_min_messages = 'notice'         # debug5..debug1 | log | notice |
                                        # warning | error
#default_transaction_isolation = 'read committed'
                                        # read uncommitted | read committed |
                                        # repeatable read | serializable
#default_transaction_read_only = off
#default_table_access_method = 'heap'   # accepted; goopg only implements heap
#default_tablespace = ''                # accepted; goopg has no real tablespaces
#default_toast_compression = 'pglz'     # pglz | lz4; accepted, column default not GUC-driven
#transaction_isolation = 'read committed'   # current-transaction read-only;
                                            # SHOW only
#application_name = ''                  # session label echoed in pg_stat_activity
#session_authorization = ''             # role identity for the session
#vacuum_freeze_min_age = 50000000       # 0-1000000000 xids
#vacuum_multixact_freeze_min_age = 5000000  # 0-1000000000 multixacts

# - Locale and Formatting -

#DateStyle = 'ISO, MDY'
#IntervalStyle = 'postgres'             # postgres | postgres_verbose |
                                        # sql_standard | iso_8601
#TimeZone = 'UTC'
#timezone_abbreviations = 'Default'     # select timezone abbreviation set
#lc_messages = ''                       # accepted; goopg emits English only
#lc_monetary = 'C'                      # accepted; money formatting is
                                        # C-locale by construction
#lc_numeric = 'C'                       # accepted; numeric output is
                                        # C-locale by construction
#lc_time = 'C'                          # accepted; internal/pgdatetime is
                                        # C-locale by construction
#default_text_search_config = 'pg_catalog.simple'# accepted; goopg has no
                                        # text-search configurations
#client_encoding = 'UTF8'               # actually defaults to whatever the
                                        # client negotiates at startup

# - Other Defaults -

#standard_conforming_strings = on
#escape_string_warning = on            # accepted; goopg has no
                                        # deprecated-backslash-escape warning
#default_with_oids = off               # removed in PG12; accepted as no-op


#------------------------------------------------------------------------------
# LOCK MANAGEMENT
#------------------------------------------------------------------------------

#max_locks_per_transaction = 64         # 10-2147483647 per-backend slots
                                        # (change requires restart)
#max_predicate_locks_per_xact = 64      # 10-1073741824
                                        # (change requires restart)
#max_predicate_locks_per_relation = -2  # negative = fraction;
                                        # see internal/mvcc/predicate_locks.go
                                        # (SIGHUP to apply)
#max_predicate_locks_per_page = 2       # 0-65536
                                        # (SIGHUP to apply)
#max_prepared_transactions = 0          # 0-262143; zero disables 2PC
                                        # (change requires restart)
#deadlock_timeout = 1000                # 1-2147483647 ms; wait before
                                        # running the deadlock detector


#------------------------------------------------------------------------------
# DEVELOPER OPTIONS
#------------------------------------------------------------------------------

#allow_in_place_tablespaces = off       # developer-only: permits
                                        # CREATE TABLESPACE ... LOCATION ''
                                        # to create a tablespace directly
                                        # inside pg_tblspc (for testing)
#allow_system_table_mods = off          # developer-only: permits modifying
                                        # the structure of system tables;
                                        # recognised as a no-op (goopg gates
                                        # no catalog change on it) so test
                                        # setups that SET it succeed
#debug_parallel_query = off             # off, on, regress; no-op in goopg
                                        # (no parallel executor) — accepted so
                                        # test scripts that force parallel
                                        # plans don't fail on SET


#------------------------------------------------------------------------------
# CONFIG FILE INCLUDES
#------------------------------------------------------------------------------

# Goopg does not currently honor PG-style include_dir / include_if_exists /
# include directives.  They are listed here only for documentation; adding
# them to a running postgresql.conf has no effect today.


#------------------------------------------------------------------------------
# CUSTOMIZED OPTIONS
#------------------------------------------------------------------------------

# Add settings for extensions here.
