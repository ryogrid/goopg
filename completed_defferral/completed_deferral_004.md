# Completed (resolved) Deferrals — Part 4

Resolved deferral rows archived from `.ralph/deferral_ledger.md` (part 4 of 4). See that file for still-open rows. Original ledger order preserved.

| status | date | task-id | landed | deferred | resume point | why |
| :-- | :-- | :-- | :-- | :-- | :-- | :-- |
| resolved | 2026-07-08 | M-NIGHTLY (AI-20260708-064334-001, 7th loop on the reopen) | Executed the 6th loop's exact resume point: built `BTree.DebugVerifyFastPathInserts`/`insertItemSortedVerified`/`checkFastPathSurvivors` (`internal/access/btree/btree.go`) — wraps all 3 fast-path single-item `insertItemSorted` call sites (`tryInsertNoSplit`, `insertIntoBlock`'s own no-split branch, `tryInsertOnCachedRightmost`; deliberately NOT the split/dedup-rewrite refill loops, already covered by `RewriteLogEvent`) with a `pageItems()` snapshot immediately before and immediately after each call, and records a `FastPathViolation` (block, call site, the item that call inserted, and every pre-existing `(key,TID)` that vanished) if any pre-existing entry from the "before" snapshot is missing from the "after" snapshot — a plain fast-path insert only ever adds a line pointer. Wired into `TestVerifyBtreeEngineSilentOnRealConcurrentContended` (temporarily un-skipped to run, re-skipped before commit) both as an unconditional post-run dump and a per-missing-entry cross-reference. **Result: REFUTES this loop's own hypothesis too, with a clean negative result.** A full 200000-insert/64-writer run that still lost 12 real entries recorded ZERO `FastPathViolation`s — every fast-path call, at all 3 sites, across the entire run, preserved every pre-existing entry in its own pre/post snapshot pair. Combined with the 6th loop's finding (pageItems() at rewrite time already lacks the lost entry), this pins the loss window down precisely for a traced example (key=47087, TID={3,1508}, inserted blk=16 lineIdx=6 seq=216430, 564 later inserts touch blk=16, one rewrite/split event at seq=315252 whose own pageItems() read already lacked it): the entry survived every fast-path call's own pre/post check up to blk=16's last fast-path touch before the split, then vanished in the gap between that call's `unpinW` and the split-triggering call's `pinW`+`pageItems()` read — i.e. while nobody held a pin on the page, during (or across) a buffer-pool eviction. `pool.DebugValidateCleanEvictions` (loops 3-4) was still active and fired once this run, but on an unrelated block (133), not blk=16 — consistent with loop 4's "mismatch-firing and missing-entry count are uncorrelated" finding, and pointing away from the "clean" (skip-flush) eviction fast path specifically. | The dirty flush-then-evict path (`evictVictim`'s `wasDirty` branch → `flushSlot`, `internal/storage/bufpool.go` ~1123-1185) has never been directly instrumented to compare "the bytes actually written to disk at flush time" against "the last known-good pageItems() snapshot for that block" — `DebugValidateCleanEvictions` only checks the OTHER branch (the `!wasDirty` skip-flush fast path). Also unverified this loop: whether `claimVictim` can select a slot with a nonzero pin count as an eviction victim at all (if it can, that alone would be the bug, independent of flush correctness). | Resume point: instrument `evictVictim`'s dirty branch (or `flushSlot` itself) to snapshot `pageItems()` on the page immediately before the `WriteAt`/flush call, keyed by block, and compare against the most recent fast-path/rewrite "post" snapshot already recorded for that block via the now-shared `logSeqNext`-ordered `insertLog`/`rewriteLog`/`fastPathViolations` logs (all three already exist and compare for temporal order) — a mismatch there would directly catch a stale or torn flush. As a quick first pass before building new instrumentation, audit `claimVictim` (bufpool.go) to confirm it actually excludes slots with `pinCount > 0` from victim selection. | Landed real, reusable, zero-cost-when-off instrumentation (`DebugVerifyFastPathInserts`, kept committed like every prior `DebugTrace*`/`DebugValidate*` aid in this thread) that definitively falsified this loop's OWN leading hypothesis with hard per-call evidence, narrowing the loss window to a specific unpin/re-pin gap rather than "somewhere in the fast path" — a strong, evidence-backed result even though it is a negative one; this is the 7th consecutive loop on the same nightly item, and the newly-implicated dirty-flush path is a distinct, well-scoped subsystem better started fresh next loop than rushed at time-of-cutoff. |
| resolved | 2026-07-08 | M-NIGHTLY (AI-20260708-064334-001, 8th loop on the reopen) | Audited `claimVictim` (`internal/storage/bufpool.go` ~1068) per the 7th loop's suggestion — correctly excludes any slot with `statePin(old) != 0`; not the bug. Then built `storage.Pool.OnFlushSnapshot` (`bufpool.go`, called from `flushSlot` immediately before the `WriteBlock` call, nil by default) plus `BTree.RecordFlushSnapshot`/`DebugTraceFlushes`/`FlushSnapshotEvent`/`FlushSnapshotRecordsForBlock` (`internal/access/btree/btree.go`) — decodes `pageItems()` from the exact bytes a dirty-page flush is about to write to disk, sharing `insertLog`/`rewriteLog`'s `logSeqNext` counter. Wired into `TestVerifyBtreeEngineSilentOnRealConcurrentContended` (temporarily un-skipped to run, re-skipped before commit) and extended its per-missing-entry diagnostic to cross-reference flush-snapshot presence against the most recent flush at/after each lost entry's insert seq. **Strongest, most reproducible signature found in this 8-loop investigation**: for ALL 18 missing entries in a fresh repro (not a subset), the entry is present in the first recorded flush-snapshot of its block after being inserted, and already ABSENT in the very next recorded flush-snapshot of that same block — often only tens to a few hundred `Seq` ticks later (e.g. key=48709 TID={6,1512}: present at flush seq=254194 [332 items], gone at flush seq=254306 [333 items], 112 ticks later; same present-then-immediately-absent pattern held for all other 17 missing entries checked). This reconciles with, rather than contradicts, the 7th loop's zero-`FastPathViolation`s finding: that check only compares a page's survivor set immediately before/after each individual `insertItemSorted` call, so once the entry is already missing by the time any fast-path call next touches the reloaded page, every such call correctly reports no violation (nothing vanished on ITS watch). | With the rewrite path (6th loop), clean-eviction path (4th loop), and fast-path insert sites (7th loop) all already cleared, and the dirty-flush WRITE side now also cleared (this loop — the flush always writes exactly what was in memory, faithfully), the one remaining uninstrumented mechanism able to make the entry vanish between "last good flush" and "next flush already missing it" is the RELOAD side of an eviction cycle: `Pool`'s cache-miss read path (`pinLoad` / `Manager.ReadBlock`) potentially serving stale or wrong bytes for the block after it was correctly flushed. | Resume point: instrument the read/reload side directly — snapshot `pageItems()` on any block reload that follows a prior flush of the same block (`Pool.pinLoad`'s cache-miss branch in `internal/storage/bufpool.go`, or `Manager.ReadBlock` in `internal/storage/smgr.go`), and compare against the most recent `FlushSnapshotEvent` recorded for that exact block+Rel via the same Seq-ordered cross-reference technique already built this loop — a mismatch there would catch a stale/wrong read red-handed and finally close the loop this investigation opened at the 7th loop. Do NOT re-open `claimVictim`, the fast-path insert sites, the split/dedup-rewrite path, the clean-eviction path, or the dirty-flush write side — all five now conclusively cleared. | Landed real, reusable, zero-cost-when-off instrumentation (`OnFlushSnapshot`/`RecordFlushSnapshot`/`DebugTraceFlushes`, kept committed like every prior `DebugTrace*`/`DebugValidate*` aid in this thread) that produced the cleanest, most universally reproducible signature of the whole 8-loop thread (18/18 missing entries, not a subset) and narrowed the loss window from "somewhere in an unpin/re-pin gap" to specifically the reload side of that gap — a strong, evidence-backed pivot point; this is the 8th consecutive loop on the same nightly item, and building read-side instrumentation is a distinct unit of work better started fresh next loop than rushed at time-of-cutoff. |
| resolved | 2026-07-08 | M-NIGHTLY (AI-20260708-064334-001, 9th loop on the reopen) | Executed the 8th loop's exact resume point: built `storage.Pool.OnBlockReload` (`internal/storage/bufpool.go`, called from `pinLoad`'s cache-miss branch immediately after `Manager.ReadBlock` succeeds, before the slot is published for `Pin`, nil by default) plus `BTree.RecordReloadSnapshot`/`DebugTraceReloads`/`ReloadSnapshotEvent`/`ReloadSnapshotRecordsForBlock` (`internal/access/btree/btree.go`), sharing `insertLog`/`rewriteLog`/`flushLog`'s `logSeqNext` counter. While wiring the cross-reference, found and fixed a real bug in the 8th loop's OWN instrumentation: `OnFlushSnapshot` fired BEFORE `flushSlot`'s `WriteBlock` call (pre-write "intent" time) while the new `OnBlockReload` fires AFTER `ReadBlock` returns (post-read "completed" time) — Seq-ordered comparison across the two logs as originally built would have been apples-to-oranges (a reload's real disk read could complete before a flush's real disk write yet log a HIGHER Seq purely from `insertLogMu` lock-acquisition jitter). Moved `OnFlushSnapshot` to fire after `WriteBlock` succeeds so both hooks consistently log "operation durably completed" time. Re-ran the repro AFTER this fix and the signature not only survived, it strengthened: for every one of 14 lost entries in a fresh run, the FIRST reload of the affected block recorded after the last flush that had the entry ALREADY lacks it (e.g. blk=142: flush seq=609678 itemCount=360 present=true; the very next reload, seq=609829, itemCount=359 present=false — one item short of what was just durably written). Critically, this is not a one-off race that later reads recover from: EVERY subsequent reload of the same block for the rest of the run (3179 reload-events checked across all 14 missing entries, spanning Seq deltas from a few hundred to well over 100000 ticks later) still lacks the entry, while the item count keeps climbing as other writers insert on top — a transient read/write race would show SOME later reloads recovering the entry once real disk state settles; a permanent, unrecovering loss instead means either (a) the "good" flush's `WriteBlock` did not durably land the entry despite its in-memory snapshot having it, or (b) a second, older/stale write to the same block landed on disk AFTER the good flush and clobbered it — a classic lost-update pattern. `slotEvent` traces for one affected block (173) show three DIFFERENT slot indices (17, 25, 31) holding the same tag across ~3000 Seq ticks — slot 17's dirty eviction (flush) immediately followed by slot 25's fresh `pinLoad` (reload) then an immediate CLEAN eviction (no insert landed on it while resident) then slot 31's own `pinLoad` (another reload) — the block churns through eviction/reload cycles on the 64-slot pool under 64-writer contention fast enough that back-to-back cycles for the SAME block, on DIFFERENT physical slots, are routine. | Manual code review of `evictVictim`'s dirty branch and `smgr.go`'s `relFile.readBlock`/`writeBlock` (both properly serialized per-relFile under `r.mu`; `evictVictim` only calls `bm.Delete(oldTag)` strictly AFTER `flushSlot`'s `WriteBlock` returns) did not surface an obvious lock gap for hypothesis (a) — a non-durable good write; hypothesis (b) — a second, stale write clobbering the good one — remains unrefuted and is the more likely remaining mechanism, but proving it needs either wall-clock (not `bt`'s `insertLogMu`-guarded Seq, which still has residual cross-goroutine lock-jitter even after this loop's pre/post-write fix) instrumentation at the `smgr.relFile` layer directly, or a raw independent re-read of the block right after the "good" flush (mirroring `debugValidateCleanEviction`'s technique for the clean-eviction path) to catch a losing racer red-handed. | Resume point: instrument `relFile.writeBlock` and `relFile.readBlock` directly (`internal/storage/smgr.go`, both already under `r.mu`) with a monotonic op-sequence counter LOCAL to the `relFile` (not `bt`'s `insertLogMu`-guarded Seq, to eliminate ANY cross-goroutine lock-ordering jitter) so a caller can determine, for a specific block, the TRUE order of every `WriteAt`/`ReadAt` syscall — then re-run this repro and check whether two `writeBlock` calls for the SAME block are ever interleaved such that an OLDER in-memory copy's write lands after the newer one's, which would confirm hypothesis (b) directly and identify whichever caller is flushing stale content (likely a second concurrent `evictVictim`/`WriteDirtyPages`/`flushBatch` caller holding an outdated slot). Do NOT re-open `claimVictim`, the fast-path insert sites, the split/dedup-rewrite path, the clean-eviction path, or the dirty-flush write side — all five remain conclusively cleared; the OnFlushSnapshot/OnBlockReload Seq-ordering fix this loop makes the flush-vs-reload cross-reference itself trustworthy, so also do not re-litigate whether the reload path is implicated — it is, with hard per-block evidence. | Landed real, reusable, zero-cost-when-off instrumentation (`OnBlockReload`/`RecordReloadSnapshot`/`DebugTraceReloads`, kept committed like every prior `DebugTrace*`/`DebugValidate*` aid in this thread) plus a genuine correctness fix to the 8th loop's own `OnFlushSnapshot` placement (pre-write → post-write) that was necessary for this loop's cross-reference to be trustworthy; converted "reload side is implicated" from a hypothesis into hard evidence (permanent, non-recovering loss on 3179/3179 checked reload-events) and narrowed the remaining mechanism to two concrete, falsifiable hypotheses — a strong pivot point; this is the 9th consecutive loop on the same nightly item, and building relFile-level wall-clock instrumentation is a distinct unit of work better started fresh next loop than rushed at time-of-cutoff. |
| resolved | 2026-07-08 | M-NIGHTLY (AI-20260708-064334-001, 10th loop on the reopen) | Executed the 9th loop's exact resume point, but using existing infrastructure instead of building a new counter: discovered `internal/storage/io_trace.go` (`GOOPG_IO_TRACE=1`, added the day before in commit `8ebb71cd` for a related but distinct corruption investigation) already records a content-hash + line-pointer-count event, timestamped by `time.Now()` taken immediately after each real `ReadAt`/`WriteAt` returns, under the SAME `relFile.mu` that serializes the syscalls themselves — exactly the jitter-free true-completion-order data the 9th loop's resume point asked for. Added `storage.IOTraceEventsForTag(tag)` (new exported accessor, sorted by `T`) and wired a new per-missing-entry diagnostic block into `TestVerifyBtreeEngineSilentOnRealConcurrentContended` that walks a lost entry's block's full syscall-level event history and checks whether any `postRead` hash fails to match the immediately-preceding `postWrite` hash (the direct signature of hypothesis (b): a stale/superseded write being read back). Re-ran the repro with `GOOPG_IO_TRACE=1`: 49 lost entries this run across 38 distinct blocks, 60863 total IO-trace events — zero read-mismatches. Every single disk read, for every implicated block, exactly returned the byte content of the most recently completed write to that same (relFile, block) — hypothesis (b) is REFUTED at the smgr/syscall layer. Combined with the 8th loop's proof that the flush WRITE side always faithfully wrote in-memory content, the ENTIRE storage/smgr layer (bufpool eviction+reload+flush, and the OS read/write path) is now cleared — the 9th loop's Seq-based "reload already lacks the entry" signature was a real, reproducible symptom but its storage-layer implication was a false lead (residual cross-log Seq jitter, exactly the risk the 9th loop's own writeup flagged). The loss is a genuine IN-MEMORY content bug, not a storage-layer one. Reviewed `bufpool.go`'s `pinLoad`/`tryPinSlot`/`claimVictim` tag-publish-before-load window on inspection (the tag is published in `bufmap` before `ReadBlock` populates the slot, gated by `stateValid`/`stateIO` bits so concurrent `tryPinSlot` calls correctly return nil and wait; the whole claim/evict/insert sequence runs under a single `pinMu`) — no obvious gap found, but this is inspection only, not disproven by instrumentation the way the syscall layer was. | The stale/superseded write hypothesis (b) is refuted at the smgr/syscall layer, but the underlying loss mechanism is still unidentified — it must now be an in-memory content mutation that silently drops or replaces an already-inserted item while leaving the page's item COUNT unchanged (already observed: a "good" flush at itemCount=379 with the entry present, followed 6 Seq-ticks later by another flush of the SAME block, STILL itemCount=379, entry now absent — same count, different content, not a split/dedup event, since those already-cleared paths change item count or route through the already-audited rewrite log). No instrumentation yet exists at the `contentMu`-guarded mutation level itself (one layer below the flush/reload snapshots built in loops 8-9) to catch which specific critical section performs this silent drop. | Resume point: instrument the exact critical section(s) that mutate a pinned page's bytes under `contentMu` with a before/after content-hash pair (mirroring `io_trace.go`'s hash-based approach but scoped to the in-memory mutation itself, not the syscall) bracketing every insert/split/dedup call site already covered by `DebugTraceInserts`/`RewriteLogEvent` — the goal is to catch, for the SAME block across two consecutive flushes with unchanged item count, exactly which `contentMu` hold is where the entry's bytes stop being present. Do NOT re-open `claimVictim`, the fast-path insert sites' own correctness, the split/dedup-rewrite path, the clean-eviction path, the dirty-flush write side, or `smgr.go`'s `readBlock`/`writeBlock`/`relFile.mu` serialization — all now conclusively cleared (six mechanisms ruled out across 9 loops of targeted instrumentation, this loop's syscall-level cross-check being the strongest and most direct of them). Do NOT re-litigate hypothesis (b) (a stale write superseding a newer one) — refuted with hard syscall-level evidence (60863 events, zero mismatches), not just unconfirmed by inspection. | Reused existing `io_trace.go` instrumentation rather than building new relFile-local counters (cheaper and already jitter-free by construction), landed a small reusable accessor (`IOTraceEventsForTag`, zero-cost when `GOOPG_IO_TRACE` unset) and a new diagnostic block in the target test; definitively cleared the entire storage/smgr layer as a suspect, which is a major narrowing even though the root cause itself is still open — the search space is now confined to a single subsystem (`contentMu`-guarded in-memory page mutation) after 10 loops of elimination across bufpool eviction, reload, flush, fast-path insert, split/dedup-rewrite, and now syscall IO. This is the 10th consecutive loop on the same nightly item; building content-mutation-level instrumentation is a distinct unit of work better started fresh next loop than rushed at time-of-cutoff. |
| resolved | 2026-07-08 | M-NIGHTLY (AI-20260708-064334-001, 11th loop on the reopen) | Executed the 10th loop's exact resume point: built `BTree.DebugTraceContentMu`/`recordContentMuLock`/`recordContentMuUnlock`/`ContentMuEvent`/`ContentMuRecordsForBlock` (`internal/access/btree/btree.go`), bracketing every `pinW`/`unpinW` hold with a before/after `pageItems()` snapshot (before = right after `s.Lock()`, after = right before `s.Unlock()`), sharing `insertLog`/`rewriteLog`/`flushLog`/`reloadLog`'s `logSeqNext` counter. Wired a per-missing-entry diagnostic into `TestVerifyBtreeEngineSilentOnRealConcurrentContended` (temporarily un-skipped to run, re-skipped before commit) that, for each lost `(key,TID)`, finds the last traced `contentMu` hold that had the entry present and the very next traced hold on the same block, to pin down whether the loss happens INSIDE one traced hold or in the GAP between two. **Found a flaw in the 10th loop's own premise, not the target bug directly**: `pinW`/`unpinW` is NOT actually the sole `contentMu` choke point as its doc comment (trusted uncritically by all 10 prior loops) claimed — `storage.Pool.pinLoad` (`internal/storage/bufpool.go` ~1561-1572) independently `Lock()`s/`Unlock()`s the SAME per-slot `contentMu` directly around its own `ReadBlock` call during a cache-miss reload, entirely bypassing `bt.pinW`/`bt.unpinW`. Fixed the misleading doc comments on `pinW` and the new `DebugTraceContentMu` field this loop. In a fresh repro (1 entry lost this run — smaller than usual, run-to-run variance), the diagnostic showed exactly this "GAP LOSS" pattern: the entry was present at one traced `pinW`/`unpinW` hold's `Unlock` (seq=311449) and already absent at the very next traced hold's `Lock` (seq=311559) on the same block (196), with NOTHING recorded at the `pinW`/`unpinW` level in between. Cross-referencing the SAME window against the pre-existing (9th-loop) flush/reload logs closed the gap precisely: a flush-snapshot at seq=311541 has 268 items INCLUDING the entry (`RewriteSnapshotHasTID` — exact TID check, not just count), and the very next reload-snapshot at seq=311542 — one `logSeqNext` tick later, i.e. essentially back-to-back with no other traced btree-level event for this block in between — has 267 items, entry gone. This is the tightest localization of the loss window found in this entire 11-loop investigation. | This REOPENS rather than closes the question the 10th loop believed it had answered. The 10th loop's IO-trace check only flags a `postRead` that matches an OLDER `postWrite` instead of the MOST RECENT one; it structurally cannot detect a still-plausible variant of hypothesis (b) where a SECOND, chronologically-later `postWrite` (e.g. from a DIFFERENT physical slot racing to flush the same block tag during heavy eviction churn — the 9th loop already observed 3 different slot indices serving one block's tag in a single run) legitimately becomes the new most-recent write with STALE (pre-insert) content, so the following read correctly matches it and is never flagged as a mismatch by the 10th loop's algorithm, yet the insert is still lost. This variant was not checked by the 10th loop and is not yet confirmed or refuted. | Resume point: for blk=196's exact seq=311541/311542 window (or the equivalent window in a fresh repro — this run only lost 1 entry, atypically few), walk `internal/storage/io_trace.go`'s log for that block by WALL-CLOCK time (`ioTraceEvent.T`), not recurring `lpCnt` (ambiguous — the same `lpCnt` value recurs many times over a full run, as directly observed this loop), to check whether TWO `postWrite` events land for this block in immediate succession there, with the second one's item count/hash regressing from the first. If so, that is the concrete two-slots-racing-to-flush-the-same-tag mechanism to fix in `claimVictim`/`evictVictim`'s victim-selection and tag-transition sequence (`bufpool.go` ~1527-1557). If not, add a dedicated hook directly inside `pinLoad`'s own reload hold (distinct from `pinW`/`unpinW`, since this loop proved they are separate mutation sites over the same `contentMu`) to catch a same-slot double-load or a destination-buffer aliasing bug instead. Do NOT re-open `claimVictim`'s pin-count exclusion, the fast-path insert sites, the split/dedup-rewrite path, the clean-eviction path, or `relFile.readBlock`/`writeBlock`'s own `r.mu` serialization — all still conclusively cleared; DO re-open whether the 10th loop's "zero mismatches" conclusion covers the two-writers-race variant of hypothesis (b) — it does not, by construction of that loop's own matching algorithm. | Landed real, reusable, zero-cost-when-off instrumentation (`DebugTraceContentMu`/`ContentMuEvent`, kept committed like every prior `DebugTrace*`/`DebugValidate*` aid in this thread) that both executed the assigned resume point AND surfaced a genuine documentation/premise error (the false "sole choke point" claim) that had gone unchallenged for 10 loops — correcting it is itself real forward progress, independent of the root cause still being open. Also produced the single tightest flush-to-reload loss window recorded in this investigation (one `logSeqNext` tick), directly reopening a specific, falsifiable sub-question the 10th loop's evidence does not actually cover. This is the 11th consecutive loop on the same nightly item; distinguishing the two remaining hypotheses (two-writers-race vs. pinLoad-local reload bug) is a distinct, well-scoped unit of work better started fresh next loop than rushed at time-of-cutoff. |
| resolved | 2026-07-08 | M-NIGHTLY (AI-20260708-064334-001, 12th loop on the reopen) | Executed the 11th loop's exact next step: extended the existing GOOPG_IO_TRACE=1 wall-clock IO-trace check (internal/amcheck/verify_nbtree_realtree_test.go, right after the 10th loop's stale-postRead check) to also flag any adjacent postWrite pair whose line-pointer count REGRESSES. A naive version flagged 29 hits on a fresh repro, but 27/29 were ordinary page splits (goopg splits by byte size not item count, so the surviving fraction is 50.7%-53.8%, not exactly half -- an initial "at most half+2" cutoff still misclassified these); refined to a magnitude-based classifier (drop >25% of prior count = presumed split, not flagged) which left exactly ONE genuine hit. That hit lands EXACTLY on the already-localized loss and is now cross-validated at BOTH the contentMu (in-memory) and IO-trace (smgr/syscall) layers: for the missing (key=33666, TID={1,2883}) entry at blk=377, contentMu-hold seq=908007 has the entry present (afterCount=401); a reload-snapshot AT seq=908093 (BETWEEN two postWrites) already shows itemCount=399 with the entry absent -- a stale on-disk copy predating the insert; flush-snapshot seq=908124 (itemCount=401, entry present) legitimately flushes the correct version; the IO tracer shows that exact flush landing as postWrite lpCnt=401 at t=43.393270170s; then, only 1.9ms later, a SECOND postWrite lands with lpCnt=400 (=399 stale items + 1 later insert layered on top of the stale reload) at t=43.395185889s, durably clobbering the correct 401-item disk copy; the next contentMu-hold (seq=908216) confirms the in-memory slot itself is now the stale 399-item version (beforeCount=399). This confirms hypothesis (b) variant 2: a cache-miss reload racing against a legitimate flush of the SAME block loads a STALE on-disk copy into the slot, silently discarding the concurrently-flushed correct in-memory content; a subsequent write on top of the now-stale slot re-flushes the loss permanently. Test skip message (verify_nbtree_realtree_test.go line ~762) updated with this finding for the next loop. | The actual synchronization gap in storage.Pool.claimVictim/evictVictim/pinLoad's victim-selection and tag-transition sequence (internal/storage/bufpool.go ~1527-1572) that allows a reload (pinLoad, ~1561-1572) to load stale bytes for a tag whose correct, newer version was just flushed (evictVictim, ~1527-1557) -- and that stale reload to then win the race and get re-flushed -- is still unfixed; only the mechanism is now confirmed, not repaired. | internal/storage/bufpool.go: audit claimVictim/evictVictim/pinLoad for a missing exclusion between a slot's dirty-flush completing (WriteBlock durable) and any concurrent reload (ReadBlock) for the SAME tag being allowed to publish into a slot -- specifically whether the reload path re-checks the tag's dirty/flush-in-flight state after its own ReadBlock returns, and whether two slots can transiently hold the same tag during a flush->reload handoff; add a targeted fix (likely: reload must re-validate or block on any in-flight flush for the same tag before publishing its read) then re-run TestVerifyBtreeEngineSilentOnRealConcurrentContended un-skipped to confirm. | this is a genuine concurrency race in the buffer pool's flush/reload handoff, not a narrow-scope fix -- confirming the mechanism (this loop) and designing+landing the synchronization fix (next loop) are properly separate units of work given Ralph's one-task-per-loop rule. |
| resolved | 2026-07-08 | M-NIGHTLY (AI-20260708-064334-001, 13th loop on the reopen) | Attempted the 12th loop's exact next step (land the synchronization fix in claimVictim/evictVictim/pinLoad), but before writing code did a from-scratch hand-trace of the full protocol (bufmap Insert/Delete/Lookup/compact, claimVictim's CAS-based victim claim, evictVictim's dirty/clean paths, pinLoad/pinSlow/Pin's fast+slow paths and per-slot semaphore wait, relFile's r.mu-serialized readBlock/writeBlock, bt.pinW/unpinW's contentMu.Lock() usage, MarkDirty, Unpin's underflow-panic guard, and the checkpointer's flushBatch/WriteBlockAIO/PrefetchBlock) rather than re-running prior loops' instrumentation, specifically hunting for the "two slots hold the same tag" or "reload publishes without waiting on an in-flight flush" gap the 12th loop's next-step named. Could not construct such a window by inspection: bufmap.Insert always fails if the tag is already present (serialized under bufmap.mu); evictVictim's bm.Delete(oldTag) runs strictly after flushSlot's WriteBlock returns, still under pinMu; any concurrent Pin(oldTag) during the flush correctly observes stateIO=true and waits on the per-slot semaphore, only re-attempting its own fresh disk read after the delete becomes visible (itself strictly after the flush completed, by pinMu ordering), so a woken waiter cannot observe pre-flush bytes via this path. Also ruled out: Unpin double-decrement (panics immediately on underflow, would crash the test rather than silently corrupt); torn writes during the checkpointer's flushBatch (bt.pinW takes s.Lock()==contentMu.Lock() exclusively for every page mutation, which Go's sync.RWMutex correctly excludes against flushBatch's contentMu.RLock()). Found one genuinely new, previously-unaudited gap while tracing the checkpointer path: storage.Manager.WriteBlockAIO/PrefetchBlock (internal/storage/smgr.go) bypass relFile.mu entirely when an AIOEngine is attached (`eng != nil` calls `eng.Submit(...)` directly instead of `f.writeBlock`/`f.readBlock`), and internal/aio/aio.go's Engine.Submit provides no per-file/per-offset ordering of its own (confirmed by reading Submit/registerInFlight — the inflight map is stats-only bookkeeping). This bypass is wired in production (internal/initdb/open.go:303 `mgr.SetAIO(...)`), unlike this unit test's own pool (buildRealTreeConcurrent never calls SetAIO, and never invokes FlushAllPaced/flushBatch during the insert race window — only via Close() after all writers finish), so it cannot be the mechanism this specific test reproduces; and even reasoning through the production case, contentMu's RWMutex (flushBatch holds RLock, any eviction/reload needs Lock) still forces an evictor/reloader to wait for flushBatch's real AIO Wait() to return before touching s.page, so no concrete corruption path was found from the bypass either, in the time available this loop. Filed as its own new fix_plan item (storage/aio-relfile-mu-bypass) rather than conflated with this task. | The sync-only-path mechanism this test reproduces (a cache-miss reload observing stale on-disk bytes despite a just-completed flush of the same tag) still has no identified code-level defect after 13 loops combining live instrumentation (loops 1-12) and hand-tracing (this loop) — every lock/state-bit/bufmap invariant constructible from reading the code holds. Separately, the newly-found WriteBlockAIO/PrefetchBlock relFile.mu bypass is a confirmed, real gap (production-reachable, not unit-test-reachable) that was never examined by any of the prior 12 loops, which only audited relFile.readBlock/writeBlock (the synchronous path) and explicitly never looked at the AIO submission path or the checkpointer's interaction with claimVictim. | For the M-NIGHTLY reopen itself: pivot to a check kind no prior loop has tried — (a) directly instrument bufmap.Insert/Delete/Lookup with per-call logging (never done in 13 loops; every loop so far only inferred bufmap correctness by reading the code) to verify, for blk=377's exact tag, that bufmap truly holds a single (tag→slot) mapping at every instant during the loss window; or (b) audit the 15-bit slot generation counter (`stateGen`/`newGen := (curGen+1)&0x7FFF`) for a CROSS-slot gen collision (two different slots coincidentally sharing the same gen value concurrently), distinct from the already-ruled-out same-slot wraparound-after-~2500-claims case. For the new AIO bypass finding: see the storage/aio-relfile-mu-bypass fix_plan item — design a per-(rel,block) in-flight-AIO registry consulted by readBlock/writeBlock/WriteBlockAIO/PrefetchBlock instead of a blanket per-file mutex (which would regress checkpointer throughput), and verify with a new concurrency test exercising a real AIOEngine alongside concurrent synchronous smgr calls to the same block. | This is the 13th consecutive loop on the same nightly item without a landed fix; per this project's "correctness is non-negotiable" and "quality over speed" rules, a guessed/unverified change to this heavily-audited hot path was judged riskier than another loop of investigation-only work, especially since hand-tracing this loop could not even reproduce (on paper) the gap the 12th loop asked to be fixed -- landing a fix for a gap that may not exist risks false confidence and a wasted un-skip attempt. The AIO bypass finding is real, novel, and worth its own dedicated design task rather than a rushed patch alongside an already-13-loop-deep, still-unresolved investigation. |
| resolved | 2026-07-08 | Pre-existing, HEAD-reproducible test hang (unrelated finding while gating the M0122-0007 proconfig fix) — RESOLVED same day, later loop. | Root cause was test-scaffolding-only, not the suspected AIO/relFile.mu production regression: `recordingExecAIOEngine.Submit` (`internal/executor/storage_test.go`, duplicated from `internal/storage/storage_test.go`'s recording engine) never called `op.OnComplete`, so `Manager.PrefetchBlock`'s AIO path (smgr.go:230 `unlock := f.lockBlock(blk)` ... `OnComplete: unlock`) left `relFile.blockBusy[blk]` permanently registered — any later `lockBlock` call on the same block (a second prefetch, or the eventual synchronous read) blocked forever on the channel receive at smgr.go:709. Verified production is unaffected: `internal/initdb/open.go`'s `aioEngineAdapter.Submit` correctly forwards `op.OnComplete` into `aio.Op.Callback`, fired by all three real engine methods (sync/worker/io_uring) regardless of `Wait` timing — confirmed by reading `aioEngineAdapter.Submit` (open.go:2059) and its doc comment. Fixed by adding the same `if op.OnComplete != nil { op.OnComplete() }` call the sibling recording engine in `internal/storage/storage_test.go` already has. Confirmed non-vacuous: `git stash` on the test file alone reproduces the hang (`timeout 15 go test -run TestSeqScanFiresPrefetchesAcrossBlocks` → `Terminated`), unstashed → PASS in 0.007s. Gates: `go build ./...`/`go vet ./internal/executor/...` clean; `go test ./internal/executor/...` PASS (3.7s); `go test ./internal/storage/...` PASS (4.5s); `RALPH_PRECOMMIT_SCOPE=smoke scripts/ralph-precommit-test.sh` PASS (0 failed, all 3 workloads — re-verified on a clean stash first to confirm the one failed txn seen on the first smoke run was a pre-existing unrelated flake, not caused by this test-only diff); `scripts/tpch-spotcheck.sh` PASS (Q12=2, Q13=33). | n/a — fully landed, test-only fix. | Isolated to test scaffolding; the M-NIGHTLY AIO/relFile.mu bypass thread's other findings remain independently valid but are not implicated in this specific hang. |  |
| resolved | 2026-07-08 | M0122-0007 (`CREATE INDEX ... TABLESPACE` / `ALTER TABLE/INDEX ... SET TABLESPACE`, follow-up to the `CREATE TABLE ... TABLESPACE` row above) | Closed deferred item (3) from the row above: `CreateIndexStmt.Tablespace` (parser, `parseCreateIndexTail` — real `gram.y` IndexStmt order: after `WITH (storage_parameter)`, before `WHERE`) plus a new `AlterTableSetTablespace` action kind (`AlterTableAction.TablespaceName`) for the bare `SET TABLESPACE name` form of both `ALTER TABLE` (an ordinary comma-combinable `alter_table_cmd`, `AT_SetTableSpace`, unlike `SET SCHEMA`/`SET LOGGED` which the caller intercepts as whole-statement fields before the per-action parser runs) and `ALTER INDEX` (checked ahead of the pre-existing `SET (reloptions)` branch in both dispatchers, which otherwise unconditionally expects `(`). All three sites resolve through `resolveTablespaceClause` (renamed from `resolveCreateTableTablespace` — the resolution logic was already fully relation-generic) onto the new `catalog.Index.Tablespace` field, rendered as `pg_class.reltablespace` by the same index-row loop in `catalog.go`'s `registerSystemTables` that previously hardcoded `"0"` for every index (both the real-btree `createBTreeIndex` path and the gist/spgist/gin/brin catalog-only path). Tests: `parser.TestParseCreateIndexTablespace`/`TestParseAlterTableSetTablespace`/`TestParseAlterIndexSetTablespace` (`internal/parser/create_tablespace_test.go`); `executor.TestCreateIndexTablespaceResolvesAndStores`/`TestCreateIndexTablespaceCatalogOnlyMethod`/`TestCreateIndexTablespaceUnknownErrors42704`/`TestAlterTableSetTablespaceUpdatesTable`/`TestAlterIndexSetTablespaceUpdatesIndex`/`TestPGClassRendersIndexReltablespace` (`internal/executor/create_index_tablespace_test.go`); all confirmed non-vacuous via `git stash` (new parser fields/action kind/catalog field don't exist without the fix — compile failure). Design: `docs/design/0095-0003-in-place-tablespace.md` new "CREATE INDEX / ALTER TABLE/INDEX ... SET TABLESPACE" section; `docs/design/README.md` row updated. Gates: `go build ./...`/`go vet ./...` clean; `go test ./internal/parser/... ./internal/catalog/... ./internal/executor/...` PASS; `go test ./internal/planner/... ./internal/initdb/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke scripts/ralph-precommit-test.sh` PASS (0 failed txns, all 3 workloads). | Items (1)/(2) from the row above remain fully open and now apply equally to `Index.Tablespace`, not just `Table.Tablespace`: **no physical relocation** (goopg never moves either a table's or an index's on-disk file into `pg_tblspc/<oid>/.../<relfilenode>`) and **no restart durability** (`Index.Tablespace` is not carried through `btreeIndexProps`/the CREATE INDEX WAL record, nor through any pg_index-heap-driven reload path, so it resets to 0 after a non-graceful restart — same class of gap as `Table.Tablespace`/`Table.Unlogged`). | `internal/executor/operators_ddl.go`'s `btreeIndexProps` struct + the `wal.CreateIndexPayload` it feeds (would need a new `Tablespace uint32` field plumbed through both, mirroring how `Fillfactor`/`DeduplicateItems` already round-trip) for index restart durability; `internal/initdb/open.go`'s table-load path + `catalog.PGClassRow` for table restart durability (same resume point the row above already named) — natural to batch both relkinds' tablespace durability into one future loop since it's the same WAL/heap-reload mechanism either way. | Scoped to close exactly the named "CREATE INDEX ... TABLESPACE / ALTER TABLE/INDEX ... SET TABLESPACE remain unparsed" gap (deferred item 3 of the row above) without expanding into physical relocation or WAL/restart durability, which are materially larger and already tracked. One task per loop. |
| resolved | 2026-07-08 | M0122-0007 (tablespace restart-durability follow-up, closes item (2) of the "CREATE TABLE ... TABLESPACE" row and item (2) of the "CREATE INDEX ... TABLESPACE" row above) | `pg_class.reltablespace` now survives a non-graceful restart for both tables and indexes. `catalog.PGClassRow` gained `RelTablespace uint32`, decoded by `DecodePGClassPhysicalRow` at the real PG18 `FormData_pg_class` byte offset 92 (verified against `pgClassColDefs`'s own offset comments — sits between `relfilenode`(88) and `relpages`(96)). `internal/initdb/open.go`'s `loadUserTablesFromHeap` now sets `tbl.Tablespace = tr.RelTablespace` (the value was already being written into the heap row by `buildUserPGClassRow`, just never read back). `buildUserPGClassRowForIndex` (`internal/executor/pg18_user_catalog_rows.go`) previously hardcoded index `reltablespace` to `0` even for an index explicitly created `... TABLESPACE ts1` — fixed to render `idx.Tablespace`; `loadUserIndexesFromHeap` decodes it back and threads it through a new `tablespace uint32` parameter on `catalog.Catalog`'s `RegisterIndexDuringRecovery`/`indexRegistryRecovery` interface (mirrors the existing `fillfactor`/`deduplicateItems` parameters). For the WAL-only replay fallback path (`replayIndexDDLRecords`, used when no pg_index heap row exists yet), `wal.CreateIndexPayload` gained a `Tablespace uint32` field in its self-describing extension block (new `ciExtHasTablespace` flag bit, mirrors `Fillfactor`'s encoding) and `btreeIndexProps`/`createBTreeIndex` now set `idx.Tablespace` from `xp.Tablespace` BEFORE the CREATE INDEX WAL record is built (same "before WAL emission, not a post-call assignment" discipline every other M0122-0006-follow-up index property already follows) — this also let the previous `execCreateIndex` post-call `bidx.Tablespace = idxTablespaceOID` block be deleted outright. `ALTER TABLE/INDEX ... SET TABLESPACE` (previously catalog-metadata-only for the session lifetime, per both rows above) now resyncs the heap-persisted pg_class row immediately after the mutation (table: existing `deleteCatalogRowsForOID`+`syncTableToCatalogHeap` idiom; index: `resyncIndexClassHeapRow`, which already stamps the old row's xmax internally), so an uncheckpointed crash right after the ALTER no longer loses the change either. Tests: `TestEncodeCreateIndexExtensionRoundTrip`'s new "tablespace only" case + the gen1 backward-compat zero-value assertion (`internal/wal/index_ddl_test.go`); `TestTableTablespaceSurvivesRestartViaCatalogHeap`/`TestIndexTablespaceSurvivesRestartViaCatalogHeap`/`TestAlterTableSetTablespaceSurvivesRestartViaCatalogHeap` (`internal/initdb/tablespace_restart_test.go`, full `Init`→`Open`→DDL→`Close`→`Open` cycles against a real on-disk data dir, no JSON snapshot), all confirmed non-vacuous via `git stash` (fail with `Tablespace = 0, want <oid>` pre-fix). Also live-verified against the real `cmd/goopg` binary: `CREATE TABLESPACE ts1 LOCATION ''`, `CREATE TABLE ts_live (...) TABLESPACE ts1`, `CREATE INDEX ts_live_idx ON ts_live (...) TABLESPACE ts1`, then a real `goopg stop`/`goopg start` — `pg_class.reltablespace` for both rows unchanged across the restart; likewise for `ALTER TABLE ... SET TABLESPACE`. Design: `docs/design/0095-0003-in-place-tablespace.md` new "Restart durability (2026-07-08 follow-up)" section; `docs/design/README.md` row updated. Gates: `go build ./...`/`go vet ./...` clean; `go test ./internal/catalog/... ./internal/wal/... ./internal/initdb/... ./internal/executor/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke scripts/ralph-precommit-test.sh` PASS (0 failed txns, all 3 workloads). | Physical relocation (item (1) of both rows above) remains fully open — still no code moves a relation's file into `pg_tblspc/<oid>/.../<relfilenode>`. **New, distinct gap found live-verifying this fix**: the tablespace REGISTRY itself (`CREATE TABLESPACE`, `catalog.InMemory.tablespaces`) has no restart durability at all — `CreateTablespace`/`DropTablespace` (`internal/catalog/catalog.go` ~12571-12599) mutate a purely in-memory map with no WAL emission and no heap row, so a tablespace created via `CREATE TABLESPACE ts1 LOCATION ''` vanishes from `pg_tablespace` entirely after a restart (confirmed live: a second `CREATE TABLE ... TABLESPACE ts1` issued in a fresh server session after a restart fails with `42704 tablespace "ts1" does not exist`, even though `pg_tblspc/<oid>/` and its `PG_18_*` version directory are still on disk). Any table/index whose `reltablespace` this loop just made durable would, after such a restart, point at an OID with no corresponding live `pg_tablespace` row — `\d`/`pg_dump` would report an orphaned OID instead of the tablespace name. | `internal/catalog/catalog.go`'s `CreateTablespace`/`DropTablespace` need the same class of fix `CREATE DATABASE`/`CREATE SCHEMA` already got (per `[[goopg_catalog_ddl_durability_two_mechanisms]]` memory: goopg-private WAL record + startup replay driver, since `pg_tablespace` has no backing heap relation to sync into) — a new `RecordKindCreateTablespace`/`RecordKindDropTablespace` WAL record pair plus a `replayTablespaceDDLRecords` driver in `internal/initdb`, mirroring `replayDatabaseDDLRecords`/`replaySchemaDDLRecords` exactly. | Out of scope for this loop's reltablespace-COLUMN durability fix — a new WAL-record-kind + replay-driver is materially separate work (the established "goopg-private WAL record" pattern, not a heap-decode fix), and was only discovered incidentally while live-verifying the column fix, not something the fix's own scope touched. |
| resolved | 2026-07-09 | M0122-0007 (tablespace-registry restart durability, closes the "new, distinct gap" from the row above) | `catalog.InMemory.tablespaces` (`CreateTablespace`/`DropTablespace`) now survives a restart — the exact gap the row above found but deliberately left open. New `wal.RecordKindCreateTablespace`/`RecordKindDropTablespace` (124/125, `EncodeCreateTablespace`/`DecodeCreateTablespace`/`EncodeDropTablespace`/`DecodeDropTablespace`, `internal/wal/recovery.go`) are no-ops for physical replay (goopg's tablespace registry has no backing heap relation), mirroring `RecordKindCreateSchema`/`RecordKindDropSchema` exactly (create carries `oid | name | owner | location`, preserving the OID; drop carries only `name`). New `internal/initdb/tablespace_ddl_recovery.go`'s `replayTablespaceDDLRecords`, byte-for-byte structured like `replaySchemaDDLRecords` (same `os.Stat`-missing-dir no-op guard, same `wal.IsGoopgNativeRecord` PG-canonical-collision guard, same local recovery-hook interface type-asserted against the `catalog.Catalog` argument), wired into `internal/initdb/open.go`'s `Open` right after `replaySchemaDDLRecords` (must precede `loadUserTablesFromHeap`/`loadUserIndexesFromHeap` so a table/index's already-durable `reltablespace` OID doesn't transiently look orphaned in the same `Open` call). New `catalog.InMemory.RegisterTablespaceDuringRecovery(name, owner, location, oid)`/`UnregisterTablespaceDuringRecovery(name)` re-populate/remove the `tablespaces` map entry, mirroring `RegisterSchemaDuringRecovery`/`UnregisterSchemaDuringRecovery` (including the `nextOID` bump-past-recovered-OID behavior). `execCreateTablespace`/`execDropTablespace` (`internal/executor/operators_ddl.go`) now append the corresponding WAL record right after the in-memory mutation succeeds (create: after the `pg_tblspc/<oid>` directory is created; drop: before the directory is removed), mirroring CREATE/DROP SCHEMA's "mutate, then WAL" ordering. Tests: `TestTablespaceDDLRecoveryReplaysCreate` (OID preserved across `Init`→`Open`→WAL-append→`Close`→`Open`), `TestTablespaceDDLRecoveryReplaysDropAfterCreate` (CREATE+DROP cancels out), `TestReplayTablespaceDDLRecordsHandlesMissingWalDir` (no-op on fresh initdb) — `internal/initdb/tablespace_ddl_recovery_test.go`, confirmed non-vacuous via `git stash` on the four impl files (fails to build: `undefined: wal.EncodeCreateTablespace`/`replayTablespaceDDLRecords` etc.). Live-verified against the real `cmd/goopg` binary: `SET allow_in_place_tablespaces = on; CREATE TABLESPACE ts1 LOCATION ''; CREATE TABLE t1(a int) TABLESPACE ts1;`, then a real `goopg stop`/`goopg start` — `pg_tablespace` still lists `ts1`, `t1.reltablespace` unchanged (16442), and a fresh `CREATE TABLE t2(a int) TABLESPACE ts1` in the post-restart session succeeds (previously `42704 tablespace "ts1" does not exist`); repeated with `DROP TABLESPACE ts1` before a restart — confirmed `pg_tablespace` shows 0 rows for `ts1` post-restart. Design: `docs/design/0095-0003-in-place-tablespace.md` new "Tablespace-registry restart durability (2026-07-09 follow-up)" section; `docs/design/README.md` row updated. Gates: `go build ./...` clean; `go test ./internal/catalog/... ./internal/wal/... ./internal/initdb/... ./internal/executor/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke scripts/ralph-precommit-test.sh` PASS (0 failed txns, all 3 workloads). | n/a for the tablespace-registry mechanism itself — fully resolved with a permanent regression guard. The two sibling gaps this row's parent row already flagged remain open and untouched by this loop: (1) physical relocation (no code moves a relation's file into `pg_tblspc/<oid>/.../<relfilenode>`); (2) `pg_tablespace.spcowner`/ACL fidelity was not audited this loop — `owner` round-trips as a raw string through the new WAL record exactly as it already existed in the live `tablespaceRow`/`tablespaceVirtualRows`, no new gap introduced but also no new verification performed. | n/a — mechanism fully closed. Physical relocation resume point: unchanged from prior rows (needs a design for relocating relation files into `pg_tblspc/<oid>/...`, a materially separate capability). | Scoped to exactly the named "tablespace-registry restart durability" gap (the WAL-record + replay-driver mechanism); physical relocation and CREATE/DROP DATABASE full DDL remain separately tracked, larger tasks not attempted here. One task per loop. |
| resolved | 2026-07-09 | M0122-0011 (`NOT IN` anti-semi-join unnesting, closes the M0069-0005 "Anti-semi-join optimization for NOT IN subqueries not implemented" `unimplemented_feat.json` entry) | Non-correlated `x NOT IN (subquery)` now unnests to a `Join.NullAware` `JoinTypeAnti` hash join instead of only the slower runtime-cache execution path (`isUnnestableNonCorrelatedIn`/`unnestNonCorrelatedInExpr`, `internal/planner/unnest.go`; new `Join.NullAware` field, `internal/planner/plan.go`). The executor (`internal/executor/operators_join_agg.go`'s `openLazyHashJoin`/`nextLazy`) gives `NullAware` Anti joins real three-valued-NULL NOT IN semantics — build-side NULL poisons the whole predicate to empty output, an empty build side emits every outer row unconditionally (incl. NULL outer values), otherwise a NULL probe value is excluded — rather than the pre-existing Anti join's NOT-EXISTS-shaped "NULL probe key never matches → keep" rule, which would have been a silent correctness regression if naively reused. See `docs/design/0040-0001-subquery-caching-and-unnest.md`'s new Follow-up section for the full three-valued-NULL derivation. | (1) The CORRELATED `NOT IN` path (`unnestInExpr`'s pre-existing `if in.Negated { joinType = JoinTypeAnti }`, reachable today for shapes like `x NOT IN (SELECT y FROM t WHERE t.corr = outer.corr)`) has the same NOT-EXISTS-shaped-Anti-join gap — it does not set `NullAware` — but this is a PRE-EXISTING latent bug, not introduced this loop (the guard predates this change; `canUnnestInExprDepth`'s correlated branch never gated on `in.Negated`). (2) Live-verified TPC-H Q16 (`ps_suppkey NOT IN (SELECT s_suppkey FROM supplier WHERE s_comment LIKE '%Customer%Complaints%')`) still returns the correct row count for the current dataset (18192, matching `ci/logs/action-items.md`'s already-on-file non-blocking baseline) but its `EXPLAIN` shows the `InExpr` survives as a residual filter — the new unnest path is NOT triggering for Q16's real join shape, even though an equivalent synthetic two-table-join probe (`part2 JOIN partsupp2 ... AND ps_suppkey NOT IN (...)`) unnests correctly. Root cause not isolated. | (1) fixing correlated NOT IN correctly needs the "subquery empty"/"subquery has NULL" facts tracked PER CORRELATION-KEY GROUP (the single pair of global build-side flags this loop's fix uses is only sound for the single-global-build non-correlated case) — materially larger, needs its own design pass mirroring how a real "hashed subplan" tracks per-group NULL state; start at `internal/planner/unnest.go`'s `unnestInExpr` (~line 1315) and `canUnnestInExprDepth`'s correlated branch (~line 1143, which would need a `Negated` NullAware gate mirroring the non-correlated one). (2) instrument `unnestSubqueriesInPlan`/`findFilterContainingInExpr` against the REAL `bench/tpch/runtime_goopg/data` Q16 plan (not a synthetic probe) to see which pass runs first / why the Filter-find or gating fails on that specific tree shape — possibly an NLI (`nl_index_join.go`) conversion or join-reorder interaction that runs before or interferes with the unnest pass on that particular index-scan-heavy plan. | (1) is a pre-existing gap discovered, not caused, by this loop's fix — correctly scoping it needs per-group NULL/emptiness tracking (a "hashed subplan"-style rewrite), a materially larger and separate piece of work from the non-correlated single-global-build case this loop closed; one task per loop. (2) is an optimizer-coverage gap, not a correctness bug (Q16 safely keeps using the pre-existing, still-correct runtime-cache path) — investigating why one specific real-schema query's plan shape evades the new fast path is lower priority than a correctness fix and was left for a dedicated loop rather than risk widening this loop's already correctness-sensitive scope. |
| resolved | 2026-07-09 | M0122-0011 (Q16 non-triggering-unnest re-investigation, resolves item (2) of the `NOT IN` anti-semi-join-unnesting row above — "the new unnest path is NOT triggering for Q16's real join shape") | Re-ran the item (2) resume point (plan Q16 against a schema mirroring the real HammerDB-equivalent one: PK btree indexes on part/partsupp/supplier plus SF1-magnitude `TableStats.RowCount`, matching `index_utilisation_test.go`'s `hammerdbPKs`) — the `NullAware` Anti join unnest fires correctly. Went one step further than the original resume point asked: also planned Q16 against a bare `tpch.Catalog()` with NO indexes/stats at all (throwaway probe, reverted) — the unnest fires there too, and checked out every commit across the whole M0122-0011 chain (`be47cc93` through `ef323e88`) with no revision found where Q16 fails to unnest. The original "not triggering" observation could not be reproduced against any committed planner revision. | None — no planner defect exists to fix; the item is refuted, not fixed. Landed `internal/planner/q16_unnest_test.go` (`TestPlanQ16NotInUnnestsWithRealSchema`) as a permanent regression guard pinning the now-confirmed-correct behavior against a realistic schema, so a future genuine regression here is caught immediately instead of requiring another multi-loop investigation. | N/A — fully resolved, no further resume point. If a future nightly/live run again shows Q16's `InExpr` surviving as a residual filter, suspect a stale/un-rebuilt binary first (the leading hypothesis this loop settled on for the original 2026-07-09 observation) before re-opening planner-side investigation. | The original observation was most likely a stale/un-rebuilt `cmd/goopg` binary at observation time (the live EXPLAIN was captured against a real running server, not a freshly rebuilt one, per the original row's phrasing) rather than a planner defect — every committed revision plans it correctly. |
| resolved | 2026-07-09 | M0122-0009 (WAL segment recycling, closes the "we delete instead of recycle-by-rename" gap named in `internal/wal/retention.go`'s file header) | `Writer.RemoveOldSegments`/`state.removeOldSegments` now recycle up to `ceil(Config.MinWALSize/SegmentSize)` of the newest obsolete WAL segments (zero-fill + rename into the lowest free future segment slot) instead of always unlinking; `Config.MinWALSize` wired from the previously-unread `min_wal_size` GUC. See `docs/design/0122-0009-wal-segment-recycling.md`. | Only the `min_wal_size` floor half of upstream's `XLOGfileslop` (`postgres/src/backend/access/transam/xlog.c:2230`) sizing formula is implemented. The checkpoint-distance-estimate term (`CheckPointDistanceEstimate`/`CheckPointCompletionTarget`, which lets upstream recycle further ahead than the floor under high write volume) and the `max_wal_size` ceiling on the recycle target are both unimplemented — `spares` is driven by `MinWALSize` alone. | `internal/wal/writer.go`'s `removeOldSegments`: replace the flat `spares` computation with upstream's `XLOGfileslop` shape (min/max segno bounds plus a distance estimate derived from observed inter-checkpoint WAL volume, which the checkpointer doesn't currently track anywhere reusable — would need a new running-average hook similar to `CheckPointDistanceEstimate`'s exponential moving average in `xlog.c`). | The checkpoint-distance estimate needs new state (an EMA of inter-checkpoint WAL volume) that doesn't exist anywhere in goopg yet — a materially separate, smaller feature in its own right, not a natural extension of this loop's rename/zero-fill mechanism. The `min_wal_size`-alone floor is a safe, correctness-preserving simplification (see the design doc's Non-goals section for why the missing `max_wal_size` cap can't cause a correctness issue, only a larger-than-upstream recycled-spare footprint). One task per loop. |
| resolved | 2026-07-09 | M-NIGHTLY (AI-20260709-010336-082, 3rd pgbench reopen) | Fixed a real cross-connection race in `internal/access/btree/btree_vacuum.go`'s `unlinkEmptyLeaf`/`unlinkEmptyLeafFPI`: both compute `leftLive`/`rightLive` via an unlocked `liveSibling` pre-pass, then apply those STALE values verbatim as the sibling relink writes. `bt.splitMu` (held for the whole function) is per-*BTree-Go-instance and each backend opens its own `*BTree` per statement, so it does NOT serialize against a concurrent Insert-driven split on a DIFFERENT connection's `*BTree` for the SAME relation — that split's OWN sibling relink (in `insertIntoBlock`) re-reads fresh under its own `pinW` immediately before writing and is correct in isolation, but VACUUM's later blind write then stomps it back to the pre-split neighbour. This exactly reproduces the confirmed on-disk symptom from the working-set writeup (block 678 `btpo_prev` stuck at 677 when the true chain was 677->15798->678). Fix: both relink sites now re-derive the live neighbour from the sibling block's CURRENT on-disk Prev/Next (a fresh `liveSibling` walk) INSIDE the same `pinW` hold that performs the write, self-correcting if a concurrent split raced ahead — a no-op if nothing raced. Verified via fresh repro (init s10, `pgbench -c 60 -j 12 -T 30` racing an explicit `VACUUM pgbench_accounts` loop every 0.3s) — 0 failed transactions and no more Prev/Next-mismatch findings from `bt_index_check`/direct block dump on that class. go test ./internal/access/btree/... ./internal/amcheck/... ./internal/executor/... and scripts/tpch-spotcheck.sh (Q12=2/Q13=33) all pass. | Fresh repro under the fix surfaced a DIFFERENT, still-open corruption class in the SAME run: `bt_index_check` now reports `high key invariant violated ... block 4026`. Direct dump confirmed block 4026 is an INTERNAL (Level=1) page whose LAST downlink key (`800ed0d5`) exceeds its own HighKey (`800cfbe7`) — all 246 preceding keys are correctly < HighKey. This is the Lehman-Yao "move right" gap: `insertIntoBlock` (`internal/access/btree/btree.go:2114`), used both for the leaf fast path and for the RECURSIVE parent-downlink insert after a child split, never checks the target block's current HighKey against the item being inserted before deciding pageHasSpaceFor/inserting — if the target page (leaf OR the ancestor named in a stale `path[]`) was concurrently split by another connection's `*BTree` instance between when the caller decided this block was correct and when `pinW`+insert actually runs, the item can land on a now-too-narrow page, silently exceeding its high key instead of following PG's real protocol of stepping right to the actual owning sibling first. | `internal/access/btree/btree.go`'s `insertIntoBlock` needs a move-right pre-check right after `pinW(blk)`/before `pageHasSpaceFor`: if `op.HasHighKey() && op.Next != InvalidBlockNumber && btree.CompareKeys(it.key, op.HighKey) > <leaf:0/internal:-1 boundary>`, unpin and re-descend/step to `op.Next` (following the sibling chain) before retrying, instead of inserting in place. Needs the same treatment for the leaf fast path (`tryInsertNoSplit`/`tryInsertOnCachedRightmost`) and the parent-recursive call site — grep `func.*insertIntoBlock`/`tryInsertNoSplit`/`tryInsertOnCachedRightmost` in `internal/access/btree/btree.go`. Repro recipe: `pgbench -i -s 10 --no-vacuum` then `pgbench -c 60 -j 12 -T 30` racing a `VACUUM pgbench_accounts` loop (every 0.3s) on a fresh data dir; verify with `bt_index_check('pgbench_accounts_pkey'::regclass, true)` after a clean stop. | Root cause is architectural (missing Lehman-Yao move-right protocol across ALL structural-insert entry points, not a single call site) and `bt.splitMu`'s cross-connection non-serialization (a separately-confirmed prior-loop finding) is the enabling condition for every variant found so far (sibling-relink stomp this loop, high-key overrun this loop's repro) — fixing this loop's one task (VACUUM relink) already required careful scoping; the move-right fix touches the hottest insert path and needs its own dedicated loop with fresh repro+verify, not tacked onto this one. |
| resolved | 2026-07-09 | M0122-0007 | Foreign-server (`CREATE`/`DROP SERVER`) restart durability via `wal.RecordKindCreateForeignServer`/`RecordKindDropForeignServer` (126/127) + `internal/initdb/foreignserver_ddl_recovery.go`; `DROP SERVER`/`DROP FOREIGN DATA WRAPPER ... CASCADE` repointed at the durable `foreignServers` registry instead of the non-durable `compatObjects` bookkeeping (see fix_plan.md M0122-0007 for full write-up). | `CREATE USER MAPPING FOR <user> SERVER <server>` (`catalog.InMemory.userMappings`, keyed by `"<user>\x00<server>"`) has zero WAL wiring — a user mapping still vanishes on restart even though its referenced server now survives. Deliberately left out of this loop's scope: a mapping's restart durability is only useful once its server also survives a restart (now true), not vice versa, so this was the correct fix to land first. | `internal/executor/operators_ddl.go`'s `case "user mapping":` (CREATE, ~line 15566) and `case "user mapping":` (DROP, ~line 14971) call sites; mirror the foreign-server recipe exactly — new WAL record kinds (128/129 next free), `catalog.InMemory.RegisterUserMappingDuringRecovery`/`UnregisterUserMappingDuringRecovery`, `internal/initdb/usermapping_ddl_recovery.go`. | Kept this loop's blast radius to a single registry (foreign servers) per the M0122 per-task design-review discipline; user mappings are a distinct registry with their own key shape (composite user+server, not a single name) that deserves its own design-doc pass rather than being folded in silently. **Resolved same day** — see the next row. |
| resolved | 2026-07-09 | M0122-0007 (user-mapping registry restart durability, closes the resume point in the row above) | `catalog.InMemory.userMappings` now survives a restart, mirroring the foreign-server fix exactly: new `wal.RecordKindCreateUserMapping`/`RecordKindDropUserMapping` (128/129) + `internal/initdb/usermapping_ddl_recovery.go`'s `replayUserMappingDDLRecords`; `catalog.InMemory.RegisterUserMappingDuringRecovery`/`UnregisterUserMappingDuringRecovery` re-populate the registry with the original OID; `execCompatCreate`'s `case "user mapping":` now WAL-emits on `CREATE USER MAPPING`, and the DROP USER MAPPING site emits `EncodeDropUserMapping` the same way. Unlike the foreign-server fix, no design-review correctness gap was found — `DROP USER MAPPING` already gated on the durable `userMappings` registry, not `compatObjects`, so nothing needed repointing. Tests: `TestEncodeDecodeCreateUserMappingRoundTrip`/`TestEncodeDecodeDropUserMappingRoundTrip`/`TestDecodeUserMappingRejectsWrongKindAndTruncatedPayload` (`internal/wal`), `TestRegisterUserMappingDuringRecoveryPreservesOID`/`TestUnregisterUserMappingDuringRecoveryRemovesEntry` (`internal/catalog`), `TestUserMappingDDLRecoveryReplaysCreate`/`TestUserMappingDDLRecoveryReplaysDropAfterCreate`/`TestReplayUserMappingDDLRecordsHandlesMissingWalDir` (`internal/initdb`, full `Init`+`Open`+restart+`Open` cycle). Live-verified against a real `cmd/goopg` binary across two consecutive restarts: `CREATE USER MAPPING` survives the first restart (same `umid`/`srvid`/`umoptions`), `DROP USER MAPPING` survives the second (0 rows, not just the create side). Gates: `go build ./...`/`go vet ./internal/wal/... ./internal/catalog/... ./internal/initdb/... ./internal/executor/...` clean; `go test` on the same four packages PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke scripts/ralph-precommit-test.sh` PASS (0 failed transactions, all 3 workloads). | None identified — this closes the named resume point. `ALTER USER MAPPING` (option changes) has no parser support yet and is out of scope. | N/A — fully closed, no further deferral. |  |
| resolved | 2026-07-09 | M0122-0009 (max_wal_size ceiling + CheckPointDistanceEstimate, closes the resume point in the row above naming this exact gap) | New `computeSpareSegments` (`internal/wal/writer.go`) ports upstream's `XLOGfileslop` (xlog.c) sizing formula as segment counts relative to the retention keep-segment; new `Checkpointer.updateCheckPointDistanceEstimate`/`CheckPointDistanceEstimate()` (`internal/wal/checkpointer.go`) ports `UpdateCheckPointDistanceEstimate`'s jump-up/decay-slowly EMA verbatim, updated every `runCheckpoint` cycle from the redo-LSN delta. New `Writer.RemoveOldSegmentsWithEstimate` + `SlotAwareRetainer.CheckPointDistanceEstimateFn`/`CompletionTarget` (`internal/wal/retention.go`) wire it through; `cmd/goopg/main.go` reads `max_wal_size` (new `initdb.OpenOptions.WALMaxSize` → `wal.Config.MaxWALSize`) and `checkpoint_completion_target` and wires `rt.Checkpointer.CheckPointDistanceEstimate` into the retainer. `RemoveOldSegments` (the pre-existing public API) is unchanged behaviourally — it now forwards to the same formula with both new inputs zeroed, which is proven to reduce to the original `ceil(MinWALSize/SegmentSize)` floor exactly. Tests: `TestComputeSpareSegmentsMatchesMinWALSizeFloorWhenNoEstimate`/`TestComputeSpareSegmentsGrowsWithDistanceEstimate`/`TestComputeSpareSegmentsCapsAtMaxWALSize`/`TestRemoveOldSegmentsWithEstimateHonoursDistanceAndMax`/`TestSlotAwareRetainerUsesCheckPointDistanceEstimateFn` (`internal/wal/retention_test.go`), `TestCheckpointerUpdatesCheckPointDistanceEstimate` (`internal/wal/checkpointer_test.go`). Design: `docs/design/0122-0009-wal-segment-recycling.md` new "Follow-up (2026-07-09)" section; `docs/design/README.md` row updated. Gates: `go build ./...`/`go vet ./...` clean; `go test`/`go test -race ./internal/wal/...` PASS; `go test ./internal/initdb/... ./cmd/goopg/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke scripts/ralph-precommit-test.sh` PASS (0 failed transactions, all 3 workloads). | None identified — this closes the named resume point, the last open sizing gap under M0122-0009. Timeline-aware recycling (`RemoveNonParentXlogFiles`) and a `wal_recycle` opt-out GUC remain out of scope per the design doc's Non-goals (pre-existing, unrelated to this loop). | N/A — fully closed, no further deferral for this specific sizing gap. |  |
| resolved | 2026-07-09 | M0122-0010 | `internal/access/btree/btree_vacuum.go`: fixed `unlinkEmptyInternalPage`/`unlinkEmptyInternalPageFPI`'s stale sibling-relink (the same cross-connection race already fixed for LEAF pages in `unlinkEmptyLeaf`/`unlinkEmptyLeafFPI` under AI-20260709-010336-082, now closed for internal pages too — both now re-derive the live neighbour via a fresh `liveSibling` walk under the write-side `pinW`, not the value captured by the unlocked pre-pass). Regression test `TestUnlinkEmptyInternalPagePreservesConcurrentSplice` (`internal/access/btree/btree_vacuum_internal_race_test.go`) deterministically simulates the race (captures real prev/next, splices a synthetic live page in between, invokes the unlink with the stale prev/next) — confirmed non-vacuous via `git stash` (fails pre-fix). Design doc `docs/design/0055-0003-btree-page-deletion-and-recycling-protocol.md` §2.5 added; `docs/design/README.md` row extended. | `applyParentDownlinkRemoval` (`internal/access/btree/btree_vacuum.go`, shared by both the leaf and internal-page unlink paths) removes the parent's downlink purely by a previously-captured slot INDEX (from `resolveParentDownlink`/`findDownlinkSlotInParent`), with no re-validation at write time that the item still at that index is the intended child's downlink. This is the SAME index-drift race AI-20260706-201855-001 fixed for the intra-instance case (there `splitMu` closed it, since both racing operations shared one `*BTree` instance) — but `splitMu` never serialises across connections (established repeatedly this month), so a concurrent split on a DIFFERENT connection's instance that inserts a new downlink into the SAME parent page ahead of the captured slot can still make `applyParentDownlinkRemoval` delete an unrelated live child's downlink instead of the intended one. | `internal/access/btree/btree_vacuum.go`'s `applyParentDownlinkRemoval`: change its signature to take the target `childBlk` (not just a slot index) and re-scan/re-locate the matching `it.ptr.Block == childBlk` item under the same `pinW` that performs the removal (mirroring this loop's sibling-relink fix pattern), falling back to a no-op if the block is no longer present (already removed by a racing unlink). Both call sites (`unlinkEmptyLeaf`'s WAL path and `unlinkEmptyInternalPage`'s WAL path) need the signature change; the FPI fallbacks (`unlinkEmptyLeafFPI`'s `removeDownlinkFromParent`, `unlinkEmptyInternalPageFPI`'s same call) already re-locate by block match via `findParentDownlinkByBlock`/`removeDownlinkFromParent`, so only the two WAL-record-emitting paths are exposed. Repro recipe: same class as AI-20260709-010336-082's fresh-repro pattern (isolated port, `pgbench -i -s 10 --no-vacuum` then `pgbench -c 60+ -j 12+ -T 30` racing an explicit `VACUUM` loop every 0.3s), verify via `bt_index_check` for an orphaned-live-child symptom (a live page whose downlink is missing from its parent, or a duplicate/garbled parent item list) rather than the sibling-link symptom this loop's repro targeted. | Fixing the WAL-emitting `applyParentDownlinkRemoval` path needs a signature change touching both its callers plus careful handling of the "index already stale, block not found" no-op case (crash-replay idempotency here overlaps with the `resolveParentDownlink`/`findDownlinkSlotInParent` control-field contract the WAL record carries) — bigger than this loop's bounded sibling-relink scope; kept ONE task per loop per project discipline. |
| resolved | 2026-07-09 | M0122-0007 (physical-storage-isolation slice 4b-i — namespace the table/index maps internally, second piece of the "per-database catalog namespace" epic) | `catalog.InMemory`'s `tables`/`indexes`/`byTable` maps now live behind a `namespaces map[uint32]*tableNamespace` keyed by dbOid and a `(c *InMemory) ns(dbOid uint32) *tableNamespace` accessor; all 226 direct in-file references (+27 in same-package white-box tests) replaced with `c.ns(DefaultDBOid).tables`/etc. | The public catalog entry points (`LookupTable`, `CreateTable`, `DropTable`, `LookupIndex`, `CreateIndex`, `DropIndex`, `RenameTable`, `RenameIndex`, `AllTables`, `AllIndexes`, `TablesInSchema`, `RegisterRealTable`, `TryRegisterUserTable`, OID-keyed lookups, ...) still take no `dbOid` parameter and every one of their hundreds of callers in `internal/executor`/`internal/planner` still gets routed to `DefaultDBOid` implicitly via `ns()`'s hardcoded argument inside catalog.go — so every database on a goopg server still shares one physical table/index namespace; `CREATE DATABASE ... TEMPLATE` still cannot copy anything meaningful and a dump+restore round-trip into a second database still collides on the first schema-level name (same user-visible gap as the slice-2/M0110-0001 rows above). | `docs/design/0122-0018-per-database-catalog-namespace.md` sub-slice "4b-ii — Give catalog entry points an explicit dbOid parameter": thread an explicit `dbOid uint32` parameter through every public entry point listed under "Blast radius" (now backed by `c.ns(dbOid)` from 4b-i), update every external caller to pass `catalog.DefaultDBOid` (behavior-preserving), then 4c wires `ctx.CurrentDatabaseOid` through the read paths for real. Closed by the 4b-ii row appended below (2026-07-09, same day) — landed via a variadic parameter instead of a required one, so the literal "update every external caller" text above turned out unnecessary; see that row. | Deliberately split out of the original single "4b" scope (see design doc's 4b-i/4b-ii split note) — threading an explicit `dbOid` parameter through dozens of public functions and hundreds of external call sites across package boundaries is a second, independently-large mechanical pass; landing it half-migrated in the same loop as the internal data-structure swap would be strictly worse (silently wrong) than finishing 4b-i cleanly and starting 4b-ii from an honest 0% in its own pass. |
| resolved | 2026-07-09 | M0122-0007 (physical-storage-isolation slice 4b-ii — give catalog entry points a dbOid parameter, third piece of the "per-database catalog namespace" epic) | All 17 blast-radius entry points (`LookupTable`, `CreateTable`, `DropTable`, `LookupIndex`, `CreateIndex`, `DropIndex`, `RenameTable`, `RenameIndex`, `AllTables`, `AllIndexes`, `TablesInSchema`, `RegisterRealTable`, `TryRegisterUserTable`, `LookupTableByOID`, `LookupIndexByOID`, `InheritanceChildren`, `PartitionChildren`) gained a trailing variadic `dbOid ...uint32` parameter backed by a new `resolveDBOid(dbOid []uint32) uint32` helper, instead of the originally-planned required parameter + hundreds of mechanically-edited external call sites (a measured `grep` pass found 800+ non-test call sites across all 17 — `LookupTable` alone has 300+). The `Catalog` interface's 9 overlapping methods and its two implementers (`*InMemory`, `*SearchPathCatalog`) got the same treatment. New `TestDBOidParameterRoutesToDistinctNamespace` (`internal/catalog/dbid_namespace_test.go`) proves two dbOid-keyed namespaces are genuinely isolated. Zero observable behavior change — no caller anywhere passes a non-default dbOid yet. | Every database on a goopg server still shares one physical table/index namespace in practice: `internal/executor`/`internal/planner`'s hundreds of call sites still omit the now-optional `dbOid` argument, so they all still implicitly resolve to `DefaultDBOid`. `CREATE DATABASE ... TEMPLATE` still cannot copy anything meaningful and a dump+restore round-trip into a second database still collides on the first schema-level name (same user-visible gap named in every prior row of this bucket) — 4b-ii only made the routing *possible*, it did not make any read or write path actually use it. | `docs/design/0122-0018-per-database-catalog-namespace.md` sub-slice "4c — Route READ paths through the connection's real dbOid": thread `ctx.CurrentDatabaseOid` (available since 4a, `internal/executor/context.go`) through the analyzer/planner's name-resolution call sites (`LookupTable`, `LookupIndex`, schema-qualified name resolution) instead of omitting the argument. Audit embedded/test callers with no live connection (empty `CurrentDatabase`/zero `CurrentDatabaseOid`) to fall back to `DefaultDBOid` explicitly. Then 4d does the same for write paths + `RelFileNode.DBOid`. | 4c/4d are explicitly the sub-slices with real observable behavior change (the design doc's own ordering) — landing them requires auditing every name-resolution call site for the zero-value/no-connection fallback case, which is genuinely separate, riskier work from 4b-ii's zero-behavior-change plumbing; keeping this loop to exactly 4b-ii avoids conflating "the parameter now exists" with "the parameter is now used for real routing", which the design doc's own "Recommended order" section explicitly warns against skipping ahead on. |
| resolved | 2026-07-10 | M0122-0007 4d-ii-part-2b item 1 (cross-file `IndexesOnTable` sweep) | Threaded `catalog.NamespaceDBOid(ctx.CurrentDatabaseOid)` through every remaining un-scoped `IndexesOnTable` call site: `operators_fk.go` (1), `operators_cluster.go` (3), `operators_reindex.go` (2), `deferred_unique.go` (1), `context.go` (1), `operators_vacuum.go` (1), `operators_upsert.go` (4), `ssi.go` (5), `operators_storage.go` (5), and the remaining ~24 sites in `operators_ddl.go` left over from item 3's narrower scope. `expr.go`'s `buildForeignKeyDefString` (pg_get_constraintdef's FK branch) gained a variadic `dbOid ...uint32` param threaded from `ctx.CurrentDatabaseOid` at its one production call site. Also added `catalog.SearchPathCatalog.IndexesOnTable` (mirroring the existing `LookupTable`/`LookupIndex` overrides) — `SearchPathCatalog` previously had no override for it at all, so every `internal/planner` call site that receives only a `catalog.Catalog` interface value (all of them go through `ctx.PlanCatalog`/`ctxPlanCatalog`, which — per the prior row's fix — is now always a dbOid-seeded `SearchPathCatalog`) silently promoted straight to the embedded `InMemory.IndexesOnTable` with no dbOid argument, always resolving DefaultDBOid regardless of the connection's real database. This ONE addition transparently fixes all 6 planner-package call sites (`nl_index_join.go:655`, `planner.go:7379,8108,8225,11196,11400`) without touching planner.go itself — e.g. `resolveDefaultDoNothingArbiter` (bare `ON CONFLICT DO NOTHING`'s implicit-arbiter resolution). Verified as a real bug (not a doc claim) via new test `TestPlanUpsertDoNothingNoTargetFindsArbiterUnderDistinctDBOid` (`internal/executor/operators_upsert_test.go`), confirmed to fail (`ArbiterIndex` stays nil) against a revert of just the `SearchPathCatalog.IndexesOnTable` addition. | `internal/executor/applyworker.go`'s `primaryKeyOnlyRow` (line ~662) still calls `cat.IndexesOnTable(tbl)` with no dbOid — left untouched because the whole logical-replication apply-worker path is uniformly un-migrated for dbOid-awareness (its sibling `w.cat.LookupTable` call at line 217 also carries no dbOid argument; `NewApplyWorker` receives a bare `cat catalog.Catalog`, never a per-subscription-dbOid-seeded `SearchPathCatalog`), so threading only this one call would be a partial, inconsistent fix. Item 2 (`RelFileNode.DBOid` wiring at creation time) also remains fully unimplemented. | For applyworker.go: give `ApplyWorker` a per-subscription dbOid concept first (there is none today), then thread it through `w.cat.LookupTable`/`primaryKeyOnlyRow`/every other `w.cat.*` call in `internal/executor/applyworker.go` together, not piecemeal. For item 2: `internal/catalog/catalog.go`'s `NewInMemory`'s `dbOid: DefaultDBOid` seed and the `base/1/`+`base/5/` dual-mirror, per the design doc's "Blast radius" section. | applyworker.go wasn't in item 1's original file list (`operators_fk.go`/`operators_cluster.go`/`operators_reindex.go`/`operators_sequence.go`/`operators_storage.go`/`operators_pg_get_publication_tables.go`/DML operators) and logical replication's dbOid-awareness is a materially larger, structurally different gap (no per-subscription dbOid concept exists anywhere in the apply-worker path yet) — deferred rather than bolted on inconsistently. `operators_sequence.go` and `operators_pg_get_publication_tables.go` were re-measured this loop and have zero `IndexesOnTable`/`AllUserViews`/`AllUserMatViews` call sites, so they needed no change. |
| resolved | 2026-07-10 | M0122-0007 4d-ii-part-2b item 1's `applyworker.go` corner | Gave `ApplyWorker` a per-subscription dbOid concept and closed the loop's own prior resume point. `catalog.Subscription` gained a `DBOid uint32` field, set by `PubSub.CreateSubscriptionAsOwner`'s new variadic `dbOid ...uint32` param (mirrors `CreateTable`/`CreateIndex`); `executor.execCreateSubscription` passes `catalog.NamespaceDBOid(ctx.CurrentDatabaseOid)`; `wal.EncodeCreateSubscription`/`DecodeCreateSubscription` gained a trailing 4-byte `dbOid` field (backward-compatible: a pre-existing WAL record with no trailer decodes as `dbOid=0`) so `internal/initdb/pubsub_ddl_recovery.go`'s replay restores it. Rather than threading a `dbOid` arg through every `w.cat.*` call site in `internal/executor/applyworker.go` individually, `server.applyWorkerCatalog` (new helper, `internal/server/applylauncher.go`) wraps `cfg.Catalog` in a `catalog.SearchPathCatalog{DBOid: sub.DBOid}` at `DefaultLaunchApplyWorker`'s one `executor.NewApplyWorker` call site — every un-dbOid-threaded `LookupTable`/`LookupIndex`/`IndexesOnTable` call inside applyworker.go then transparently resolves the subscribing database's own tables via the override item 1's planner-package fix already added, with zero changes to applyworker.go itself. New tests: `TestApplyWorkerAppliesInsertUnderDistinctSubscriptionDBOid` (`internal/executor/applyworker_test.go`, confirmed to fail — the applied row silently lands in the wrong DefaultDBOid same-named table — against a revert of just the `SearchPathCatalog` wrap), `TestCreateSubscriptionRoutesToConnectionRealDBOid` / `TestCreateSubscriptionPostgresConnectionStaysOnDefaultDBOid` (`internal/executor/operators_ddl_pubsub_test.go`), `TestApplyWorkerCatalogSeedsSubscriptionDBOid` (`internal/server/applylauncher_test.go`). 4d-ii-part-2b is now fully landed (all 3 items); only 4e remains. | None for this item — fully landed. Unrelated pre-existing hazards reconfirmed while gating (not fixed, not new): `go test -race ./internal/server/...` `TestConnectExceedsPositiveDatconnlimitRejected` data race (already documented, `internal/activity/registry.go`); `TestStripeAppendConcurrentDrainConsistency` (`internal/wal`) is a pre-existing timing-flaky test, passes in isolation. | `docs/design/0122-0018-per-database-catalog-namespace.md`'s "4d-ii-part-2b" section item 1, and the doc's status line / "Recommended order" section — 4e (dependent-object-walk fixups + `CREATE DATABASE ... TEMPLATE` copy mechanism) is the next resume point. | This closes 4d-ii-part-2b entirely (all 3 items now landed), matching the design doc's "Recommended order" — 4e is next, per the "ONE task per loop" rule this loop stops here rather than starting 4e cold. |
| resolved | 2026-07-10 | M0122-0007 4e (sequence ownership) | Namespaced `seqRegistry`'s key scheme by dbOid (`seqKey` composes `"<dbOid>:<name>"`; `seqState` gained an immutable `dbOid` field). All ~18 public entry points in `internal/executor/operators_sequence.go` gained the standard trailing `dbOid ...uint32` convention (new `resolveSeqDBOid` helper mirroring `catalog.resolveDBOid`); the two Range-based scans (`FindSequenceOwnedBy`, `DropSequencesOwnedByTable`) plus `AllSequenceInfos` now filter by dbOid. Threaded all ~45 call sites across `operators_ddl.go`, `operators_tx.go` (`SeqRestoreEntry` gained a `DBOid` field), `expr.go`, and `operators_sequence.go`'s own `eval*`/`autoGenerateSerialValues` (new `ctxSeqDBOid` helper). Threaded the `DEFAULT nextval(...)` expression-evaluator path (`operators_generated.go`'s `evalGenExpr`/`evalGenFuncCall`/`applyDefaultsForMissing`) from its 3 real call sites, including a new `ApplyWorker.dbOid()` helper reading `w.cat`'s `SearchPathCatalog.DBOid`. `wal.SequenceStatePayload` gained a trailing `DBOid` field and `EncodeDropSequence`/`DecodeDropSequence` gained a trailing 4-byte dbOid, both backward-compatible (missing trailer = dbOid 0 → DefaultDBOid via NamespaceDBOid). `internal/initdb/sequence_ddl_recovery.go`'s WAL-replay dedup map re-keyed by dbOid for the same collision reason. New tests `TestSerialSequenceDoesNotAliasAcrossDistinctDBOid`/`TestDropTableDoesNotCascadeSequenceAcrossDistinctDBOid` (`internal/executor/sequence_dbid_routing_test.go`), each confirmed to fail against a revert of just `seqKey`'s dbOid component. | `computeGeneratedColumns`'s ~14 call sites (operators_merge.go/operators_storage.go) were deliberately left zero-arg dbOid — a `GENERATED ALWAYS AS (...) STORED` expression must be IMMUTABLE in PostgreSQL and can never legally contain `nextval()`, so this is a documented non-gap, not an oversight. `catalog.InMemory.constraintViewDeps` (view constraint-dependency tracking) remains 4e's only unthreaded item. | `internal/catalog/catalog.go`'s `constraintViewDeps map[string][]string` (near line 2280) — see design doc 4e section, "Not yet threaded". | Sequence ownership is now fully landed and gate-verified; view-constraint-dep tracking is its own bounded future loop per the "ONE task per loop" rule. |
| resolved | 2026-07-10 | M0122-0007 4e (sequence ownership follow-on) | Same loop as above. | `pg_sequence`/`pg_sequences` virtual-row builders (`internal/catalog/catalog.go`'s `pgSequence.VirtualRows`, `internal/initdb/pg_sequences_view.go`, `internal/initdb/information_schema_sequences_view.go`) still call `SequenceParamsFunc`/`AllSequenceInfos` with no dbOid and are hardcoded to `c.ns(DefaultDBOid)`, so `SELECT * FROM pg_sequences`/`pg_sequence` on a distinct-dbOid connection still only sees DefaultDBOid's sequences — unchanged from pre-4e behavior, not a regression, but not fixed either. | `internal/catalog/catalog.go`'s `pgSequence.VirtualRows`/`pg_sequences_view.go`/`information_schema_sequences_view.go` — needs the same "per-connection virtual catalog scoping" mechanism already solved for other virtual tables (thread DBName → Context, a per-connection lister in `valuesOp.Open`; see `[[per_connection_virtual_catalog_scoping]]` memory), applied to these two VirtualRows closures which currently have no per-connection context at all (`func() [][]string` with zero args). | These closures' zero-arg signature is a structurally different, separate mechanism from `seqRegistry`'s key-scheme fix (which this loop's item was scoped to); bundling it in would have expanded scope well beyond "ONE task per loop". |
| resolved | 2026-07-10 | M0122-0007 4e (view constraint-dependency tracking) | Closed 4e's last remaining dependent-object-walk item (referenced as still-open in the two rows above). `internal/catalog/catalog.go`'s `constraintViewDeps map[string][]string` was keyed `"tableOID:constraintName"` (already dbOid-safe — `nextOID` is a single cluster-wide counter) but stored **bare, unqualified view names** as values; `UnregisterViewConstraintDeps` (called by `execDropOneView` on every `DROP VIEW`) matched and removed entries by bare name across every key in the whole map, so `DROP VIEW v` in database A silently erased a same-named, unrelated view `v`'s dependency entry in database B — a real cross-database data-corruption path defeating that other database's `DROP CONSTRAINT RESTRICT` protection (not merely a lookup miss). Fixed by qualifying the stored value: `RegisterViewConstraintDep`/`UnregisterViewConstraintDeps` (`internal/catalog/catalog.go`) gained the standard trailing `dbOid ...uint32` parameter (`resolveDBOid`), storing/matching `"<dbOid>:<viewName>"` instead of the bare name; `ViewsDependingOnConstraint` needed no signature change (a dependent view can only live in its table's own database, so `tableOID` alone already pins one database) but now strips the `dbOid:` prefix before returning, since RESTRICT-error callers only want the bare name. The two `internal/executor/operators_ddl.go` call sites (`execCreateView`'s `RegisterViewConstraintDep`, `execDropOneView`'s `UnregisterViewConstraintDeps`) now pass `catalog.NamespaceDBOid(o.ctx.CurrentDatabaseOid)`. New test `TestDropViewDoesNotEraseConstraintDepAcrossDistinctDBOid` (`internal/executor/ddl_write_dbid_routing_test.go`) creates a same-named view `v` under two distinct connection dbOids, drops one, and asserts the other's entry survives — confirmed to fail (deps erased to `[]`) against a scratch-copy revert of just the value-qualification (a neutered qualifier folding dbOid to a constant), isolating the key-scheme fix from the two mechanically-inert call-site threads. This closes 4e entirely except the `CREATE DATABASE ... TEMPLATE` copy mechanism itself. | None for this item — fully landed. The `CREATE DATABASE ... TEMPLATE` copy mechanism (4e's final remaining item, blocked on both this item and sequence ownership landing first) is unchanged/still open — it was never in this item's own scope. | `docs/design/0122-0018-per-database-catalog-namespace.md`'s "Recommended order and stopping points" section — `CREATE DATABASE ... TEMPLATE` (`CreateDatabaseUsingFileCopy`/`copydir`, `postgres/src/backend/commands/dbcommands.c`) is 4e's and the whole M0122-0007 slice-4 epic's final remaining resume point. | This item is now fully landed and gate-verified (matches the "resolved" convention of the sequence-ownership row above); the `CREATE DATABASE ... TEMPLATE` mechanism is a materially larger, structurally different feature (an actual file-copy DDL command) and is its own bounded future loop per the "ONE task per loop" rule. |
| resolved | 2026-07-10 | M0122-0007 4e (pg_indexes/pg_tables per-dbOid content, 2 of the ~13 sibling builders flagged by the row above) | Extracted `pg_indexes.VirtualRows` and `pg_tables.VirtualRows` (`internal/catalog/catalog.go`) into new exported `catalog.InMemory.PGIndexesRowsForDBOid(dbOid uint32)` / `PGTablesRowsForDBOid(dbOid uint32)` methods, parameterizing their `c.ns(DefaultDBOid)` references on `dbOid` — same closure-extraction pattern `PGClassRowsForDBOid` used (the closures themselves now just call `...RowsForDBOid(DefaultDBOid)`, byte-identical default behavior). Wired per-connection `executor.Context.PgIndexesRows`/`PgTablesRows func() [][]string` fields (mirroring `PgClassRows`), set in `internal/server/dispatch.go`'s `wireExtensionRows` (new `pgIndexesRowLister`/`pgTablesRowLister` interfaces), consumed by new `tbl.Name == "pg_indexes"`/`"pg_tables"` branches in `internal/executor/operators.go`'s `valuesOp.Open`. So both views now list the CONNECTING database's own indexes/tables, not always `DefaultDBOid`'s. New tests `TestPgIndexesRowsScopedToConnectionDBOid`/`TestPgTablesRowsScopedToConnectionDBOid` (`internal/executor/fk_dbid_routing_test.go`), mirroring `TestPgClassRowsScopedToConnectionDBOid`. Live end-to-end verified against a real `cmd/goopg` binary + real `psql`: `CREATE DATABASE freshdb2` → connect → `CREATE TABLE only_in_freshdb2 (id int PRIMARY KEY, ...)` → `SELECT * FROM pg_tables`/`pg_indexes WHERE schemaname='public'` in `freshdb2` shows only that table/its PK index; the same queries against `postgres` db show 0 rows (no cross-database leak either direction). Gates: `go build ./...`/`go vet ./internal/...` clean; `go test ./internal/catalog/... ./internal/executor/... ./internal/server/... ./internal/planner/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke scripts/ralph-precommit-test.sh` PASS (0 failed txns, all 3 workloads). | 11 sibling `VirtualRows` closures remain Bug-B-open (list `DefaultDBOid`'s objects regardless of connection): `pg_attrdef`, `pg_constraint`, `pg_inherits`, `pg_index`, `pg_statistic_ext`, `pg_policy`, `pg_depend`, `pg_trigger`, `pg_rewrite`, `information_schema.routines`/`parameters`/`routine_*_usage`, `pg_foreign_table`. `pg_sequence`/`pg_sequences`/`information_schema.sequences` remain separately flagged by the 2026-07-10 "sequence ownership follow-on" row. `pg_attribute`/`pg_type` (heap-backed) remain the structurally different, deeper gap noted by the row above. | For each of the 11 remaining `VirtualRows` closures: apply the exact same closure-extraction pattern this row and the `pg_class` row above used — pull the closure body into an exported `PGXxxRowsForDBOid(dbOid uint32)` method, parameterize its `c.ns(DefaultDBOid)` references, add a matching `executor.Context.PgXxxRows` field + `wireExtensionRows`-site wiring + `valuesOp.Open` branch. `pg_constraint`/`pg_depend`/`pg_index` are the next highest-value targets (pg_dump/psql `\d` catalog joins). | Continuing the "one small batch per loop" cadence the row above established; this loop did the two next-highest-value targets (`pg_tables`/`pg_indexes`, both directly probed by HammerDB's checkschema and psql's `\d`/`\dt`) named as the explicit next step in the prior loop's working_set.md, rather than all 11 remaining builders at once. |
| resolved | 2026-07-10 | M0122-0007 4e (pg_constraint per-dbOid content, next highest-value of the ~13 sibling builders flagged above) | Extracted `pg_constraint.VirtualRows` (`internal/catalog/catalog.go`, the largest of these builders — ~300 lines emitting CHECK / UNIQUE·PK·EXCLUDE / NOT NULL / FOREIGN KEY constraint rows across 4 separate passes) into a new exported `catalog.InMemory.PGConstraintRowsForDBOid(dbOid uint32) [][]string`, parameterizing its 5 `c.ns(DefaultDBOid)` table/index references on `dbOid` (same closure-extraction pattern `PGClassRowsForDBOid`/`PGIndexesRowsForDBOid` used); `c.domains` (domain CHECK constraints) intentionally stays global, matching the same not-yet-namespace-scoped precedent `PGClassRowsForDBOid` already documents for composite types. The registered closure now just calls `PGConstraintRowsForDBOid(DefaultDBOid)`, byte-identical default behavior. Wired per-connection `executor.Context.PgConstraintRows func() [][]string` (mirroring `PgIndexesRows`/`PgTablesRows`), set in `internal/server/dispatch.go`'s `wireExtensionRows` (new `pgConstraintRowLister` interface), consumed by a new `tbl.Name == "pg_constraint"` branch in `internal/executor/operators.go`'s `valuesOp.Open`. New test `TestPgConstraintRowsScopedToConnectionDBOid` (`internal/executor/fk_dbid_routing_test.go`), mirroring `TestPgIndexesRowsScopedToConnectionDBOid`: two tables each with a `PRIMARY KEY`, one under `DefaultDBOid` and one under a distinct dbOid, never cross-leak their `_pkey` constraint rows. Live end-to-end verified against a real `cmd/goopg` binary + real `psql`: `CREATE DATABASE freshdb3` → connect → `CREATE TABLE only_in_freshdb3 (id int PRIMARY KEY, val int CHECK (val > 0))` → `SELECT conname, contype, conrelid FROM pg_constraint` in `freshdb3` shows only that table's 3 constraints (pkey/check/not-null) at its own dbOid-local OID; the same query against `postgres` db shows only `postgres`'s own `only_in_postgres_*` constraints (no cross-database leak either direction). Gates: `go build ./...`/`go vet ./...` clean; `go test ./internal/catalog/... ./internal/executor/... ./internal/server/... ./internal/planner/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke scripts/ralph-precommit-test.sh` PASS (0 failed txns, all 3 workloads). | 10 sibling `VirtualRows` closures remain Bug-B-open (list `DefaultDBOid`'s objects regardless of connection): `pg_attrdef`, `pg_inherits`, `pg_index`, `pg_statistic_ext`, `pg_policy`, `pg_depend`, `pg_trigger`, `pg_rewrite`, `information_schema.routines`/`parameters`/`routine_*_usage`, `pg_foreign_table`. `pg_sequence`/`pg_sequences`/`information_schema.sequences` remain separately flagged by the 2026-07-10 "sequence ownership follow-on" row. `pg_attribute`/`pg_type` (heap-backed) remain the structurally different, deeper gap noted by the pg_class row above. | For each of the 10 remaining `VirtualRows` closures: apply the exact same closure-extraction pattern this row and the two rows above used — pull the closure body into an exported `PGXxxRowsForDBOid(dbOid uint32)` method, parameterize its `c.ns(DefaultDBOid)` references, add a matching `executor.Context.PgXxxRows` field + `wireExtensionRows`-site wiring + `valuesOp.Open` branch. `pg_depend`/`pg_index` are the next highest-value targets (pg_dump/psql `\d` catalog joins); `pg_inherits` is next after that for partition-child enumeration. | Continuing the "one small batch per loop" cadence the two rows above established; this loop did the single next-highest-value target (`pg_constraint`, directly probed by `pg_dump`'s `getConstraints`/`getDomainConstraints` and psql's `\d` constraint listing) named as the explicit next step in the prior loop's working_set.md, rather than all 11 remaining builders at once. |
| resolved | 2026-07-10 | M0122-0007 4e (pg_index per-dbOid content, next highest-value of the ~10 sibling builders flagged above) | Extracted `pg_index.VirtualRows` (`internal/catalog/catalog.go`) into a new exported `catalog.InMemory.PGIndexRowsForDBOid(dbOid uint32) [][]string`, parameterizing its `c.AllIndexes()`/`c.toastBearingTables()` calls on `dbOid` (same closure-extraction pattern `PGConstraintRowsForDBOid` used). `AllIndexes` already accepted a variadic `dbOid ...uint32` param (pre-existing, unused by this closure until now) so only its own call site needed threading; `toastBearingTables` gained a required `dbOid uint32` parameter (it had exactly one caller — this closure — so the signature change is not variadic like the newer convention, mirroring its lone-caller status) and its two `c.ns(DefaultDBOid)` references were parameterized. The registered closure now just calls `PGIndexRowsForDBOid(DefaultDBOid)`, byte-identical default behavior. Wired per-connection `executor.Context.PgIndexRows func() [][]string` (mirroring `PgConstraintRows`), set in `internal/server/dispatch.go`'s `wireExtensionRows` (new `pgIndexRowLister` interface), consumed by a new `tbl.Name == "pg_index"` branch in `internal/executor/operators.go`'s `valuesOp.Open`. New test `TestPgIndexRowsScopedToConnectionDBOid` (`internal/executor/fk_dbid_routing_test.go`), mirroring `TestPgConstraintRowsScopedToConnectionDBOid`: two tables each with a `PRIMARY KEY`, one under `DefaultDBOid` and one under a distinct dbOid, resolved via `LookupTable` and asserted never to cross-leak their pg_index `indrelid` rows. Live end-to-end verified against a real `cmd/goopg` binary + real `psql`: `CREATE DATABASE freshidx1` → connect → `CREATE TABLE only_in_freshidx1 (id int PRIMARY KEY, val int)` + `CREATE INDEX ... ON only_in_freshidx1(val)` → raw (uncast) `SELECT * FROM pg_index` in `freshidx1` shows exactly those 2 index rows (indrelid matching the table's own `pg_class.oid`, confirmed via a companion `pg_class` query); the same query against `postgres` db shows only `postgres`'s own `only_in_postgres_pkey` row (no cross-database leak either direction). Gates: `go build ./...`/`go vet ./...` clean; `go test ./internal/catalog/... ./internal/executor/... ./internal/server/... ./internal/planner/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke scripts/ralph-precommit-test.sh` PASS (0 failed txns, all 3 workloads). | 9 sibling `VirtualRows` closures remain Bug-B-open (list `DefaultDBOid`'s objects regardless of connection): `pg_attrdef`, `pg_inherits`, `pg_statistic_ext`, `pg_policy`, `pg_depend`, `pg_trigger`, `pg_rewrite`, `information_schema.routines`/`parameters`/`routine_*_usage`, `pg_foreign_table`. `pg_sequence`/`pg_sequences`/`information_schema.sequences` remain separately flagged by the 2026-07-10 "sequence ownership follow-on" row. `pg_attribute`/`pg_type` (heap-backed) remain the structurally different, deeper gap noted by the pg_class row above. | For each of the 9 remaining `VirtualRows` closures: apply the exact same closure-extraction pattern this row and the rows above used — pull the closure body into an exported `PGXxxRowsForDBOid(dbOid uint32)` method, parameterize its `c.ns(DefaultDBOid)` references, add a matching `executor.Context.PgXxxRows` field + `wireExtensionRows`-site wiring + `valuesOp.Open` branch. `pg_depend` is the next highest-value target (pg_dump/psql `\d` catalog joins); `pg_inherits` after that for partition-child enumeration. | Continuing the "one small batch per loop" cadence the rows above established; this loop did the single next-highest-value target (`pg_index`, directly probed by `pg_dump`'s index-metadata queries and psql's `\d`/`indexrelid::regclass` catalog joins) named as the explicit next step in the prior loop's working_set.md, rather than all 10 remaining builders at once. |
| resolved | 2026-07-10 | M0122-0007 4e (pg_attrdef/pg_depend per-dbOid content, next highest-value pair of the ~9 sibling builders flagged above) | Extracted both `pg_attrdef.VirtualRows` and `dependVirtualRows` (`internal/catalog/catalog.go`) TOGETHER, unlike every prior sibling row above — `dependVirtualRows`'s own doc comment already recorded that a SERIAL column's NORMAL ('n') attrdef→sequence `pg_depend` row must "agree on the oids" with `pg_attrdef`'s own numbering, both drawn from the shared `attrDefRowsLocked` helper, so scoping only one of the two views to a connecting dbOid would desync that shared numbering under a non-default database. `attrDefRowsLocked()` (hardcoded `c.ns(DefaultDBOid)`, two occurrences) became `attrDefRowsLockedForDBOid(dbOid uint32)`; new exported `catalog.InMemory.PGAttrdefRowsForDBOid(dbOid uint32) [][]string` and `PGDependRowsForDBOid(dbOid uint32) [][]string` (renamed from `dependVirtualRows`) both call it with the SAME dbOid, and `PGDependRowsForDBOid` also parameterizes its own two `c.ns(DefaultDBOid)` references (the sequence-ownership table/sequence enumeration). The two registered closures now just call `PGAttrdefRowsForDBOid(DefaultDBOid)`/`PGDependRowsForDBOid(DefaultDBOid)`, byte-identical default behavior. Wired new per-connection `executor.Context.PgAttrdefRows`/`PgDependRows func() [][]string` fields (mirroring `PgIndexRows`), set in `internal/server/dispatch.go`'s `wireExtensionRows` (new `pgAttrdefRowLister`/`pgDependRowLister` interfaces), consumed by new `tbl.Name == "pg_attrdef"`/`"pg_depend"` branches in `internal/executor/operators.go`'s `valuesOp.Open`. New tests `TestPgAttrdefRowsScopedToConnectionDBOid`/`TestPgDependRowsScopedToConnectionDBOid` (`internal/executor/fk_dbid_routing_test.go`), mirroring `TestPgIndexRowsScopedToConnectionDBOid`: two tables (one with a plain DEFAULT, one with a SERIAL PK) each under a distinct dbOid, never cross-leak their `pg_attrdef` rows (by `adrelid`) or their 'n'-deptype `pg_depend` attrdef→sequence rows (by `refobjid`). Live end-to-end verified against a real `cmd/goopg` binary + real `psql`: `CREATE DATABASE freshdep1` → connect → `CREATE TABLE only_in_freshdep1 (id serial PRIMARY KEY)` → raw `pg_attrdef` and `pg_depend WHERE classid=2604` in `freshdep1` each show exactly that table's own default/sequence OIDs; the same queries against `postgres` db show only `postgres`'s own rows (no cross-database leak either direction). Gates: `go build ./...`/`go vet ./...` clean; `go test ./internal/catalog/... ./internal/executor/... ./internal/server/... ./internal/planner/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke scripts/ralph-precommit-test.sh` PASS (0 failed txns, all 3 workloads). | 8 sibling `VirtualRows` closures remain Bug-B-open (list `DefaultDBOid`'s objects regardless of connection): `pg_inherits`, `pg_statistic_ext`, `pg_policy`, `pg_trigger`, `pg_rewrite`, `information_schema.routines`/`parameters`/`routine_*_usage`, `pg_foreign_table`. Confirmed collaterally during this loop's own live verification (not a regression — `pg_depend` was DefaultDBOid-only for every dbOid before this loop too): `pg_depend WHERE classid=1259` (the deptype='a' sequence-OWNED-BY row) from a fresh, distinct-dbOid database now correctly returns ZERO rows (no cross-db leak) instead of leaking `DefaultDBOid`'s row, but it is still missing its OWN row entirely, because that row class resolves through the package-level `catalog.SequenceParamsFunc(qualifiedName string) (SeqParams, bool)` — a function with NO dbOid parameter in its signature at all, keyed only by qualified name via the process-global `seqRegistry`/`LookupSequence`. This folds into the already-open "sequence ownership follow-on" row (the same `pg_sequence`/`pg_sequences`/`information_schema.sequences` gap), not a new row. `pg_attribute`/`pg_type` (heap-backed) remain the structurally different, deeper gap noted by the pg_class row above. | For each of the 8 remaining `VirtualRows` closures: apply the exact same closure-extraction pattern this row and the rows above used. `pg_inherits` is the next highest-value target (partition-child enumeration, `pg_dump`'s inheritance queries). Separately, whoever next picks up the "sequence ownership follow-on" row should also give `SequenceParamsFunc` a trailing `dbOid ...uint32` parameter (mirroring every other per-connection accessor's convention) so `PGDependRowsForDBOid`'s sequence-ownership loop can finally thread the connecting dbOid through it instead of hitting the global registry — that is the concrete resume point for closing this residual gap. | Continuing the "one small batch per loop" cadence the rows above established; this loop did the next-highest-value PAIR (`pg_attrdef`+`pg_depend`, done together rather than singly because their shared `attrDefRowsLocked` oid-numbering invariant made a single-view fix actively unsafe) named as the explicit next step in the prior loop's working_set.md, rather than all 9 remaining builders at once. |
| resolved | 2026-07-10 | M0122-0007 4e (pg_inherits per-dbOid content, follow-up 28, next highest-value single builder the follow-up 27 ledger row flagged) | Extracted `pg_inherits.VirtualRows` (`internal/catalog/catalog.go`) into new exported `catalog.InMemory.PGInheritsRowsForDBOid(dbOid uint32) [][]string`, parameterizing its two `c.ns(DefaultDBOid)` references (partition/legacy-inheritance table loop + partitioned-index loop) to `c.ns(dbOid)`; the registered closure now just calls `PGInheritsRowsForDBOid(DefaultDBOid)`, byte-identical default behavior. Wired new per-connection `executor.Context.PgInheritsRows func() [][]string` field (mirroring `PgDependRows`), set in `internal/server/dispatch.go`'s `wireExtensionRows` (new `pgInheritsRowLister` interface), consumed by a new `tbl.Name == "pg_inherits"` branch in `internal/executor/operators.go`'s `valuesOp.Open`. New test `TestPgInheritsRowsScopedToConnectionDBOid` (`internal/executor/fk_dbid_routing_test.go`), mirroring `TestPgDependRowsScopedToConnectionDBOid`: a `PARTITION BY RANGE`/`PARTITION OF` parent-child pair under each of two distinct dbOids, never cross-leak their `pg_inherits` rows (by `inhrelid`). Live end-to-end verified against a real `cmd/goopg` binary + real `psql`: `CREATE DATABASE freshinh1` → connect → `CREATE TABLE part_a (id int) PARTITION BY RANGE(id); CREATE TABLE part_a_p1 PARTITION OF part_a FOR VALUES FROM (1) TO (100)` → `freshinh1`'s `pg_inherits` shows exactly its own 1 row while `postgres`'s shows 0; then created a second, distinct partition pair (`part_b`/`part_b_p1`) in `postgres` and confirmed `postgres` now shows exactly its own 1 row while `freshinh1` still shows only its own unchanged row — no cross-database leak either direction. Gates: `go build ./...`/`go vet ./...` clean; `go test ./internal/catalog/... ./internal/executor/... ./internal/server/... ./internal/planner/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke scripts/ralph-precommit-test.sh` PASS (0 failed txns, all 3 workloads). | 7 sibling `VirtualRows` closures remain Bug-B-open (list `DefaultDBOid`'s objects regardless of connection): `pg_statistic_ext`, `pg_policy`, `pg_trigger`, `pg_rewrite`, `information_schema.routines`/`parameters`/`routine_*_usage`, `pg_foreign_table`. `pg_attribute`/`pg_type` (heap-backed) and the `oid::regclass` cast gap (observed again this loop — `inhrelid::regclass` on the live server printed the raw OID, not the table name, confirming that gap is still open and unrelated to this fix) remain separately flagged, as does the "sequence ownership follow-on" row (`SequenceParamsFunc` dbOid parameter). | For each of the 7 remaining `VirtualRows` closures: apply the exact same closure-extraction pattern this row and the rows above used. `pg_statistic_ext` or `pg_policy` are reasonable next single-builder targets (no known cross-builder oid-numbering coupling like the `pg_attrdef`/`pg_depend` pair had). | Continuing the "one small batch per loop" cadence the rows above established; this loop picked the single highest-value remaining builder (`pg_inherits`, named as the explicit next step in the prior loop's working_set.md) rather than bundling it with an unrelated sibling. |
| resolved | 2026-07-10 | M0122-0007 4e (pg_policy per-dbOid content, follow-up 29, next highest-value single builder the follow-up 28 ledger row flagged) | Extracted `pg_policy.VirtualRows` (`internal/catalog/catalog.go`) into new exported `catalog.InMemory.PGPolicyRowsForDBOid(dbOid uint32) [][]string`, parameterizing its one `c.ns(DefaultDBOid)` reference (the loop over every table's `Policies`) to `c.ns(dbOid)`; the registered closure now just calls `PGPolicyRowsForDBOid(DefaultDBOid)`, byte-identical default behavior. Wired new per-connection `executor.Context.PgPolicyRows func() [][]string` field (mirroring `PgInheritsRows`), set in `internal/server/dispatch.go`'s `wireExtensionRows` (new `pgPolicyRowLister` interface), consumed by a new `tbl.Name == "pg_policy"` branch in `internal/executor/operators.go`'s `valuesOp.Open`. New test `TestPgPolicyRowsScopedToConnectionDBOid` (`internal/executor/fk_dbid_routing_test.go`), mirroring `TestPgInheritsRowsScopedToConnectionDBOid`: a `CREATE POLICY ... USING` under each of two distinct dbOids, never cross-leak their `pg_policy` rows (by `polname`). Live end-to-end verified against a real `cmd/goopg` binary + real `psql`: `CREATE DATABASE polA` / `CREATE DATABASE polB` → `polA`: `CREATE TABLE ta (a int); CREATE POLICY pol_a ON ta USING (a > 0)` → `polB`: `CREATE TABLE tb (a int); CREATE POLICY pol_b ON tb USING (a > 0)` → `SELECT polname FROM pg_policy` in `polA` returns exactly `pol_a`, the same query in `polB` returns exactly `pol_b` — no cross-database leak either direction. Gates: `go build ./...`/`go vet ./...` clean; `go test ./internal/catalog/... ./internal/executor/... ./internal/server/... ./internal/planner/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke scripts/ralph-precommit-test.sh` PASS (0 failed txns, all 3 workloads, run via the pre-commit hook at commit time). | 6 sibling `VirtualRows` closures remain Bug-B-open (list `DefaultDBOid`'s objects regardless of connection): `pg_statistic_ext`, `pg_trigger`, `pg_rewrite`, `information_schema.routines`/`parameters`/`routine_*_usage`, `pg_foreign_table`. `pg_attribute`/`pg_type` (heap-backed) and the `oid::regclass` cast gap remain separately flagged, as does the "sequence ownership follow-on" row (`SequenceParamsFunc` dbOid parameter). | For each of the 6 remaining `VirtualRows` closures: apply the exact same closure-extraction pattern this row and the rows above used. `pg_statistic_ext` is a reasonable next single-builder target (no known cross-builder oid-numbering coupling). | Continuing the "one small batch per loop" cadence the rows above established; this loop picked the single highest-value remaining builder (`pg_policy`, named as one of two reasonable next single-builder targets in the prior loop's working_set.md, chosen over `pg_statistic_ext` since its `VirtualRows` closure had exactly one `c.ns(DefaultDBOid)` reference vs. `pg_statistic_ext`'s use of the dbOid-agnostic `c.statisticsObjs` global slice, making `pg_policy` the cleaner single-builder fix). |
| resolved | 2026-07-10 | M0122-0007 4e (pg_trigger per-dbOid content, follow-up 30, next single-builder pick after follow-up 29's pg_policy) | Extracted `pg_trigger.VirtualRows` (`internal/catalog/catalog.go`) into new exported `catalog.InMemory.PGTriggerRowsForDBOid(dbOid uint32) [][]string`, parameterizing its one `c.ns(DefaultDBOid)` reference (the loop over every table's `Triggers`) to `c.ns(dbOid)`; the registered closure now just calls `PGTriggerRowsForDBOid(DefaultDBOid)`, byte-identical default behavior. Wired new per-connection `executor.Context.PgTriggerRows func() [][]string` field (mirroring `PgPolicyRows`), set in `internal/server/dispatch.go`'s `wireExtensionRows` (new `pgTriggerRowLister` interface), consumed by a new `tbl.Name == "pg_trigger"` branch in `internal/executor/operators.go`'s `valuesOp.Open`. New test `TestPgTriggerRowsScopedToConnectionDBOid` (`internal/executor/fk_dbid_routing_test.go`), mirroring `TestPgPolicyRowsScopedToConnectionDBOid`: `CREATE FUNCTION ... RETURNS trigger` + `CREATE TRIGGER ... BEFORE INSERT` under each of two distinct dbOids, never cross-leak their `pg_trigger` rows (by `tgname`). Live end-to-end verified against a real `cmd/goopg` binary + real `psql`: `CREATE DATABASE trgA` / `CREATE DATABASE trgB` → `trgA`: `CREATE TABLE ta (a int); CREATE FUNCTION fn_a() RETURNS trigger ...; CREATE TRIGGER trig_a BEFORE INSERT ON ta ...` → `trgB`: same pattern with `tb`/`fn_b`/`trig_b` → `SELECT tgname FROM pg_trigger` in `trgA` returns exactly `trig_a`, the same query in `trgB` returns exactly `trig_b` — no cross-database leak either direction. Gates: `go build ./...`/`go vet ./...` clean; `go test ./internal/catalog/... ./internal/executor/... ./internal/server/... ./internal/planner/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33, re-run this loop); pgbench smoke enforced automatically by `.githooks/pre-commit` at commit time. | 5 sibling `VirtualRows` closures remain Bug-B-open (list `DefaultDBOid`'s objects regardless of connection): `pg_statistic_ext`, `pg_rewrite`, `information_schema.routines`/`parameters`/`routine_*_usage`, `pg_foreign_table`. `pg_rewrite` is now the cleanest next single-builder target — same `tbl.Rules`-over-`c.ns(dbOid).tables` shape as this row's `pg_trigger` fix, no known cross-builder oid-numbering coupling. `pg_statistic_ext` still needs the bigger "give `c.statisticsObjs` a dbOid key" treatment first (it is a process-global `map[string]*StatisticsObject`, not a per-table slice). `pg_attribute`/`pg_type` (heap-backed) and the `oid::regclass` cast gap remain separately flagged, as does the "sequence ownership follow-on" row (`SequenceParamsFunc` dbOid parameter). | For each of the remaining `VirtualRows` closures: apply the exact same closure-extraction pattern this row and the rows above used. `pg_rewrite` is the reasonable next single-builder target (identical shape to this row's fix, table-level `Rules` slice, no known blocker). | Continuing the "one small batch per loop" cadence the rows above established; this loop picked `pg_trigger` over `pg_rewrite` only because it was inspected first and confirmed table-`Triggers`-shaped (simple) in the prior loop's working_set.md — `pg_rewrite` turned out to be the identical shape and is queued next. |
| resolved | 2026-07-10 | M0122-0007 4e (pg_rewrite per-dbOid content, follow-up 31, next single-builder pick after follow-up 30's pg_trigger) | Extracted `pg_rewrite.VirtualRows` (`internal/catalog/catalog.go`) into new exported `catalog.InMemory.PGRewriteRowsForDBOid(dbOid uint32) [][]string`, parameterizing its one `c.ns(DefaultDBOid)` reference (the loop over every table's `Rules`) to `c.ns(dbOid)`; the registered closure now just calls `PGRewriteRowsForDBOid(DefaultDBOid)`, byte-identical default behavior. Wired new per-connection `executor.Context.PgRewriteRows func() [][]string` field (mirroring `PgTriggerRows`), set in `internal/server/dispatch.go`'s `wireExtensionRows` (new `pgRewriteRowLister` interface), consumed by a new `tbl.Name == "pg_rewrite"` branch in `internal/executor/operators.go`'s `valuesOp.Open`. New test `TestPgRewriteRowsScopedToConnectionDBOid` (`internal/executor/fk_dbid_routing_test.go`), mirroring `TestPgTriggerRowsScopedToConnectionDBOid`: `CREATE RULE ... AS ON INSERT TO ... DO INSTEAD NOTHING` under each of two distinct dbOids, never cross-leak their `pg_rewrite` rows (by `rulename`). Live end-to-end verified against a real `cmd/goopg` binary + real `psql`: `CREATE DATABASE rwA` / `CREATE DATABASE rwB` → `rwA`: `CREATE TABLE ta (a int); CREATE RULE rule_a AS ON INSERT TO ta DO INSTEAD NOTHING` → `rwB`: same pattern with `tb`/`rule_b` → `SELECT rulename FROM pg_rewrite` in `rwA` returns exactly `rule_a`, the same query in `rwB` returns exactly `rule_b` — no cross-database leak either direction. Gates: `go build ./...`/`go vet ./...` clean; `go test ./internal/catalog/... ./internal/executor/... ./internal/server/... ./internal/planner/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33, re-run this loop); pgbench smoke enforced automatically by `.githooks/pre-commit` at commit time. | 4 sibling `VirtualRows` closures remain Bug-B-open (list `DefaultDBOid`'s objects regardless of connection): `pg_statistic_ext`, `information_schema.routines`/`parameters`/`routine_*_usage`, `pg_foreign_table`. `pg_statistic_ext` still needs the bigger "give `c.statisticsObjs` a dbOid key" treatment first (it is a process-global `map[string]*StatisticsObject`, not a per-table slice) — same shape as the still-open `SequenceParamsFunc` gap. `information_schema.routines`/`parameters`/`routine_*_usage` and `pg_foreign_table` have not yet been inspected for which shape they are. `pg_attribute`/`pg_type` (heap-backed) and the `oid::regclass` cast gap remain separately flagged, as does the "sequence ownership follow-on" row (`SequenceParamsFunc` dbOid parameter). | For each of the remaining `VirtualRows` closures: apply the exact same closure-extraction pattern this row and the rows above used where the underlying data is already table-scoped (`information_schema.routines`/`parameters`/`routine_*_usage`, `pg_foreign_table` — pending shape inspection); `pg_statistic_ext` and the sequence registry need the bigger "add a dbOid key to the process-global map" treatment first. | Continuing the "one small batch per loop" cadence the rows above established; `pg_rewrite` was queued as the next pick by follow-up 30's ledger row (identical `tbl.Rules`-over-`c.ns(dbOid).tables` shape, no known blocker), confirmed correct by this loop. |
| resolved | 2026-07-10 | M0122-0007 4e (pg_foreign_table per-dbOid content, follow-up 32, next single-builder pick after follow-up 31's pg_rewrite) | Extracted `pg_foreign_table.VirtualRows` (`internal/catalog/catalog.go`) into new exported `catalog.InMemory.PGForeignTableRowsForDBOid(dbOid uint32) [][]string`, parameterizing its two `c.ns(DefaultDBOid)` references (the sorted-keys scan over `tables` and the per-table lookup) to `c.ns(dbOid)`; the registered closure now just calls `PGForeignTableRowsForDBOid(DefaultDBOid)`, byte-identical default behavior. Wired new per-connection `executor.Context.PgForeignTableRows func() [][]string` field (mirroring `PgRewriteRows`), set in `internal/server/dispatch.go`'s `wireExtensionRows` (new `pgForeignTableRowLister` interface), consumed by a new `tbl.Name == "pg_foreign_table"` branch in `internal/executor/operators.go`'s `valuesOp.Open`. New test `TestPgForeignTableRowsScopedToConnectionDBOid` (`internal/executor/fk_dbid_routing_test.go`), mirroring `TestPgRewriteRowsScopedToConnectionDBOid`: `CREATE SERVER` + `CREATE FOREIGN TABLE ... SERVER ...` under each of two distinct dbOids, never cross-leak their `pg_foreign_table` rows (matched by `ftrelid`, since the row itself carries no table-name column — resolved each expected table's OID via `catalog.InMemory.LookupTable(name, dbOid)` instead). Gates: `go build ./...`/`go vet ./...` clean; `go test ./internal/catalog/... ./internal/executor/... ./internal/server/... ./internal/planner/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33, re-run this loop). | 2 sibling `VirtualRows` closures remain Bug-B-open (list `DefaultDBOid`'s objects regardless of connection): `pg_statistic_ext`, `information_schema.routines`/`parameters`/`routine_*_usage`. Additionally, discovered (not fixed) this loop: `pg_foreign_table`'s `ftserver` OID lookup (`c.foreignServers[t.ForeignServerName]`) still resolves against a single process-global `map[string]*ForeignServer` with NO dbOid key at all — `CREATE SERVER` has no per-database namespacing anywhere, so a server named identically in two different databases would collide (last-writer-wins in the shared map), and a foreign table's `ftserver` would resolve to whichever database registered that name most recently rather than its own database's server. This is the same shape as the still-open `c.statisticsObjs`/`SequenceParamsFunc` process-global-registry gaps. `pg_attribute`/`pg_type` (heap-backed) and the `oid::regclass` cast gap remain separately flagged, as does the "sequence ownership follow-on" row (`SequenceParamsFunc` dbOid parameter). | For the 2 remaining `VirtualRows` closures: `information_schema.routines`/`parameters`/`routine_*_usage` still needs shape inspection (next candidate); `pg_statistic_ext` needs the bigger "give `c.statisticsObjs` a dbOid key" treatment. Separately, whoever gives `c.foreignServers` a dbOid key (mirroring the `statisticsObjs`/`seqRegistry` treatment) should thread it through both `CreateForeignServer`/`DropForeignServer` and this row's `PGForeignTableRowsForDBOid`'s `c.foreignServers[...]` lookup at `internal/catalog/catalog.go`. | Continuing the "one small batch per loop" cadence the rows above established; `pg_foreign_table` was queued as the next pick by follow-up 31's ledger row (identical shape to `pg_rewrite`'s fix, no known blocker for the row-content scoping itself) — the `c.foreignServers` cross-database name-collision gap was out of scope for this loop's row-content fix and is recorded here rather than silently left undocumented. |
| resolved | 2026-07-10 | M0122-0007 4e follow-up 33 (resolves the "pg_index live verification — collateral discovery" row above, 2026-07-10) | Threaded `catalog.NamespaceDBOid(ctx.CurrentDatabaseOid)` through the `oid::regclass`/`'name'::regclass` cast's 5 lookup call sites in `internal/executor/expr.go` (`im.LookupTableByOID`, `im.ToastRelName`, `ctx.Catalog.AllIndexes`, `ctx.Catalog.LookupTable` in both the `CastExpr` regclass arm and the `regclass` function-call arm, `im.LookupIndex`), and gave `catalog.InMemory.ToastRelName` a variadic `dbOid uint32` parameter (previously hardcoded to `DefaultDBOid`). New test `TestRegclassCastScopedToConnectionDBOid` (`internal/executor/fk_dbid_routing_test.go`), confirmed failing pre-fix via `git stash`. | This was purely a cast/output-function fix, not a `VirtualRows` closure — the 2 remaining sibling builders (`pg_statistic_ext`, `information_schema.routines`/`parameters`/`routine_*_usage`) still need the bigger "give the underlying registry a dbOid key" treatment and are unaffected by this row. | `internal/catalog/catalog.go` (`ToastRelName`), `internal/executor/expr.go` (both regclass arms) — already landed, no resume needed for this specific gap. | N/A — resolved in full this loop, not a partial fix. |
| resolved | 2026-07-10 | M0122-0007 4e (c.foreignServers registry-dbOid-key gap, named in follow-up 32's row above; inspected but NOT fixed this loop) | None — `information_schema.routines`/`parameters`/`routine_*_usage` was inspected instead (see next row) and picked over this one; `oid::regclass` (see the row above) was fixed instead of either. | `c.foreignServers` (`internal/catalog/catalog.go`) is still a process-global `map[string]*ForeignServer` keyed by name only — `RegisterForeignServer`/`DropForeignServer`/`ListForeignServers`/`ForeignServerOID` (+ their `*DuringRecovery` WAL-replay counterparts) all resolve without any dbOid, so a `CREATE SERVER` of the same name in two different databases collides (last-writer-wins), and both `pg_foreign_server`'s and `pg_user_mappings`' own `VirtualRows` closures list every server/mapping cluster-wide regardless of connection dbOid (the same Bug-B shape the 10 already-fixed sibling builders had) on top of the registry-key gap. Unlike the 10 simple closure-extraction follow-ups, this one is NOT bounded to a `VirtualRows` closure: `EncodeCreateForeignServer`/`DecodeCreateForeignServer` (`internal/wal/recovery.go`) carry no dbOid field either, so a correct fix touches the WAL record format (a version-compatible append, per `RegisterForeignServerDuringRecovery`'s existing replay contract) in addition to the in-memory registry and both `VirtualRows` closures. | Add a `dbOid uint32` field to `ForeignServer` and thread it through the 4 live-path functions + 2 recovery functions + `EncodeCreateForeignServer`/`DecodeCreateForeignServer` (append, not insert, to stay WAL-format-compatible) + `internal/executor/operators_ddl.go`'s CREATE/DROP SERVER call sites + `pg_foreign_table`'s `c.foreignServers[t.ForeignServerName]` lookup (`internal/catalog/catalog.go` line ~12984) + `pg_foreign_server`/`pg_user_mappings`'s own `VirtualRows` closures. Same shape as `catalog.SequenceParamsFunc`'s still-open dbOid-parameter gap below. | Larger and riskier than a single-loop bounded slice (WAL format change + 2 VirtualRows closures + the registry itself, vs. the 10 prior follow-ups' single closure-extraction each); deferred in favor of the smaller, fully-bounded `oid::regclass` cast fix (this loop's actual pick) so the tree stays at a clean, fully-tested stopping point rather than a partial WAL-format migration. |
| resolved | 2026-07-10 | M0122-0007 4e follow-up 34 (resolves the "sequence ownership follow-on" row above, 2026-07-10) | Gave `catalog.SequenceParamsFunc` (and its executor-side implementation `sequenceParamsForCatalog`, `internal/executor/operators_sequence.go`) a `dbOid uint32` parameter — `seqRegistry` itself was already fully dbOid-keyed (the "sequence ownership" row above), so this was purely a signature fix at the one remaining un-dbOid'd call boundary. Extracted `pg_sequence.VirtualRows` (`internal/catalog/catalog.go`) into new `catalog.InMemory.PGSequenceRowsForDBOid(dbOid uint32) [][]string` (mirroring the `pg_foreign_table`/`pg_trigger`/`pg_rewrite` precedent); fixed `PGDependRowsForDBOid`'s own pre-existing `SequenceParamsFunc(...)` call to pass its own `dbOid` (follow-up 27 had already dbOid-parameterized that function's sequence enumeration via `c.ns(dbOid)`, but its `SequenceParamsFunc` lookup silently still hit `DefaultDBOid`'s registry — a latent bug this fix also closes). Wired new per-connection `executor.Context.PgSequenceRows func() [][]string` field, set in `internal/server/dispatch.go`'s `wireExtensionRows` (new `pgSequenceRowLister` interface), consumed by a new `tbl.Name == "pg_sequence"` branch in `internal/executor/operators.go`'s `valuesOp.Open`. New test `TestPgSequenceRowsScopedToConnectionDBOid` (`internal/executor/fk_dbid_routing_test.go`). Live end-to-end verified against a real `cmd/goopg` + `psql`: two `CREATE DATABASE`s, one `CREATE SEQUENCE` each with distinct START/INCREMENT — each database's `pg_sequence` shows exactly its own sequence with its own params, no cross-leak. | `pg_sequences` (`internal/initdb/pg_sequences_view.go`) and `information_schema.sequences` (`internal/initdb/information_schema_sequences_view.go`) both still call `executor.AllSequenceInfos()` with no dbOid, hardcoded to `DefaultDBOid`'s sequences regardless of connection — narrows the still-open remainder of the "sequence ownership follow-on" row above to exactly these 2 views (the `pg_sequence`/`SequenceParamsFunc` half that row also named is now fully resolved). | `internal/initdb/pg_sequences_view.go` / `information_schema_sequences_view.go` — both need the same `executor.Context.PgSequenceRows`-style per-connection wiring `PGSequenceRowsForDBOid` just established, but reading `executor.AllSequenceInfos(dbOid)` (already dbOid-capable, just needs the caller to pass one) instead of `SequenceParamsFunc`. | `pg_sequence` (the canonical system catalog `pg_dump`'s `getSequences` actually reads) was the highest-value, fully-bounded slice; `pg_sequences`/`information_schema.sequences` are two more `initdb`-package VirtualRows closures of the same already-solved shape, left as their own bounded follow-up per "ONE task per loop". |
| resolved | 2026-07-10 | M0122-0007 4e follow-up 35 (closes the remainder follow-up 34 left open above) | Fixed `pg_sequences` (`internal/initdb/pg_sequences_view.go`) and `information_schema.sequences` (`internal/initdb/information_schema_sequences_view.go`), both of which called `executor.AllSequenceInfos()` with no dbOid, hardcoded to `DefaultDBOid`'s sequences regardless of connection. Unlike `pg_sequence` (singular, a `catalog.InMemory`-backed table needing the `PGSequenceRowsForDBOid` cross-package interface), both views read straight from this package's own `seqRegistry` via the already dbOid-aware `AllSequenceInfos(dbOid ...uint32)` — no `catalog.InMemory` indirection needed. Moved the row-shaping logic into two new exported `internal/executor/operators_sequence.go` functions, `PGSequencesRows(dbOid uint32)` / `InformationSchemaSequencesRows(dbOid uint32)`; `internal/executor/operators.go`'s `valuesOp.Open` gained matching `tbl.Name == "pg_sequences"` / `tbl.Name == "sequences" && tbl.Schema == "information_schema"` branches calling them directly with `catalog.NamespaceDBOid(ctx.CurrentDatabaseOid)` — mirrors the `pg_stat_slru`/`pg_stat_io` direct-call pattern (same-package state, no `Context`-field indirection), not the `pg_class`/`pg_sequence`-style cross-package interface. New test `TestPGSequencesAndInfoSchemaSequencesRowsScopedToConnectionDBOid` (`internal/executor/fk_dbid_routing_test.go`). Live end-to-end verified against a real `cmd/goopg` + `psql`: two `CREATE DATABASE`s, one `CREATE SEQUENCE` each with distinct START/INCREMENT — each database's `pg_sequences` and `information_schema.sequences` show exactly its own sequence with its own params, no cross-leak in either view. | None for this sub-cluster — the "sequence ownership follow-on" row's full scope (`pg_sequence`/`pg_sequences`/`information_schema.sequences`/`SequenceParamsFunc`) is now entirely resolved across follow-ups 34 and 35. | n/a — closed. | `pg_sequence` (follow-up 34) and `pg_sequences`/`information_schema.sequences` (this row) were split across 2 loops per "ONE task per loop"; this row completes that split. |
| resolved | 2026-07-10 | M0122-0024 | `CREATE TABLE name OF type_name (column_name WITH OPTIONS column_constraint [...])` now genuinely parses and applies per-column constraint overrides (NOT NULL/DEFAULT/CHECK/UNIQUE/PRIMARY KEY/REFERENCES/COLLATE/...) onto the composite-derived columns — real PostgreSQL grammar `TypedTableElement: columnOptions | TableConstraint` (`gram.y:3809-3812`), previously rejected outright with "typed-table column option list is not supported" for ANY parenthesised list after `OF type_name`. | The `TableConstraint` half of the same grammar rule — a bare `PRIMARY KEY (cols)`/`UNIQUE (cols)`/`CHECK (expr)`/`FOREIGN KEY ...`/`CONSTRAINT name ...` entry at table level, interleavable with `column_name WITH OPTIONS ...` entries in the very same paren list — is explicitly parse-rejected (`internal/parser/ddl.go`'s new `OF type_name` list loop), not supported. PostgreSQL's own canonical doc example (`CREATE TABLE employees OF employee_type (PRIMARY KEY (name), salary WITH OPTIONS DEFAULT 1000)`) mixes both forms in one statement; goopg can only do the second half today. | `internal/parser/ddl.go`, the `if p.cur().Kind == TokenKeyword && p.cur().Keyword == KwOf` block (~line 3008): the loop's leading guard `if ... KwConstraint || KwPrimary || KwUnique || KwCheck || KwForeign { return nil, p.errAtCur(...) }` needs replacing with real table-constraint parsing that appends into `CreateTableStmt.TableChecks`/`TableUniques`/`PrimaryKey`/table-level-FK fields (the same fields the ordinary `( ... )` column-list loop around line 3404 already populates) — reusing that existing table-constraint parsing logic directly (rather than re-deriving it) is the cheapest path once picked up. Executor side needs no new work: `execCreateTable`'s existing table-constraint-building code downstream of `s.Columns = derived` already consumes those same `stmt` fields regardless of whether the table is typed. | Table-level constraints in the OF-type list touch the same ~600-line inline table_constraint/column-def dispatch loop the ordinary CREATE TABLE body uses (`parseCreateTableTail`'s big element loop, no standalone reusable function to call into today) — extracting or duplicating that safely was out of bounds for this loop after the column-WITH-OPTIONS half (parser refactor + executor merge + tests) already filled a full bounded slice; the column form is also the more commonly-used half in practice and the narrower, cleanly-testable piece. |
| resolved | 2026-07-10 | M0122-0024 follow-up | Closed the M0122-0024 row's own deferred `table_constraint` half: `PRIMARY KEY (cols)`/`UNIQUE (cols)`/`CHECK (expr)`/`EXCLUDE ...`/`FOREIGN KEY ...`/`CONSTRAINT name ...` entries in the `CREATE TABLE name OF type_name (...)` list now parse and apply, interleavable with `column_name WITH OPTIONS ...` entries in any order — matching PG's own canonical doc example (`employees OF employee_type (PRIMARY KEY (name), salary WITH OPTIONS DEFAULT 1000)`) verbatim. Landed via the resume point the prior row named: extracted the ordinary CREATE TABLE column list's ~330-line table-constraint dispatch chain (`internal/parser/ddl.go`'s `parseCreateTableTail` per-element loop, the `KwPrimary`/`KwUnique`/`KwCheck`/`"exclude"`/`KwForeign`/`KwConstraint` if-else chain) into a new shared `parseTableConstraintElement(stmt *CreateTableStmt) (bool, error)` — a mechanical dedent + `return nil, err` → `return false, err` transform, verified behavior-preserving by running the full `internal/parser` suite unchanged immediately after extraction and before wiring in the second call site. Both the ordinary CREATE TABLE loop and the `OF type_name` list loop now call this one helper; a `matched == false` return falls through to LIKE/column-definition parsing (ordinary loop) or the `column_name WITH OPTIONS ...` parse (typed-table loop) respectively. Executor needed zero changes (confirmed by inspection: `execCreateTable`'s constraint-building code downstream of `s.Columns = derived` reads `stmt.PrimaryKey`/`TableChecks`/`TableUniques`/`TableForeignKeys`/`NamedConstraints` unconditionally, never gated on `s.OfType == nil`). | None — this was the full remaining scope of the M0122-0024 grammar rule (`TypedTableElement: columnOptions \| TableConstraint`); both halves are now implemented. | n/a — fully resolved, no further deferral. | n/a |
| resolved | 2026-07-10 | unimplemented_feat #135 (pg_get_expr) | Fixed the live `pg_index` renderer `catalog.InMemory.PGIndexRowsForDBOid` (`internal/catalog/catalog.go`), which hardcoded `indexprs`/`indpred`/`indcoloptions` to `""` — for a `text` column that reads back as a non-NULL empty string, not SQL NULL. It diverged from the executor heap-row twin `buildUserPGIndexRow` (`internal/executor/pg18_user_catalog_rows.go`, already correct) and broke two live-query behaviors: `indpred IS NOT NULL` (the canonical "is this a partial index?" probe) matched EVERY index, and `pg_get_expr(indpred, indrelid)` returned `''` instead of the WHERE predicate on a partial index (and `''` instead of NULL on a plain one). Now emits `VirtualNull` for non-partial `indpred` and `idx.PredicateString` for partial, mirroring the heap twin; `indexprs`/`indcoloptions` emit `VirtualNull`. Also confirmed pg_get_expr's pass-through is architecturally correct for goopg: all populated pg_node_tree columns (adbin/conbin/relpartbound) store pre-formatted deparsed SQL text, so no node-tree reconstruction is needed. | Expression-index `indexprs` is never populated: `catalog.Index.ColExprs` (the parsed expression-column targets of `CREATE INDEX ... ((expr)))`) is not surfaced into `pg_index.indexprs`, so `pg_get_expr(indexprs, indrelid)` on an expression index still returns NULL rather than the deparsed expression text. Real PostgreSQL stores the serialized expr node tree there and pg_get_expr decompiles it (ruleutils.c pg_get_expr_worker); goopg would instead store the pre-deparsed SQL text (same text-based model it already uses for indpred). psql \d and pg_dump are unaffected because both reconstruct index defs via `pg_get_indexdef`/`buildIndexDefString` directly from index metadata, not from `indexprs`. | `internal/catalog/catalog.go` `PGIndexRowsForDBOid`: replace the always-`VirtualNull` indexprs cell with a deparse of `idx.ColExprs` (joined for multi-expr indexes, matching the ordinal-0 entries already emitted into `indkey`); reuse the same expression-to-SQL deparser `PredicateString` is built from (`defaultExprToSQL`/`formatExprForAttrdef`). Mirror the same change in the heap twin `buildUserPGIndexRow` (`internal/executor/pg18_user_catalog_rows.go:1171`) so the live and heap-persisted renderings stay in sync. | The live indpred NULL-sentinel bug was the concrete, reproducible defect (a green E2E test now guards it); populating indexprs for expression indexes is a separate, lower-impact slice (no client path other than a direct `pg_get_expr(indexprs)` reads it) and was scoped out to keep the change bounded and the sibling-path audit tight. |
| resolved | 2026-07-10 | unimplemented_feat #135 (pg_get_expr, indexprs slice) | Populated the LIVE `pg_index.indexprs` cell for expression indexes so `pg_get_expr(indexprs, indrelid)` on a live query returns the deparsed expression text instead of NULL. Added shared `catalog.IndexExprsText(idx)` (`internal/catalog/catalog.go`) — joins `idx.ColExprStrings[i]` for expression key columns (`Columns[i]==""`, the ordinal-0 `indkey` entries) verbatim, comma-separated, returning `("", false)` when there are no expression columns so the caller emits `VirtualNull`. Wired into `PGIndexRowsForDBOid`. Byte-matched to PostgreSQL 18.3: `lower(b)` (bare func, no parens), `(a + c), upper(b)` (binexpr keeps its single natural-deparse parens, func doesn't; NO extra wrapping), `(a * c)` (only the expression column of a mixed `(a, (a*c))` index, plain column omitted), `NULL` for a plain index. The natural deparse in `ColExprStrings` already carries the right parens, so the helper joins them verbatim — an earlier draft that reused `buildIndexDefString`'s `indexKeyIsBareFuncCall` paren rule double-wrapped binexprs into `((a + c))` and was corrected. Tests: `TestPgIndexIndexprsExpressionIndex` (E2E through pg_get_expr, PG-captured expectations) + `TestIndexExprsTextParenAndNullRules` (direct helper unit test guarding the double-paren and NULL contract), `internal/executor/pg_index_indexprs_test.go`. | The HEAP-persisted twin `buildUserPGIndexRow` (`internal/executor/pg18_user_catalog_rows.go`) still writes `indexprs=NULL` for expression indexes (unlike the live path), so an expression index's `indexprs` is NOT restored from the heap on a checkpointed restart. This is a DELIBERATE non-fix, not an oversight: `DecodePGIndexPhysicalRow` (`internal/catalog/codec.go`) infers `indpred`'s presence from "all bytes remaining after indoption", which is only unambiguous while `indexprs` (the immediately-preceding nullable varlena column, ordinal 19) is NULL — two consecutive nullable varlenas are indistinguishable from the data bytes alone (a NULL varlena consumes zero bytes in `encodeRowPG`), and the decoder receives only the tuple DATA portion, not its null bitmap (some callers, e.g. `stampCatalogRows`, "only have the data bytes handy"). Writing a non-NULL indexprs to the heap would make an expression index's `indpred` decode as a bogus predicate on restart — corruption strictly worse than the current NULL. | To close: make `DecodePGIndexPhysicalRow` null-bitmap-aware so it can distinguish indexprs (19) from indpred (20) — requires changing its signature to receive the tuple null bitmap (or a per-column null flag) and updating all callers (`loadUserIndexesFromHeap` Pass 2/3, `stampCatalogRows`), plus a decode step for the indexprs varlena and a new `PGIndexRow.IndExprs`/`IndHasExprs` field threaded into `RegisterIndexDuringRecovery`. Then `buildUserPGIndexRow` can emit `catalog.IndexExprsText(idx)` symmetrically. Also note `Index.ColExprStrings` itself is not currently WAL/heap-persisted for restart, so expression-index recovery has a broader gap than just this column. Add a `TestExpressionIndexIndexprsSurvivesCheckpointedRestart` (graceful `rt.Close()`). | Scoped out to keep this loop bounded to the actual reproducible defect (live `pg_get_expr(indexprs)` returned NULL); the heap-decode side is a materially separate mechanism (null-bitmap plumbing through a shared decoder + all its callers) with a real corruption-risk landmine, better done as its own task with the true-restart test technique. |
| resolved | 2026-07-11 | M0122-0004 (MERGE duplicate-source cardinality) | Verified PG's MERGE duplicate-source rule (SQLSTATE 21000 "MERGE command cannot affect row a second time") is fully implemented across partition routing, incl. the cross-partition UPDATE-move case flagged by unimplemented_feat.json M0100-0007; added PG's previously-omitted errhint ("Ensure that not more than one source row matches any one target row.") at operators_merge.go's hasDuplicate raise site, plus first-ever regression tests (internal/executor/merge_dup_source_test.go, 4 shapes). | none — feature complete; unimplemented_feat.json M0100-0007 flipped open→resolved | verify-before-implement: entry was medium-confidence/unclear-audit; re-verification (4 end-to-end probes) proved it already correct, only the cosmetic errhint was missing |  |
| resolved | 2026-07-12 | m0003 (KindDate carrier / 0003-0013) | Closed the "KindDate carrier with date-only Format()" deferral (unimplemented_feat.json). goopg tags date-ness on the shared KindTime datum via the flagDate bit (not a distinct DatumKind); the concrete gap was decodePhysicalPGValueMctx's `date` case (internal/executor/codec.go) returning a flagless NewTimeDatum, so a storage-decoded date rendered via Datum.Format() as the full timestamp shape (2001-02-16 00:00:00.000000) instead of the date shape a literal produces — affecting date::text casts, string concat, and array/composite element rendering (the wire path in server/dispatch.go was already correct, re-deriving format from the column type). Fix: new NewDateDatum constructor (internal/executor/datum.go) sets flagDate; decode `date` case uses it so a decoded date is byte-identical to a literal. compareDatum/datumKey are flag-agnostic → no ordering/grouping/join impact. Test: internal/executor/codec_date_test.go TestDateDecodeCarriesDateFlag. | A dedicated KindDate enum value is deliberately NOT introduced (flagDate is the carrier, functionally equivalent for rendering + codec). Index-only-scan date reconstruction from the btree int4-days key (if any path bypasses the physical row decode) was not audited this loop — verify NewDateDatum is used there too if such a path is added. | The flag approach matches every existing date-producing site (literals via expr.go, ±infinity via NewDateInfinity); a new DatumKind would touch every KindTime switch for no user-visible gain. |  |
| resolved | 2026-07-12 | M0122-0004 (RANGE interval-offset sign) | Closed deferral (2) of row 716: `rangeOffsetNegative` (internal/executor/operators_window.go) no longer uses the component-sign heuristic (months<0\|\|days<0\|\|micros<0) for interval RANGE offsets. It now reproduces PG's `interval_sign(offset) < 0` from `in_range_interval_interval` (postgres/src/backend/utils/adt/timestamp.c) — the sign of the linear span `span = time_micros + (months*30 + days)*USECS_PER_DAY` (interval_cmp_value), via the same overflow-safe day/frac decomposition compareDatum already uses for interval ordering. `INTERVAL '1 mon -10 days'` (a +20-day span) is now correctly accepted as a positive offset instead of wrongly raising 22013; ±infinity sentinels handled (NOEND positive / NOBEGIN negative). Regression: TestRangeOffsetNegativeIntervalSign (internal/executor/window_compat_test.go); design doc 0122-0004-range-offset-window-frame.md Follow-up section. | none (this row resolves a prior deferral). | n/a — deferral (1) of row 716 (per-type in_range catalog / parse-time 0A000) remains open and un-touched by this loop. |  |
| resolved | 2026-07-12 | M0122-0004 (bucket closure) | Verify-before-implement: closed the M0122-0004 SQL-language/executor bucket (.ralph/fix_plan.md checkbox flipped `[ ]`→`[x]`). The prior "Still open in this bucket" note (2026-07-11) listed exactly two remaining items and BOTH are resolved: (A) RANGE value-offset frame bounds — resolved 2026-07-11 by `frameBoundsRange` (internal/executor/operators_window.go, PG `in_range` semantics; analyzer requires one ORDER BY column, 42P20) and refined 2026-07-12 loop #66 by the `rangeOffsetNegative` interval-sign fix (commit b7bdb819); (B) sub-day + multi-component interval strings — resolved by the sub-day `KindInterval` micros carrier (Datum.IntervalMicrosValue, 2026-07-11) + the multi-field/HH:MM:SS tokenizer (internal/parser/interval.go ParseIntervalBody, commit fda418cf + follow-ups). Confirmed both live via a throwaway executor probe (`RANGE BETWEEN 1 PRECEDING AND 1 FOLLOWING` over x=1,2,3,5 → s=3,6,5,5 matching PG; `INTERVAL '1 day 05:30:00'`→`1 day 05:30:00`) and 56 passing CompatWindow/Interval/GroupingSets/RangeOffset executor tests. unimplemented_feat.json window+interval entries already all marked RESOLVED; a sweep of its confirmed-open audits finds zero remaining SQL-language items in this bucket. | none (this row closes a bucket; no NEW PG behavior left unimplemented within M0122-0004 scope). | n/a — the ONLY residual is unrelated: deferral (1) of ledger row 716 (per-type `in_range` catalog / parse-time 0A000 for RANGE offsets against unsupported column types) remains open and is tracked there, NOT under M0122-0004's named scope. |  |
| resolved | 2026-07-12 | unimplemented_feat #135 (pg_get_expr, indexprs heap slice) | Closed the 2026-07-10 rows' deferred heap-persisted `indexprs` slice: made `catalog.DecodePGIndexPhysicalRow(data, bitmap []byte)` null-bitmap-aware so it disambiguates the two trailing nullable `pg_node_tree` varlenas (indexprs=col 19, indpred=col 20) via the PG `heap_fill_tuple` bitmap (bit i set ⇒ NOT NULL) instead of inferring `indpred` from remaining byte length. New `PGIndexRow.IndExprs`/`IndHasExprs` fields; new helpers `pgIndexBitNotNull` (nil bitmap ⇒ treat as present) and `pgIndexVarlenaLen` (advance past indexprs to reach indpred, mirroring `decodePGIndexVarlenaText`'s header handling). `buildUserPGIndexRow` (`internal/executor/pg18_user_catalog_rows.go`) now emits the expression text via the same `catalog.IndexExprsText` helper the live renderer uses instead of a hardcoded `NullDatum` — the canonical heap writer already stamps `HEAP_HASNULL`+bitmap via `NullBitmapPG`/`NewHeapTupleWithNulls`, so no write-path change. Callers: `internal/initdb/open.go` passes the real `ht.Bitmap` from `PageGetHeapTuple`; `operators_ddl.go`'s `resyncIndexHeapRow` matcher passes `nil` (matches only on fixed-offset `indexrelid`). Backward-compatible: every legacy on-disk row already carried a bitmap since `indexprs` was always NULL (plain: bits 19,20 clear; partial: 19 clear, 20 set), so the bitmap-aware decoder reads them correctly with no migration. Alignment verified: both cols are `pg_node_tree` → `physicalPGTypeAlign` 4-byte 'i'; `encodeRowPG` always 4-aligns and the decoder mirrors with `pgIndexAlign4`, staying PG-standby-readable (PG's `att_align_pointer` aligns past goopg's zero pad bytes). Design: `docs/design/0122-0019-pg-index-node-tree-columns.md` "Follow-up (2026-07-12)" section; README row updated. Tests: `TestBuildUserPGIndexRowExprPredNullBitmapRoundTrip` (`internal/executor/pg18_user_catalog_rows_test.go`, all four NULL/non-NULL combos). Gates: `go build ./...` clean; catalog+executor+initdb(CreateIndex) suites PASS; tpch-spotcheck + pgbench smoke (see commit). | initdb heap-scan recovery `loadUserIndexesFromHeap` (`internal/initdb/open.go` Pass 3) still skips index columns whose `indkey` attnum is 0 (expression columns), so a pure expression index is reconstructed via the CREATE INDEX WAL-replay path, not the heap scan — the newly-decoded `IndExprs` text is not yet consumed to rebuild an expression index's key columns from a checkpointed heap. `Index.ColExprStrings` restart persistence therefore still rides WAL, not the heap row. | `internal/initdb/open.go` Pass 3: when `pgIdx` has expression columns (attnum 0), thread `row.IndExprs` (split on the same `", "` join `IndexExprsText` uses) into `RegisterIndexDuringRecovery`'s `ColExprStrings`, so a graceful `rt.Close()`+reopen rebuilds an expression index without the WAL fallback. Add `TestExpressionIndexIndexprsSurvivesCheckpointedRestart`. | Bounded this loop to the decoder/writer disambiguation (the actual corruption landmine + byte-fidelity for a PG-standby direct heap read of `pg_index.indexprs`); rebuilding expression-index key columns from the recovered text on the heap-scan path is a separate, larger recovery feature (the WAL-replay path already covers restart correctness today). |
| resolved | 2026-07-13 | perf-optimize3-dash S4 gate finding (pre-existing, FIXED) | fix: parsePrepare infinite loop on array param types. `PREPARE f(regclass[]) AS ...` (regress constraints.sql:816) spun forever in the param-type loop — parseTypeNameAfterCast fails on `[` WITHOUT consuming it, so each iteration appended "unknown" and re-parsed the same token; the orphaned backend (psql killed at the harness 120s timeout, parse keeps running) allocated until the server OOMed (heap profile: parsePrepare 1.47GB live = 85.6%; introduced eb6b10ef 2026-05-25 M0097-0021). This background balloon poisoned EVERY regress-suite run (3/3 reproduction) and compounded with the FPI-eviction storm into the full-server wedge. Fix: array-suffix consumption (type[] → "type[]", mirroring the CAST path) + a non-progress guard that skips the offending token on parse error. Pinned by internal/parser/prepare_array_param_test.go. | Nothing deferred for the loop itself. The sibling finding — viewProxyTable panic (view_dml.go:168, index out of range on UPDATE via view, also constraints.sql) — is recovered-per-connection and NOT fixed here. | viewProxyTable: reproduce via regress constraints.sql UPDATE-on-view statements; guard Columns index before [1]. | Parser bug was in scope to fix because it blocked the S4 regress gate on every run; the view panic does not block (backend recovers, psql continues). |
| resolved | 2026-07-14 | 08 doc 03 (C3 RangeScan kill-migration) — S1 REVERTED (regression) | UPDATE: S1 (bdaa325a) was REVERTED (4998c81b) after the S3 perf-acceptance gate FAILED. The change was functionally correct (unit test + G-race btree/executor + G-tpch + G-crash all green, zero throughput cost 14766 vs 14882 tps) but delivered NO benefit and a severe regression: (a) scale-100 uniform pgbench -N 600s soak — pkey doubled BYTE-IDENTICALLY to the un-migrated baseline (166.5MB->333.4MB) because uniform -N updates each aid ~0.4x over 10M rows, so dead-pointing entries (created by first non-HOT updates) are almost never re-probed before their leaf splits → on-probe kills rarely fire; (b) re-probe-heavy A/B (1000 hot keys, c=1 deterministic, heap negative-control matched at 109% → runs comparable) — migrated pkey grew +24.5MB vs baseline +1.3MB = ~18x REGRESSION. Root cause: marking dead DUPLICATE index entries LP_DEAD removes them from the btree dedup-consolidation candidate set, so the no-space purge's posting-list packing of duplicate hot-key TIDs (baseline's primary space saver) no longer applies → leaves split instead of consolidating. LP_DEAD-marking and dedup are COMPETING space strategies; for duplicate-heavy UPDATE-probe churn dedup wins. | The doc-03 approach (mirror the read-path indexScanOp kill collector onto the UPDATE probe) is ABANDONED — inherent design flaw, not an implementation bug. The read path is unaffected (read scans don't churn duplicate keys). The C3-S5 residual pkey doubling under pgbench -N remains OPEN. | Do NOT re-land on-probe kill collection. Closing the residual needs a dedup-aware no-space purge (consolidate duplicate TIDs into posting lists FIRST; LP_DEAD-mark only entries that cannot dedup) OR a background btree vacuum. Run dirs: analysis/perf-optimize3/runs/s5c3_s1accept_bdaa325a (soak) + .../s5c3_s1_hotkey_ab (A/B). Doc 03 §8 has the full table. | Careful perf verification (the S3 gate) caught a functionally-green change that regresses the very metric it targeted — the project's core anti-pattern (a change passing its own check while breaking the real goal). Reverting is the correct outcome; the finding + the dedup-vs-LP_DEAD interaction is the durable takeaway. |
| resolved | 2026-07-14 | M-NIGHTLY (AI-20260712-020530-002, loop #91 — design 0021-0012 landed) | Implemented the design deferred by the two 2026-07-12 rows above: `acquireTupleLock`/`tryAcquireTupleLock` (`internal/executor/context.go`, `tupleLockManager()`) now route to the always-on package-global `tableLockMgr` under the per-statement `BackendID` when `c.LockMgr == nil` in production, instead of being total no-ops. Refined the original design sketch on discovery: the tag is acquired **lazily**, only at the three "about to sleep" sites in `stampLockInner` (lock-only-conflict / multixact-wait / xid-wait) plus the DML write-path's `waitForConflictingRowLock` — mirroring `heap_acquire_tuplock`'s placement immediately before `XactLockTableWait`/`MultiXactIdWait` — never up-front (`lockRowsOp.stampLock`'s unconditional switch, `updateViaIndex`'s pre-read acquire, and `scanMatching`'s per-row dispatch-loop acquire were all removed); an up-front acquire on every non-conflicting request would reintroduce an upgrade-deadlock (`tuplelock-upgrade-no-deadlock` perm 9). `tupleLockHwMode` ports upstream's four-mode `tupleLockExtraInfo[].hwlock` matrix (`AccessShareLock`/`RowShareLock`/`ExclusiveLock`/`AccessExclusiveLock` for KeyShare/Share/NoKeyUpdate/Update) replacing the old two-mode collapse, so FOR KEY SHARE no longer wrongly queues behind an unrelated no-key UPDATE holder. `selfIsLockMember`/`selfRowLockMember` port `current_is_member` to skip the tag on a same-backend upgrade. `executor.ReleaseTupleLocks(backendID)` releases per-statement tag holdings from `dispatch.go`'s Query-message defer. Fixed two sibling unit tests that polled `lm.Waiters(...)` (the old "always up-front, so contention shows as a waiter" assumption) and would hang forever under the new "second locker becomes the tag's immediate HOLDER" model: `TestUpdateViaIndexScanBlocksOnForeignTupleLock`, `TestForShareCompatibleMultipleHolders` (also dropped its now-invalid "2 holders after 2 compatible FOR SHAREs" assertion). Re-promoted `TestPort_IsolationTuplelockUpgradeNoDeadlock` to `runIsoSpecStrict`; flipped `docs/test-port/postgres-oracle-target-inventory.csv` row 612 `defer`→`pass` + regenerated the `.md` (isolation pass 119→120). Gates: `go build ./...` clean; full `internal/executor` suite green; `internal/lockmgr`/`internal/server` green; the flaky test looped 20× strict standalone — zero flakes; full isolation suite (121 specs) clean except 3 failures confirmed pre-existing/unrelated via `git stash` (`insert-conflict-specconflict`, `detach-partition-concurrently-4`, `partition-drop-index-locking`); `make race-gate` (44 packages) clean except one pre-existing flaky timing test in the untouched `internal/wal` package (passes standalone, only flakes under race-gate's full-suite CPU contention); `scripts/tpch-spotcheck.sh` Q12=2/Q13=33 PASS; pgbench CI-parity smoke (standard/-N/-S) 0 failed transactions. | None outstanding for this slice — both prior deferrals (row-lock FIFO fairness, the DML-conflict-path serialising lock) are now closed by the same fix (the tag is shared by both the SELECT FOR UPDATE/SHARE read path and the UPDATE/DELETE write path). | n/a — landed, not deferred. If future isolation runs surface new flakiness in this area, start from `acquireTupleLockForWait` (`operators_lockrows.go`) and `waitForConflictingRowLock` (`operators_storage.go`) — the only two acquire sites. | n/a — this row resolves the two 2026-07-12 deferrals above; no new scope was left unimplemented. Design doc `docs/design/0021-0012-tuple-lock-fifo-wiring.md` "Implementation" + "Validation results" sections have the full detail. |
| resolved | 2026-07-14 | M-NIGHTLY (AI-20260714-011651-006..095) follow-up — regress/errors fixed (item (b) first pick) | Root-caused and fixed both defects behind `regress/errors`' output mismatch (picked up resume item (b), first of the sub-0.2s quick suites). (1) `internal/parser/function.go`'s `CREATE FUNCTION` AS-clause parser unconditionally rejected the two-item `AS 'objfile', 'linksymbol'` form with a hardcoded `only one AS item needed for language "sql"` error — but upstream's `interpret_AS_clause` (functioncmds.c) only rejects this for non-C languages; `LANGUAGE C` requires exactly two items (obj file + link symbol). This broke test_setup.sql itself (`CREATE FUNCTION binary_coercible(oid, oid) ... AS :'regresslib', 'binary_coercible' LANGUAGE C ...`), which every regress case sources as a fixture — the whole suite was running without `binary_coercible`/`binary_coercible`-dependent fixtures defined. Fix: defer the two-item check to the loop's exit (LANGUAGE can appear before or after AS) and only reject when the final language isn't "c". (2) `internal/analyzer/analyzer.go`'s `analyzeLockingClauses` checked `len(s.GroupBy) > 0` to reject `SELECT ... GROUP BY ... FOR UPDATE`, but `GROUP BY GROUPING SETS (())` (regress' errors.sql degenerate-empty-set case) flattens to a zero-length `s.GroupBy` while `s.GroupingSets` is still non-nil — the check silently let it through, wrongly executing instead of raising `0A000`. Fix: `len(s.GroupBy) > 0 || s.GroupingSets != nil`. Tests: `TestParseCreateFunctionLanguageCTwoItemAS`/`TestParseCreateFunctionNonCTwoItemASRejected` (parser), `TestAnalyzeForUpdateRejectsGroupingSets` (analyzer). Flipped `docs/test-port/regress-diff-baseline.csv`'s `errors` row `fail`→`pass`. Spot-checked the other sub-0.2s candidates named in the resume plan (`expressions`, `equivclass`, `explain`, `guc`, `regex`, `random`) — all still genuinely diverge on unrelated normalization gaps, left untouched (baseline stays `fail`), matching ONE-task-per-loop scope. Gates: `go build ./...` clean; `go vet`/`go test` on `internal/parser`, `internal/analyzer`, `internal/executor` all green; `TestPort_RegressSuite/errors` PASS (was SKIP/deferred); `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke bash scripts/ralph-precommit-test.sh` PASS (0 failed, all 3 workloads). | None outstanding for `errors` — both defects fully fixed, baseline flipped. The other 83 mismatches (`expressions`/`equivclass`/`explain`/`guc`/`regex`/`random` confirmed still-broken plus the rest) and the `aggregates` LATERAL/correlated-subquery bug remain open, tracked by the row above. | Next quick suite to pick from the same resume list: `expressions`, `equivclass`, `explain`, `guc`, `regex`, or `random` (each sub-0.2s, use `GOOPG_REGRESS_DIFF_DIR=<dir> go test -run 'TestPort_RegressSuite/^<name>$' ./internal/testport/` then `diff <dir>/<name>_expected.txt <dir>/<name>_actual.txt`). | The CREATE FUNCTION AS-clause parse bug silently affected EVERY regress suite's shared `test_setup.sql` fixture load (not just `errors`) — fixing it may have quietly helped other suites too; not re-verified against the full 84-mismatch bucket this loop (out of scope, would need re-triaging all 84 which is its own multi-loop project per the row above). |
| resolved | 2026-07-14 | M-NIGHTLY (AI-20260714-011651-006..095) follow-up — regexp_match/regexp_matches array-literal quoting fixed | Picked up the resume list's next quick suite (`regex`, after `errors` two loops ago). `regexpMatchArrayDatum` (`internal/executor/expr.go`) built its `{elem1,elem2}` array literal via a bare `strings.Join`, so an empty-string match rendered as `{}` (indistinguishable from a zero-element array) instead of PG's `{""}`, and any matched text containing a comma/brace would have silently corrupted the element count on read-back (untested pre-existing latent bug, not hit by the regress diff but caught by a new guard test). Fixed by delegating to the already-existing, already-tested `formatTextArrayWithNulls` helper (used elsewhere in the same file) instead of a second hand-rolled quoter — mirrors PG's `array_out` needquote rule (`postgres/src/backend/utils/adt/arrayfuncs.c` ~line 1130: quote if empty, case-insensitively "NULL", or contains `"`/`\`/`{`/`}`/delimiter/whitespace). Tests: `internal/executor/regexp_match_test.go` +2 cases. Design doc follow-up appended to `docs/design/0122-0002-pg-relation-size-real-sizes.md` + README index. Gates: `go build ./...` clean; `go test ./internal/executor/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke bash scripts/ralph-precommit-test.sh` PASS (0 failed, all 3 workloads). | The fix does NOT flip `regress/regex` to `pass` — the suite's dominant, still-open failures are PCRE-only backreference (`\1` in-pattern) and lookaround (`(?<=...)`, `(?=...)`) constructs that Go's `regexp` package (RE2) structurally cannot support; matching PG's ARE (advanced regex engine) semantics would require swapping the regex engine entirely, not a normalization-rule tweak. | Next suite: none of `regex`'s remaining diff is quick — see the corrected classification row below before picking another "quick suite" name from the old resume list. | The array-quoting bug was found while diffing `regex`'s output, not by design; it's real and worth having fixed regardless of whether it flips the suite, since any future regexp_match/regexp_matches caller with a comma/brace/empty-string match was silently getting corrupted output before this fix. |
| resolved | 2026-07-14 | M-NIGHTLY (run 20260714-011651 follow-up) — DateStyle partial-SET merge semantics fixed (item (2) from the row above) | `DateStyle` was a plain `TypeString` GUC with zero parsing: `SET datestyle = 'SQL'` stored the bare literal `"SQL"` (losing the order component), and conflicting (`'ISO, SQL'`)/unrecognized (`'bogus'`) specs were silently accepted. Ported PostgreSQL's `check_datestyle` (`postgres/src/backend/commands/variable.c`) into new `internal/config/datestyle.go` (`mergeDateStyle`/`parseDateStyleValue`); `Variable.canonicalize` delegates to new `canonicalizeFrom(current, value)`; `SessionRegistry.Set`/`SetInternal` now pass the session's effective current value (not the stale global `v.Value`) so a second partial SET in the same session merges correctly, matching `GERMAN`'s implicit-DMY and `DEFAULT`-token-recursion semantics too. New `internal/config/datestyle_test.go` (6 cases) + live `psql` SET/SHOW verification. Design: `docs/design/0097-0151-datestyle-partial-set-merge.md` + README row. Gates: `go build ./...` clean; `go test ./internal/config/... ./internal/server/... ./internal/executor/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke bash scripts/ralph-precommit-test.sh` PASS (0 failed, all 3 workloads). | Item (1) from the row above — PG's DateStyle multi-format timestamp/date *output* rendering (Postgres/SQL/German styles, MDY/DMY/YMD order) — remains entirely unimplemented; goopg's date/timestamp formatters (`internal/executor/datum.go` `Format()`/`AppendValueText`, `internal/server/dispatch.go` `appendTypedCellText`'s `date`/`time`/`timetz` cases, `internal/executor/copy_text.go`, `internal/wal/pgoutput.go`) are hardcoded to one literal layout regardless of the GUC, and two of those paths (datum.go's DATE case vs dispatch.go's `date` case) already disagree with each other (MDY vs ISO) for the same DATE kind — a latent sibling-path divergence, not yet confirmed to matter in practice since dispatch.go's `appendTypedCellText` is the one that actually serves SELECT output. `guc`/`date`/`timestamp`/`timestamptz`/`horology` regress suites stay `fail`; item (3) (re-diff all 84 still-failing suites) is also still unstarted. | Implement DateStyle-aware rendering: thread `getSetting`-style access to the effective `DateStyle` GUC into the ~6 hardcoded call sites above (start by resolving the datum.go-vs-dispatch.go DATE-format disagreement — determine which path is actually live for regular SELECT output, per [[pattern_sibling_paths_must_agree]]), each rendering `ISO`->`YYYY-MM-DD`, `SQL`->`MM/DD/YYYY` (or `DD/MM/YYYY` for DMY), `Postgres`->`MonDD` textual form (order-dependent), `German`->`DD.MM.YYYY`, per `postgres/src/backend/utils/adt/date.c` `EncodeDateOnly`/`EncodeDateTime`. Then re-run all 84 still-failing regress suites individually and re-baseline `docs/test-port/regress-diff-baseline.csv` for any that flip. | The output-rendering feature touches every date/time formatting call site across 4+ files and is a materially larger, riskier change (a wrong rendering silently changes visible query results across every date/timestamp column) than the bounded, independently-valuable GUC-merge correctness fix landed this loop — keeping them as separate ledger items/loops matches this project's ONE-task-per-loop discipline and the "corrected quick suite classification" row's own scoping guidance. |
| resolved | 2026-07-15 | M-NIGHTLY (run 20260714-011651 follow-up) — `fkValsForDetail` FK-violation DETAIL DateStyle-awareness fixed | Picked up the "audit the ~20 `Format()`/`AppendValueText()` call sites" resume point from the row above, landing `operators_fk.go`'s `fkValsForDetail(vals []Datum) string` (the `Key (col)=(val) is not present in table "X".` / `...is still referenced from table "Y".` DETAIL line on every `23503` FK-violation error) — renamed the CAST follow-up's `formatTimeDatumForCast` to `formatTimeDatumDateStyle` (it was never CAST-specific) and reused it; added a `ctx *Context` param to `fkValsForDetail`, threaded through all 4 production call sites (`assertParentExists` ×2, `assertNoChildRows`, `detachPartitionFKRefCheck`), all of which already had `ctx` in scope. New `TestFKValsForDetailHonorsDateStyle`. Live `psql` verification against a real `cmd/goopg` binary confirmed the fix works for the DELETE/UPDATE-parent-side check and the partition-detach check (`SET datestyle='German'; DELETE FROM parent ...` → `DETAIL: Key (d)=(14.07.2026) is still referenced...`, correctly reformatted) — both operate on `Datum`s decoded from an already-stored heap row (`Kind=5`/`KindTime`, `flagDate` set). | **New gap discovered by the same live probe:** the INSERT-side check (`assertParentExists` via `checkFKInsert`) does NOT benefit — `SET datestyle='German'; INSERT INTO child VALUES ('2026-01-02')` (violating the FK) still renders `DETAIL: Key (d)=(2026-01-02) is not present...`, the raw un-reformatted literal. A temporary debug probe showed `vals[0].Kind=3` (`KindString`) at that call site, not `KindTime` — `operators_storage.go`'s `insertOp.Next` only explicitly coerces `int2`/`int4`/`int8` columns (the "Integer range enforcement" block, ~line 1900) before FK/CHECK/domain constraint checks run; DATE/TIMESTAMP/TIMESTAMPTZ/NUMERIC columns are left as whatever `Kind` the source VALUES expression produced and only become properly typed later, at storage encode — so `fkValsForDetail`'s new `KindTime` branch never fires for a fresh INSERT's own violating value. This is wider than DateStyle: CHECK constraints, domain CHECK/NOT NULL, and FK checks during INSERT all currently evaluate against un-coerced DATE/TIMESTAMP/NUMERIC literal Datums, not the properly-typed value PG's real ExecEvalConstraint* nodes would see. | `internal/executor/operators_storage.go`'s `insertOp.Next`, ~line 1900 (the coercion `switch strings.ToLower(col.Type.Name)` currently `case "int2"/"int4"/"int8"` only): extend to `case "date", "timestamp", "timestamptz"` (and audit `numeric`) via the same `evalCast(row[i], typeName, o.plan.Pos(), o.ctx)` pattern already used for the integer cases. Audit `updateOp`/upsert siblings for the same gap per `pattern_sibling_paths_must_agree` before considering this closed. | Discovered mid-verification of this loop's own FK-DateStyle fix (live `psql` + a temporary Kind/Flags debug probe, reverted before commit); INSERT-path row coercion is a distinct, larger unit of work (touches every INSERT's constraint-check ordering, not just DateStyle output) so out of scope for ONE-task-per-loop here. |
| resolved | 2026-07-15 | M-NIGHTLY (run 20260714-011651 follow-up) — INSERT-side date/timestamp/timestamptz/numeric literal coercion fixed (resume point from the row above) | Extended `operators_storage.go`'s `insertOp.Next` coercion switch (~line 1903, previously `int2`/`int4`/`int8` only) to also cover `date`, `timestamp`, `timestamptz`, and `numeric`/`decimal` via the same `evalCast(row[i], typeName, pos, ctx)` pattern, so FK/CHECK/domain/NOT-NULL constraint checks during INSERT now see a properly typed value instead of the raw VALUES-clause literal — closing the gap the row above discovered. Shared for free with the `INSERT ... ON CONFLICT` upsert candidate-row path (same loop this coercion block already runs in, per the pre-existing `autoGenerateSerialValues` comment). **Sibling bug found and fixed by the same live-verification pass:** `evalCast`'s `"date"` case (`expr.go`) built its result via `NewTimeDatum` instead of `NewDateDatum` in both branches (string-source and `KindTime`-source), leaving `flagDate` unset — this made the newly-coerced FK DETAIL rendering show a spurious `00:00:00` suffix (date rendered as if it were a timestamp) until fixed. New tests: `TestEvalCastToDateSetsFlagDate` (`cast_datestyle_test.go`), `TestInsertCoercesDateLiteralBeforeFKCheck` + `TestInsertCoercesNumericLiteralBeforeCheckConstraint` (`insert_fk_datestyle_coerce_test.go`, full parse→plan→exec integration). Live `psql` verification (port 5540): DATE and TIMESTAMP INSERT-side FK violations under `SET datestyle='German'` both render correctly and date-only (no time suffix); CHECK-constraint violation (`23514`) and invalid numeric literal (`22P02`) on a `numeric` column still raise correctly; `int4[]` array column unaffected by the widened switch (`col.Type.IsArray` guard). | **UPDATE ... SET has the same un-coerced-literal gap, wider than DateStyle** — confirmed by code reading, not fixed this loop. `updateOp`'s new-row construction (`updateViaIndex`'s per-row callback ~operators_storage.go:3833, the seq-scan path in `updateOp.Next` ~4448/4579, and `updateWithFrom` ~4808/6062-ish) builds `newRow[i]` straight from `evalExpr(o.plan.Set[i], row, o.ctx)` with NO column-type-aware coercion step at all — not just missing date/timestamp/numeric, but also missing the original int2/int4/int8 range-check coercion `insertOp.Next` already had before this loop. `recheckChildFKs`/CHECK-constraint enforcement in `updateViaIndex` (~line 3904, added by M0122-0005) run directly against this un-coerced `newRow`/`pu.newRow`, so a `CHECK`/FK-violating `UPDATE t SET d = '<literal>'` under a non-default DateStyle would hit the same raw-literal DETAIL-rendering bug INSERT had, and separately a plain `UPDATE t SET n = 99999999999999999999` against an `int4` column would silently skip the overflow check INSERT already catches. | `internal/executor/operators_storage.go`: factor the INSERT coercion switch (~1918-1937) into a shared helper (e.g. `coerceRowForConstraintChecks(cols []catalog.Column, row Row, ctx *Context, pos int) error`) and call it from `insertOp.Next` (replacing the inline switch) plus every `updateOp` new-row-construction site listed above (`updateViaIndex`, `updateOp.Next`'s seq-scan path, `updateWithFrom`) — OR special-case at plan time by wrapping bare-literal SET-expression RHS in an implicit CAST to the target column type in `planner.go`'s `applyUpdateAssign` (~8352-8361), which would also fix the pre-existing non-DateStyle int-range gap in UPDATE in one shot. Either approach needs its own dedicated loop with UPDATE-path regression tests (unlike INSERT, UPDATE's `newRow` construction has 3 separate call sites to update in lockstep). | Discovered via code audit while implementing the INSERT-side fix (the resume-point instruction explicitly required auditing `updateOp`/upsert siblings before considering this gap closed); fixing 3 separate UPDATE code paths plus deciding between a shared-helper vs. plan-time-CAST approach is a materially larger, riskier change than the bounded INSERT-side fix landed this loop, so kept as its own ledger item per ONE-task-per-loop discipline. |
| resolved | 2026-07-15 | M-NIGHTLY (run 20260714-011651 follow-up) — UPDATE-side date/timestamp/timestamptz/numeric + int-range literal coercion fixed (resume point from the two rows above) | Factored `insertOp.Next`'s coercion switch into a shared `coerceRowForConstraintChecks(cols []catalog.Column, row Row, include func(i int) bool, ctx *Context, pos int) error` helper (`internal/executor/operators_storage.go`, just above `insertOp.Next`) — resume-point option (a). `insertOp.Next` now calls it with `include = !insertMissing[i]` (behavior-preserving refactor). Wired the same helper into every UPDATE new-row construction site, gated on `include = o.plan.Set[i] != nil` (only freshly-SET columns get re-coerced; an untouched column's value came straight from storage and is already typed): `updateViaIndex` (main RangeScan callback + its EPQ-retry rebind), `updateOp.Next`'s SeqScan path (both the non-inheritance-child and inheritance-child-remap branches of its Phase-1 collect loop, PLUS a second, previously-undiscovered EPQ-retry rebind in its separate Phase-2 write loop — the resume point's line-number guess only named one), and `updateWithFrom` (main path + its own EPQ retry). That's 7 UPDATE call sites total, not the 3 the resume point estimated. Each site's `setCol` mask closure is declared once per operator invocation (not per row) for performance. New tests (`internal/executor/update_fk_datestyle_coerce_test.go`): `TestUpdateCoercesDateLiteralBeforeFKCheck` (mirrors the INSERT test, via an indexed-PK UPDATE so it specifically exercises `updateViaIndex`; confirmed non-vacuous via `git stash` — fails pre-fix with the un-reformatted literal), `TestUpdateCoercesNumericLiteralBeforeCheckConstraint` (same non-vacuousness check via `git stash`), `TestUpdateCoercesInt4RangeOverflow` (regression guard; this one already passed pre-fix because the heap encoder independently range-checks fixed-width int4 at write time, so the "silent overflow" this ledger row worried about didn't reproduce that way — kept as a guard that both layers keep agreeing). Design doc `docs/design/0097-0151-datestyle-partial-set-merge.md` "Follow-up (2026-07-15): UPDATE-side literal coercion" section; README index row updated. Gates: `go build ./...`/`go vet ./...` clean (repo-wide); `go test -count=1 ./internal/executor/...` PASS (full package, no regressions); `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke bash scripts/ralph-precommit-test.sh` PASS (0 failed, all 3 workloads). | None new from this loop — the DateStyle-adjacent open items already tracked by prior rows (`Datum.Format()`/`AppendValueText()`'s ~20-call-site DateStyle-unawareness, TIMESTAMPTZ timezone-aware conversion, `pgoutput.go`'s gap) remain the only open scope in this thread. | N/A (fully resolved) | N/A |
| resolved | 2026-07-15 | M-NIGHTLY (run 20260715-010036 triage) — `ActivityRegistry` procNum identity-space conflation fixed | Root-caused and fixed why `partition-drop-index-locking`/`insert-conflict-specconflict`/`detach-partition-concurrently-4` (all previously `pass_required`, all `first-seen: 20260715 new tonight` in `ci/logs/action-items.md`) regressed together: `ActivityRegistry.Register(b)` assigns a backend's slot via `procNumForPID(b.PID)` (a PID hash) while every dynamic call (`UpdateState`, `WaitEventStart/End`, `PIDForProcNum`) is keyed off `connTx.ProcNum` (`internal/server/server.go`, from `TxnMgr.AcquireConnSlot()` — an unrelated MVCC proc-array slot introduced to fix a separate PID-wraparound clobbering bug, per that call site's own comment). The two spaces silently diverged for most connections, freezing `pg_stat_activity.state`/`query` at their `Register()`-time defaults for the connection's whole lifetime — a wiring gap around 0118-0073's (still-correct, still-unit-tested) idle-query-retention fix, not a regression in it. Fix: new `ActivityRegistry.RegisterAt(procNum, b)` (registry.go, mirrors the existing `RegisterBackground(idx, b)` pattern); `Register(b)` now delegates to it; the one production call site (`server.go`) now calls `RegisterAt(procNum, …)` reusing the already-computed `TxnMgr.AcquireConnSlot()` value. Design doc `docs/design/0118-0141-activity-procnum-identity-space-conflation.md` + README index. Gates: `go build ./...` clean; `go test` PASS across `internal/activity`/`internal/server`/`internal/executor`/`internal/initdb`; full `TestPort_Isolation*` battery 0 FAIL (was 3 FAIL); `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke bash scripts/ralph-precommit-test.sh` PASS (0 failed, all 3 workloads). | None new — `PIDForProcNum(c.ProcNum)` (`internal/executor/context.go`, lock-wait/blocking-PID messages) shares the same fixed wiring, so no separate follow-up is needed there. | N/A (fully resolved) | N/A |
| resolved | 2026-07-15 | M-NIGHTLY (run 20260715-010036 triage) — `internal/wal` nightly failure root-caused as test-only scheduling artifact, fixed (resume point from the row above) | Picked up the row above's `internal/wal` resume point. Reproduced `TestStripeAppendConcurrentDrainConsistency`'s "drain goroutine never ran" reliably (10/10) via synthetic host contention (`for i in $(seq 1 20); do yes >/dev/null & done` on a 16-core box) running the FULL `internal/wal` package suite (not `-run`-isolated, so `t.Parallel()` tests genuinely compete) — never reproduced in isolation or under normal load, confirming it as a real scheduling possibility in the test's own busy-loop-drain-goroutine structure, not a `stripeAppend`/`publishVisibility` bug. Fixed via a `ready` channel closed after the drain goroutine's first iteration; the main test now blocks on it before starting producers, guaranteeing the goroutine ran at least once while preserving the concurrent-drain exercise afterward. While reproducing under the same load, ALSO found and fixed a second, previously-undiscovered flake in the package: `TestDrainSafetyStress`'s `checkInvariant` read `writeLSNAtomic`/`drainedLSNAtomic`/`flushedLSNAtomic` as three independent non-atomic `Load()`s in write-first order, letting a concurrent drain advance `drainedLSNAtomic` past an already-stale captured `write` value under contention (reproduced the same way, `dr` exceeding `wr` by ~565k bytes); fixed by reordering the reads to flush→drain→write, exploiting that all three fields are monotonically non-decreasing in production (`storeMaxLSN` CAS-max for write; `xlogWrite`'s `rq.write > writtenLSN` guard bounds drain/flush) so reading `write` last guarantees it has caught up to whatever the earlier reads saw. Neither fix touches production code. Design doc `docs/design/0107-0011-wal-drain-invariant-test-scheduling-artifact.md` + README index. Gates: `go build ./...`/`go vet ./internal/wal/` clean; both fixed tests 10/10 PASS under the identical contention setup that reproduced the failures pre-fix; `go test -race -count=1 ./internal/wal/...` clean; `go test ./internal/wal/... ./internal/mvcc/...` clean (quiet host); `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke bash scripts/ralph-precommit-test.sh` PASS (0 failed, all 3 workloads). | None new for `internal/wal` — both discovered flakes are now fixed. | N/A (fully resolved for `internal/wal`; `cmd/goopg`/`internal/amcheck`/`internal/mvcc` remain open per the row above) | N/A |
| resolved | 2026-07-15 | M-NIGHTLY (run 20260715-010036 triage) — `cmd/goopg`/`internal/amcheck` "units-timeout" root-caused as a nightly classifier bug, not a product hang (resume point from the two rows above) | Reproduced the whole `units` package set failing identically (`cmd/goopg`/`internal/amcheck`/`internal/initdb`/`internal/mvcc`) in 16.5 minutes under the exact nightly cgroup config (`GOOPG_MEM_HIGH=6G MEM_MAX=8G MEM_SWAP_MAX=0 GOMEMLIMIT=5GiB GOFLAGS=-p=4`), confirming genuine resource contention, not per-package flakiness. `cmd/goopg`/`internal/amcheck` die via a bare `signal: killed` (cgroup/OOM-style, unambiguous per `ci/design/03-resources-and-parallelism.md` §C); the classifier (`ci/batch/lib/summarize.py`) SHOULD have caught this via its existing `looks_resource_killed(log) and "--- FAIL" not in log` rule but ran it over the WHOLE combined ~40-package log — one genuine `--- FAIL` anywhere (that night, `internal/wal`'s real bug, since fixed) flipped the guard for the entire stage, misreporting the pure resource-kills as regressions. Same bug in the opposite direction silently swallowed `race/internal/access/btree` and `race/internal/amcheck` (never-before-surfaced real `race`-stage failures) into one uninformative whole-stage "resource kill" notice because that log had a `signal: killed` (from `race/cmd/goopg`) and zero `--- FAIL` anywhere. Fixed with per-package block-scoped classification (`split_go_test_pkg_blocks`) in `ci/batch/lib/summarize.py`; new `ci/batch/lib/test_summarize.py` unit coverage; regenerated `ci/logs/action-items.md` from the real historical logs to confirm the fix's effect on production data. See fix_plan.md M-NIGHTLY entry for full detail. | `internal/initdb`/`internal/mvcc` (and now `race/internal/access/btree`/`race/internal/amcheck`) still show only Go's own `-timeout` SIGQUIT dump with no `signal: killed` text — an ambiguous signature the classifier correctly leaves as "regression" rather than auto-suppressing, since a genuine product deadlock would look identical from the log alone. Whether these 4 are the same class of resource-starvation artifact as `cmd/goopg`/`amcheck` (this loop's working hypothesis — `internal/mvcc`'s dump shows one goroutine "running" with 20+ idle GC workers, a GC-pressure signature, not a blocked-on-a-real-lock signature) or hide a genuine hang remains formally unconfirmed for each. | `internal/initdb`/`internal/mvcc`: re-run under the same concurrent-load repro technique as this row (already demonstrated to work) with a full, untruncated goroutine dump captured per package, and inspect whether the "running"/GC-worker-heavy pattern recurs. `race/internal/access/btree`/`race/internal/amcheck`: brand-new fix_plan.md open tasks added this loop (never previously investigated — worth real attention given `internal/access/btree`'s history of real concurrency bugs, e.g. M0110-0007's split prev-link race). | Time-boxed to ONE task per loop; the classifier fix + its verification + the fix_plan/ledger bookkeeping for the 2 newly-surfaced race items already filled this loop's budget. |
| resolved | 2026-07-15 | M-NIGHTLY `race/internal/access/btree` + `race/internal/amcheck` — root-caused and fixed (resume point from the row above) | Both packages pass standalone in isolation (`btree` 23s, `amcheck` 148s) but reliably reproduced their nightly `-timeout` SIGQUIT under the exact nightly cgroup config (`GOOPG_MEM_HIGH=6G MEM_MAX=8G MEM_SWAP_MAX=0 GOMEMLIMIT=5GiB GOFLAGS=-p=4`, `scripts/goopg-test-run.sh`) — both hung specifically inside `internal/amcheck`'s `TestVerifyBtreeEngineSilentOnRealConcurrentContended` (200K inserts/64 writers/64-slot pool), never completing even with a 25m per-test timeout (exceeded 1800s). Root cause: this test's helper `buildRealTreeConcurrent` (`internal/amcheck/verify_nbtree_realtree_test.go`) still had ALL SIX of the M-NIGHTLY AI-20260708-064334-001 investigation's temporary debug-tracing flags permanently enabled (`bt.DebugTraceInserts`/`DebugVerifyFastPathInserts`/`DebugTraceFlushes`/`DebugTraceReloads`/`DebugTraceContentMu`/`DebugTraceBufmap`, plus `pool.DebugValidateCleanEvictions`/`DebugTraceSlotEvents`) — that investigation is long RESOLVED (bufmap tombstone bug, permanent regression guard `TestBufmapInsertSkipsPastTombstoneToExistingKey`), and `bufpool.go`'s own comment for `DebugTraceSlotEvents` literally says "alongside DebugValidateCleanEvictions once the root cause is fixed" (i.e. remove it), but nobody had. Each flag funnels every pin/unpin/insert of 64 concurrently racing goroutines through a handful of shared, mutex-guarded logs (full `pageItems()` page decodes + `insertLogMu`-serialized map/slice appends) — harmless standalone but, combined with `-race`'s synchronization-primitive overhead and the nightly's memory/CPU-contended 4-concurrent-package co-load, serialized this stress test so badly it blew past any reasonable per-package timeout. `internal/access/btree`'s own hang was pure collateral CPU starvation from sharing the box with amcheck's runaway test, not a bug of its own. Fix: removed all 8 flags/hooks + wiring from `buildRealTreeConcurrent`, plus the now-permanently-inert `bt.FastPathViolations()` diagnostic-log block that only ever produced a misleading "none recorded" message once the flag was off. Standalone time: `TestVerifyBtreeEngineSilentOnRealConcurrentContended` 172.65s→7.05s (24x), full `internal/amcheck` package 148s→11.4s (13x). Re-verified under the exact nightly cgroup config that originally reproduced the hang: both `internal/access/btree` (23.8s) and `internal/amcheck` (11.8s) now PASS cleanly. Gates: `go build ./...`/`go vet` clean; `go test ./internal/amcheck/... ./internal/access/btree/...` (plain + `-race`) PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke bash scripts/ralph-precommit-test.sh` PASS (0 failed, all 3 workloads). Design doc `docs/design/root-0028-amcheck-realtree-stress-debug-instrumentation-cleanup.md` + README index. | The now-disabled debug-tracing machinery itself (`BTree.DebugTrace*`/`DebugVerifyFastPathInserts` fields and their `Record*`/`traceInsert`/etc. methods in `internal/access/btree/btree.go`, `Pool.DebugValidateCleanEvictions`/`DebugTraceSlotEvents` in `internal/storage/bufpool.go`) was left in place (still zero-cost when unset, matching the codebase's existing pattern for prior resolved M-NIGHTLY investigations) rather than deleted outright — a broader dead-code removal across two production files was judged out of scope for this bounded fix. `internal/initdb`/`internal/mvcc`'s still-open ambiguous-SIGQUIT `units`-lane items (from the row above) were not investigated this loop. | If a future investigation needs this exact instrumentation again it can re-enable the flags temporarily (never leave them on past the investigation's resolution, per this row); otherwise a follow-up cleanup loop could delete the now-fully-unused `Debug*`/`Record*`/hook machinery from `btree.go`/`bufpool.go` entirely, verifying no other test file (grepped clean this loop) has since started using it. `internal/initdb`/`internal/mvcc` resume point unchanged from the row above. | Time-boxed to ONE task per loop; the repro + root-cause + fix + full nightly-config re-verification already filled this loop's budget, and `internal/initdb`/`internal/mvcc` need their own dedicated repro loop. |
| resolved | 2026-07-15 | M-NIGHTLY (run 20260715-010036 triage) — `internal/initdb`/`internal/mvcc` last-open items confirmed resolved (resume point from the two rows above) | Re-ran the exact nightly `units`-lane repro (`ci/batch/stages/stage-units.sh`'s own command: all 44 non-excluded packages, `GOOPG_MEM_HIGH=6G MEM_MAX=8G MEM_SWAP_MAX=0 GOMEMLIMIT=5GiB GOFLAGS=-p=4`, `scripts/goopg-test-run.sh`, `-timeout 30m`) now that this loop's `amcheck` debug-instrumentation fix (row above) is in place — `internal/initdb` (237.79s) and `internal/mvcc` (1.30s) both PASS cleanly, 0 `FAIL` across the whole run, comfortable margin under the 30m budget, no `signal: killed`/SIGQUIT/panic anywhere in the log. Confirms — not merely hypothesizes — that their `AI-20260715-010036-001`/`-002` nightly timeouts were the same collateral resource-starvation class as `cmd/goopg`/`amcheck`'s already-classified resource kills: `amcheck` runs in the (non-`-race`) `units` lane too, as one of the same 44 concurrently-scheduled packages, and its pre-fix debug-tracing-bloated stress test (172s standalone) was disproportionately eating the shared 6G/8G memory-capped `-p=4` co-load window that `initdb`/`mvcc` shared. With that hog removed, the whole lane now finishes with margin to spare. Design doc `docs/design/root-0028-amcheck-realtree-stress-debug-instrumentation-cleanup.md` "Follow-up (2026-07-15)" section + README index row updated. This closes the last open item from the `20260715-010036` nightly triage thread — all 11 `AI-20260715-010036-*` items now resolved. Gates: `go build ./...`/`go vet` clean (unchanged from prior loops, no product code touched this loop); full 44-package nightly-config repro run itself IS the verification; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke bash scripts/ralph-precommit-test.sh` PASS (0 failed, all 3 workloads). | None new — the `20260715-010036` nightly triage thread is fully closed. | N/A (fully resolved) | N/A |
| resolved | 2026-07-15 | M0119-0004 (DU-002 follow-up — VARIADIC function call-site argument collapsing, resolves the open item from the prior 2026-07-15 VARIADIC-array signature-matching row) | Fixed `SELECT sum_variadic(1, 2, 3)` (and any N-argument call against a `VARIADIC arr <type>[]` routine) previously failing `function sum_variadic(integer, integer, integer) does not exist`. Root cause: `resolveRoutineOverload` (`internal/executor/plpgsql_runtime.go`, sole call-resolution path for `evalStoredRoutineFuncCall`) required an exact `len(c.ArgTypes) == len(args)` match with no VARIADIC awareness — a materially different mechanism from the sibling row's DDL identity-resolution fix, and from `callOp.Open`'s already-VARIADIC-aware `CALL <procedure>(...)` path (M0097-0022), which resolves through an entirely separate code path. New `callArgTypesForCandidate` (accepts `n >= variadicPos`, type-checks excess positions against the VARIADIC parameter's element type) and `bundleVariadicArgs` (collapses trailing args into one array `Datum` via the existing `buildArrayDatum` helper before dispatch, matching every dispatch path's `args[i]`-to-`r.ArgTypes[i]` positional binding contract). New `internal/executor/variadic_call_test.go` (0/1/3/5-arg calls through both `LANGUAGE plpgsql` and `LANGUAGE sql` routines). Design doc `docs/design/0119-0004-variadic-call-argument-collapsing.md` + README index. Gates: `go build ./...`/`go vet ./...` clean; `go test ./internal/executor/... ./internal/catalog/... ./internal/parser/...` PASS; `go test -short $(go list ./... \| grep -v /internal/testport)` (full repo, short mode) 0 FAIL; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke bash scripts/ralph-precommit-test.sh` PASS (0 failed, all 3 workloads). | None — this row closes the open item. Remaining out-of-scope edges (recorded, not blocking): explicit `VARIADIC arr => ARRAY[...]` call-site syntax (opt out of collapsing) is unhandled, same as the pre-existing `CALL` path; overload disambiguation between a variadic and same-arg-count fixed-arity candidate isn't specially prioritized (`routineArgsExactMatch` still checks the original, non-expanded `c.ArgTypes`). | Neither edge is exercised by any known test or prior deferral-ledger item; deferring rather than expanding this bounded fix's scope. | Per-database catalog namespace (the DU-002 probe's actual remaining blocker) is unrelated and unaffected — see the 2026-07-06/2026-07-15 rows for that separate, milestone-scale resume point. |
| resolved | 2026-07-15 | M0122-0007 4e follow-up (`catalog.PGDependRowsForDBOid` missing opclass→opfamily `pg_depend` edge, resolves the open item from the prior 2026-07-15 `userOperatorFamilies`/`userOperatorClasses` row) | Added the missing unconditional `pg_depend` row this ledger's own prior row specified as the resume point: `PGDependRowsForDBOid` now emits one row per `c.userOperatorClasses` entry with `oc.DBOid == dbOid && oc.FamilyOID != 0` — `classid=2616` (pg_opclass), `objid=<class OID>`, `refclassid=2753` (pg_opfamily), `refobjid=<class.FamilyOID>`, `objsubid`/`refobjsubid=0`. One correction against the prior row's guessed deptype: verified live against `postgres/src/backend/commands/opclasscmds.c:731-735` (`DefineOpClass`'s `/* dependency on opfamily */ recordDependencyOn(&myself, &referenced, DEPENDENCY_AUTO)`) that the real deptype is `'a'` (AUTO), not `'n'` (NORMAL) as the resume point assumed from memory — confirmed harmless for restore-ordering purposes either way (`pg_dump.c`'s `getDependencies` query only excludes `deptype='p'`/`'e'`, treating `'a'` and `'n'` identically for its topological sort), but corrected to match upstream byte-for-byte since a later loop or user might diff `pg_depend` directly. Confirmed via a live re-run of the minimal repro from the prior row's "resume point" (`CREATE OPERATOR CLASS ... FAMILY op_family AS STORAGE bigint` after `CREATE OPERATOR FAMILY op_family USING btree`, both in one script) round-tripping cleanly, and via `TestPort_PgDumpConnectionSetup`'s DU-002 soft probe: the round-trip's failure point moved past the family/class ordering collision entirely to a NEW, unrelated blocker (see deferred column). Gates: `go build ./...`/`go vet ./...` clean repo-wide; `go test ./internal/catalog/...` PASS; `go test -short $(go list ./... \| grep -v /internal/testport)` (full repo, short mode) 0 FAIL; `go test -v -run '^TestPort_PgDumpConnectionSetup$' ./internal/testport/` PASS (soft-log confirms the advance); `RALPH_PRECOMMIT_SCOPE=smoke bash scripts/ralph-precommit-test.sh` PASS. | The DU-002 probe's new blocker: `conversion "aliasconv" already exists` restoring `CREATE CONVERSION public.aliasconv FOR 'mskanji' TO 'unicode' FROM public.aliasconv_func` into the fresh, empty `dumprestore_du002` database (fixture at `internal/testport/pgdump_connsetup_test.go:5416`). Root-caused (not fixed) by inspection: `UserConversion` (catalog.go ~line 3141) has no `DBOid` field at all, and `c.userConversions` (catalog.go ~line 2325) is one flat, server-wide `[]*UserConversion` slice with no per-database key of any kind — the same collision shape the M0122-0007 4e series has now fixed 8 times over (Domain/UserCollation/enumTypes/compositeTypes/RangeType/Routines/AccessMethod/UserOperatorFamily+UserOperatorClass), just not yet audited for conversions specifically. | Apply the identical M0122-0007 4e pattern to `UserConversion`: add `DBOid uint32`, fold a leading dbOid into the conversion lookup key (name+namespace, mirroring `userOpFamilyKey`/`accessMethodKey`), thread a trailing variadic `dbOid ...uint32` through the register/lookup/drop functions (grep `RegisterUserConversion`/`LookupUserConversion`/conversion-drop in catalog.go first — exact function names not yet confirmed this loop), wire all `internal/executor` call sites (grep `CREATE CONVERSION`/`DROP CONVERSION` in `operators_ddl.go`) via `catalog.NamespaceDBOid(o.ctx.CurrentDatabaseOid)` matching the operator-family precedent, and normalize the WAL-recovery registration path (if one exists) to `DefaultDBOid`. Re-run `TestPort_PgDumpConnectionSetup`'s DU-002 soft probe to confirm the blocker advances past `aliasconv`. | This is the next flat, dbOid-less registry in the same audit sequence (access-method → operator-family/class → pg_depend-ordering → conversions), discovered only because the pg_depend fix unblocked the probe far enough to reach it; recording it fresh rather than expanding this loop's scope, matching the series' established "root-cause, land, record-next-blocker" cadence (one task per loop). |
| resolved | 2026-07-15 | M0122-0007 4e follow-up (`catalog.userConversions` cross-database isolation, resolves the open item from the prior 2026-07-15 `PGDependRowsForDBOid` row) | Applied the identical M0122-0007 4e pattern to `UserConversion`, mirroring `UserCollation` byte-for-byte (same slice-of-pointers-with-a-DBOid-field shape, not a map -- `userConversions` stayed a `[]*UserConversion`, only gaining a `DBOid` filter predicate in every scan): added `UserConversion.DBOid uint32`; `CreateConversion`/`DropConversion` gained a trailing variadic `dbOid ...uint32` (via `resolveDBOid`), filtering/stamping `DBOid` alongside the existing `NamespaceOID` check; `CreateConversionDuringRecovery` stamps `uc.DBOid = DefaultDBOid` (WAL replay carries no dbOid yet, matching every sibling recovery path's documented convention) -- `DropConversionDuringRecovery` needed no change since it delegates to `DropConversion(name, schema)` with no dbOid arg, which now correctly resolves to `DefaultDBOid` via the same convention. New `ListUserConversionsForDBOid(dbOid)` (mirrors `ListUserCollationsForDBOid`) and `PGConversionRowsForDBOid(dbOid)` (mirrors `PGCollationRowsForDBOid`, minus a BKI-pinned-builtins prefix since pg_conversion's ~130 built-ins all live in pg_catalog and are dump-filtered, unlike pg_collation's 7 dumpable builtins) replace the old unfiltered `ListUserConversions()`-backed `pgConversion.VirtualRows` closure. Per-connection wiring added end-to-end: `executor.Context.PgConversionRows func() [][]string` field (context.go), a `pg_conversion`-named branch in `operators.go`'s virtual-row materializer (mirrors the `pg_collation` branch immediately above it -- this is the single site that serves both the simple and extended query protocols, since both share the same `ectx`), and `dispatch.go`'s `pgConversionRowLister` interface + `wireExtensionRows` wiring (mirrors `pgCollationRowLister`). Threaded `catalog.NamespaceDBOid(o.ctx.CurrentDatabaseOid)` through both live `operators_ddl.go` call sites (`CREATE CONVERSION`'s `im.CreateConversion(uc, schema, ...)`, `DROP CONVERSION`'s `im.DropConversion(...)`). New `internal/catalog/create_conversion_dbscope_test.go` (`TestCreateConversionCrossDatabaseIsolation`, mirrors `TestCreateCollationCrossDatabaseIsolation`). Confirmed via a live re-run of `TestPort_PgDumpConnectionSetup`: the DU-002 probe's failure point moved past `conversion "aliasconv" already exists` entirely to a NEW, unrelated blocker (see deferred column). Gates: `go build ./...`/`go vet ./...` clean repo-wide; `go test ./internal/catalog/... ./internal/initdb/... ./internal/executor/... ./internal/server/... ./internal/wal/...` PASS; `go test -short $(go list ./... \| grep -v /internal/testport)` (full repo, short mode) 0 FAIL; `go test -v -run '^TestPort_PgDumpConnectionSetup$' ./internal/testport/` PASS (soft-log confirms the advance to the ALTER CONVERSION blocker); `RALPH_PRECOMMIT_SCOPE=smoke bash scripts/ralph-precommit-test.sh` PASS twice (0 failed, all 3 workloads both times). | The DU-002 probe's new blocker is a parser gap, not a catalog-scoping gap: `ALTER CONVERSION public.aliasconv OWNER TO postgres;` fails `syntax error at or near "expected keyword table (got conversion)"` -- the parser's `ALTER ... OWNER TO` production apparently only recognizes a fixed set of object-type keywords after `ALTER`, and `CONVERSION` is not among them (the error message's phrasing -- "expected keyword table" -- suggests the parser fell through to a table-only `ALTER` production, meaning `CONVERSION` isn't recognized as a valid `ALTER <objtype>` keyword at all, not merely that the `OWNER TO` clause is unsupported for it). This is a materially different mechanism (parser grammar coverage, not catalog per-database key collision) from every M0122-0007 4e row so far. | `internal/parser`'s `ALTER` statement grammar (grep for the `ALTER FUNCTION`/`ALTER COLLATION` OWNER-TO productions as the sibling precedent -- both those object types clearly parse today, since the DU-002 probe reached this far past them) needs an `ALTER CONVERSION <name> OWNER TO <role>` production (and likely `ALTER CONVERSION ... RENAME TO`/`SET SCHEMA` alongside it, mirroring what `ALTER COLLATION` already supports, for parity -- check pg_dump's `dumpConversion` in `postgres/src/bin/pg_dump/pg_dump.c` for which ALTER forms it actually emits before broadening scope). Once parsed, wire the executor side: `catalog.InMemory` needs `SetConversionOwner`/`RenameConversion`/`SetConversionSchema` mirroring the `UserCollation` trio this loop's own fix left as precedent (`SetCollationOwner`/`RenameCollation`/`SetCollationSchema`, catalog.go ~12493-12573). Re-run `TestPort_PgDumpConnectionSetup`'s DU-002 soft probe to confirm the blocker advances past the ALTER CONVERSION OWNER TO statement. | This is a parser-grammar gap surfaced only because this loop's catalog-scoping fix unblocked the probe far enough to reach the first `ALTER CONVERSION` statement in the restore script; recording it fresh rather than expanding this loop's scope, matching the series' established "root-cause, land, record-next-blocker" cadence (one task per loop). |
| resolved | 2026-07-15 | M0122-0007 4e follow-up (parser gap: `ALTER CONVERSION <name> OWNER TO <role>`, resolves the open item from the row immediately above) | Added a dedicated `AlterConversionStmt` AST node (`internal/parser/ast.go`) and its `parseAlter()` grammar branch (`internal/parser/ddl.go`, immediately after the `ALTER COLLATION` branch), mirroring `AlterCollationStmt` byte-for-byte minus `REFRESH VERSION` (collation-only), confirmed against `postgres/src/backend/parser/gram.y`'s three `ALTER CONVERSION_P` productions (`RENAME TO`/`SET SCHEMA`/`OWNER TO`) so all three, not just the probe's `OWNER TO`, were modelled for grammar parity. Catalog: `RenameConversion`/`SetConversionOwner`/`SetConversionSchema` + `*DuringRecovery` counterparts (`internal/catalog/catalog.go`, right after `DropConversionDuringRecovery`), mirroring the `UserCollation` trio this same series' prior parser-gap row named as precedent. Executor: `execAlterConversion` (`internal/executor/operators_ddl.go`, right after `execAlterCollation`) + a `*parser.AlterConversionStmt` case in the DDL dispatch switch. WAL durability (collation's ALTER forms already had it, so conversion's got it too rather than landing without, since `CreateConversion`/`DropConversion` already WAL-log): 3 new record kinds `RecordKindAlterConversionRename`/`Owner`/`SetSchema` = 130/131/132 + Encode/Decode pairs (`internal/wal/recovery.go`) + `internal/initdb/conversion_ddl_recovery.go` replay wiring (mirrors `collation_ddl_recovery.go`) + the physical-replay no-op classification case. Two additional wiring sites the collation precedent's own code didn't surface until `Plan()` actually failed on the new stmt type: `internal/planner/planner.go`'s DDL-passthrough type list and `internal/server/dispatch.go`'s command-tag switch both needed a `*parser.AlterConversionStmt` case (a new DDL statement type is wired at 4 sites total -- parser, executor, planner, and dispatch's tag lookup -- not the 2 the `AlterCollationStmt` grep alone suggested). New tests: `internal/parser/alter_conversion_test.go` (rename/owner/setschema parse shapes, including the probe's exact `ALTER CONVERSION public.aliasconv OWNER TO postgres` SQL) and `internal/executor/alter_conversion_test.go` (mirrors `alter_collation_test.go`'s rename/owner/setschema/IfExists/42704 coverage). Confirmed via a live re-run of `TestPort_PgDumpConnectionSetup`: the DU-002 probe's failure point moved past `aliasconv`'s `ALTER CONVERSION ... OWNER TO` entirely to a NEW, unrelated blocker (see deferred column). Gates: `go build ./...`/`go vet ./...` clean repo-wide; `go test ./internal/parser/... ./internal/catalog/... ./internal/wal/... ./internal/initdb/... ./internal/executor/... ./internal/planner/... ./internal/server/...` PASS; `go test -v -run '^TestPort_PgDumpConnectionSetup$' ./internal/testport/` PASS (soft-log confirms the advance). | The DU-002 probe's new blocker is a cross-database catalog-key-collision, the same shape this series has fixed repeatedly for other registries, now hitting text search dictionaries: restoring into the fresh `dumprestore_du002` database fails `text search dictionary "simple_dict" already exists` -- `catalog.UserTSDict`/`InMemory.CreateTSDict` (added in the 2026-07-06 pg_dump-slice-437 row, see that row's own "no WAL/recovery persistence" deferred note) was never audited for the DBOid-scoping treatment this series applies to every other flat registry, and per that same 2026-07-06 row's deferred column, `CreateTSDict` also still has no WAL/recovery durability at all (both gaps are independent of each other and both remain open). | `internal/catalog/catalog.go`'s ts-dictionary registry (grep `CreateTSDict`/`ListUserTSDicts`/`UserTSDict` -- neighbors of `CreateConversion`/`ListUserConversionsForDBOid`) needs the identical M0122-0007 4e pattern: a `DBOid uint32` field, a trailing variadic `dbOid ...uint32` on `CreateTSDict`/`DropTSDict` (if `DropTSDict` exists) via `resolveDBOid`, a `ListUserTSDictsForDBOid`/`PGTSDictRowsForDBOid`-shaped lister mirroring `ListUserConversionsForDBOid`/`PGConversionRowsForDBOid`, and per-connection wiring through `executor.Context`/`operators.go`'s virtual-row materializer/`dispatch.go`'s `wireExtensionRows` (mirrors the `pg_conversion` wiring this same series added two rows above). Verify via `TestPort_PgDumpConnectionSetup`'s DU-002 soft probe -- expect the blocker to advance past `simple_dict` to whichever object the restore script reaches next. | This is the next flat, dbOid-less registry in the same audit sequence (access-method -> operator-family/class -> pg_depend-ordering -> conversions -> ALTER CONVERSION grammar -> text-search dictionaries), discovered only because this loop's parser fix unblocked the probe far enough to reach it; recording it fresh rather than expanding this loop's scope, matching the series' established "root-cause, land, record-next-blocker" cadence (one task per loop). |
| resolved | 2026-07-16 | wal-pg-identical-stream A8-vacuum + A8-newroot (resolves the earlier "A8-vacuum / A8-newroot deferred" row) | Flipped BtreeVacuum -> `EncodeBtreeVacuumPG` (RM_BTREE/VACUUM 0xC0; post-vacuum leaf as block-0 apply-FPI) and BtreeNewRoot -> `EncodeBtreeNewRootPG` (RM_BTREE/NEWROOT 0xA0; new root at block-0 WILL_INIT + updated metapage at block-2, both apply-FPIs). Both reuse the A0 FPI encoder + the existing RmgrBtree default replay arm (`replayDecodedXLogHeapFPIBlocks`) -- ZERO new replay code, identical to A8-split. `LogBtreeVacuumFunc` narrowed `(rel, blk, kept [][]byte, flags uint16)` -> `(rel, blk, page storage.Page)` (2 emit sites: `btree_vacuum.go` VACUUM path + `btree.go` dedup-recovery rewrite, both pass `slot.Page()`); `LogBtreeNewRootFunc` `(rel, rootBlk, level, items)` -> `(rel, rootBlk, rootPage, metaBlk, metaPage)` (2 emit sites: `createNewRoot` + `resetToEmptyRoot`). NewRoot now mutates the metapage in memory under `splitMu` BEFORE the emit so its bytes ride the same record (retired the now-dead `updateRootMetaWithLSN`); co-holding root+meta is deadlock-free (each newroot holds its private root slot then the shared metapage -- no lock cycle since root slots are never contended). Metapage FPI is hole-safe: the meta struct lives at page[24:48], below `pd_lower=48` (set by `writeMeta`), so it is preserved in `page[:pd_lower]` and never in the dropped `[pd_lower:pd_upper]` hole. Test mocks migrated: `btree_vacuum_wal_test.go` kept-items capture -> page-derived count/flags; `lpdead_kill_test.go` kept-items+`ReplayVacuumPage` assertions -> `PageItemKeys(page)`. New `internal/wal/btree_vacuum_newroot_pg_test.go` round-trips both (decode asserts opcode + apply-image block refs; apply asserts pages restored byte-for-byte + pd_lsn stamped). Gates: `go build`/`go vet`, wal + `-race`, executor, access/btree + `-race`, initdb crash-recovery, e2e replication/failover, isolation + regress -- all green. | **BtreeUnlinkPage (23)** is now the ONLY non-PG-format btree structural record. | Flip UnlinkPage via PG's real incremental `xl_btree_unlink_page` main-data (leftsib/rightsib/level/safexid/leafleftsib/leafrightsib/leaftopparent per `postgres/src/include/access/nbtxlog.h:310-329`), NOT FPI -- preserving goopg's self-correcting apply-time relink re-derivation. See the dedicated UnlinkPage row for the full plan. | Vacuum/newroot are FPI-feasible because the post-op page(s) are live under the pinned slot at emit (newroot's metapage is threaded by updating it in memory before the emit); unlinkpage is not (an emit-time sibling snapshot goes stale under a concurrent cross-connection split). MarkHalfDead has no producer. |
| resolved | 2026-07-16 | TestPort_IsolationSuite false-green harness race (resolves the finding row above, part 1) | FIXED the false-green: nested the spec loop under a synchronous inner-group `t.Run("specs", ...)` so the parent's `defer c.Stop()` runs only AFTER all parallel spec subtests finish (the inner-group t.Run blocks on its parallel children), keeping the shared cluster alive for the whole run. Added `isClusterUnreachable(diff)` — a "defer" whose diff is a TCP connection failure (connection refused / dial tcp / no such host / connection reset) is now a hard `t.Fatal`, not a skip, so a dead cluster can never masquerade as PASS again. Verified: suite now runs specs for real — 62 PASS / 61 SKIP / 0 FAIL (was 0 PASS / 123 all-SKIP), single cluster shutdown at the very end, no spec regressed by the A2–A8 WAL flips. commit e78dc869. | Part 2 of the finding (go-run test-server orphans piling up in `/tmp/TestPort_*`) is NOT fixed by this commit — still manually PID-killed each session. | Add a cleanup/reaper that kills the goopg CHILD by PID (not just the `go run` parent) in the cluster Stop path, or a periodic `/tmp/TestPort_*` sweep. The `go run ./cmd/goopg` child survives when the test harness kills the `go run` parent. | The false-green suite was the correctness-critical half (a gate that validated nothing while reporting green); the orphan accumulation is a resource-hygiene nuisance, not a correctness gap, so it is split out and left as a bounded follow-up. |
| resolved | 2026-07-16 | wal-pg-identical-stream A9-smgr-create (resolves the smgr half of the A9 routing-robustness row above) | LANDED (commit 44144816): relation-file creation flipped to a PG `RM_SMGR`/`XLOG_SMGR_CREATE` record (`EncodeSmgrCreatePG`) — 16-byte `RelFileLocator{spcOid,dbOid,relNumber}`+`ForkNumber` main-data, no block ref, with the CREATING transaction's xid in the header (PG-faithful, mirrors `log_smgrcreate`). The real xid is also the routing fix: a main-data-only record reaches the decoded replay path only when `nativeHeaderMatchesMainData` is false, and a non-zero header xid always mismatches `classifyXLogRecord`'s xid=0, so it routes correctly regardless of the RelFileLocator's leading byte — the smgr_create_pg_test.go proves the tablespace-OID-16395 (=16384+11, the exact `RecordKindSmgrCreate` collision) case routes to the decoded path via the xid. New `RmgrStorage`/`XLOG_SMGR_CREATE` decoded arm decodes the body and calls `applySmgrCreate` (factored out of native `replaySmgrCreate`). xid plumbing (thread-safe, mirrors A6's explicit-xid pattern): added `Pool.PinNewWithXID`/`ExtendRelationBatchWithXID` variants (plain `PinNew`/`ExtendRelationBatch` stay → xid=0 for bootstrap/catalog-internal creates, all in default/global tablespaces = routing-safe); wired every user-relation create path to `ctx.Tx.XID` (materialized before create): heap (`writeHeapRowReturning`/`PG`, TOAST, batch-extend), postgres-DB mirror, and index build via `btree.Options.CreateXID` (`BulkCreateWithXID`/`CreateWithXID` from the 4 executor CREATE INDEX sites). Gates: build/vet, wal+storage+btree+executor units, crash-recovery (230s), e2e ×3, isolation (real coverage), regress (284s) — all green. | A9-clog-truncate NOT yet flipped (the clog half of the row above). checkpoint-opcode / xact-inval-fold / legacy-frame-retire unchanged (see the A9 row two entries up). | clog-truncate: PG's `xl_clog_truncate` carries `xl_xid=0` (confirmed clog.c `WriteTruncateXlogRec` — no `SetCurrentTransactionId`), so the xid-routing trick does NOT apply. Use the size-check disambiguation instead: teach `nativeHeaderMatchesMainData` (or the routing) to reject when the main-data length != the native fixed size for the classified kind (native `RecordKindClogTruncate` = 5 bytes vs PG's 16). Then widen the body to `{pageno int64, oldestXact u32, oldestXactDb u32}` (pageno = oldestXid/clogXactsPerPage), thread `oldestXactDb` (datoid) through `mvcc.CLog.TruncateCLOG`/`SetTruncateLogger` (currently absent — stopgap `oldestXactDb=0` since goopg redo only uses pageno+oldestXact), add a `RmgrCLOG` decoded arm, and update the initdb xact-recovery clog scanner to decode the PG body. | The smgr half was the higher-value, cleaner flip (PG stamps a real xid, which both achieves byte-parity and self-solves routing). clog-truncate is lower-frequency (vacuum/checkpoint maintenance) and — because PG uses xid=0 — needs a different routing mechanism (size-check) plus a datoid plumb, so it is split into its own increment. |
| resolved | 2026-07-16 | wal-pg-identical-stream A9-clog-truncate (resolves the clog half of the A9 routing row) | LANDED (commit 8e546df1): CLOG truncation flipped to a PG `RM_CLOG`/`CLOG_TRUNCATE` record (`EncodeClogTruncatePG`) — 16-byte `xl_clog_truncate{pageno int64, oldestXact, oldestXactDb}`, no block ref, `xl_xid=0` (PG's `WriteTruncateXlogRec` carries `InvalidTransactionId` — clog truncation is a maintenance op). Because xid=0 can't drive routing (the smgr-create approach), `nativeHeaderMatchesMainData` gained a native-size guard (`nativeFixedRecordSize`): a same-classified main-data-only record whose length != the native fixed size (ClogTruncate=5, SmgrCreate=10 vs the PG 16) is not treated as native and routes to the decoded path. This resolves the pageno-low-byte-==-33 collision (`clog_truncate_pg_test.go` drives exactly pageno=33 and asserts the PG record routes decoded while a native 5-byte record still routes native). Blast radius = exactly the SmgrCreate/ClogTruncate kinds (belt-and-suspenders for a colliding bootstrap smgr-create too). New `RmgrCLOG` decoded arm = physical no-op (matches native `RecordKindClogTruncate`); the initdb `replayCLogFromWAL` scan got a PG-format branch (`DecodeXLogClogTruncate`) that re-applies the idempotent truncation (native `EncodeClogTruncate`/`DecodeClogTruncate` kept as dead fallback for pre-flip WAL). Gates: build/vet, wal+mvcc+initdb units, crash-recovery (217s — exercises the clog replay AND the core-routing size guard that every table-creation smgr-create now traverses), e2e ×3, isolation (real), regress (278s). | `oldestXactDb` (datoid) body field stamped 0. checkpoint-opcode / xact-inval-fold / legacy-frame-retire unchanged (see the A9 row above). | Thread the real oldest-datfrozenxid database OID from the checkpointer's horizon computation (open.go:1743/1769) through `mvcc.CLog.TruncateCLOG(oldestXid, datoid)` + the `SetTruncateLogger(func(oldestXid, datoid))` signature (currently oldestXid-only) into `EncodeClogTruncatePG`'s second arg. Low priority: goopg's clog redo (and the PG-parseability we need) only consume pageno+oldestXact; the datoid matters only for a real PG standby's per-database recovery-conflict handling, which goopg has no consumer for yet. | The size-guard was the load-bearing part (correct routing for an xid=0 record) and is now general/robust; the datoid is a cosmetic body field with no goopg or near-term parity consumer, so it is split out as a bounded stopgap-documented follow-up rather than blocking the flip on a `TruncateCLOG` signature change. |
| resolved | 2026-07-16 | wal-pg-identical-stream Phase-A exit gate — RESOLVED (supersedes the "blocked on FPI<->logical unification" row above) | **Phase-A exit gate is GREEN** (commits 45f901aa + b30d1aff). Structural: real `pg_waldump` parses all §A records (`TestPGWaldumpParsesEmittedWAL` + `TestPort_WALPgWaldumpCompat`, real cluster workload). Replay: a real PG 18 standby fully replays goopg WAL (`TestE2E_FailoverGoopgToPG`, async + sync_remote_apply, re-enabled). The earlier row diagnosed the replay PANIC (`WAL contains references to invalid pages` at a Heap/INSERT for a fresh page) as needing FPI<->logical unification (doc 01 §5) — that was WRONG. A pg_waldump dump of a CREATE-TABLE-then-INSERT workload showed goopg emits `Heap INSERT blk0` then a SEPARATE `XLOG FPI blk0`; the real fix is much smaller: stamp `XLOG_HEAP_INIT_PAGE` (info 0x80) + `REGBUF_WILL_INIT` on the first insert into an empty page (`markHeapInsertDirty`: `initPage = pageLinePointerCount==1`; `EncodeHeapInsertPG` sets the info bit + block WILL_INIT), so PG's heap redo `XLogInitBufferForRedo`+`PageInit`'s the not-yet-extended page before applying — exactly PG's own first-insert-on-a-new-page behaviour. The trailing first-touch FPI then harmlessly restores the same image. goopg's own replay is unaffected (opcode masked by 0x70; `replayDecodedXLogHeapInsert` decides extend-vs-apply from NBlocks). Full gates green (crash-recovery 221s, e2e incl. real-PG failover 31s, pg_waldump 1.4s, isolation 30s, regress 286s — row counts unchanged). **So FPI<->logical unification is NOT required for Part A and is dropped as a blocker.** | (1) **btree first-insert-on-a-new-leaf** equivalent for indexed-table PG-standby replay is not gate-tested (the failover uses an index-less `bench_log`); an indexed-table failover could still hit a missing btree page on the standby. (2) Automated WAL byte-for-byte diff vs a live PG for a pgbench run (no tooling exists; structural parse + real-PG replay are the current proxies). (3) A9 cleanup items unchanged: checkpoint-opcode (byte-identical no-op — already RmgrXLog/shutdown on-wire), xact-inval-fold + legacy-frame-retire (dead-code removals). | (1) If an indexed-table failover panics on a btree page, mirror the INIT_PAGE fix on the btree side: mark the first insert into a freshly-initialised btree leaf/page as WILL_INIT / a page-creating record (btree in PG uses XLOG_BTREE_INSERT with the page registered REGBUF_WILL_INIT for a new page, or relies on the split/newroot FPIs). Add an indexed-table variant to `TestE2E_FailoverGoopgToPG` to exercise it. (2) Script a `pg_waldump`-based byte-diff if literal parity is required. | The INIT_PAGE fix is the PG-faithful, minimal way PG creates a first page during redo; it fully unblocks the replay gate for the heap path. The btree path is a bounded follow-up (only matters for a heterogeneous PG standby of an indexed table); the byte-diff is a stronger-than-needed parity check that structural pg_waldump + real-PG replay already substantially cover. |
| resolved | 2026-07-16 | wal-pg-identical-stream A9 cleanup pair — xact-inval-fold + legacy-frame-retire (closes the two dead-code items carried in the A9 rows above) | **Both LANDED.** (1) A9-xact-inval-fold (commit d02f7d91): standalone `RecordKindXactCommitInval` (byte 32) deleted — invals already rode A6's `HAS_INVALS` chunk on `xl_xact_commit` (`xactCommitCarriesInvals`), so the record, its Encode/Decode, native replay case, `DecodeXactMarker` arm, native-kind-set entry, and both initdb scanner arms were dead weight; `TestApplyRecordXactCommitInvalUnlinksInitFiles` migrated to `EncodeXactCommitPG(xid, true)`. (2) A9-legacy-frame-retire (commit 81d850bc, −730/+256): the pre-M0014 8-byte IEEE-CRC frame (`encodeRecord`/`decodeRecord`) deleted; Config normalization forces `PageHeaders=true` (+TimelineID=1 default) so goopg has exactly ONE on-disk WAL format (PG-compat page-headered XLogRecord stream); a pre-A9 legacy dir is rejected by NewWriter (`ErrWALFormatMismatch`) and ReadAll — re-init required. `waitInsertionsToFinish` gained a written-frontier guard so `FlushUpTo(unwritten)` returns `ErrLSNNotWritten` instead of spinning (the legacy early-return used to break that loop); first version read state-loop-owned `s.writeLSN` and was caught by `-race` — final version consults only the atomic `writeLSNMirror`. Legacy-byte-math tests migrated (retention → fill-by-observed-end-LSN @512B segs; discover-checkpoint → page-aligned segs + stripe path; buffer round-trip → body via `XLog.MainData` when payload[0] collides with a natively-sized kind; format-mismatch → hand-crafted legacy seed bytes; eager-close → two sub-segment records since the stripe path confines a record to one segment). Gates: wal unit + `-race`, initdb incl. crash-recovery (220s), pg_waldump structural ×2, `TestE2E_FailoverGoopgToPG` async+sync — all GREEN. | A9-checkpoint-opcode remains the ONLY open A9 item (deferred: hot-standby entanglement; byte-identical no-op on the wire). Part-A followups unchanged: btree INIT_PAGE analog, WAL byte-diff tooling, clog-truncate datoid. | checkpoint-opcode resume point: route via `framePGAssembled(RmgrXLog, online\|shutdown)`, plumb kind + `oldestActiveXid` from the checkpointer, drop the `len==88` classify hack, re-verify hot-standby bring-up (`PMSIGNAL_BEGIN_HOT_STANDBY`). | User explicitly requested both cleanups NOT be deferred ("do two code cleanup tasks. not defer."); landing them ends the two-format maintenance burden (every writer/reader change no longer needs a legacy twin) and removes a silent-misroute class (native kind byte vs PG main-data collisions now confined to the two size-guarded kinds). |
| resolved | 2026-07-16 | wal-pg-identical-stream A9-checkpoint-opcode (closes the last A9 item; supersedes the "deferred: hot-standby entanglement" note in the A9 rows above) | LANDED (commit 115121c7). Checkpoints carry their EXPLICIT opcode via `EncodeCheckpointPG` (`framePGAssembled(RmgrXLog, XLOG_CHECKPOINT_ONLINE\|SHUTDOWN)`); `classifyXLogRecord`'s classify-by-len==88 hack (which could only stamp SHUTDOWN) is deleted. The entanglement resolved by emitting the FULL upstream online-checkpoint chain rather than avoiding it: (1) `XLOG_CHECKPOINT_REDO` (0xE0, PG17+) inserted AT the redo point — appended inside `Pool.PublishRedoBarrier`'s critical section so redo==marker-start is atomic (fpiPublishMu→WAL-stripe lock order matches the FPI decide→append sections; the marker makes no FPI decision → no deadlock); PG recovery FATALs `unexpected record type found at redo point` without it. (2) `XLOG_RUNNING_XACTS` (RM_STANDBY, upstream LogStandbySnapshot, xlog.c:7243) from the new `CheckpointerConfig.RunningXactsFn` ← mvcc `FreshSnapshot()` (InProgress=top-level xids, Xmin=oldestRunning, Xmax-1=latestCompleted; subxid_overflow conservatively true when xacts run, exact-false when idle) — required for a hot-standby to reach STANDBY_SNAPSHOT_READY after an ONLINE checkpoint. (3) The checkpoint record with real `oldestActiveXid` (InvalidTransactionId on shutdown, like upstream). BASE_BACKUP fallout fixed in the same change: backup_label CHECKPOINT LOCATION + pg_control `CheckPoint` now carry the checkpoint RECORD's own start via new `Checkpointer.LastCheckpointRecordLSN()` (plumbed through `executor.Checkpointer` + `UpdateControlCheckpoint(dataDir, redo, ckptRec)`) — the record no longer sits at redo, and the standby rejects `invalid resource manager ID in checkpoint record` if the label points at the RUNNING_XACTS record. Read side header-driven: `isCheckpointRecord`/`replayStart` consume the 88-byte struct from `XLog.MainData` (decoded path; explicit-opcode records can never match native classify — no RecordKind maps to (RmgrXLog,0x00\|0x10\|0xE0)) with legacy Payload arms kept for pre-flip WAL. Real-PG failover: 228s (retry-timeout) → 5s. Gates: wal unit+`-race`, initdb crash-recovery (222s), pg_waldump ×2, isolation (24s), regress (262s), e2e ×7 (real-PG failover async+sync, PG→goopg failover, physical+logical replication, standby-attach-restart, WAL-redo smoke, server+executor units) — all GREEN. | Part-A followups unchanged: btree INIT_PAGE analog (indexed-table PG-standby failover untested), WAL byte-diff tooling, clog-truncate datoid. `EncodeCheckpointCompat` kept as a test-only struct builder; legacy 1-byte `EncodeCheckpoint()` still behind `PGCompatCheckpoints=false`. | If an indexed-table failover is added: mirror INIT_PAGE on btree. If checkpoint chains need byte-parity審査: pg_waldump-diff the [redo, ckpt] window vs a real PG CHECKPOINT. | The "hot-standby entanglement" was not a reason to defer — it was the specification: PG17+ made the ONLINE chain (REDO marker + RUNNING_XACTS + record) mandatory wire protocol, so the faithful emission is the ONLY correct flip; the always-SHUTDOWN hack was also hiding a 45× e2e slowdown. |
| resolved | 2026-07-16 | wal-pg-identical-stream B1.1 — pg_namespace converted to heap journaling (first Section-B catalog; kinds 34/35/100/101 retired) | LANDED: schema DDL journals real pg_namespace heap rows (CREATE = XLOG_HEAP_INSERT + 2 XLOG_BTREE_INSERT_LEAF on 2684/2685; RENAME/OWNER = non-HOT XLOG_HEAP_UPDATE via the B0.2 producer — its first caller; DROP = XLOG_HEAP_DELETE). TID-carrying write-through cache on the schema registry (`catalog.SchemaHeapTID`); startup reload = generic heap scan (`reloadUserSchemasFromHeap`, oid ≥ FirstUserOID) at the retired scanner's exact pass slot; mirror set += 2615/2684/2685; parse-recovery fallback uses a frozen-xid sync. Deleted: RecordKinds 34/35/100/101 + encoders/decoders + schema_ddl_recovery.go + wal/schema_alter_ddl.go + dispatch/native-list entries + 3 test files (grep-zero). Full gate green incl. regress 298s + new ALTER-restart durability test + pg_waldump + real-PG failover. | (1) nspacl heap column written as "{}" — per-schema GRANTs render only in the virtual view (NamespaceACLText), not the heap row. (2) pg_depend rows for schema→owner (B3). (3) TRUE per-DB pg_namespace rows (schema-per-database semantics) need a dbOid-aware registry — goopg's registry stays cluster-global. (4) Index entries for dropped/renamed schemas linger until vacuum (PG parity, but goopg never vacuums catalogs). | (1) wire NamespaceACLText into buildPGNamespaceRow + re-UPDATE the row on GRANT/REVOKE ON SCHEMA. (2) B3 pg_depend conversion. (3) registry keyed by (dbOid, name) + per-DB reload loop. (4) catalog vacuum rides the future catalog-maintenance work. | The exemplar proves the whole 02b recipe end-to-end (tuple builder → emit swaps → index helpers → reload desc → deletion checklist); pg_proc/pg_sequence (B1.2/B1.3) now follow a beaten path. |
| resolved | 2026-07-16 | wal-pg-identical-stream B1.2 — pg_proc converted to heap journaling (kinds 61-64/121-123 retired) | LANDED: function/procedure DDL journals real pg_proc heap rows — 30-column PG18-physical builder (`sys_pg_proc.go`; oidvector/text[]/char blobs via executor twins of the initdb encoders); CREATE = XLOG_HEAP_INSERT, CREATE OR REPLACE + ALL five ALTER FUNCTION variants = one non-HOT XLOG_HEAP_UPDATE carrying the total post-mutation row, DROP (incl. deferred-at-commit) = XLOG_HEAP_DELETE. Routine metadata the physical columns cannot carry (arg names/modes/defaults raw SQL, RETURNS TABLE, BEGIN ATOMIC/RETURN forms, dep tracking) rides a JSON blob in proargdefaults — PG reads that column ONLY when pronargdefaults>0, so default-less functions gain no PG-side deviation. Reload = `reloadUserRoutinesFromHeap` (generic scan, oid ≥ FirstUserOID, JSON→Routine, seeds the OID-keyed TID cache on the Routines store). Mirror set += 1255. All seven kinds + codec block + function_ddl_recovery.go + both test files deleted (grep-zero); scanner tests replaced by TestPort_FunctionSurvivesRestart (SQL-level create/replace/drop across restart). Full gate green. | (1) **pg_proc index maintenance (2690/2691)**: both are multi-level btrees (3397 bootstrap entries); goopg has no descent-aware catalog-btree insert, so user functions get NO index entries — exactly today's behavior (the indexes never carried user rows), but a real-PG standby resolving a user function via SearchSysCache(PROCOID) index scan will miss it (seqscan paths see it). (2) proallargtypes/proargmodes written empty (OUT-arg metadata in the JSON blob only). (3) proargdefaults JSON is goopg-private — a real PG standby calling pg_get_expr on a function WITH defaults would choke (was already deviant). | (1) Build a descent-aware insert for PG-format catalog btrees (walk internal→leaf via the existing page decoders, split leaves, WAL via xl_btree_insert) — shared infrastructure for every remaining catalog with multi-level bootstrap indexes (pg_type 2703, pg_operator 2688, …), so it likely lands as its own B2-prep slice. (2)+(3) ride the same slice: once real index maintenance exists, promote OUT-arg metadata into the physical columns. | Heap-row parity + kind retirement now, index-record stream parity later, mirrors the A-phase btree-INIT deferral family; blocking B1.2 on new btree machinery would have stalled the whole B1 chain. |
| resolved | 2026-07-17 | wal-pg-identical-stream B1.3 — pg_sequence catalog row converted (definition on the heap; kinds 65/66 survive per the 02c scope decision) | LANDED: CREATE/ALTER SEQUENCE journal a real Form_pg_sequence heap row (base/<db>/2224 + index 5002 entry); the upsert lives in WALLogSequenceState (the definition funnel) behind a definition FINGERPRINT so counter-only snapshots (setval / TRUNCATE RESTART pre-logs / nextval horizons) emit no pg_sequence update — matching PG, which never touches pg_sequence on setval. All 9 DropSequence emit sites paired with a heap DELETE. TID cache = seqKey-keyed sync.Map {tid, defSig}; startup reseed scans the heap AFTER the kind-65 replay (reloadSequenceHeapTIDs) so post-restart ALTERs update in place (pinned by TestPort_SequenceCatalogRowSurvivesRestart). pg_waldump structural workload extended with schema/function/sequence DDL. | Kinds 65 (SequenceState) and 66 (DropSequence) SURVIVE by design: the counter (last_value/log_cnt/is_called) and goopg's own recovery still ride the name-keyed kind-65 records — B1.3b retires both by flipping the counter to XLOG_SEQ_LOG (RM_SEQ=15) on the 1-tuple sequence-relation page + making the heap row goopg's definition source. OWNED BY / identity markers still in kind 65 (pg_depend = B3). pg_sequence READ model stays registry-virtual. | B1.3b: (1) sequence relation gets a real 1-page heap (FormData_pg_sequence_data), nextval emits XLOG_SEQ_LOG (pre-log 32); (2) reload reads definition from pg_sequence heap + counter from the seq page; (3) delete kinds 65/66 + sequence_ddl_recovery.go; (4) seqrelid-keyed registry replaces name-keying. | The catalog-row half was cleanly separable and unblocks B1 completion; the counter flip is a Part-A-style record flip on a new rmgr and deserves its own gated landing. |
| resolved | 2026-07-17 | wal-pg-identical-stream B2-prep — descent-aware catalog-btree insert (closes residual (1) of the B1.2 row above) | LANDED. Discovery: the multi-level descent machinery (readSysBtreeMeta/descendSysBtreeToLeaf/insertIntoExistingLeaf/rebuildSysBtreeWithNewEntry, sys_catalog_btree_multilevel.go, M0106-0010 batched-41) ALREADY existed — the B1.2 residual's "no descent-aware insert" premise was stale. Real gaps closed: (1) keyMetaForSysBtree registration — 2690/2691 added, and 2684/2685 were found MISSING too (latent B1.1 hole: a filled pg_namespace leaf-root would have errored "split: unsupported system btree OID"); (2) variable-length IndexTuple support for 2691 (proargtypes oidvector): btreeIndexKeyMeta.variable flag, variable-tolerant collectAllLeafTuples/splitLeafRootAndInsert/rebuildSysBtreeWithNewEntry, buildBulkSysBtreeLayoutVariable (executor twin of initdb pgBuildBtreeBulkLoadVariable), cmpKeyProcNameArgsNsp (btoidvectorcmp: name, vector length, elements, nsp), buildIndexTupleProcNameArgsNsp (INDEX_VAR_MASK, twin of pgBuildIndexTupleProcKey); (3) pg_proc wiring: insertPgProcIndexEntries at BOTH syncRoutineToCatalogHeap branches (fresh entries at the new TID on non-HOT update, PG-style); mirror set += 2690/2691. Bonus fix (latent B1.2, exposed by the new gate): buildPGProcRow wrote NON-NULL empty strings into prosqlbody/proallargtypes/proargmodes/protrftypes/probin/proconfig/proacl — PG branches on attisnull, so every SQL-function call on a real PG standby would have hit stringToNode(""); now NullDatum (pg_class builder convention). Tests: 4 new units (tuple layout pin, comparator ordering, multi-level variable insert+rebuild round-trip, syncRoutine index wiring incl. update path); e2e failover extended — goopg CREATE FUNCTION streamed via WAL, promoted PG resolves it by name (FuncnameGetCandidates → 2691) and EXECUTES it (PROCOID → 2690 + prosrc) = the first real-PG read of a runtime pg_proc row. | Residual (2)+(3) of the B1.2 row (proallargtypes/proargmodes OUT-arg promotion; proargdefaults JSON deviation for functions WITH defaults) remain open — they need the physical-column promotion, not index machinery. Catalog-index dead-entry vacuum (B1.1 residual (4)) still open, now also covers 2690/2691. | Promote OUT-arg metadata into proallargtypes/proargmodes during B2 (same builder pattern as proargnames); dead-entry vacuum rides the future catalog-vacuum slice. | Index maintenance was the blocking residual for B2 (pg_type 2703/2704 etc. are multi-level too); the OUT-arg/JSON items are independent column-fidelity work. |
| resolved | 2026-07-17 | wal-pg-identical-stream B2.1a — pg_type index maintenance (2703/2704) | LANDED. All ten pg_type heap-row writes (enum/composite/domain/range/multirange + array peers, ACL resync) funnel through writeTypeHeapRowWithIndexes (operators_ddl.go), which derives index keys from the built row (oid/typname/typnamespace = cols 0-2 of pgTypeColumnsPG18) and maintains pg_type_oid_index 2703 (oid, 16B) + pg_type_typname_nsp_index 2704 (name+nsp, 80B, reusing the 2663 builders/comparators); keyMetaForSysBtree += both; mirror set: mirrorTypeCatalogFiles (heap + both indexes → base/5). TWO structural gaps found+closed: (1) 2704 shipped as a metapage-only EMPTY placeholder that insertCanonicalSysBtreeLeaf silently skipped (nBlocks<=1) — allocateEmptySysBtreeLeafRoot now lazily allocates the leaf-root on first insert (PG _bt_getroot write-path analog; keeps pre-existing data dirs working); (2) the empty placeholder meant a real PG standby could not resolve ANY type BY NAME — LookupTypeName probes TYPENAMENSP (2704) for builtins too, so even `SELECT 1::int4`/`::text` failed with `type "..." does not exist`; bootstrapPgTypeTypnameNspIndex (btree_index_bootstrap.go) now bulk-loads one (typname, nsp=11) entry per bootstrap pg_type heap row via the shared pgTypeBootstrapEntryMap. Tests: bootstrap structural pin (multi-leaf walk, sorted, text present, 3 copies identical), executor funnel pin (lazy rooting + 2703/2704 entries), e2e failover extended with runtime CREATE TYPE AS ENUM + post-promote `'public.b2prep_mood'::regtype::text` (exercises runtime AND bootstrap 2704 entries; enum chosen precisely because it emits ONLY heap-insert/FPI records). EMPIRICAL PIN from the first failed gate run: a surviving RmgrGoopgCatalog(128) record in streamed WAL hard-kills a real PG standby — `FATAL: XX000: resource manager with ID 128 not registered` in the startup process → postmaster shutdown → unrecoverable; CREATE DOMAIN (kind 119) reproduced it deterministically. | (1) Kinds still emitting rmid-128 records are live standby-killers for mixed goopg→PG replication: sequence 65/66 (B1.3b), range 81/82/117/118 (B2.1c), domain 119/120 (B2.1b), plus B3+ families — the e2e failover workload must avoid them until each conversion lands. (2) pg_enum/pg_range heaps still unwritten (virtual views only); enum registry has NO restart durability (labels in-memory only); ALTER DOMAIN mutations not durable. (3) The type-sync heap writes hardcode DefaultDBOid (pre-B0.3 pattern) while the index inserts route via tableCatalogHeapDBOid — same DB for normal sessions; distinct-OID DB sessions have a pre-existing heap-routing mismatch worth folding into B2.1b. | B2.1b: retire 119/120 + domain_ddl_recovery.go, registry reload from pg_type heap (JSON metadata in typdefaultbin? or dedicated column — decide per 02b), Domain HeapTID plumbing, durable ALTER DOMAIN, re-add the e2e domain-cast assertion (drafted in this loop, reverted when kind 119 killed the standby). Then B2.1c ranges, B2.1d enum labels (pg_enum heap + 3 indexes). | Index maintenance + bootstrap fix was self-contained and unblocked the PG-side named-type surface; the registry/WAL-record conversions are each their own recipe application. |
| resolved | 2026-07-17 | wal-pg-identical-stream B2.1b — domains converted to heap journaling (kinds 119/120 retired) | LANDED. CREATE DOMAIN journals pg_type heap rows (B2.1a funnel) + NEW pg_constraint heap rows per CHECK (sys_pg_constraint.go: 28-col PG18 builder from pg_constraint.h; contype='c', contypid=domain, conbin=raw expr text — adbin deviation convention; the pg_constraint VIEW stays VIRTUAL, heap is write+reload only — pg_class precedent). DROP DOMAIN = xmax stamps on pg_type rows + constraint rows. ALTER DOMAIN (all 7 arms — rename/owner/renameconstraint/add/dropconstraint/set/dropdefault/set/dropnotnull) is now DURABLE (previously NO ALTER survived restart): pg_type changes ride non-HOT xl_heap_update via the new generic catalog TypeHeapTID cache (SetTypeHeapTID — seeded by every writeTypeHeapRowWithIndexes and by the reload for ALL user pg_type rows, so enum/range/composite ALTERs can reuse it); constraint mutations = pg_constraint DELETE+INSERT. Reload = reloadUserDomainsFromHeap (catalog_heap_reload.go): pg_type scan (typtype='d', oid>=FirstUserOID) rebuilds the skeleton FULLY PHYSICALLY — no JSON sidecar: typtypmod→Base.Args via pgTypeArgsFromTypmod (decode twin of executor pgAttTypmod — numeric/char/varchar/bit), typdefaultbin raw SQL→ParseExpr, base name via pgTypeCanonical; pg_constraint scan (contypid>=FirstUserOID) restores CHECKs incl. InValues re-derived from the synthesized `VALUE = ANY (ARRAY[...])` conbin text (domainInValuesFromConbin) — LOAD-BEARING because runtime enforcement reads ONLY InValues (expr.go:871; general CHECK exprs are dump-fidelity-only). BUG FOUND+FIXED by the new durability test: RegisterDomainDuringRecovery force-keyed domains under DefaultDBOid(1) while live sessions resolve their DB OID (postgres=5) for LookupDomain — post-restart CHECK enforcement was SILENTLY DISABLED (pre-existed the conversion; never caught — no test asserted post-restart enforcement); recovery registration now respects d.DBOid (reload passes cat.DBOID()). Kinds 119/120 + DomainCheckPayload/CreateDomainPayload + Encode/Decode block + dispatch arm + domain_ddl_recovery.go(+test) + wal/domain_ddl_test.go DELETED (grep-zero). Tests: TestPort_DomainSurvivesRestart (pre+post-restart CHECK enforcement, RENAME, SET DEFAULT via heap-backed pg_type read, DROP); e2e failover += CREATE DOMAIN (pure heap records — the kind-119 standby-killer is gone) + post-promote 42::public.b2prep_dom; pg_waldump workload += domain DDL. | (1) pg_constraint index maintenance (2664-2667 empty) → a real PG standby loads domain constraints via 2666 index scan and sees NONE (accepts values goopg rejects) — rides the full pg_constraint conversion (B3). (2) Domain DEFAULTs are never applied at INSERT time by goopg (pre-existing, verified independent of restart) — pinned in the durability test comment. (3) Table constraints stay registry-only until B3. (4) Type-sync heap writes still hardcode DefaultDBOid (pre-B0.3 routing) — fold into a B2 cleanup. (5) Enum-based domains lose BaseIsEnum on reload until B2.1d makes enums durable. | B2.1c ranges next (retire 81/82/117/118 + range_type_ddl_recovery.go; pg_range heap row + 3542/2228 entries; same TypeHeapTID/physical-reconstruction pattern), then B2.1d enum labels (pg_enum heap + 3502/3503/3534; enums currently have NO restart durability). | Domains were the acute standby-killer (kind 119 reproduced the rmid-128 FATAL in the B2.1a gate); ranges/enums follow the now-proven template. |
| resolved | 2026-07-17 | wal-pg-identical-stream B2.1b addendum — three PG-standby-visibility fixes surfaced by the new e2e domain assertion | (1) **Force-FPI for catalog index/mirror writes**: Pool.maybeEmitFPI implements PG's FPI-once-per-checkpoint suppression (needsImage/nativeImageLSN watermark), but goopg's catalog-INDEX writes and base/5 MIRROR copies emit NO incremental record — so the SECOND touch of the same page within a checkpoint window emitted NOTHING and the change never reached a standby (latent since M0106 batched-38: a second CREATE TABLE in one window lost its 2662/2663 entries on the standby; masked goopg-to-goopg because clean shutdown flushes page files and restart reloads from the HEAP). Fixed by swapping bare MarkDirty → MarkDirtyForceFPI (already existed for stampCatalogRows) at all 10 sys-btree + mirror sites. PG-deviation: extra FPIs per catalog-index touch (DDL-rare); the faithful endgame is real XLOG_BTREE_INSERT_LEAF incremental records (B5-adjacent). (2) **pg_constraint_contypid_index (2666) full bootstrap**: PG's domain typcache (GetDomainConstraints) opens 2666 on the FIRST use of any domain type; goopg had NO placeholder file, NO pg_index row, NO relcache/pg_class entry → standby XX000 'could not open relation with OID 2666' on every domain cast. Added to the 3 placeholder lists + pgIndexEntry table (initdb.go) + named relcache list (relcache_init.go), mirroring the batched-48 2665 precedent. (3) **Domain rows carry base I/O procs**: PG's DefineDomain copies the base type's typinput/typoutput/typreceive/typsend into the domain row and getTypeOutputInfo reads the DOMAIN row directly — goopg wrote zeros → 'no output function available for type b2prep_dom' when rendering a domain value. New executor pgTypeIOProcsForOID map (18 base types, text fallback) wired into buildUserPGTypeRowForDomain. | Enum/range/multirange pg_type rows still carry zero I/O procs (enum needs enum_in/out 3532/3533 — rides B2.1d where pg_enum rows make enum values PG-usable at all; ranges ride B2.1c). Real btree incremental records replace the force-FPI deviation eventually. | B2.1c/d: set I/O procs in the enum/range builders when converting. | Each fix was found by walking the SAME failing assertion through three successive PG-side error layers (name resolution → typcache index open → output function) — the e2e domain cast is a deep-coverage probe. |
| resolved | 2026-07-17 | wal-pg-identical-stream B2.1c — range types converted to heap journaling (kinds 81/82/117/118 retired) | LANDED. CREATE TYPE AS RANGE journals its 4 pg_type rows (pre-existing sync, now with 2703/2704 entries via B2.1a) + a NEW pg_range heap row (sys_pg_range.go: 7-col Form_pg_range mirror of initdb pgRangeColDefs, keyed rngtypid — pg_range has NO oid column) with entries in pg_range_rngtypid_index(3542, PKEY) + pg_range_rngmultitypid_index(2228), both bootstrap-populated leaf-roots registered in keyMetaForSysBtree. DROP TYPE stamps the pg_range row (deleteRangeCatalogRow) + 4 pg_type rows; ALTER TYPE RENAME/OWNER (range branch) = non-HOT xl_heap_update of all four pg_type rows via TypeHeapTID (resyncRangeHeapAfterAlter; the pg_range row is all-OID and immutable under rename/owner). Reload = reloadUserRangeTypesFromHeap: pg_range rows (rngtypid>=FirstUserOID) carry the LINKAGE (rngsubtype→SubtypeName via pgTypeCanonical, rngmultitypid, rngsubopc→OpclassOID, rngcollation→CollationOID); one pg_type pass supplies names/typarray/typowner for the range + multirange rows — fully physical, no JSON. RegisterRangeTypeDuringRecovery: same DBOid-keying fix as B2.1b domains (respect rt.DBOid=cat.DBOID(); forced-DefaultDBOid keying would hide reloaded ranges from session lookups). BONUS: all NINE pg_type builders now carry real I/O proc OIDs verified against pg_proc.dat (enum_in/out/recv/send 3506/3507/3532/3533; record_* 2290/2291/2402/2403; range_* 3834-3837; multirange_* 4231-4234; array_* 750/751/2400/2401) — previously zeros, which made any value render on a real PG standby fail with 'no output function available' (the B2.1b domain fix generalized). Kinds + codecs (EncodeCreateRangeType..DecodeAlterRangeTypeOwner) + dispatch arm + range_type_ddl_recovery.go(+test) + wal/range_type_ddl_test.go DELETED (grep-zero, stale comments updated). Tests: TestPort_RangeTypeSurvivesRestart (create/rename/drop + pg_range JOIN probe across restart, PASSED first run); e2e failover += CREATE TYPE AS RANGE + post-promote '[1,5)'::public.b2prep_rng (real PG range_in end-to-end from goopg runtime rows, PASSED first run); waldump workload += range DDL. | rngcanonical/rngsubdiff stay 0 (goopg defines neither for user ranges — PG accepts 0 as 'none'). Multirange VALUES on PG need nothing further (multirange_in reads pg_range by rngmultitypid → 2228 entry present). Sequence kinds 65/66 (B1.3b) remain the LAST live RmgrGoopgCatalog standby-killers in the B1/B2.1 families; enums still have no restart durability (B2.1d next). | B2.1d: pg_enum heap rows + 3502/3503/3534 entries + enum registry reload; ALTER TYPE ADD VALUE = pg_enum INSERT; then B2.2+ (operators/casts/collations/aggregates) per doc 02d. | Template application was near-mechanical after B2.1b — one session, both durability + e2e gates green on first run. |
| resolved | 2026-07-17 | wal-pg-identical-stream B2.1d — enum labels converted to pg_enum heap journaling (enums restart-durable for the FIRST time) | LANDED. catalog.EnumValue gained a persisted OID (allocated at RegisterEnum/AddEnumValueResult; preserved by RegisterEnumDuringRecovery which respects et.DBOid — same keying convention as domains/ranges; the virtual pg_enum VIEW renders real label OIDs, falling back to synthetic 20000+i only for pre-conversion registrations). sys_pg_enum.go: 4-col Form_pg_enum builder; writeEnumLabelRow = heap INSERT + entries in pg_enum_oid_index(3502, 16B oid), pg_enum_typid_label_index(3503, 80B oid+name — new buildIndexTupleOidNameKey/cmpKeyOidName), pg_enum_typid_sortorder_index(3534, 16B oid+float4bits — new buildIndexTupleOidFloat4Key/cmpKeyOidFloat4); all three bootstrap as EMPTY placeholders and lazy-root on first insert (B2.1a machinery). Wiring: CREATE TYPE AS ENUM writes one row per label (syncEnumTypeToCatalogHeap); ALTER TYPE ADD VALUE = INSERT of the new label; RENAME VALUE = delete-by-label-OID + reinsert (stable OID); enum RENAME/OWNER = resyncEnumHeapAfterAlter (non-HOT pg_type updates via TypeHeapTID); DROP TYPE stamps all label rows (deleteEnumLabelRowsByTypid, keyed enumtypid at data offset 4). Reload = reloadUserEnumsFromHeap in open.go (BEFORE domains so future enum-based-domain BaseIsEnum can resolve). CRITICAL ENCODING PIN found via the e2e: goopg's shared encodeValuePG writes float4 as TEXT VARLENA (M0111-0002 deferral) while every relcache/pg_attribute descriptor declares attlen=4 — pg_proc's procost/prorows only align on real PG by LUCK (1-digit text varlena + padding before a 4-aligned column = exactly 4 bytes; SRFs with prorows=1000 would misalign columns 20+!); pg_enum's enumlabel (name, align 'c' = NO padding) exposed it as a 2-byte shift ('ppy' for 'happy'). Fix: enumsortorder rides the 'xid' encode-hint storing IEEE-754 float32 BITS — byte-identical to real PG float4 on disk; reload decodes via math.Float32frombits. Tests: TestPort_EnumSurvivesRestart (create/add-value/rename-value/drop across restart incl. stale-label rejection + pg_enum JOIN count); e2e failover += 'happy'::public.b2prep_mood post-promote (enum_in on real PG reading goopg runtime rows — PASSED); waldump workload += enum create/add-value. Gates: executor+initdb+wal+catalog units, regress 341.7s, isolation 25s, durability x7, e2e — green. | (1) The float4-as-text SHARED-encoder deviation is now PINNED as latent PG-side corruption for SRF pg_proc rows (prorows=1000 → 5-byte varlena → +4 shift of proargtypes on real PG!) and any future float4 catalog column followed by a <4-aligned column — the real fix is M0111-0002's binary float4 encode/decode engine-wide; until then use the xid-bits hint per catalog. (2) BEFORE/AFTER fractional sortorders round-trip via float32 bits fine. (3) Sequence kinds 65/66 (B1.3b) are now the ONLY live RmgrGoopgCatalog records in the B1/B2.1 families. | B2.2+ per doc 02d (operators, opclass/opfamily, casts, conversions, collations, aggregates — most already have partial heap sync like syncAggregateToCatalogHeap); or B1.3b XLOG_SEQ_LOG. Consider promoting the float4-binary encode (M0111-0002) before converting pg_aggregate (aggtransspace etc. are int4 but pg_statistic floats loom). | Enum labels were the last un-durable type-family surface; the sortorder encoding discovery has cross-catalog blast radius worth its own pinned residual. |
| resolved | 2026-07-17 | wal-pg-identical-stream B1.3b — XLOG_SEQ_LOG flip; kinds 65/66 retired (RmgrGoopgCatalog now emits ZERO records in B1/B2.1 scope) | LANDED. (1) WAL: RmgrSeq=15 (MaxKnownRmgr bumped), wal/seq_log.go — EncodeSeqLogPG (block-ref 0 WILL_INIT + main data xl_seq_rec{1663,db,rel} + raw on-page tuple, sequence.c:414 wire shape), BuildSequencePage (SEQ_MAGIC 0x1717 in an 8-byte MAXALIGN'd special; tuple placed BY HAND because storage.PageAddItemRaw computes offsets from BlockSize and CLOBBERED the special area — heap pages have none so it never mattered), replayDecodedXLogSeqLog (page rebuild, seq_redo-identical) + dispatch arm. (2) Executor: WriteSequencePageAndLog (sys_pg_sequence.go) writes the page at the USER-DATA location base/<ctx.CurrentDatabaseOid>/<seqrelid> (NOT the catalog base/1+mirror scheme — PG opens base/<connected-db>/<relfilenode>) via MarkDirtyChangeRecord (record covers the change, no FPI); WALLogSequenceState + maybePreLogNextval swap EncodeSequenceState → page+record (SEQ_LOG_VALS=32 cadence kept); 9 EncodeDropSequence sites removed (heap deletes ARE the journal). (3) Catalog rows: sequences get REAL pg_class rows — relkind='S' with **relam=0** (relcache.c:1841 Assert(relam==InvalidOid) for sequences ABORTED every standby read while we wrote heap-AM=2; views also fixed to relam=0) — via syncTableToCatalogHeap at execCreateSequence; table reload accepts relkind 'S' (registers IsSequence+Virtual placeholder). (4) OWNED BY: NEW narrow pg_depend surface (sys_pg_depend.go, deptype='a' auto rows keyed objid=seqrelid; delete-then-insert on re-point) wired into syncSequenceDefinitionToCatalogHeap; identity from pg_attribute.attidentity (PGAttributeRow.AttIdentity now decoded at fixed offset 89 + restored into Column.IdentityColumn/Always in the table reload); serial spelling derived (owned+non-identity+int type). (5) Reload: reloadUserSequencesFromHeap... [reloadSequencesFromHeap] replaces replaySequenceDDLRecords (DELETED) + folds reloadSequenceHeapTIDs: definition = FULL pg_sequence decode; counter = page read (fallback to seqstart for missing page); name/schema = the sequence's pg_class row. (6) Gap fixes en route: mirror set += 2224/5002/2608 (the B1.3 TID reseed had been reading ZERO rows from base/5 forever — silent); base/1 placeholder list += 5002 (0-byte auto-created file made every runtime index insert silently skip via the nBlocks==0 fixture guard). e2e failover: CREATE SEQUENCE INCREMENT 3 + setval(90) on goopg → promoted REAL PG reads last_value=90/is_called=t from the page and nextval() returns 93 — native continuation. Debug trail: signal-6 Assert (cassert build) → relam; 'could not open base/5/16406' → user-data location; 'cache lookup failed' → 5002 placeholder; 'bad magic 00000000' → PageAddItemRaw special-clobber. | (1) OLD data dirs need re-init (kind-65 scanner deleted; counters have no page there). (2) DROP SEQUENCE leaves the orphan page file (PG-tolerated; smgr unlink deferred). (3) pg_depend rows only for sequence-OWNED-BY (full pg_depend = B3); no pg_depend index maintenance. (4) The e2e workload now exercises sequences — kinds 65/66 were the LAST standby-killers in scope. | B2.2+ (operators/opclass/casts/conversions/collations/aggregates per doc 02d) or B3 (constraints/depend/attrdef). | This closes the B1+B2.1 arc: every schema/function/sequence/domain/range/enum DDL is PG-wire-identical. |
| resolved | 2026-07-17 | M0111-0002 — binary float4/float8 heap storage (closes the float-as-text deviation + the SRF pg_proc misalignment pinned in the B2.1d row) | LANDED. encodeValuePG float4/float8 arms now emit fixed IEEE-754 LE 4/8-byte values (were TEXT VARLENA since M0111-0002 deferred the flip), finally matching pgPhysicalTypeIsVarlena + the attlen=4/8 pg_attribute/relcache descriptors goopg has ALWAYS written — the mismatch misaligned every column AFTER a float on real-PG reads (SRF pg_proc rows with prorows=1000 lost proargtypes → no goopg-created SRF was resolvable by name on a standby; pg_enum's 'ppy' corruption was the same class). Decode: the PG-physical arm splits float4/float8, reads fixed bytes, formats via PGFloatOut and reuses the numeric-parse tail (floatTextDatum) — downstream Datums BYTE-IDENTICAL to the pre-flip decode (KindNumeric finite / KindString NaN±Infinity), so no goopg-visible behavior change. PGFloatOut RELOCATED executor→catalog.PGFloatOut (executor alias keeps the 4 sibling call sites; wal/pgoutput.go grew float4/float8 fixed-width arms calling it — the logical-decode sibling the pglz memory warns about). pg_enum.enumsortorder reverted from the B2.1d xid-bits encode-hint to plain float4 (byte-identical pages, no migration) with a full-precision string datum (newNumericFromFloat truncates at 6dp; deep BEFORE/AFTER midpoints exceed it). Arrays were already fixed-width. Gates: full battery green (units incl. storage, regress 339s w/ float4/float8 regress tests, isolation 30s, durability, waldump, e2e); the one FAIL was the documented pre-existing stripe-concurrency flake (passes 3/3 isolated + full package rerun). e2e += CREATE FUNCTION ... RETURNS SETOF (prorows=1000) on goopg → promoted PG resolves AND executes it. | (1) STORAGE FORMAT CHANGE: pre-flip data dirs' float columns decode wrong — re-init required (standard; same landing-class as B1.3b). (2) float4[]/float8[] array ELEMENTS were already fixed — unchanged. (3) pg_statistic-family catalogs (stanumbers etc.) now unblocked. | pg_statistic/pg_statistic_ext conversions (B3) can proceed; B2.2 operators/casts next per doc 02d. | The deviation had metastasized into per-catalog workarounds (xid-bits) and latent real-PG corruption — flipping the engine once removes the whole class. |
| resolved | 2026-07-17 | wal-pg-identical-stream B2.2a — pg_cast converted to heap journaling (kinds 38/39 retired) | LANDED. sys_pg_cast.go: 6-col PG18 pg_cast row builder (source/target names→OIDs via TypeNameToOID), heap INSERT + pg_cast_oid_index 2660 (16B) + pg_cast_source_target_index 2661 (16B two-oid key) via the descent machinery + keyMetaForSysBtree registrations; DROP CAST stamps xmax via CastByTypes-captured OID; mirrorCastCatalogFiles(2605/2660/2661). Reload reloadUserCastsFromHeap: fully physical base/<DBOID()>/2605 scan, oid<16384 skipped, endpoint OIDs reversed to names (builtin→table→domain→enum), RegisterCastDuringRecovery. Scanner cast_ddl_recovery.go(+test)+wal/cast_ddl_test.go deleted; kinds 38/39+codecs excised. DEBUG LESSON: reload was correct from the first build — the durability test's WHERE castsource='int'::regtype matched nothing because goopg regtype input leaves builtin type NAMES as strings and the WHERE swallows the oid-coercion error (separate open row); probes must compare numeric OIDs (+ oid>=16384 — int4→bool has builtin row 10034). | (1) pg_cast write errors remain best-effort-silent (sibling convention, enum/domain identical). (2) No e2e PG-side cast-execution probe: a cast's standby effect needs the FUNCTION too (already covered) — pg_cast row itself validated via waldump workload. (3) castfunc stores goopg routine OIDs (131072+) — consistent with pg_proc heap, fine for pg_dump cross-ref. | B2.2 slice 2: pg_aggregate (kinds 46-49; complete the partial syncAggregateToCatalogHeap + index 2650 + reload). | pg_cast was the survey's smallest full-recipe catalog — validates the B2.1 template on a CompatNoopStmt-routed DDL. |
| resolved | 2026-07-17 | wal-pg-identical-stream B2.2b — pg_aggregate converted to heap journaling (kinds 46-49 retired) | LANDED. CREATE AGGREGATE journals a synthesized prokind='a' pg_proc row via the B1.2 routine funnel (sys_pg_aggregate.go routineForAggregate: prolang=internal, prosrc=aggregate_dummy, provolatile='i', prorettype=finalfn rettype else stype — copied from the virtual pg_proc view) + the pg_aggregate row with NEW pg_aggregate_fnoid_index (2650) maintenance; ALTER RENAME/OWNER = canonical pg_proc heap UPDATEs; DROP stamps xmax on both rows. Reload reloadUserAggregatesFromHeap scans prokind='a' pg_proc rows; the UserAggregate definition rides Routine.Aggregate in the proargdefaults JSON meta; routine reload skips 'a' rows. Scanner aggregate_ddl_recovery.go(+test)+wal/aggregate_ddl_test.go deleted; kinds 46-49+codecs+dispatch excised. TestPort_AggregateSurvivesRestart incl. post-restart EXECUTION green. | (1) pg_aggregate.aggtransfn/aggfinalfn store 0 for builtin funcs outside the hand-curated BuiltinProc set (pre-existing: ResolveAggFuncOIDAndRetType) — a real PG standby cannot execute such aggregates (needs the full pg_proc.dat OID table or curated-set growth); goopg-side reload is unaffected (names ride the JSON meta). (2) UserAggregate.Variadic not rendered into provariadic (pre-existing; provariadic=0 always). (3) e2e failover PG-side aggregate-execution probe deferred until (1). | B2.2 slice 3: pg_operator (kinds 83/84; 7 emit sites incl. commutator/negator shells; indexes 2688/2689). | Slice validates the routine-funnel reuse pattern for proc-backed catalogs — collations/conversions next per the survey. |
| resolved | 2026-07-17 | wal-pg-identical-stream B2.2c — pg_operator converted to heap journaling (kinds 83/84 retired) | LANDED. sys_pg_operator.go: 15-col FormData_pg_operator builder + upsertOperatorCatalogRow (INSERT or canonical heap UPDATE at the new operatorHeapTIDs cache — PG's OperatorShellMake/OperatorUpd two-pass maps onto it; CREATE also upserts the referenced commutator/negator so shells and back-patches journal); DROP stamps xmax with MaterializeWriterXID FIRST (unmaterialized XID stamped xmax=0 silently — same latent bug fixed in B2.2b deleteAggregateCatalogRows); indexes 2688 (16B oid) + 2689 (88B name+3-oid, executor twin of pgBuildIndexTupleNameOidOidOidKey); reload fully physical (only oprleft/oprright type names + schema name reverse; proc links are OIDs in the registry). Scanner operator_ddl_recovery.go(+test)+wal/operator_ddl_test.go deleted; kinds 83/84+CreateOperatorPayload codec+dispatch excised; LookupBuiltinProcByOID added (oprresult from oprcode). TestPort_OperatorSurvivesRestart green (forward-COMMUTATOR shell, back-patch link, drop, oprcode join). | (1) User-operator EXECUTION out of goopg scope (create_operator regress excluded 'v0'; planner never routes user symbols) — heap rows serve pg_dump/standby reads only. (2) Expression lexer rejects #-bearing operator symbols. (3) oprresult=0 when oprcode resolves outside routines+curated-builtin sets (same class as B2.2b residual 1). | B2.2 slice 4: pg_collation (42-45/93) + pg_conversion (40/41/130-132) — FIX their *DuringRecovery DefaultDBOid force-keying (catalog.go:12653/:12776, domain bug class) in the same landing. | Two-pass shell/back-patch was the survey's main operator risk — the TID-cache upsert dissolves it. |
| resolved | 2026-07-17 | wal-pg-identical-stream B2.2d — pg_collation + pg_conversion converted to heap journaling (kinds 40-45/93/130-132 retired) | LANDED. sys_pg_collation.go (12-col FormData_pg_collation builder, ""↔NULL text fields, collversion NULL) + sys_pg_conversion.go (8-col, conproc=FuncOID); B2.2c upsert/TID-cache pattern (collationHeapTIDs/conversionHeapTIDs); ALTER RENAME/OWNER/SET SCHEMA = canonical heap UPDATEs; DROP = xmax w/ MaterializeWriterXID; indexes 3085+3164 (signed-int4 middle key: collencoding=-1) populated, 2670 populated, 2669+2668 lazy-rooted placeholders; fully physical reloads (conversion proc-name fallback via routines/curated-builtin reversal); scanners + 10 kinds + 20 codecs + 2 dispatch cases deleted (764-line pure excision). BONUS: ALTER CONVERSION RENAME now moves the compat-registry entry (rename→drop previously failed 42704 — DropCompatObject is DROP's existence gate). Durability tests + waldump workload extended. | (1) KEY DISCOVERY: collation/conversion registries key live postgres-DB sessions under DefaultDBOid (NamespaceDBOid 5→1), NOT the resolved session DB — the survey's "domain bug class" assumption was WRONG for these registries (reload under cat.DBOID()=5 missed every lookup); *DuringRecovery now respects caller-set DBOid but reload passes none. Cross-database collations/conversions remain non-dbOid-aware in recovery (pre-existing ledger row stands). (2) ALTER COLLATION REFRESH VERSION remains registry-only (collversion always NULL). | B2.2 slice 5 (FINAL): opclass/opfamily/amop/amproc kinds 85-92 (9 emit sites; 2686/2653/2654 empty placeholders → lazy-root; scanner operator_class_ddl_recovery.go hosts the operatorCatalog helper). | Slice validates the name+int4 signed-compare key class and closes the survey's DBOid-keying question with an empirical rule: check NamespaceDBOid per registry. |
| resolved | 2026-07-17 | wal-pg-identical-stream B2.2e — opclass/opfamily/amop/amproc converted to heap journaling (kinds 85-92 retired); B2.2 COMPLETE | LANDED. sys_pg_opclass_family.go: 4 FormData builders (pg_opfamily 2753 5-col, pg_opclass 2616 9-col — AM OID BEFORE the name in both, unlike the name-first pg_class/pg_type family; pg_amop 2602 9-col, pg_amproc 2603 6-col) + 9 emit swaps; INSERT-at-create/xmax-at-drop only (no ALTER RENAME/OWNER surface → no TID cache); indexes 2754/2755/2687/2655 populated + 2686/2653/2654 lazy-rooted; 3 new key builders (oid+name+oid 80B, oid+oid+oid+int2 24B, oid+char+oid 24B) byte-verified vs their initdb twins. ClassOID (goopg-only field driving pg_depend's INTERNAL-vs-AUTO deptype; pg_amop/pg_amproc have no such column) now rides PG's OWN channel — an INTERNAL pg_depend row on the opclass (opclasscmds.c storeOperators), extending B1.3b's narrow pg_depend writer; reload re-derives ClassOID from those rows, and an ALTER-FAMILY-ADD member's absent row leaves the correct zero. AmProcMember.Method re-derives from the owning family. Scanner + wal test deleted; kinds 85-92 + 4 payload structs + 16 codecs + dispatch excised (505-line pure deletion). TestPort_OpClassFamilySurvivesRestart green first try; waldump += full opclass/opfamily DDL. | (1) goopg bootstraps NO pg_amop_oid_index (2756) / pg_amproc_oid_index (2757) — PG18 has both; not maintained here (pre-existing bootstrap gap, unchanged by this slice). (2) DROP OPERATOR CLASS/FAMILY does NOT cascade-delete its pg_amop/pg_amproc rows or their pg_depend rows — matches the pre-existing registry-drop behavior; PG cascades via dependency.c. Full dependency-driven cascade = B3 pg_depend scope. (3) pg_depend index maintenance (2673/2674) still absent (B1.3b residual, unchanged). | B3: full pg_depend conversion (subsumes residuals 2 + 3) or the next rmid-128 family per doc 02d — text-search 104-116, statistics 95-99, access-method 70/71, transform 36/37, event-trigger 56-60, pub/sub 50-55, foreign-data 126-129, roles/db-config 67-80/94/102-103/124-125. | Closing slice of B2.2: every type/operator-family catalog now journals as PG-identical heap DML. The ClassOID problem proved the ledger's own thesis — the faithful fix (pg_depend INTERNAL rows) was cheaper than the heuristic and advanced B3. |
| resolved | 2026-07-18 | wal-pg-identical-stream B3.1 — pg_transform converted to heap journaling (kinds 36/37 retired) | LANDED. sys_pg_transform.go: 5-col FormData_pg_transform builder (all OIDs; trftype/trflang from the registry's name fields via TypeNameToOID/LanguageNameToOID) + heap INSERT with pre-INSERT xmax stamp of any prior version (RegisterTransform idempotent per (type,lang) w/ stable OID = CREATE OR REPLACE analog; low-traffic → no TID cache, delete+insert keeps one live row) + 3574/3575 entries (empty placeholders → lazy-root); DROP stamps xmax via LookupTransform-captured OID. reloadUserTransformsFromHeap reverses trftype (reloadTypeNameForOID) + trflang (new languageNameForOID over the 4 langs LanguageNameToOID models). Scanner transform_ddl_recovery.go(+test)+wal/transform_ddl_test.go deleted; kinds 36/37+4 codecs+dispatch excised; rmgr-mapping test retired-kind sample → RecordKindCreateStatistics. TestPort_TransformSurvivesRestart green; waldump += CREATE/DROP TRANSFORM. First B3 slice. | (1) trffromsql/trftosql store 0 for builtin functions outside the curated BuiltinProc set (pre-existing Transform.FromFuncOID/ToFuncOID limitation, shared w/ CREATE CAST) — a real PG standby couldn't execute such a transform; goopg reload unaffected (functions ride their own pg_proc reload). (2) pg_transform is per-database (base/<db>) — no cross-DB concern. | B3.2: pg_am (kinds 70/71) — NOTE its indexes 2651/2652 are ABSENT from goopg's bootstrap entirely (not even empty placeholders; verified), so this slice must FIRST add the pg_am_name_index/pg_am_oid_index bootstrap like B2.1a did for pg_type 2704, THEN convert. | Smallest B3 catalog; validates the phase's recipe carries over from B2.2 unchanged (empty-placeholder lazy-root, type-name reversal, low-traffic delete+insert). |
| resolved | 2026-07-18 | wal-pg-identical-stream B3.2 — pg_event_trigger converted to heap journaling (kinds 56-60 retired) | LANDED. sys_pg_event_trigger.go: 7-col FormData builder (6 scalars + evttags text[] declared Type{Name:"text",IsArray:true} → symmetric generic array encode/decode; NULL when no WHEN TAG filter) + upsert/TID-cache (eventTriggerHeapTIDs on InMemory — this catalog HAS ALTER surface: ENABLE/DISABLE/RENAME/OWNER all canonical heap UPDATEs at the cached TID); DROP xmax after MaterializeWriterXID; indexes 3467 (evtname {72,1} NameData) + 3468 (oid), both empty placeholders lazily rooted. Reload decodes evttags to "{a,b}" text, split via new exported executor.ParseTextArrayLiteral. Scanner event_trigger_ddl_recovery.go(+test)+wal/event_trigger_ddl_test.go deleted; kinds 56-60 + 10 codecs + dispatch excised. TestPort_EventTriggerSurvivesRestart green incl. evttags array round-trip; waldump += event-trigger DDL. | (1) evtowner OWNER TO requires the new owner be a superuser (event_trigger.c) — goopg tracks no per-role rolsuper, so only OID 10 is accepted (pre-existing approximation, unchanged). (2) evtfoid stores the resolved pg_proc OID; a builtin function outside the curated set would be 0 (shared limitation, but event-trigger functions are always user CREATE FUNCTION so N/A in practice). | B3.3 pg_ts_dict/config/parser/template + config_map (kinds 104-116; 4 scanners tsdict_ddl_recovery.go/tsconfig_ddl_recovery.go die together — the big B3 slice). | Second B3 slice; first to physically round-trip a text[] catalog column (evttags) — the generic IsArray codec path handles both directions, no bespoke array plumbing needed. |
| resolved | 2026-07-18 | wal-pg-identical-stream B3.3 — pg_publication + pg_publication_rel converted to heap journaling (publication kinds 50-52 retired; subscription 53-55 deferred to B4) | LANDED. sys_pg_publication.go: 9-col pg_publication builder (scalar; pubtruncate/pubviaroot=false, unmodeled) + publicationHeapTIDs TID cache (ALTER OWNER heap UPDATE) + 5-col pg_publication_rel (one row/member, prrelid=resolved pg_class OID, prqual/prattrs NULL); indexes 6110/6111 + 6112/6113 empty placeholders lazily rooted; CREATE writes base+member rows, DROP xmax's both (prpubid-matched). reloadUserPublicationsFromHeap: 2-pass (pub_rel grouped by prpubid → qualified names via LookupTableByOID, then pub base rows) → pubsub.CreatePublicationDuringRecovery; runs after the table reload. Publication half removed from pubsub_ddl_recovery.go scanner + kinds 50-52/7 codecs/dispatch-narrowed; pub codec-tests trimmed to subscription-only. TestPort_PublicationSurvivesRestart green; waldump += CREATE/DROP PUBLICATION. | (1) SUBSCRIPTION (kinds 53-55) NOT converted: pg_subscription is a SHARED catalog (BKI_SHARED_RELATION, global/) → B4 with the other shared catalogs; the pubsub scanner + CreateSubscription codecs stay live until then. (2) goopg models neither pubtruncate/pubviaroot nor row filters (prqual) / column lists (prattrs) — all journal as false/NULL (pre-existing registry limitation, unchanged; a real PG standby sees the same defaults goopg itself uses). (3) goopg serves pg_publication/_rel as REGISTRY-backed virtual views that SHADOW the nailed heaps in its OWN query path — the heaps are the standby's copy; goopg-facing membership is asserted via pg_publication_tables. (4) ALTER PUBLICATION ADD/DROP TABLE has no durability record in goopg — table membership is CREATE-time-fixed, so member rows are written only at CREATE (matches pre-existing behavior). | B3.4: pg_ts_dict/config/parser/template + config_map (kinds 104-116; 4 scanners die together — the big B3 slice) OR foreign-data (126-129, 3 per-DB catalogs). pg_am (70/71) needs index-bootstrap first; statistics (95-99) needs stxexprs node-tree + stxkeys attnum join. pg_depend LAST. | Third B3 slice; first multi-catalog heap slice with a name→OID member join (pg_publication_rel) and the first to split a shared scanner (publication converted, subscription left for B4). |
| resolved | 2026-07-18 | wal-pg-identical-stream B3.4 — foreign-data trio (pg_foreign_data_wrapper + pg_foreign_server + pg_user_mapping) converted to heap journaling (kinds 126-129 retired) | LANDED. sys_pg_foreign.go: 3 builders (FDW 7-col/server 8-col/user-mapping 4-col; OPTIONS text[]; acl NULL); name→OID cross-refs at write (srvfdw=FDW OID, umserver=server OID, umuser=role OID/0=PUBLIC) reversed at reload via new LookupForeignDataWrapperByOID/LookupForeignServerByOID/RoleNameForOID. THE FDW GAINED RESTART DURABILITY IN THIS SLICE (it had none — no kind, no reload); CREATE SERVER also writes the FDW row so srvfdw resolves on a standby. No TID cache (low-traffic delete+insert). Indexes 112/548+113/549+174/175 empty placeholders lazily rooted. Reload FDW→server→user-mapping after role reload (umuser→name). DROP FDW cascades to servers (each xmax'd). Scanners+wal tests deleted; kinds 126-129/8 codecs/2 dispatch excised; rmgr-map test retired-sample→RecordKindDropSubscription. TestPort_ForeignDataSurvivesRestart green; waldump += FDW/SERVER DDL. | (1) fdwacl/srvacl always NULL (goopg tracks no per-FDW/server ACL — pre-existing; a standby sees the same default). (2) fdwhandler/fdwvalidator store 0 for builtin funcs outside the curated set (shared limitation; FDW handlers are user CREATE FUNCTION in practice). (3) goopg serves these as REGISTRY-backed virtual views that shadow the nailed heaps in its own query path — heaps are the standby copy; durability asserted via the goopg-facing views (pg_foreign_data_wrapper/pg_foreign_server/pg_user_mappings). (4) ALTER SERVER/FDW OPTIONS mutations not re-journaled (no ALTER durability record existed; OPTIONS fixed at CREATE — matches prior behavior). | B3.5: pg_ts_dict/config/parser/template + config_map (kinds 104-116; 4 scanners, the big slice) — the last non-pg_am/statistics/depend B3 group. Then pg_am (70/71, index-bootstrap first), statistics (95-99, node-tree), pg_language/description/init_privs, and LAST pg_constraint/attrdef/depend. | Third multi-catalog slice; first to ADD durability to a catalog that had none (FDW) as a required side effect of converting a dependent one (server.srvfdw). |
| resolved | 2026-07-18 | wal-pg-identical-stream B3.5 — pg_ts_dict converted to heap journaling (kinds 104/105/114/115/116 retired) | LANDED. sys_pg_ts_dict.go: 6-col builder (all direct — dicttemplate=UserTSDict.Template OID, dictinitoption=serialized text/NULL) + tsDictHeapTIDs TID cache (ALTER RENAME/SET SCHEMA/OPTIONS heap UPDATEs) + DROP xmax; indexes 3604 {80,2}+3605 empty placeholders lazily rooted; reload reverses dictnamespace→schema; new FindTSDict helper. Split the shared tsdict/tsconfig scanner: tsdict_ddl_recovery.go deleted, tsconfig_ddl_recovery.go KEPT (config kinds 106-113 → B3.6); combined codec+recovery test files split to config-only. Dispatch case narrowed to config kinds. BONUS FIX: DROP TS DICTIONARY returns on successful registry drop (was falling through to DropCompatObject gate keyed by current name → rename-then-drop failed 42704; same class as B3.3). TestPort_TSDictSurvivesRestart green; waldump += full TS-dict DDL. | (1) TEXT SEARCH CONFIGURATION (pg_ts_config 3602 + pg_ts_config_map 3603, kinds 106-113) NOT converted — the config_map child references pg_ts_dict.oid (mapdict), so B3.6 depends on B3.5 landing first; the tsconfig scanner + its codecs stay live. (2) pg_ts_parser/pg_ts_template are builtin-only (no CREATE durability kinds) — no conversion needed. (3) goopg serves pg_ts_dict as a registry-backed virtual view that shadows the nailed heap in its own query path — heap is the standby copy; durability asserted via the goopg-facing pg_ts_dict view (which reads the registry the reload repopulates). | B3.6: pg_ts_config + pg_ts_config_map (kinds 106-113; config base row + config_map child keyed (mapcfg,maptokentype,mapseqno), mapdict→dict OID resolved via the now-durable pg_ts_dict). Then pg_am (70/71, index-bootstrap), statistics (95-99, node-tree), pg_language/description/init_privs, LAST pg_constraint/attrdef/depend. | Split the biggest B3 group into dict (this) + config (B3.6); dict is the config_map's mapdict target so it lands first. |
| resolved | 2026-07-18 | wal-pg-identical-stream B3.6 — pg_ts_config + pg_ts_config_map converted to heap journaling (kinds 106-113 retired); TS group COMPLETE | LANDED. sys_pg_ts_config.go: 5-col base builder (cfgparser=UserTSConfig.Parser OID) + tsConfigHeapTIDs TID cache (RENAME/SET SCHEMA base-row UPDATEs) + 4-col config_map (mapcfg=config OID, maptokentype=numeric via new catalog.TSTokenTypeID, mapseqno=dict index, mapdict=pg_ts_dict OID). Mappings inline on UserTSConfig → every mutation (CREATE+copy-loop, ADD/DROP/REPLACE/ALTER MAPPING) re-syncs the whole config_map row-set (syncTSConfigMapRows: stamp all mapcfg rows, rewrite from Mappings) — ONE path replacing 8 kinds. Indexes 3712+3608 {80,2}+3609 {24,3 oid+int4+int4} empty placeholders lazily rooted. Reload 2-pass (config_map grouped by mapcfg, token int→alias via TSTokenTypeAlias; base rows → UserTSConfig w/ inline Mappings → CreateTSConfigDuringRecovery) after pg_ts_dict reload. DROP config returns on registry-drop (B3.5 rename-then-drop compat-gate fix). Scanner tsconfig_ddl_recovery.go + config-only test files deleted; kinds 106-113 + 16 codecs + dispatch excised. TestPort_TSConfigSurvivesRestart green; waldump += full TS-config DDL. | (1) goopg serves pg_ts_config/_map as registry-backed virtual views shadowing the nailed heaps in its own query path — heaps are the standby copy; durability asserted via the goopg-facing pg_ts_config/pg_ts_config_map views (which read the registry the reload repopulates). (2) cfgparser stored as the builtin default-parser OID (3722) — goopg models one parser; a config on a non-default parser is out of scope (no CREATE TS PARSER durability). (3) maptokentype limited to the 23 DefaultParserTokenTypes aliases (unknown alias → mapping row skipped). | B3 remaining: pg_am (70/71, indexes 2651/2652 ABSENT → bootstrap populated w/ 7 builtins first), statistics (95-99, stxexprs node-tree blocker), pg_language/description/init_privs (§3b), LAST pg_constraint/attrdef(69)/pg_depend. | Closes the biggest B3 group (TS, 13 kinds across dict+config+config_map) — the inline-Mappings re-sync pattern collapsed 8 mapping-op record kinds into one heap-rewrite path. |
| resolved | 2026-07-18 | wal-pg-identical-stream B3.7 — pg_am converted to heap journaling (kinds 70/71 retired) | LANDED. sys_pg_am.go: 4-col builder (oid,amname,amhandler OID,amtype char); NARROW surface (B1.3b pg_depend precedent) — NO index maintenance because goopg bootstraps NEITHER pg_am_oid_index (2652) NOR pg_am_name_index (2651) — they are ABSENT entirely (verified), so a runtime insert has nowhere to land; reload seq-scans the pg_am heap (oid>=FirstUserOID). CREATE = xmax-stamp-then-insert (no OR REPLACE); DROP xmax's the row via new FindAccessMethod OID capture. Scanner access_method_ddl_recovery.go(+test)+wal/access_method_ddl_test.go deleted; kinds 70/71+4 codecs+dispatch excised. TestPort_AccessMethodSurvivesRestart green; waldump += CREATE/DROP ACCESS METHOD. | (1) pg_am_oid_index (2652) + pg_am_name_index (2651) are ABSENT from goopg's bootstrap entirely — a real PG standby's AMOID/AMNAME syscache can't resolve builtin OR user AMs by index. Adding them POPULATED with the 7 built-in AM rows (B2.1a 2704 bulk-load pattern: bootstrapPgAm*Index funcs + entry() lines + placeholder/mapped-local list + relcache_init descriptors) is a separate standby-completeness task. This slice does NOT regress the standby (goopg never had the indexes); goopg's own AM lookups use AccessMethodOIDByName (hardcoded builtin map) + the reload's registry. (2) goopg serves pg_am as a registry-backed virtual view shadowing the heap in its own query path — heap is the standby copy; durability asserted via the goopg-facing pg_am view. | B3 remaining: statistics (95-99, stxexprs pg_node_tree blocker — scope to col-list or skip), CreateView/MatView (102/103, pg_rewrite analog), and LAST pg_constraint/attrdef(69)/pg_depend. (pg_language/description/init_privs have NO durability kinds — nothing to convert.) | Cleanest possible AM conversion: the missing indexes made index maintenance moot, so the narrow heap-row+reload (B1.3b shape) retires 70/71 without the bootstrap detour. |
