# Completed (resolved) Deferrals — Part 3

Resolved deferral rows archived from `.ralph/deferral_ledger.md` (part 3 of 4). See that file for still-open rows. Original ledger order preserved.

| status | date | task-id | landed | deferred | resume point | why |
| :-- | :-- | :-- | :-- | :-- | :-- | :-- |
| resolved | 2026-07-04 | M0119-0004 (slice-365 follow-up, root-0025 deferred item 2, this loop) | View-of-view chaining is now supported: `viewAutoUpdatableChain` (`internal/planner/view_dml.go`, renamed from `viewAutoUpdatableBase`) recurses when a simple auto-updatable view's single `FROM` relation is itself a simple auto-updatable view, walking down to the ultimate real base relation (mirroring `rewriteHandler.c`'s recursive view rewrite). Required correcting the eligibility check: `catalog.InMemory.CreateView` sets `Table.Virtual = true` on every view (not just system-catalog virtual relations), so the pre-existing `b.Virtual` guard — which previously worked only because the separate `b.View != nil` check already excluded views — had to become `b.Virtual && b.View == nil` to admit a view as a valid intermediate while still excluding real system catalogs. New `viewChainQuals` computes two AND-combinations across the chain: `all` (every level's own qual, unconditional — the UPDATE/DELETE row-visibility restriction) and `checked` (only the quals PostgreSQL's CASCADED/LOCAL CHECK OPTION propagation enforces — a CASCADED check option, the default for an unqualified `WITH CHECK OPTION`, forces every inner level to be checked too regardless of that level's own setting; LOCAL checks only levels that themselves declare CHECK OPTION). Tests: `TestChainedViewInsertUpdateDeleteRewriteToBase`, `TestChainedViewCheckOptionCascadeReachesInnerView`, `TestChainedViewCheckOptionLocalDoesNotForceInnerCheck`, `TestChainedViewCheckOptionInnerEnforcedRegardlessOfOuter`, `TestChainedViewInnerNotAutoUpdatableRejectsWholeChain` (`internal/executor/view_dml_test.go`). Design doc `docs/design/root-0025-updatable-views.md` gained a "Follow-up: view-of-view chaining" section; `docs/design/README.md` row updated. | `viewCheckName` (the name surfaced in the 44000 error message) remains the outermost DML-targeted view's name even when the check that actually failed belongs to an inner level in the chain — PostgreSQL's per-level `WithCheckOption.relname` would report the specific inner view's name instead. Root-0025 deferred items (1) column subset/reorder/rename and (3) `UPDATE...FROM`/`DELETE...USING` a view remain open exactly as recorded in that row; item (4) CHECK OPTION on partition/inheritance-child-routed rows also remains open. | (a) error-text fidelity: `checkViewCheckOption` (`internal/executor/operators_fk.go`) would need to track which level's qual failed (not just the combined AND) to report a per-level `relname`, similar to how PG's `WithCheckOption` list is walked and reported per-node in `execMain.c`'s `ExecWithCheckOptions`; (b) items (1)/(3)/(4) resume points are recorded in the root-0025 ledger row already on file, unaffected by this loop. | (a) is a minor error-message fidelity gap with no behavioral difference for the row itself (the write is still correctly rejected) — not worth the added per-level bookkeeping for a bounded chaining-focused loop; (b) each remains a materially separate, bounded piece of work as already recorded. |
| resolved | 2026-07-04 | M0119-0004 (slice-365 follow-up, root-0025 deferred item 1, this loop) | Column subset/reorder/rename auto-updatable views are now supported: `viewColumnMap` (`internal/planner/view_dml.go`, replacing `viewTargetsPassthrough`) relaxes the eligibility check from "unrenamed, in-order, full-column-list passthrough" to "every target-list entry is a bare column reference to the `FROM` relation" (matching PostgreSQL's `view_col_is_auto_updatable` — a view column is updatable iff it is a plain `Var` over the base relation), returning a `colMap []int` (view ordinal → base ordinal) instead of a bool; a base column simply absent from the view's target list, appearing in a different position, and/or exposed under a different name (via either a per-target `AS` alias or an explicit `CREATE VIEW v (a, b)` column list — `execCreateView` already folds either into `catalog.Table.Columns[i].Name`, so the standalone `len(tbl.ViewColumnAliases) > 0` gate was deleted outright as redundant) are all now accepted. `viewAutoUpdatableChain` composes each chain level's own `colMap` down to the ultimate base, returning `colMaps [][]int` parallel to `chain`. New `viewProxyTable` builds a synthetic `*catalog.Table` with `base`'s exact physical column count/order/ordinals (so a scanned row still indexes identically) but the exposed ordinals renamed to the view's own names and unexposed ordinals hidden behind an unmatchable empty name; `planInsert`/`planUpdate`/`planDelete` pass this proxy to `singleBindingContext` in place of `base` so the DML statement's column list/`SET`/`WHERE`/`RETURNING` resolve using the view's own vocabulary via the ordinary name-based `resolveContext` machinery, with no separate query-rewrite pass — the plan node's actual scan/mutation `Table` field stays the true `base` throughout. The same proxy technique closes a loose end the chaining follow-up left open: `viewQualOnBase` now resolves each chain level's own stored `WHERE` against a proxy shaped like its *immediate* `FROM`'s exposure (not unconditionally against `base`), so a chain mixing renaming at different levels still resolves correctly. For `INSERT` with no explicit column list, source values now map through the view's own column order (`outerColMap`), not base's physical order, matching PostgreSQL's `transformInsertStmt`. Tests: `TestUpdatableViewColumnSubsetReorderRename` (`internal/executor/view_dml_test.go`) covers per-target-alias rename, explicit-column-list rename, reorder with a column-list-free INSERT, and subset (successful INSERT against the exposed column + 42703 against the hidden one); `TestNonUpdatableViewDMLRejected`'s renamed-column case (`vren`) was replaced with an expression-column case (`vexpr`) since renaming a plain column reference is no longer rejected. Design doc `docs/design/root-0025-updatable-views.md` gained a "Follow-up: column subset/reorder/rename" section; `docs/design/README.md` row updated. | `INSERT ... ON CONFLICT` arbiter-target resolution (`resolveArbiterIndex`) and `DO UPDATE SET`/`DO UPDATE ... WHERE` resolution (`planOnConflict`) still resolve directly against `base`, not through the view's proxy — targeting a renamed view column in `ON CONFLICT (...)` or `DO UPDATE SET renamed_col = ...` fails `42703` rather than resolving through the rename. Root-0025 deferred items (3) `UPDATE...FROM`/`DELETE...USING` a view and (4)/(5, now renumbered) CHECK OPTION on partition/inheritance-child-routed rows remain open exactly as recorded in the item-2 ledger row. | `internal/planner/planner.go`'s `planInsert`: thread `resolveTbl` (the same proxy already built for column-list/RETURNING resolution) into `planOnConflict`'s `tbl` parameter, and into `resolveArbiterIndex`'s target-column lookup, translating the resolved ordinal back to `base` via the view's `colMap` before using it to look up `base`'s actual unique index (arbiter indexes are physically defined on `base`, keyed by `base`'s own column ordinals, so the index lookup itself must still use `base`'s names/ordinals even though the user wrote the arbiter target using the view's names). | `INSERT ... ON CONFLICT` against a *renaming* view is a narrow enough combination (two independently-already-narrow features intersecting) that plumbing the proxy through arbiter-index resolution — a different code path than the `SET`/`WHERE`/`RETURNING` resolution this loop already generalized — was left for a dedicated follow-up rather than widening this loop's diff further; the failure mode meanwhile is a clean `42703`, not silent data corruption. |
| resolved | 2026-07-04 | M0119-0004 (slice-365 follow-up, root-0025 deferred item 3, this loop) | `UPDATE ... FROM` / `DELETE ... USING` against a simple auto-updatable view no longer rejects unconditionally: `planUpdate`/`planDelete` (`internal/planner/planner.go`) now compute `viewAutoUpdatableChain`/`viewQual`/`resolveTbl` unconditionally when the target is a view (removing the old `len(s.From) > 0`/`len(s.Using) > 0` early-return special case) and the FROM/USING cross-product branches reuse the same `resolveScope` the FROM/USING-free branches already used for `SET`/`WHERE`/`RETURNING` resolution, ANDing `viewQual` into `FromPred`/`UsingPred` so the cross-product still only matches rows the view itself exposes. `WITH CHECK OPTION` is threaded through too: the FROM branch's `Update` node now carries `ViewCheckQual`/`ViewCheckName`, and `updateWithFrom` (`internal/executor/operators_storage.go`) gained a `checkViewCheckOption` call at the point `parentNewRow` is finalized, gated to `fst.tbl == o.plan.Table` (the exact base relation only — matches the FROM-free path's existing partition/inheritance-child exclusion, deferred item 5, unchanged). Also found and fixed a previously-untested latent bug spanning every view-DML form, not just this one: `viewProxyTable`'s synthetic table keeps `base`'s own `Name` (needed for ordinal-keyed lookups elsewhere, e.g. partition routing), so a DML statement's resolve-context binding — built with `s.Target.Alias` as the alias at every `resolveTbl`-driven site — left an unaliased qualified reference to the view by its *own* name (`UPDATE v SET x=1 WHERE v.id=1`, no `AS`) unresolvable (`42703`): the qualifier matched neither the binding's empty alias nor the proxy's borrowed base name. This was latent because no existing view-DML test qualified a column reference with the view's own name; `UPDATE ... FROM`/`DELETE ... USING` needed it to disambiguate the target from the FROM/USING relation(s), exposing it immediately. Fixed via new `viewResolveAlias(explicit, viewName string) string` (`internal/planner/view_dml.go`), applied at all four `resolveTbl`-driven binding sites (`planInsert`'s RETURNING context; `planUpdate`'s FROM and FROM-free contexts; `planDelete`'s USING and USING-free contexts) — `INSERT ... ON CONFLICT` resolution is unaffected (it already bypasses the proxy entirely, per item 1's "Known residual" note). Test: `TestUpdatableViewUpdateFromDeleteUsing` (`internal/executor/view_dml_test.go`). Design doc `docs/design/root-0025-updatable-views.md` new "Follow-up: `UPDATE ... FROM` / `DELETE ... USING` a view" section; `docs/design/README.md` row updated. | Root-0025 deferred items now: (5) CHECK OPTION on partition/inheritance-child-routed rows, and the narrower `ON CONFLICT`-against-renamed-view residual (item 1's "Known residual"). | (5) remap `newRow`/`parentNewRow` through the child's column map before the CHECK OPTION eval in `updateScanTables`'s child branch (`internal/executor/operators_storage.go`), or resolve a second `ViewCheckQual` variant per child column layout. `ON CONFLICT` residual: thread `resolveTbl`/`colMap` into `planOnConflict`'s `tbl` param and `resolveArbiterIndex`'s target-column lookup, translating back to base ordinals before the actual index lookup (`internal/planner/planner.go`). | (5) is a rare view-over-partitioned-table combination, same rationale as when it was first documented; the `ON CONFLICT` residual is a narrow two-independently-narrow-features intersection with a safe (`42703`, not silent corruption) failure mode — both left for a dedicated pass rather than widening this bounded item-3 loop further. |
| resolved | 2026-07-04 | M0119-0004 (slice-365 follow-up, root-0025 deferred item 5, this loop) | CHECK OPTION is now enforced on partition- and inheritance-child-routed rows, not just the base/parent table's own rows. `updateScanTables`'s per-row callback (`internal/executor/operators_storage.go`, plain FROM-free UPDATE path) gated the `checkViewCheckOption` call to `scanTbl == tbl`; `updateWithFrom`'s FROM cross-product branch gated it to `fst.tbl == o.plan.Table`. Both gates were unnecessarily conservative: each function already computes a `parentNewRow` (or, for the FROM-free path's non-inheritance-child branch, `newRow` itself, now aliased to `parentNewRow`) in the base table's own column ordinal space before the gate ever ran — true inheritance children remap through `buildInheritColMap`/`remapChildRowToParent`/`remapParentRowToChild`, and partition children need no remap at all since PG requires a partition's columns to exactly mirror the partitioned table's layout. Removed both gates; the check now runs against `parentNewRow` unconditionally in both functions. New test `TestViewCheckOptionEnforcedOnPartitionAndInheritanceChildRows` (`internal/executor/view_dml_test.go`) covers a partition-routed row via plain UPDATE and UPDATE...FROM, and an inheritance-child row via plain UPDATE; verified to fail against the pre-fix code (git-stashed the operators_storage.go hunk, re-ran, confirmed `err=nil` instead of the expected 44000). Design doc `docs/design/root-0025-updatable-views.md` gained a "Follow-up: CHECK OPTION on partition/inheritance-child-routed rows" section; `docs/design/README.md`'s root-0025 row updated. **Root-0025 is now fully closed except the narrow `INSERT...ON CONFLICT`-against-renamed-view residual from item 1.** | NONE — item 5 fully resolved. | N/A — item fully closed, no resume needed. | Verification: `go build ./...` clean; `go vet ./internal/planner/... ./internal/executor/...` clean; `go test ./internal/planner/... ./internal/executor/... ./internal/catalog/... ./internal/parser/... ./internal/server/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); pgbench smoke via the pre-commit hook; new test confirmed RED on the pre-fix tree. |
| resolved | 2026-07-04 | M0119-0004 (root-0025 item 5 follow-up, new discovery, this loop) | While closing root-0025 item 5, discovered (not fixed, project-wide, view-independent) that `updateOp.updateViaIndex` (`internal/executor/operators_storage.go`) has NO partition/inheritance-child fan-out at all — unlike `updateScanTables`/`updateWithFrom`, which both build a scan-target list including `catalog.InMemory.PartitionChildren`/`InheritanceChildren`. `updateViaIndex` opens exactly one B-tree (`ix.Index`, scoped to `o.plan.Table`) and only ever writes to that one table's own storage. | `UPDATE parent SET ... WHERE indexed_col = X` (no `ONLY`), whenever `planner.planIndexScanFromWhere` finds a usable index on `parent` itself (any plain, non-partitioned table with a matching index — a partitioned parent has no index of its own to match, so it always falls through to `updateScanTables`, which DOES fan out), silently skips any row that lives only in a plain-inheritance child's own storage: no error, no NOTICE, `UPDATE 0` (or a smaller count than PG would report) even though a matching descendant row exists. This is orthogonal to views/CHECK OPTION entirely — reproduces on any two plain tables joined by INHERITS. | `internal/executor/operators_storage.go`'s `updateViaIndex`: needs the same per-table fan-out `updateScanTables` already has (loop over `imU.InheritanceChildren(tbl.OID)`, probe each child's own index if it has a compatible one, else fall back to a child SeqScan+Filter), applying the same parent-ordinal remap (`buildInheritColMap`/`remapChildRowToParent`/`remapParentRowToChild`) already proven out in this loop's CHECK OPTION fix. Resume by writing a plain (non-view) two-table INHERITS regression test first (`UPDATE parent SET val=1 WHERE id=X` where id=X exists only in a child) to confirm/bound the gap before altering `updateViaIndex`. | out of scope for a view-focused loop — the fix is a materially separate, general-purpose executor change (index-path inheritance fan-out) unrelated to the CHECK OPTION gate this loop closed; discovered incidentally via the same-shape regression test for item 5. Closed by the 2026-07-04 root-0026 follow-up row below. |
| resolved | 2026-07-04 | M0119-0004 (root-0026 follow-up, this loop) | Fixed the item-7/row-above gap: `updateOp.Next()` (`internal/executor/operators_storage.go`) now builds the parent + partition/inheritance-children scan-target list (`updateScanTables`) BEFORE deciding whether to take the `updateViaIndex` fast path, and only takes that path when `len(updateScanTables) == 1` (no children) — falling through to the existing, already-correct `updateScanTables` SeqScan+fan-out path whenever the target has partition or accessible inheritance children, rather than adding per-child index probing. Design `docs/design/root-0026-update-via-index-inheritance-fanout.md`; tests `TestUpdateViaIndexFansOutToInheritanceChild(WithParentRow)` (`internal/executor/storage_ddl_test.go`) — the second covering a direct parent-owned row alongside a sibling child row reached by the same statement shape, ported from a concurrently-developed equivalent fix on the (now-deleted, redundant) `m0119-updateviaindex-inherit-fanout` branch, which independently found and fixed the identical bug via a `tableHasScanChildren` helper — see [[concurrent_ralph_loops_corrupt_tree]] for how the two loops reconciled without corrupting the tree. | Deliberately simpler than the resume-point sketch in the row above (no per-child index probe/remap): any PK-equality `UPDATE`/`DELETE` on a table with partition/inheritance children now always takes the O(n) SeqScan+fan-out path instead of O(log n) — correctness is restored but the perf win is lost for that specific combination (uncommon: PK-equality UPDATE + a table that also has INHERITS/PARTITION BY children). Also newly discovered (by the reconciled branch), NOT fixed here: a bare `SELECT`'s own IndexScan path (`indexScanOp`) has the identical parent-vs-children fan-out gap — `SELECT val FROM parent WHERE indexed_col = X` misses a child-only row while `WHERE non_indexed_col = X` (forcing SeqScan/Append) finds it correctly. | `internal/planner`'s SELECT planning for an inheritance/partition parent with an equality predicate on an indexed column — find where it builds a bare IndexScan instead of the Append(children) plan a non-indexed predicate gets, and either restrict IndexScan-on-parent-with-children the same way this fix restricts `updateViaIndex`, or extend the Append/child-probe machinery to the index-eligible case. Bound with a plain two-table INHERITS + `SELECT ... WHERE indexed_col = X` regression test first. | picking the simpler "disable the fast path" fix (vs. the per-child B-tree probe originally sketched) keeps this a single-loop, low-risk, easily-verified change; the SELECT-side twin is a materially separate operator (`indexScanOp` vs `updateOp`) and its own bounded task. |
| resolved | 2026-07-04 | M0119-0004 (slice-365 follow-up, root-0025 item 1's "Known residual", this loop) | `INSERT ... ON CONFLICT` against a renaming auto-updatable view now resolves correctly: `planOnConflict` (`internal/planner/planner.go`) previously always resolved the conflict-target column list (`resolveArbiterIndex`), `DO UPDATE SET`/`WHERE`, and the `excluded` pseudo-relation binding against `tbl` — already reassigned from the view to `base` by the time `planInsert` calls it — instead of `resolveTbl` (`viewProxyTable`'s column-name proxy every other view-DML resolution site already substitutes). `planOnConflict`/`resolveArbiterIndex` now take `resolveTbl`/the view's pre-rewrite name and derive a `scopeTbl`/`scopeAlias` pair (`resolveTbl`/`viewResolveAlias(targetAlias, viewName)` when set, else unchanged) used everywhere a name must resolve in the statement's own vocabulary: `resolveArbiterIndex` translates each plain conflict-target column name via `cat.LookupColumn(resolveTbl, name)` to the matching base column name before matching against `idx.Columns` (always base names) — an unresolvable name now raises `42703` naming the column instead of silently falling through to a spurious `42P10`; the arbiter-expr context and the DO-UPDATE 2-binding merged scope (primary + `excluded`) both bind `scopeTbl` under `scopeAlias`. `resolveDefaultDoNothingArbiter` (bare `ON CONFLICT DO NOTHING`, no explicit target) needed no change — it resolves the chosen index's own stored `ColExprs`, already written in base vocabulary. Test: `TestUpdatableViewOnConflictRenamedColumn` (`internal/executor/view_dml_test.go`) — a view renaming both columns, `ON CONFLICT (rid) DO UPDATE SET rval = voc1.rval + excluded.rval` (arbiter target + primary-alias-qualified bare ref + `excluded`, together), a conflict-free insert through the same view, and `ON CONFLICT (rid) DO NOTHING` leaving the row untouched; confirmed RED on the pre-fix tree (`42P10`, reverted only `internal/planner/planner.go` and reran). Design doc `docs/design/root-0025-updatable-views.md` gained a "Follow-up: `INSERT ... ON CONFLICT` against a renamed view column" section; `docs/design/README.md`'s root-0025 row updated. **Root-0025 is now fully closed — no open items remain within this milestone's own scope.** | NONE — item 1's residual fully resolved. | N/A — item fully closed, no resume needed. | Verification: `go build ./...` clean; `go vet ./internal/planner/... ./internal/executor/...` clean; `go test ./internal/planner/... ./internal/executor/... ./internal/catalog/... ./internal/parser/... ./internal/server/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); pgbench smoke via the pre-commit hook; new test confirmed RED on the pre-fix tree. |
| resolved | 2026-07-04 | M0110-0001 (DU-002 slice 439 triage item 1 follow-up, this loop) | `rolvaliduntil` now round-trips through the pg_authid heap file (`global/1260`), not just the WAL crash-tail + live `RoleAttrs` sidecar. The cited blocker (no PG-timestamp-literal parser) turned out already solved: `parseCopyTimestamp` (`internal/executor/copy_text.go`, used today for `COPY` text input and generic `timestamptz` literal coercion) parses the common layouts a `VALID UNTIL '...'` literal takes. `buildAuthidUserRow` (`internal/executor/pg_authid_sync.go`) now takes a `validUntil string` param, encoding a real `timestamptz` column (`NewTimeDatum`) via `parseCopyTimestamp` when it parses, else `NullDatum` (unchanged fallback); `ReadPgAuthidRows` decodes column 11 back to text via new `formatValidUntilText` (was dead code before — always NULL — and would have mis-decoded a `KindTime` datum via the wrong `.StringValue()` accessor). All three call sites (bootstrap superuser + sidecar override, per-role, predefined) updated. | PG's `infinity`/`-infinity` timestamptz sentinels (and any other `parseCopyTimestamp`-unparseable literal) still fall back to `NULL` in the heap file — goopg's `timestamptz` type has no infinity representation anywhere in the engine (encode/decode/compare/arithmetic), so a `VALID UNTIL 'infinity'` role still loses its heap-file persistence across a checkpoint-pruned clean restart (WAL-tail + live sidecar round-trip is unaffected). | `internal/executor/codec.go`'s `"timestamp","timestamptz"` encode/decode cases (~L388/L1088): add an `INT64_MIN`/`INT64_MAX`-equivalent sentinel Datum representation (mirrors PG's `DT_NOBEGIN`/`DT_NOEND`), then teach `buildAuthidUserRow`/`ReadPgAuthidRows` to use it instead of falling back to NULL for `infinity`/`-infinity`. | full engine-wide timestamptz infinity support is a materially bigger, separate feature (touches every comparison/arithmetic/output site, not just this one column) — this loop's bounded scope is the heap-file round-trip for concrete literal values, which was the actual gap named by the original row. |
| resolved | 2026-07-04 | M0119-0004 (root-0026 SELECT-side twin, root-0026-select-index-fanout branch) | `SELECT ... FROM parent WHERE indexed_col = X` (no `ONLY`) against a table with plain-`INHERITS` children now correctly fans out instead of silently returning zero rows for a child-only row. `planner.planIndexScanFromWhere`/`tryRangeIndexScan` (`internal/planner/planner.go`) gained an `enforceInheritanceFanout bool` param — when true (threaded from the SELECT call site as `!fromOnly`), refuses to attach an `IndexScan` node whenever `tbl` has any `catalog.AccessibleInheritanceChildren`, mirroring the pre-existing `len(tbl.PartitionKey) > 0` guard for partitioned parents; the caller (`Plan`'s `isSimpleSingle` branch) then falls back to the plan shape `planScanRangeVar` already builds for that case (a `SetOp{All:true}` UNION ALL chain of per-table `SeqScan`s), which already fans out correctly. `planUpdate`/`planDelete`'s own call sites of the same function pass `enforceInheritanceFanout=false` (see `deferred`/`why` below). New test `TestSelectIndexScanFansOutToInheritanceChild` (`internal/executor/storage_ddl_test.go`) covers both the equality and range-predicate IndexScan sub-paths plus a `FROM ONLY` control case; confirmed RED pre-fix via `git stash` of just the `planner.go` hunk. | Performance residual only (no correctness gap remains for SELECT): a parent table with inheritance children now always takes the UNION ALL/SeqScan path for an indexed-predicate SELECT, even though the parent's own rows (and any child with a matching index) could in principle still be probed via B-tree — same class of residual already accepted for the `updateOp` fix this row's twin. Also, `planUpdate`/`planDelete`'s calls to `planIndexScanFromWhere` were deliberately left at `enforceInheritanceFanout=false` rather than flipped to `true` for uniformity: `updateOp.Next()` (`internal/executor/operators_storage.go`) already gates its own index fast path at the executor level on `len(updateScanTables) == 1`, and `deleteOp.Next()` never consults this plan node's index choice for its fan-out decision at all (always recomputes `scanTables` itself) — so flipping either would be a correctness no-op, done only to avoid widening this branch's diff into the concurrently-landing `updateOp` fix. | `internal/planner/planner.go`'s `planIndexScanFromWhere`/`tryRangeIndexScan`: future work could extend the per-child-index-probe optimization (same "Future optimization" idea recorded for the `updateOp` twin) to the SELECT path too, or simply flip the `planUpdate`/`planDelete` call sites to `enforceInheritanceFanout=true` once profiling shows the redundant plan-time check is worth adding for symmetry/clarity — neither is required for correctness. | perf follow-up and DML-call-site uniformity are both optional, non-correctness-affecting polish; this row exists to track the SELECT-side correctness gap specifically, which is now fully closed. |
| resolved | 2026-07-04 | M0119-0004 | Materialized-view restart persistence: `CREATE MATERIALIZED VIEW` no longer reverts to a plain table after a restart (closes the "Deferred" gap recorded in `docs/design/0110-0001-pg-dump-tap-port.md`'s `ALTER MATERIALIZED VIEW ... SET SCHEMA` follow-up section). Root cause: `buildUserPGClassRow` (`internal/executor/pg18_user_catalog_rows.go`) hardcoded `relkind` to `"r"`/`"p"` only, so a matview's pg_class heap row was written as an ordinary table; `loadUserTablesFromHeap` (`internal/initdb/open.go`) filtered strictly on `relkind == "r"`, silently downgrading a reloaded matview to `IsMatView=false`/`View=nil` (its physical row data survived — matviews have real heap storage unlike plain views — but the matview-ness and refresh query did not). Fixed: `buildUserPGClassRow` now emits `relkind='m'` for `tbl.IsMatView`; `loadUserTablesFromHeap` accepts `relkind` `"r"` or `"m"` and sets `IsMatView` from the recovered row; new `wal.RecordKindCreateMatView` (byte 102) carries `tableOID/populated/querySQL`, emitted from `syncTableToCatalogHeap` via `ctx.Pool.LogChangeRecord` (mirroring `RecordKindCreateIndex`'s emission hook, not `RecordKindColumnDefaults`'s `ctx.WAL.Append`, because the initdb-level test harness only wires `ctx.Pool`); new `internal/initdb/matview_ddl_recovery.go`'s `replayMatViewRecords` re-parses the query via `parser.Parse` after `loadUserTablesFromHeap`. Tests: `internal/initdb/matview_ddl_recovery_test.go` (`TestMatViewSurvivesRestartViaWAL`, `TestMatViewWithNoDataSurvivesRestartViaWAL`), both confirmed RED on the pre-fix tree via `git stash` of the 4 production files. Design: `docs/design/0110-0001-pg-dump-tap-port.md` new "Follow-up: materialized-view restart persistence" section. | Plain (non-materialized) `CREATE VIEW` has **zero** on-disk persistence at all — `execCreateView` never calls `syncTableToCatalogHeap`, so a plain view does not survive a restart even as a downgraded table (it simply ceases to exist, `LookupTable` returns not-found). This is a separate, larger, pre-existing gap, confirmed via code inspection (no `syncTableToCatalogHeap`/WAL-record call anywhere in `execCreateView`), not attempted in this loop. | `internal/executor/operators_ddl.go`'s `execCreateView`: give plain views the same treatment this loop built for matviews — `relkind='v'` in `buildUserPGClassRow` (currently unhandled), a `loadUserTablesFromHeap` relkind='v' branch that reconstructs a *columnless-from-pg_attribute* Table (a view has no `pg_attribute`-backed physical columns the way a table/matview does — this loop did not investigate how `loadUserTablesFromHeap`'s attribute-driven column-reload path should behave for a view, which is the open design question), and a `RecordKindCreateView`-equivalent WAL record carrying the raw SQL body, replayed the same way `matview_ddl_recovery.go` does. | Scoped this loop to the matview-specific gap the design doc's existing "Deferred" section named as the concrete resume point; the plain-view gap is a materially different data shape (no physical columns/storage) discovered only while confirming plain views weren't already handled the same way, and deserves its own bounded investigation rather than widening this loop's diff. |
| resolved | 2026-07-04 | M0119-0004 (deferred item (2) from the "Plain `CREATE VIEW` restart persistence" row above) | `DROP MATERIALIZED VIEW` no longer leaks the matview's physical heap file. `execDropOneMatView` (`internal/executor/operators_ddl.go`) previously only removed the catalog entry (`catalog.DropView`) and stamped xmax on the on-disk pg_class/pg_attribute rows — it never called `Pool.Manager().DropRelation`/`InvalidateRel` or `FSM`/`VM.DropRelation` for the matview's own main-fork file, unlike `DROP TABLE`'s `dropTableByRefImmediate`. Fixed by adding the identical `o.ctx.Catalog.RelFileNode(tbl)` → `Pool.InvalidateRel` → `Pool.Manager().DropRelation` → `FSM.DropRelation`/`VM.DropRelation` sequence after the existing xmax-stamp block (`RelFileNode` reads only `tbl`'s own fields, so it's safe to call post-`DropView`; `DropRelation`'s `os.IsNotExist` guard makes it a no-op for a never-materialized `WITH NO DATA` or restart-downgraded-to-virtual matview with no backing file). Test: `internal/executor/operators_ddl_matview_storage_test.go`'s `TestDropMaterializedViewReleasesStorage` (populates a matview, confirms the file exists via `Pool.Manager().Exists(rel)`, drops it, asserts the file/FSM/VM are all cleared). Design: `docs/design/0110-0001-pg-dump-tap-port.md` new "Follow-up: `DROP MATERIALIZED VIEW` physical-storage leak" section. Gates: `go build ./...`/`go vet ./...` clean; `go test ./internal/executor/... ./internal/initdb/...` (view/matview-scoped + full executor package) all PASS. | Deferred item (1) from the same prior row — `CheckOption`/`SecurityBarrier`/`SecurityInvoker` still don't round-trip through the heap-persisted pg_class row (`buildUserPGClassRow` hardcodes `reloptions="{}"`) — remains open, untouched by this loop; and indexes created on a materialized view (if any) are a separate, unexamined physical-storage question not covered by this fix. | `internal/executor/pg18_user_catalog_rows.go`'s `buildUserPGClassRow`: needs a generic reloptions-heap-encoder shared across relation kinds, populated from `tbl.CheckOption`/`SecurityBarrier(Set)`/`SecurityInvoker(Set)` the same way `pg_get_viewdef`'s live rendering already does. | Scoped this loop strictly to the physical-storage leak the prior row's deferred item (2) named; the reloptions heap-encoding gap is a materially different (encoding, not storage-lifecycle) fix and deserves its own bounded loop. |
| resolved | 2026-07-04 | M0122-0004/M0119-0004 (DU-002 slice-365 predecessor row item (2), 2026-06-30 — the WITH CHECK OPTION enforcement half of that same row's item (1) was independently closed by the root-0025 loop, see `docs/design/root-0025-updatable-views.md`) | Closed the last open half of item (2): the pre-AS `WITH (check_option=<local\|cascaded>)` reloption-form spelling of a view's CHECK OPTION now parses into `CreateViewStmt.CheckOption`, exactly like the trailing `WITH [CASCADED\|LOCAL] CHECK OPTION` clause already did — upstream's `DefineView` (`postgres/src/backend/commands/view.c`) folds the trailing clause into the identical `check_option` reloption `DefElem` internally, so both spellings are one PG option, but `internal/parser/ddl.go`'s pre-AS `WITH (...)` options loop only had cases for `security_barrier`/`security_invoker` (slices 366/367) — `check_option` fell through unhandled and was silently dropped. Fixed by adding a `check_option` case to that loop matching the existing lenient (non-erroring, default-cascaded) style of the boolean options rather than raising a new enum-validation error class. No executor change needed: `execCreateView` already reads `s.CheckOption` regardless of which spelling set it, so `checkViewCheckOption` enforcement and pg_class reloptions rendering both work immediately. Also corrected two stale comments discovered while re-verifying this backlog entry against current HEAD (per M0122's mandatory verify-before-implement step): (a) the parser's own trailing-clause comment still said "goopg does not yet ENFORCE the check" — false since the root-0025 loop landed `checkViewCheckOption`, now updated; (b) confirmed enforcement + `security_barrier`/`security_invoker` reloption parsing (the other two sub-items DU-002 slice-365's row had deferred) were ALREADY fully resolved by prior loops (root-0025 design doc, `internal/parser/ddl.go` slices 366/367) — `unimplemented_feat.json`'s matching entry (`"VIEW WITH CHECK OPTION is not enforced..."`) was stale on all fronts, updated in place with a `status` field and corrected `code_audit`. Tests: `TestParseCreateViewCheckOptionReloptionForm` (`internal/parser/view_test.go`), `TestInsertViewCheckOptionReloptionFormViolation` (`internal/executor/view_dml_test.go`, confirms end-to-end `44000` via the reloption spelling). Design: `docs/design/root-0025-updatable-views.md` new "Follow-up: `WITH (check_option = ...)` reloption-form spelling" section; `docs/design/README.md` row appended in place. Gates: `go build ./...`/`go vet ./internal/parser/... ./internal/executor/...` clean; `go test ./internal/parser/... ./internal/executor/... ./internal/planner/...` PASS (full packages, no regressions). | Restart persistence for `check_option`/`security_barrier`/`security_invoker` (item (3) of the same 2026-06-30 predecessor row) is a separate on-disk-encoding gap, not touched by this loop — deliberately, since a concurrent loop was mid-flight on exactly that gap (heap-persisted `pg_class.reloptions` round-trip) when this loop ran; check the ledger for a newer M0119-0004 row before re-picking it up, it may already be resolved. | If the concurrent reloptions-heap-encoder loop's row above (search for `ApplyTableReloptions`) is not yet present/committed, resume point is `internal/executor/pg18_user_catalog_rows.go`'s `buildUserPGClassRow` (reloptions hardcoded to `"{}"`) + `internal/initdb/open.go`'s `loadUserTablesFromHeap`. | Scoped narrowly to the one remaining parser-level gap this loop could close without any risk of colliding with a concurrent peer `ralph_loop.sh` session's in-flight, file-overlapping WIP on `internal/catalog/catalog.go`/`internal/executor/pg18_user_catalog_rows.go`/`internal/initdb/open.go` (verified via `git status`/`git diff` before starting and again before committing) — restart persistence is that peer's task, not duplicated here. |
| resolved | 2026-07-04 | M0119-0004 (deferred item (1) from the "Plain `CREATE VIEW` restart persistence" row and its restatement in the `DROP MATERIALIZED VIEW` physical-storage-leak row above) | `reloptions` (fillfactor/every `autovacuum_*` setting/`vacuum_index_cleanup`, and for views `security_barrier`/`security_invoker`/`check_option`) now round-trips through the heap-persisted pg_class row across a restart, for both tables and views — not just the three view options the deferred item named. Root cause was three compounding bugs, not one: (1) the reloptions-list-building logic lived only inline in `catalog.go`'s `registerSystemTables` `VirtualRows` closure, so `buildUserPGClassRow` (`internal/executor/pg18_user_catalog_rows.go`) had drifted to hardcoding `reloptions="{}"` unconditionally — extracted to shared `catalog.TableReloptionsElements`/`catalog.BuildTableReloptions`; (2) `pgClassColumnsPG18()` declares reloptions as literal type name `"text[]"`, and `encodeValuePG`'s `"text[]"` case silently discards any non-`KindBytes` Datum in favor of an empty ArrayType blob (`emptyArrayTypeBytes(25)`) — `buildUserPGClassRow` now builds a real ArrayType blob via the existing `pgTextArrayBytes` helper and passes it as `KindBytes`; (3) `decodePhysicalPGValueMctx` had no case for `"text[]"`/`"_text"` at all (fell to the generic default varlena branch, returning raw undecoded ArrayType bytes) — added `decodePGTextArrayElements` + a `"text[]"`/`"_text"` case that joins the decoded elements back via newly-exported `catalog.ArrayTextLiteral`. Finally wired the read side: new `catalog.ApplyTableReloptions` (mirror of `TableReloptionsElements`) is called from `loadUserTablesFromHeap` (`internal/initdb/open.go`) Pass 3 — decoding the full 34-column physical row via the already-existing general decoder `executor.DecodeRowIntoMctxPGTuple` (newly exported schema getter `executor.PGClassColumnsPG18()`) since reloptions (attnum 33) is past the fixed-offset prefix `catalog.DecodePGClassPhysicalRow` covers. Tests: `internal/executor/pg18_user_catalog_rows_test.go`'s `TestBuildUserPGClassRowReloptionsSurvivesHeapEncode` (round-trips a plain/fillfactor/security_barrier+check_option table+view through the real `EncodeRowPG`→`DecodeRowIntoMctxPGTuple` path); `internal/initdb/view_ddl_recovery_test.go`'s `TestTableAndViewReloptionsSurviveRestart` (full Init/Open/DDL/Close/Open, both confirmed RED on the pre-fix tree). Design: `docs/design/0110-0001-pg-dump-tap-port.md` new "Follow-up: reloptions never survived a restart" section + `docs/design/README.md` row updated. | Two narrower gaps remain, out of scope for this loop: (a) `toast.*` reloptions are a separate mechanism (attach to the TOAST relation's own pg_class row, not the owning table's `reloptions` column) — untouched, no evidence they're broken. (b) index reloptions — `buildUserPGClassRowForIndex` (same file) still hardcodes `reloptions="{}"` unconditionally; indexes support `fillfactor` too (and B-tree/GIN/GiST-specific storage params), so an index created `WITH (fillfactor=50)` still loses that setting across a restart. | `internal/executor/pg18_user_catalog_rows.go`'s `buildUserPGClassRowForIndex`: needs an index-specific reloptions element builder (fillfactor at minimum; index AM-specific params like `fastupdate`/`gin_pending_list_limit` if/when GIN lands) feeding the same `pgTextArrayBytes`+`NewBytesDatum` encoding this loop added for tables/views, and a matching decode-and-apply call in `loadUserTablesFromHeap`'s index-loading pass (`loadUserIndexesFromHeap`). | Scoped this loop strictly to the table/view reloptions gap the deferred item named; index reloptions is a materially separate code path (`buildUserPGClassRowForIndex`/`loadUserIndexesFromHeap`, not `buildUserPGClassRow`/`loadUserTablesFromHeap`) discovered only while confirming no OTHER relation kind shared the same hardcoded-`"{}"` bug, and deserves its own bounded loop rather than widening this one's diff. |
| resolved | 2026-07-04 | M0122-0002 (regexp_matches-srf follow-up) | `regexp_matches(string, pattern[, flags])`'s `'g'`-flag multi-row SRF semantics now land for the SELECT-list/target-list position: `RegexpMatchesCol` (`internal/planner/plan.go`), detected in `buildSelectSrfProjectSet` (`internal/planner/planner.go`) alongside `generate_series`/`unnest`, expanded by `projectSetOp.openSelectSrfMode` (`internal/executor/operators_project_set.go`) via new `evalRegexpMatchesSRF`/`regexpAllMatchesArrays` (`internal/executor/expr.go`, factored out of the pre-existing `regexpFirstMatchArray`). Verified byte-for-byte against a real PostgreSQL 18.3 cluster: `'g'` flag → one row per match; no flag → at most the first match; no match → zero rows (not the scalar path's NULL). Tests: `internal/executor/regexp_matches_srf_test.go`. Design: `docs/design/0122-0002-pg-relation-size-real-sizes.md` new "Follow-up: `regexp_matches` SRF" section. Gates: `go build ./...`/`go vet ./...` clean; `go test ./internal/executor/... ./internal/planner/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33). | The FROM-clause form (`SELECT * FROM regexp_matches(...)`) is still unwired — goopg has no `planFromRegexpMatches` counterpart to `planFromUnnest`/`planFromGenerateSeries`. | `internal/planner/planner.go`: add a `planFromRegexpMatches` alongside `planFromUnnest` (~line 3905) that plans `FROM regexp_matches(...) AS alias(col)` as a row-producing source (one row per match), reusing `evalRegexpMatchesSRF`/`regexpAllMatchesArrays` from `internal/executor/expr.go` for the actual match expansion. | Bounded this loop to the far more common SELECT-list usage (`SELECT regexp_matches(col, pat, 'g') FROM t`, the form PG's own docs lead with); the FROM-clause form needs a materially separate planner code path (a new physical source node, not a ProjectSet wrapper) and is a clean, independently-resumable increment. |
| resolved | 2026-07-04 | M0122-0004 (WITH-ORDINALITY named-column half of ledger row 480) | `WITH ORDINALITY AS t(m, n)` naming an explicit outer-SELECT column against either the SRF's own element column or the trailing ordinality column no longer raises `42703`. Root cause was misdiagnosed as planner-side by an earlier loop's own note (`wrapOrdinality`/`planFromUnnest`/`planFromRegexpMatches` were always correct and never even ran before the failure) — it was `internal/analyzer/analyzer.go`'s `tableFuncColumns`, called from `lookupTable` to build the FROM-item's synthetic scope table *before* the planner runs (`planner.Plan()` calls `analyzer.Analyze()` first and returns on its error). `tableFuncColumns` never threaded `rv.TableFunc.WithOrdinality` through at all (0 hits for the identifier in that file) and had no `unnest`/`regexp_matches` cases, so it silently fell to the `default:` case's single generic `int8` column named after the alias — the ordinality column and the SRF's real per-element columns never existed in that table, so naming them explicitly hit `lookupColumn` → `42703`. `*` was unaffected only because `analyzeStar` returns immediately for an unqualified `*` with no column-existence check. Fix: `tableFuncColumns` now takes `*parser.TableFuncRef` (was `funcName string`), strips the trailing ordinality alias before dispatch and re-appends a real `int8` ordinality column afterward — mirroring `wrapOrdinality` exactly — plus gained `unnest` (N-column zip matching `len(tf.Args)`, single-arg names from the alias like `planFromUnnest`) and `regexp_matches` (`text[]`) cases that previously fell to the wrong generic default. Tests: `internal/analyzer/analyzer_test.go`'s `TestAnalyzeWithOrdinalityNamedColumn`. Verified end-to-end against a live goopg server + real PG 18.3 `psql` binary (`unnest`/`generate_series`/`regexp_matches`, single- and multi-arg `unnest`, all with named ordinality + element columns). Gates: `go build ./...` clean; `go test ./internal/analyzer/... ./internal/planner/... ./internal/executor/... ./internal/parser/...` PASS (no regressions). | The analyzer's `unnest` element type is a hardcoded `text` placeholder, not the array argument's real element type — `lookupTable`/`tableFuncColumns` run during scope-*building*, before any resolveContext/scope exists to `analyzeExpr` the array argument (the planner's `planFromUnnest` has this available and infers the real type via `exprType(arrExpr)`). This is a pre-existing imprecision (the prior `default:` fallback was equally wrong, returning `int8` instead of the true element type) — surfaces only if a later expression in the same query depends on the analyzer's inferred type for an explicitly-named `unnest` column (e.g. an operator/function overload resolution keyed on that type), not on the `42703` bug this row closes. The comma/LATERAL `ctx.OuterRows` half of ledger row 480 is separate and still fully open. | `internal/analyzer/analyzer.go`'s `tableFuncColumns`/`lookupTable`: giving the analyzer real per-arg type inference for `unnest` would need either (a) plumbing a resolveContext/scope into `lookupTable` so it can `analyzeExpr` `tf.Args[i]` before building columns, or (b) deferring FROM-item table construction until after sibling FROM items are scoped (structural change to `buildSelectScope`). Deferred because no failing test currently demonstrates a concrete wrong-answer from the `text` placeholder — recorded as a known imprecision, not an active bug, per the loop's own live-server verification showing all query shapes it tried returning correct data despite the placeholder type. |  |
| resolved | 2026-07-05 | M0097-0040 (DML table-privilege enforcement) | `INSERT`/`UPDATE`/`DELETE` now enforce table-level ACLs for a non-superuser role (`SET ROLE`/`SET SESSION AUTHORIZATION`): new `dmlPrivilegePermitted(ctx, tbl, priv)` (`internal/executor/operators_storage.go`) — bootstrap-superuser bypass, then table-owner bypass, then `catalog.HasTablePrivilege(oid, role, priv)` (the same `tableACLs` store TRUNCATE/MAINTAIN already consult, M0118-0008) — checked pre-lock in `insertOp`/`updateOp`/`deleteOp.Open`, raising `42501 permission denied for table <name>` on failure. Verified FK-cascade deletes (`fkCascadeDelete`, heap-page writes, bypasses `deleteOp` entirely) and the logical-replication apply worker (also direct heap writes) are unaffected. Tests: `internal/executor/storage_dml_test.go`'s `TestDMLRequiresTablePrivilege`. Design: `docs/design/0118-0039-truncate-conflict-privilege-model.md` Follow-up section; `unimplemented_feat.json` M0097-0040 updated in place. Gates: `go build ./...` clean; `go test ./internal/executor/... ./internal/planner/... ./internal/server/... ./internal/catalog/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33). | `SELECT` privilege is still never checked — a non-superuser role with no `SELECT` grant (and REVOKEd default) can still read any table via `seqScanOp`/index-scan paths. Column-level privileges, `WITH GRANT OPTION` propagation, and `GRANT ... TO PUBLIC` remain unmodelled (same limitations TRUNCATE's original design doc already recorded). | `internal/executor/operators_storage.go`'s `seqScanOp.Open` (and the B-tree/index-scan equivalents) would need the same `dmlPrivilegePermitted(ctx, tbl, "SELECT")` gate — but SELECT's blast radius is every read path including internal system-catalog scans issued transparently on behalf of a non-superuser session (pg_catalog/information_schema queries, `\d`-style introspection, view/CTE expansion), so it needs its own bounded loop with a dedicated regression pass verifying system-catalog reads stay unaffected, not a one-line mirror of this loop's write-path checks. | The three DML mutation ops share one simple, symmetric `Open()` shape and one error contract, making them safe to land together in one bounded loop; SELECT enforcement is a materially larger, higher-risk change (every query path, not just three ops) that deserves isolated verification rather than being folded in here. |
| resolved | 2026-07-05 | M0119-0004 (index-reloptions follow-up) | Index storage parameters (`fillfactor`/`deduplicate_items`/`fastupdate`/`gin_pending_list_limit`/`pages_per_range`/`autosummarize`) now survive a restart: added `catalog.BuildIndexReloptions`/`IndexReloptionsElements`/`ApplyIndexReloptions` (mirroring the table/view builders), wired `buildUserPGClassRowForIndex` to encode them, `loadUserIndexesFromHeap` to decode+apply them, and added `resyncIndexClassHeapRow`/a `resyncIndexHeapRow` call after `createBTreeIndex`'s WITH-clause field-setting block (the fields were previously set on `idx` only *after* `syncIndexToCatalogHeap` had already written the heap row, so it always saw zero values). Root-caused via a real restart repro: also found and fixed a second, more fundamental bug blocking this — `mirrorTouchedCatalogsToPostgresDB` never mirrored `pg_index` (2610) to `PostgresDBOid`, so `loadUserIndexesFromHeap`'s Pass 2 heap scan (which reads from `cat.DBOID()`, almost always 5/"postgres" in real usage per `detectCatalogDBOID`) never found *any* user index's `pg_index` row — every CREATE INDEX heap-recovery silently fell back to WAL replay. Added `catalog.IndexRelationId` to the mirror list. Fixing that unmasked a **third** bug, which broke an existing test (`TestRenameIndexSurvivesRestartViaWAL`) the moment heap-scan index recovery started actually running: `RegisterIndexDuringRecovery`/`UnregisterIndexDuringRecovery` did a bare `key(...)` map probe instead of the "" vs "public." collision-aware `lookupIndexLocked` resolution reads and `RenameIndexDuringRecovery` already used — so heap-scan recovery (schema resolved from the pg_class namespace OID, e.g. `"public"`) and WAL-replay recovery (raw, often-unqualified `idxName.Schema` from the original DDL statement, `""`) registered the SAME physical index under two different map keys, leaving a stale duplicate behind whenever a rename/drop only touched one of the two keys. Fixed in the same loop (not deferred) by switching both functions' dup/lookup logic to `lookupIndexLocked`. Tests: `TestBuildUserPGClassRowForIndexReloptionsSurvivesHeapEncode` (executor), `TestIndexReloptionsSurviveRestart` (initdb); `TestRenameIndexSurvivesRestartViaWAL`/`TestCreateIndexRecoveredOIDDoesNotCollide` (pre-existing, re-verified green). | One adjacent gap surfaced, NOT fixed here (kept this loop bounded): **partition-child index reloptions are never inherited** — `createPartitionChildIndexes` never copies `s.Fillfactor`/`s.DeduplicateItems`/etc. onto `childIdx` at all (only `HasPredicate`/`Predicate`/`IncludeColumns`/expression strings are carried over), so `CREATE INDEX ... WITH (fillfactor=N)` on a partitioned parent silently drops the storage parameter on every partition child, restart or not. | `internal/executor/operators_ddl.go`'s `createPartitionChildIndexes`: copy `s.Fillfactor`/`s.DeduplicateItems`/`s.FastUpdate`/`s.GinPendingListLimit`/`s.PagesPerRange`/`s.AutoSummarize` onto `childIdx` alongside the existing `HasPredicate`/`IncludeColumns` copy, then call the new `resyncIndexClassHeapRow` if heap sync is available. | Needs its own test matrix across partition levels (multi-level partitioned tables, each level's own WITH-clause precedence vs the parent's) — expanding this loop to cover it would have gone past "index reloptions survive a restart"; kept bounded per the one-task-per-loop rule and recorded here instead of silently leaving it undiscovered. |
| resolved | 2026-07-05 | M0122-0003 (`track_io_timing` runtime `SET`, closes the row at line 466 above) | `SET track_io_timing` now takes effect live per session, without a server restart. `internal/activity/registry.go`: `coldActivity` gains a `TrackIOTimingOn atomic.Bool` (per-backend effective setting) and `ActivityRegistry` gains a `trackIOTimingFastPath atomic.Bool` (process-wide, latches true the first time any backend enables it, never reset); new `UpdateTrackIOTiming`/`TrackIOTiming`/`EnableTrackIOTimingFastPath`/`LookupTrackedGoroutine` methods — the last is a drop-in replacement for `LookupCurrentGoroutine` in hot I/O-wait hooks that additionally requires the calling backend's flag to be on, short-circuiting on the fast-path flag so the default-off case still costs one atomic load (preserving M0092-0005's original perf rationale for gating these hooks at all). `internal/initdb/open.go`: the `Pool.OnPinWait`/`OnPinDone` and AIO/data-file `OnReadWait`/`OnWriteWait`/`OnExtendWait`/`OnSyncWait`(+Done) hooks are now wired **unconditionally** (previously skipped entirely unless the boot-time `opts.TrackIOTiming` was true) and call `act.LookupTrackedGoroutine()` instead of `activity.LookupCurrentGoroutine()`; `opts.TrackIOTiming` now only primes the fast-path flag at boot. `internal/server/server.go`'s `New()` registers a `cfg.Registry.OnChange("track_io_timing", ...)` hook (same pattern as the pre-existing `application_name` one) that calls `UpdateTrackIOTiming` on the setting backend's own procNum; the per-connection setup seeds each new backend's flag from `sess.Get("track_io_timing")` right after `NewSessionRegistry` so it starts at the correct boot-configured value. `internal/config/defaults.go`'s stale "hooks not installed at all when off" comment corrected. Tests: `internal/activity/registry_test.go` (`TestActivityRegistryTrackIOTimingFastPath`, `TestActivityRegistryTrackIOTimingFastPathBoot`), `internal/server/server_test.go` (`TestTrackIOTimingOnChangePropagatesToActivityRegistry`, exercising `New()`'s real OnChange wiring + a live `SessionRegistry.Set`). Design: `docs/design/0122-0003-explain-format-xml-yaml.md` cluster-status row flipped + new "`track_io_timing` runtime SET" section. Gates: `go build ./...` clean; `go test ./internal/activity/... ./internal/server/... ./internal/config/...` PASS; `go test ./internal/initdb/...` PASS (full package, -count=1, background run given package size); `go vet` clean on all four touched packages. | This closes the specific "not re-checked per session/query" gap; it does not add new timing-*collection* fields anywhere (the earlier row's own note that "there is no buffer-timing collection code path to gate in the first place" still holds) — a genuine `I/O Timings` measurement (the 6 float columns EXPLAIN/`pg_stat_io` would report once `track_io_timing` is truly on) still needs actual wall-clock instrumentation added at each wait-event site, which is the pre-existing BUFFERS/`pg_stat_io` storage-instrumentation-layer gap tracked in the other M0122-0003 rows above (unaffected by this row). | `internal/executor/instrument.go` / `internal/storage/bufpool.go`: once real per-wait-event timing is added, it should itself consult `act.LookupTrackedGoroutine()`/`ActivityRegistry.TrackIOTiming(procNum)` (now available) as the gate, rather than re-deriving a boot-time bool the way the old code did. | Scoped to exactly the runtime-SET wiring the ledger row asked for (a live per-session GUC value reaching the existing wait-event hooks) — did not expand into building the still-nonexistent timing-collection instrumentation, which is a separate, larger, already-tracked unit of work shared with BUFFERS/`pg_stat_io`. |
| resolved | 2026-07-05 | M0122-0003 (EXPLAIN `I/O Timings` line, closes the presentation-layer gap the `extend_time` row above predicted — found already in-flight this loop, uncommitted from a prior interrupted loop; this loop verified and committed it) | EXPLAIN now renders upstream's `"I/O Timings: shared read=X.XXX write=Y.YYY"` line (TEXT: `formatIOTimingsLine`/`walkPlanAnalyzeFiltered`; JSON: `"Shared I/O Read/Write Time"` keys in `planToJSONWithStats`), backed by new `nodeStats.bufReadTimeNs`/`bufWriteTimeNs` accumulators (`internal/executor/instrument.go`) diffed the same nested-stopwatch way as the existing `bufHit`/`bufRead` counters, with `bufWriteTimeNs` folding `Pool.ExtendTimeNanos()` in alongside `Pool.WriteTimeNanos()` (mirrors `pgstat_count_io_op_time`'s single shared write-time bucket for `IOOP_EXTEND`, `postgres/src/backend/utils/activity/pgstat_io.c`). This loop's own contribution: verified the in-flight implementation (`go build`, full `internal/executor` suite including the two new `TestExplainIOTimings*` cases), wrote the design doc follow-up section (`docs/design/0122-0003-explain-format-xml-yaml.md`) + `docs/design/README.md` row extension, and ran `scripts/tpch-spotcheck.sh` (PASS, Q12=2/Q13=33) before committing. | goopg's non-TEXT (JSON/XML/YAML) gating differs from upstream: real PG's `show_buffer_usage` gates the non-TEXT `ExplainPropertyFloat` I/O-timing calls on the live `track_io_timing` GUC (always present, possibly `0.000`, when the GUC is on); goopg gates on "accumulated value nonzero" instead, so a GUC-on-but-zero-blocks-touched EXPLAIN omits the key entirely instead of emitting an explicit `0`. Behaviorally identical whenever the GUC is on and any I/O occurred, or the GUC is off (values are naturally zero either way) — differs only in that one edge case. | `internal/executor/operators_explain.go`'s `planToJSONWithStats`: thread `ctx.Activity.TrackIOTiming(ctx.ProcNum)` (already used to gate the TEXT/JSON `Buffers:`-adjacent hook wiring in `internal/initdb/open.go`) through to the non-TEXT branch so the "Shared I/O Read/Write Time" keys are emitted unconditionally once BUFFERS+track_io_timing are both on, matching upstream's non-TEXT gating exactly instead of the nonzero-value proxy. | The GUC-vs-nonzero simplification was accepted upstream (in the code comment) rather than fixed in the same loop that discovered it, since it requires threading a GUC-lookup argument through `planToJSONWithStats`'s existing call chain — a narrow but distinct follow-up, not a blocker to shipping the correctly-rendering common case. |
| resolved | 2026-07-05 | M0122-0003 (non-TEXT `track_io_timing` GUC gating, resolves the row above's named residual) | `planToJSONWithStats` (`internal/executor/operators_explain.go`) now takes a `trackIOTiming bool` param threaded from `explainOp.Open`'s `ctx.Activity.TrackIOTiming(ctx.ProcNum)` (nil-`Activity`-safe) through the recursive `Plans` re-render, and gates `"Shared I/O Read Time"`/`"Shared I/O Write Time"` on it instead of on accumulated-nonzero — now matching `explain.c`'s `peek_buffer_usage` non-text unconditional-emit behavior exactly (renders `0` when the GUC is on but zero blocks were touched). TEXT's `formatIOTimingsLine` deliberately keeps its original nonzero gate (no upstream precedent for an explicit all-zero TEXT line). New tests: `TestPlanToJSONWithStatsRendersIOTimingsWhenTrackIOTimingOnEvenAtZero`, `TestPlanToJSONWithStatsOmitsIOTimingsWhenTrackIOTimingOff`; existing `TestPlanToJSONWithStatsRendersIOTimingsWhenNonzero`/`TestExplainIOTimingsJSONOmittedWithoutAccumulatedTime` updated/reverified. Design: `docs/design/0122-0003-explain-format-xml-yaml.md` "Accepted simplification" section rewritten to "fixed"; `docs/design/README.md` unaffected (existing row already covers this doc). Gates: `go build ./...` clean; `go test -count=1 ./internal/executor/...` PASS; `scripts/tpch-spotcheck.sh` PASS. | Nothing newly deferred by this fix itself — it closes exactly the gap the row above named. Still open in the M0122-0003 bucket (unchanged by this loop): the remaining 3 `pg_stat_io` op counters (`reuses`/`writebacks`/`fsyncs` + their `_bytes`/`_time` columns, each needing a genuinely new counting mechanism — strategy-ring reuse, bgwriter/checkpointer-scoped writeback attribution, fsync call-site instrumentation respectively) and `EXPLAIN (BUFFERS)` without `ANALYZE` (PG 17+ planning-time buffers, no planning-phase buffer counters exist). | (n/a — resolved same loop) | (n/a — resolved same loop, no scope-bounding needed) |
| resolved | 2026-07-06 | M0122-0003 (CTEDMLPrefix nested-node EXPLAIN instrumentation, closes the row-467/468 residual) | Nodes nested under a `CTE DML` summary line (e.g. `Insert on t`) now report `actual time=`/`rows=` under `EXPLAIN ANALYZE`, not just the `CTE DML` line itself. Root cause matched row 467/468's diagnosis exactly: `cteDMLPrefixOp.Open()` (`internal/executor/operators_cte_dml.go`) calls the package-level `Build(dml)`/`Build(o.plan.Body)` lazily, after `explainOp.Open()`'s `withInstrumentation(...)` scope around the top-level `Build(o.plan.Child)` call has already exited (its `defer` resets the package-global `instrumentScope` before `inner.Open(ctx)` runs) — so those nested `Build()` calls saw `instrumentScope == nil` and `maybeInstrument` skipped wrapping. Implemented resume-point option (a): a new `instrumentScopeCarrier` interface (`internal/executor/instrument.go`, same pattern as the existing `heapFetchCounter` hand-off) lets `maybeInstrument` hand an operator the `*instrumenter` active on its OWN `Build()` call; `cteDMLPrefixOp` implements `setInstrumentScope`/stores it, and a new `buildUnderScope(n planner.Node)` helper temporarily reinstates the package-global `instrumentScope` (save/restore) around each of its two lazy `Build()` call sites so nested nodes register in the same `nodeStatsTable` the EXPLAIN renderer already walks (via the pre-existing `planChildren`'s `*planner.CTEDMLPrefix` case, unchanged). New test `TestExplainCTEDMLPrefixNestedInsertReportsActualRows` (`internal/executor/with_explain_test.go`) asserts the nested `Insert on t` line specifically, alongside the pre-existing `TestExplainCTEDMLPrefixAnalyzeReportsActualRows` (top-level `CTE DML` line, updated to drop its now-stale "deferred" doc comment). Gates: `go build ./...` clean; `go vet ./internal/executor/...` clean; `go test -count=1 ./internal/executor/... ./internal/storage/... ./internal/planner/... ./internal/parser/... ./internal/server/... ./internal/config/...` all PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33). | Nothing newly deferred by this fix — it closes exactly the row-467/468 gap. Still open in the M0122-0003 bucket (unchanged by this loop): `EXPLAIN (BUFFERS)` without `ANALYZE` (PG 17+ planning-time buffers), the `reuses` `pg_stat_io` op counter (needs a `BufferAccessStrategy`-style ring buffer), and the 4 named writeback simplifications-vs-upstream (row 505). | (n/a — resolved same loop) | (n/a — resolved same loop, no scope-bounding needed) |
| resolved | 2026-07-06 | M0122-0003 (`EXPLAIN (BUFFERS)` without `ANALYZE`, closes gap (2) from rows 471/481/497/498) | Bare `EXPLAIN (BUFFERS, FORMAT {JSON,XML,YAML})` (no `ANALYZE`) now renders an always-present `"Planning"` group (`Shared {Hit,Read,Dirtied,Written} Blocks`, all zero) whenever `opts.Buffers` is true, independent of `ANALYZE` — matching upstream's `peek_buffer_usage` (`postgres/src/backend/commands/explain.c`), which shows this group unconditionally for any non-TEXT format once BUFFERS was requested, even when every counter is zero. `internal/executor/operators_explain.go` gains `planningBufferUsageJSON()`, called from both non-TEXT render sites (`explainOp.Open`'s ANALYZE/summary path and plan-only path) whenever `opts.Buffers` is set. TEXT format needed no change: since goopg's planner (`internal/planner`) resolves everything against the in-memory catalog and never touches `storage.Pool` during cost estimation, planning buffers are genuinely always zero, and TEXT's pre-existing positive-only gate (`show_buffer_usage`'s TEXT branch) already correctly renders no "Planning:" block for an all-zero case — no fabrication, no new gating logic needed. Tests: `internal/executor/explain_buffers_test.go`'s `TestExplainBuffersJSONWithoutAnalyzeIncludesPlanningGroup`/`TestExplainBuffersJSONWithoutBuffersOmitsPlanningGroup`/`TestExplainBuffersAnalyzeJSONIncludesPlanningGroup`/`TestExplainBuffersXMLWithoutAnalyzeIncludesPlanningGroup`. Design: `docs/design/0122-0003-explain-format-xml-yaml.md` new "`EXPLAIN (BUFFERS)` without `ANALYZE`" section; `docs/design/README.md` row extended. Gates: `go build ./...` clean; `go test -count=1 ./internal/executor/... ./internal/storage/... ./internal/planner/... ./internal/parser/... ./internal/server/... ./internal/config/...` all PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33). | Nothing newly deferred by this fix — it closes exactly gap (2) named by the rows above. Still open in the M0122-0003 BUFFERS/`pg_stat_io` bucket (unchanged by this loop): local/temp-buffer terms (goopg has no local-buffer-manager concept — every relation goes through the one shared `storage.Pool`, a materially larger architectural addition than a counter tweak) and the `reuses` `pg_stat_io` op counter (needs a `BufferAccessStrategy`-style ring buffer goopg does not implement). | (n/a — resolved same loop) | (n/a — resolved same loop, no scope-bounding needed) |
| resolved | 2026-07-06 | M0122-0003 (local/temp-buffer `* Blocks` terms, closes the resume point named by row 508) | Bare/`ANALYZE` `EXPLAIN (BUFFERS, FORMAT {JSON,XML,YAML})` now renders `Local Hit Blocks`/`Local Read Blocks`/`Local Dirtied Blocks`/`Local Written Blocks`/`Temp Read Blocks`/`Temp Written Blocks` as constant-zero keys, both in the per-node ANALYZE stats block (`internal/executor/operators_explain.go`'s `planToJSONWithStats`) and the planning-time `"Planning"` group (`planningBufferUsageJSON()`) — matching upstream's non-TEXT `show_buffer_usage` branch, which renders these terms unconditionally alongside `Shared *` once BUFFERS was requested, even at zero (`peek_buffer_usage`'s "print even if all zeroes" rule for non-text formats). goopg has no local-buffer-manager or temp-buffer concept — every relation, including temp tables, goes through the one shared `storage.Pool` — so these are genuinely always zero, not a narrower stub than the `Shared *` fields. TEXT format needed no change: `formatBuffersLine` only ever emits the `shared` clause, and upstream's own `has_local`/`has_temp` TEXT gates would also suppress an all-zero clause, so there is no TEXT-visible difference. Tests: `internal/executor/explain_buffers_test.go`'s `TestExplainBuffersJSONAlwaysIncludesLocalTempBlocks` (per-node), `TestExplainBuffersPlanningGroupIncludesLocalTempBlocks` (Planning group). Design: `docs/design/0122-0003-explain-format-xml-yaml.md` new "Local/Temp `* Blocks` terms" section; `docs/design/README.md` row extended. Gates: `go build ./...` clean; `go test -count=1 ./internal/executor/... ./internal/storage/... ./internal/planner/... ./internal/parser/... ./internal/server/... ./internal/config/...` all PASS; `scripts/tpch-spotcheck.sh` PASS. | Local/Temp/Planning-time I/O timing terms (`Local I/O Read/Write Time`, `Temp I/O Read/Write Time`) remain fully open — no collection code path exists for local/temp I/O at all (mirrors the pre-existing Shared-only `bufReadTimeNs`/`bufWriteTimeNs` scope), and `planningBufferUsageJSON()` still emits zero I/O-timing keys at all (Shared included) since it isn't threaded a `trackIOTiming` flag. `reuses` `pg_stat_io` op counter and the 4 named writeback simplifications (row above) also remain open, unaffected by this loop. | `internal/executor/operators_explain.go`: adding Shared/Local/Temp I/O timing to `planningBufferUsageJSON()` needs threading `ctx.Activity.TrackIOTiming(ctx.ProcNum)` into both of `explainOp.Open`'s non-ANALYZE JSON/XML/YAML call sites (only the ANALYZE path currently computes `trackIOTiming` locally) — a small but distinct follow-up from this loop's Blocks-only scope. `reuses`/writeback simplifications need genuinely new counting mechanisms as previously noted. | Bounded to exactly the six `* Blocks` keys the prior loop's resume point named (mirroring the `planningBufferUsageJSON` all-zero-constant precedent) — did not also add Local/Temp/Planning I/O timing, which needs new parameter threading and is a materially separate (if small) follow-up, keeping this a single narrow, low-risk slice. |
| resolved | 2026-07-06 | M0122-0003 (Local/Temp/Planning-time I/O timing terms, closes the row directly above) | `explainOp.Open`'s bare BUFFERS-without-ANALYZE non-TEXT branch now computes its own `trackIOTiming := ctx.Activity != nil && ctx.Activity.TrackIOTiming(ctx.ProcNum)` and passes it (plus the already-computed one from the ANALYZE branch) into `planningBufferUsageJSON(trackIOTiming bool)`, which now emits all six `Shared/Local/Temp I/O Read/Write Time` keys as `float64(0)` constants when the GUC is on (previously took no parameter and emitted none, even for Shared). `planToJSONWithStats`'s existing `trackIOTiming` block gained `Local I/O Read/Write Time`/`Temp I/O Read/Write Time` (constant `float64(0)`) next to the pre-existing real `Shared I/O Read/Write Time` values. Matches upstream's non-TEXT `show_buffer_usage`, which renders all six once `track_io_timing` is on, unconditionally (even at zero) — goopg has no local-buffer-manager/temp-buffer concept and its planner never touches the buffer pool, so these are genuinely always-zero constants, not a narrower stub, exactly like the `* Blocks` terms one loop prior. TEXT format needs no change (local/temp timing is always zero, so upstream's own `has_local_timing`/`has_temp_timing` TEXT gates would also suppress those clauses). Tests: `internal/executor/explain_buffers_test.go`'s `TestPlanToJSONWithStatsIncludesLocalTempIOTimingWhenTrackIOTimingOn`, `TestPlanToJSONWithStatsOmitsLocalTempIOTimingWhenTrackIOTimingOff`, `TestPlanningBufferUsageJSONIncludesIOTimingWhenTrackIOTimingOn`, `TestPlanningBufferUsageJSONOmitsIOTimingWhenTrackIOTimingOff`. Design: `docs/design/0122-0003-explain-format-xml-yaml.md` new "Local/Temp/Planning-time I/O timing terms" section; `docs/design/README.md` row extended. Gates: `go build ./...` clean; `go test -count=1 ./internal/executor/... ./internal/storage/... ./internal/planner/... ./internal/parser/... ./internal/server/... ./internal/config/... ./internal/activity/...` all PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33). | Nothing newly deferred by this fix — it closes exactly the gap named by the row above. Still open in the M0122-0003 BUFFERS/`pg_stat_io` bucket: the `reuses` `pg_stat_io` op counter (needs a `BufferAccessStrategy`-style ring buffer goopg does not implement — feature-sized) and the 4 named writeback simplifications-vs-upstream (2026-07-05 ledger row). | (n/a — resolved same loop) | (n/a — resolved same loop, no scope-bounding needed) |
| resolved | 2026-07-06 | M0122-0003 (writeback simplification (2), closes the row at line 505 above's `backend_flush_after` process-wide-not-per-session gap) | `backend_flush_after` is now genuinely `PGC_USERSET` (per-session), matching upstream: `internal/activity/registry.go`'s `coldActivity` gains `BackendFlushAfterBlocks atomic.Int32` plus `ActivityRegistry.UpdateBackendFlushAfter(procNum, n)`/`BackendFlushAfterOverride()` (the latter resolving the *calling goroutine's own* backend via `LookupCurrentGoroutine`, unconditionally — deliberately no fast-path latch like `TrackIOTimingOn`'s, since `accountBackendWrite`'s only caller, `evictVictim`'s dirty-victim path, is far rarer than the buffer-pin hot path that gate protects). `internal/storage/bufpool.go`'s `Pool` gains a `BackendFlushAfterOverride func() (int32, bool)` hook; `writeback.go`'s `accountBackendWrite` now resolves its threshold from that hook first, falling back to the pre-existing process-wide `backendFlushAfterBlocks.Load()` only when the hook is nil or reports `ok=false` (untracked caller — bgwriter/checkpointer goroutines, or a bare `Pool` in tests). `internal/initdb/open.go` wires `pool.BackendFlushAfterOverride = act.BackendFlushAfterOverride` (a plain method value, safe even were `act` nil — the method's own nil-receiver guard runs first). `internal/server/server.go`'s `New()` gains a `backend_flush_after` `OnChange` hook (mirrors the pre-existing `track_io_timing` one exactly) plus a per-connection seed from the session's boot value, both parsing the GUC's string via `strconv.Atoi`. `accountWrite`'s signature changed from `thresholdBlocks *atomic.Int32` to a plain `threshold int32` since the per-backend case no longer has one `Pool`-owned atomic to point at — each of the three call sites now resolves its own value first. Tests: `internal/activity/registry_test.go`'s `TestActivityRegistryBackendFlushAfterOverride` (per-backend independence + live updates + untracked-goroutine `ok=false`), `internal/server/server_test.go`'s `TestBackendFlushAfterOnChangePropagatesToActivityRegistry` (mirrors the `track_io_timing` OnChange test), `internal/storage/writeback_test.go`'s `TestPoolBackendWritebackOverrideTakesPrecedence` (3 subcases: override enables what the default disables, override disables what the default enables, not-ok override falls back to the default). Design: `docs/design/0122-0003-explain-format-xml-yaml.md` new "Per-session `backend_flush_after`" section; `docs/design/README.md` row extended. Gates: `go build ./...` clean; `go vet`/`go test -race ./internal/storage/... ./internal/activity/...` PASS; `go test ./internal/server/... ./internal/executor/... ./internal/initdb/... ./internal/config/...` PASS (full packages, no regressions); `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33). | Nothing newly deferred by this fix — it closes exactly named simplification (2). Still open in the M0122-0003 writeback bucket (unchanged by this loop): `reuses` (needs a `BufferAccessStrategy`-style ring buffer), simplification (1) (single-relation-per-hint instead of upstream's coalesced multi-range `WritebackContext`), simplification (3) (bgwriter/checkpointer `writeback_time` gated on boot-time `track_io_timing` via a plain `time.Since` pair, not the activity-registry wait-event clock — those two singleton background goroutines have no registered `activity` background slot), and simplification (4) (bgwriter/checkpointer rows' own `writes`/`write_bytes`/`write_time` cells stay an honest 0). | `internal/storage/writeback.go`'s `accountWrite` (simplification 1 — per-relation-segment range coalescing); `internal/activity/registry.go` (simplification 3 — would need `RegisterBackground` slots for bgwriter/checkpointer, mirroring `walProcNum`, then real wait-event brackets instead of the `time.Now()`/`time.Since` pair); `internal/storage/bufpool.go` (`reuses` — no `BufferAccessStrategy` type exists). | Scoped to exactly the one simplification (per-session `backend_flush_after`) that had a clean, self-contained precedent to mirror (`track_io_timing`'s own runtime-`SET` follow-up) — the other 3 simplifications and `reuses` each need a genuinely new mechanism (background activity slots, range coalescing, ring-buffer strategy) out of proportion to a single bounded slice. |
| resolved | 2026-07-06 | M0122-0003 (writeback simplification (3), closes the row above's bgwriter/checkpointer background-slot gap) | bgwriter/checkpointer `writeback_time` now uses the real `ActivityRegistry` wait-event clock instead of a plain `time.Now()`/`time.Since` pair, and their writeback waits are now visible in `pg_stat_activity`, not just `pg_stat_io`. Found and fixed a real bug while closing this: the checkpointer's registration (`cmd/goopg/main.go`) called `act.Register(&activity.Backend{PID: "cp-0", ...})` — the regular, numeric-PID-keyed path; `procNumForPID` maps every non-numeric PID to `r.bgBase`, the exact slot `RegisterBackground(WalWriterIdx, ...)` already claims for the WAL writer at `initdb.Open` time, so the checkpointer's `Register` call was silently clobbering the WAL writer's `activitySlot` (`acquire` unconditionally overwrites `s.cold`) on every server run — `pg_stat_activity` could show at most one of {walwriter, checkpointer}, whichever registered last. (`internal/activity/activity_test.go`'s `TestRegisterCurrentGoroutineIsolatesPerGoroutine` already carried a comment flagging this exact hazard — "Use RegisterBackground for non-numeric PIDs to guarantee distinct slots" — but only worked around it in test code; no production call site had been fixed.) Fix: `internal/activity/registry.go` gains `BgwriterIdx = 3` (a fourth reserved background-slot index; `CheckpointerIdx=1` was reserved but never actually used via `RegisterBackground` before this loop). `internal/initdb/open.go` now pre-registers the checkpointer via `RegisterBackground(CheckpointerIdx, ...)` right before constructing `wal.NewCheckpointer`, and the bgwriter via `RegisterBackground(BgwriterIdx, ...)` when `BgwriterDelay>0 && BgwriterMaxPages>0` (using upstream's `"background writer"` backend_type literal, matching `executor/pgstat_io.go`'s `ioBackendTypeNames`); both seed `act.UpdateTrackIOTiming(procNum, opts.TrackIOTiming)` once at registration (a background worker has no per-session `SET track_io_timing` to react to — boot-time GUC value only, same rationale as before). `wal.CheckpointerConfig` and `storage.Bgwriter` each gain `OnLoopStart`/`OnLoopEnd func()` fields (mirroring `wal.Config`'s pre-existing pair for the WAL writer), called once at the top/bottom of `Run`/`run`; `initdb.Open` wires them to `activity.SetCurrentGoroutine`/`ClearCurrentGoroutine`. `internal/initdb/open.go`'s `OnBgwriterWritebackWait/Done`/`OnCheckpointerWritebackWait/Done` are now textually identical to the pre-existing `OnBackendWritebackWait/Done` (`act.LookupTrackedGoroutine()` → `WaitEventStart`/`WaitEventEnd` → `Add*WritebackTimeNanos`); the local `bgwriterWritebackStart`/`checkpointWritebackStart` `time.Time` vars are gone. `cmd/goopg/main.go`'s checkpointer-goroutine launch no longer registers `"cp-0"` itself (that would have re-collided the pidMap entry). SQL-triggered checkpoints (`CheckpointNow`, run directly on a client backend's own goroutine) are unaffected — they keep attributing to the calling backend's own already-registered identity, since they never go through `Run`. Tests: `internal/initdb/background_activity_test.go` (new) — `TestOpenRegistersDistinctWalWriterAndCheckpointerSlots` (the direct regression test: both `wal-writer-0` and `cp-0` coexist post-`Open` with correct, un-clobbered `BackendType`s), `TestOpenRegistersBgwriterBackgroundSlotWhenEnabled`, `TestCheckpointerWritebackUsesActivityRegistryClock`/`TestBgwriterWritebackUsesActivityRegistryClock` (drive the `Pool` hooks from the registered background identity, confirm a real nonzero elapsed duration accumulates). Design: `docs/design/0122-0003-explain-format-xml-yaml.md` new "Registered bgwriter/checkpointer background slots" section; `docs/design/README.md` row extended. Gates: `go build ./...` clean; `go vet` clean on `internal/activity/... internal/storage/... internal/wal/... internal/initdb/... cmd/goopg/...`; `go test -race ./internal/activity/... ./internal/storage/... ./internal/wal/...` PASS; `go test -count=1 ./internal/initdb/... ./cmd/goopg/... ./internal/executor/... ./internal/server/...` PASS; `scripts/tpch-spotcheck.sh` PASS. | Nothing newly deferred by this fix — it closes exactly named simplification (3). Still open in the M0122-0003 writeback bucket (unchanged by this loop): `reuses` (needs a `BufferAccessStrategy`-style ring buffer) and simplification (4) (background writer/checkpointer rows' own `writes`/`write_bytes`/`write_time` cells stay an honest 0 — `sharedWrittenCount`/`sharedWriteTimeNanos` are deliberately backend-scoped). Simplification (1) (single-relation-per-hint instead of upstream's coalesced multi-range `WritebackContext`) is also unaffected. | `internal/storage/writeback.go`'s `accountWrite` (simplification 1 — per-relation-segment range coalescing); `internal/storage/bufpool.go` (`reuses` — no `BufferAccessStrategy` type exists; simplification 4 — bgwriter/checkpointer own writes need their own counter, distinct from the backend-scoped `sharedWrittenCount`). | Scoped to exactly the one simplification (bgwriter/checkpointer background-slot registration + wait-event clock) the prior loop's resume point named — `reuses`, simplification (1), and simplification (4) each need a genuinely new mechanism (ring-buffer strategy, range coalescing, a second write-counter dimension) out of proportion to a single bounded slice. |
| resolved | 2026-07-06 | M0122-0003 (writeback simplification (4), closes the last named writeback-bucket residual besides the feature-sized `reuses` op counter) | Background writer / checkpointer `writes`/`write_bytes`/`write_time` cells are no longer a hardcoded 0: `internal/storage/bufpool.go`'s `Pool` gains `sharedBgwriterWrittenCount`/`sharedBgwriterWriteTimeNanos` and `sharedCheckpointWrittenCount`/`sharedCheckpointWriteTimeNanos`, incremented in `WriteDirtyPages` (bgwriter) and `flushBatch`/`FlushAllPaced` (checkpointer) respectively — deliberately separate counters from the backend-scoped `sharedWrittenCount` these two paths never touch, since upstream's own per-backend `pgBufferUsage` excludes them too. New `OnBgwriterWriteWait/Done`/`OnCheckpointerWriteWait/Done` hooks bracket each context's real write (mirrors `evictVictim`'s `OnFlushWait/OnFlushDone`; the checkpointer pair brackets `flushBatch`'s whole AIO write-and-wait section once per batch, not once per block, since a batch's writes are outstanding concurrently). `internal/initdb/open.go` wires all four hooks with the same `act.LookupTrackedGoroutine()` → `WaitEventStart(..., WaitDataFileWrite)`/`WaitEventEnd` shape every other `On*Wait/Done` pair in that function already uses. `internal/executor/pgstat_io.go`'s `fetchIOStatRows` renders the new counters into the `(background writer\|checkpointer, relation, normal)` rows' `writes`/`write_bytes`/`write_time` cells. Tests: `internal/storage/writeback_test.go`'s `TestPoolBgwriterAndCheckpointerWrittenCountsAreTracked` (both contexts increment independently; backend-scoped `BufferCounters()` written stays 0 throughout), `internal/executor/pgstat_io_test.go`'s `TestPgStatIOBgwriterCheckpointerWritesRendered`. Design: `docs/design/0122-0003-explain-format-xml-yaml.md` new "Background writer / checkpointer `writes` counters" section + README index row extended. Gates: `go build ./...` clean; `go vet` clean on `internal/storage/... internal/executor/... internal/initdb/...`; `go test -race ./internal/storage/...` PASS; `go test -count=1 ./internal/executor/... ./internal/initdb/... ./internal/wal/... ./cmd/goopg/... ./internal/server/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33). | Nothing newly deferred by this fix — it closes exactly named simplification (4). The M0122-0003 writeback/pg_stat_io bucket now has exactly one open item left: the feature-sized `reuses` op counter (needs a `BufferAccessStrategy`-style ring buffer goopg does not implement). | `internal/storage/bufpool.go` (`reuses` — no `BufferAccessStrategy` type exists yet; would need per-strategy ring-buffer tracking wired through every buffer-access-strategy call site, none of which exist in goopg today) | Out of scope for this loop — feature-sized, needs a new storage-engine mechanism (buffer access strategies) goopg has never modeled, not a bounded slice of existing code. |
| resolved | 2026-07-06 | M0119-0004 (pg_dump slice 436 — `ALTER TYPE ... ADD VALUE [BEFORE\|AFTER]` + `RENAME VALUE` enum-label-order round-trip) | Added slice 436 to `TestPort_PgDumpConnectionSetup` (`internal/testport/pgdump_connsetup_test.go`): the pre-existing `mood` enum fixture now runs `ALTER TYPE public.mood ADD VALUE 'meh' BEFORE 'ok'`, `ALTER TYPE public.mood ADD VALUE 'ecstatic' AFTER 'happy'`, then `ALTER TYPE public.mood RENAME VALUE 'meh' TO 'blah'` before the dump, plus a new exact-order assertion (`CREATE TYPE public.mood AS ENUM (\n    'sad',\n    'blah',\n    'ok',\n    'happy',\n    'ecstatic'\n);`) verified byte-for-byte against real local PG 18.3's own `pg_dump` output for the identical DDL sequence (confirmed empirically via a scratch `initdb`+`pg_ctl`+`psql`+`pg_dump` run against `postgres/local_install/bin`; real PG's `dumpEnumType` never re-emits `ALTER TYPE ... ADD VALUE`/`RENAME VALUE` themselves — it folds `pg_enum ORDER BY enumsortorder`'s FINAL label set straight into one `CREATE TYPE ... AS ENUM` statement). **No engine bug found**: goopg's existing `catalog.AddEnumValueResult`/`RenameEnumValue` (`internal/catalog/catalog.go` ~16330-16530) already computed correct float4-midpoint `SortOrder` values and spliced/renamed `EnumType.Values` in place, and `pg_enum`'s `VirtualRows` (same file, ~6425) already projected `enumsortorder` from that slice — this is a clean regression-guard-only slice (first END-TO-END pg_dump exercise of a path that was previously only parser-unit-tested, e.g. `internal/parser/m0097_0017_test.go`). Design: `docs/design/0110-0001-pg-dump-tap-port.md` new "Slice 436" section. Gates: `go build ./...` clean; `go test -count=1 -run TestPort_PgDumpConnectionSetup ./internal/testport/ -v` PASS; `go test -count=1 ./internal/executor/... ./internal/planner/... ./internal/parser/... ./internal/catalog/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33, run for due diligence though the change is test-file-only with zero production-code edits). | Nothing newly deferred — no engine gap surfaced. The next open pg_dump-side candidates surveyed but not picked this loop (all confirmed already covered by earlier slices, not new gaps): `MATCH PARTIAL` FKs (real PG itself rejects `MATCH PARTIAL` as "not yet implemented", so there is nothing to round-trip), `(LIKE src INCLUDING …)` table clause (PG expands it at parse time into plain columns/constraints, already covered by the many per-feature slices dumping those same column/constraint shapes — no LIKE-specific pg_dump behavior exists to exercise), and `GRANT ... GRANTED BY` (not yet probed against real PG this loop; a legitimate future candidate). | n/a — no fix needed, so no code location to resume at for THIS finding. Next-candidate resume point: probe `GRANT ... WITH GRANT OPTION GRANTED BY <role>` against real PG's `pg_dump` to see whether the grantor is dump-visible/round-tripped at all (goopg's ACL model may not track a grantor per-grant), and `CREATE TEXT SEARCH {PARSER,TEMPLATE,DICTIONARY,CONFIGURATION}` (only the CATALOG VIEWS for these are covered so far — slices 12-15 established the empty-view case for built-ins, but no slice yet exercises a user-defined TS dictionary/configuration CREATE+dump). | Single bounded slice per the M0119-0004 rule; verified against real PG 18.3 output rather than assumed from memory, per project practice. |
| resolved | 2026-07-06 | M0119-0004 (pg_dump slice 437/446 restart-persistence follow-up) | Closes the restart/WAL-persistence deferral both the slice 437 (`CREATE TEXT SEARCH DICTIONARY`) and slice 446 (`CREATE TEXT SEARCH CONFIGURATION`/`ADD MAPPING`) ledger rows recorded in full: a dictionary/configuration/mapping created before a crash or restart now survives it, mirroring the `CREATE CAST`/`CREATE TRANSFORM`/`CREATE CONVERSION`/`CREATE COLLATION` restart-persistence precedent exactly. 5 new WAL record kinds (`internal/wal/recovery.go`): `RecordKindCreateTSDict`(104)/`RecordKindDropTSDict`(105)/`RecordKindCreateTSConfig`(106)/`RecordKindAddTSConfigMapping`(107)/`RecordKindDropTSConfig`(108), each with `Encode`/`Decode` pairs tested in `internal/wal/tsdict_tsconfig_ddl_test.go` (round-trip + wrong-kind + truncated-payload cases, mirroring `conversion_ddl_test.go`); `ApplyRecord`'s dispatcher gained one `(false, nil)` case for all five (catalog-only, no physical page state). New catalog recovery hooks (`internal/catalog/catalog.go`): `CreateTSDictDuringRecovery`/`DropTSDictDuringRecovery`, `CreateTSConfigDuringRecovery`/`AddTSConfigMappingDuringRecovery`/`DropTSConfigDuringRecovery` (idempotent overwrite-by-OID, mirroring `CreateConversionDuringRecovery`). Two new recovery drivers (`internal/initdb/tsdict_ddl_recovery.go`, `internal/initdb/tsconfig_ddl_recovery.go`, mirroring `conversion_ddl_recovery.go`) wired into `internal/initdb/open.go` right after `replayConversionDDLRecords` (schema-scoped, so must run after `replaySchemaDDLRecords`); 6 new end-to-end restart tests in `internal/initdb/tsdict_tsconfig_ddl_recovery_test.go` (create/drop-after-create/missing-wal-dir × {DICTIONARY, CONFIGURATION-with-mapping}) exercise a real `Init`→`Open`→WAL-append→`Close`→`Open` cycle, mirroring `conversion_ddl_recovery_test.go`. `internal/executor/operators_ddl.go`'s CREATE/DROP TS DICTIONARY/CONFIGURATION cases and `execAlterTSConfigAddMapping` now append the matching WAL record after each successful catalog mutation. | Everything else slice 437/446 already recorded as deferred remains open and untouched by this loop: `verify_dictoptions` template-specific option validation (tsearchcmds.c), `ALTER TEXT SEARCH DICTIONARY` (rename/owner/set-schema), 42710 duplicate-mapping detection in `execAlterTSConfigAddMapping`, every other `ALTER TEXT SEARCH CONFIGURATION` form (`ALTER MAPPING REPLACE`/`DROP MAPPING`/`RENAME TO`/`SET SCHEMA`), the `CONFIGURATION = source_config` COPY form, and `CREATE TEXT SEARCH PARSER`/`CREATE TEXT SEARCH TEMPLATE` (both still fully unimplemented, no analog since goopg loads no C functions). | Resume at `execAlterTSConfigAddMapping` (`internal/executor/operators_ddl.go`) for the 42710 duplicate-mapping pre-check (needs a scan of `UserTSConfig.Mappings` for an existing `TokenType` before appending), or at the top-level `ALTER TEXT SEARCH CONFIGURATION` parse dispatch (`internal/parser/ddl.go`, currently falls through to `parseSkipToSemicolon` for every non-ADD-MAPPING form) to add `RENAME TO`/`SET SCHEMA`/`DROP MAPPING` support next. | Scoped strictly to the restart-persistence gap flagged twice (slices 437 and 446); the remaining items are each independently scoped smaller follow-ups already itemized in those rows' own resume points, not re-litigated here. |
| resolved | 2026-07-06 | M0119-0004 (slice 446 follow-up — duplicate-mapping detection, and correcting the SQLSTATE both the slice-446 row and its restart-persistence follow-up row guessed) | Verified against a scratch upstream PostgreSQL 18.3 instance that re-running `ALTER TEXT SEARCH CONFIGURATION ... ADD MAPPING FOR <tok> WITH ...` for an already-mapped token type actually raises `23505 duplicate key value violates unique constraint "pg_ts_config_map_index"` (mapseqno restarts at 1 for every plain ADD MAPPING call in `MakeConfigurationMapping`, tsearchcmds.c, so the second call always collides with the first call's seqno-1 row) — NOT the `42710` both prior ledger rows assumed (42710 is CREATE TEXT SEARCH CONFIGURATION's own duplicate-*name* check, an unrelated code path). `catalog.InMemory.AddTSConfigMapping` (`internal/catalog/catalog.go`) now returns `(*UserTSConfig, duplicate bool)` instead of a bare `bool`, scanning the target configuration's existing `Mappings` for the token type before appending; `internal/executor/operators_ddl.go`'s `execAlterTSConfigAddMapping` raises the 23505 error with PG's exact message text and a `Detail` reproducing the real `(mapcfg, maptokentype, mapseqno)` key tuple. Test: `internal/executor/tsconfig_duplicate_mapping_test.go` (`TestAlterTSConfigAddMappingDuplicateRaises23505`) — duplicate token type errors, distinct token type still succeeds. Design doc: `docs/design/0110-0001-pg-dump-tap-port.md` new "Slice 446 follow-up: duplicate-mapping detection is 23505, not 42710" section; `docs/design/README.md` row extended. | Every other `ALTER TEXT SEARCH CONFIGURATION` form remains open (`ALTER MAPPING REPLACE`, `DROP MAPPING`, `RENAME TO`, `SET SCHEMA` — only bare `ADD MAPPING` is applied); the `CONFIGURATION = source_config` COPY-from-existing-configuration form of CREATE also remains open. | Extend the `AlterTSConfigAddMappingStmt` parse path (`internal/parser/ddl.go`) and its executor dispatch the same way `ALTER CONVERSION`/`ALTER SERVER` already handle their own multi-form ALTER dispatch, adding the remaining grammar forms one at a time. | Scoped to the single bounded item named as the immediate next candidate in the prior loop's `working_set.md`/ledger rows; the ALTER-form grammar expansion is a separate, larger follow-up. |
| resolved | 2026-07-06 | M0119-0004 (slice 446 follow-up — `ALTER TEXT SEARCH CONFIGURATION` ALTER MAPPING FOR tok WITH dict override form) | `ALTER TEXT SEARCH CONFIGURATION name ALTER MAPPING FOR tok [, ...] WITH dict [, ...]` (`ALTER_TSCONFIG_ALTER_MAPPING_FOR_TOKEN`, no `REPLACE` keyword) is now parsed and applied (previously fell through to the discarded compat no-op alongside `OWNER TO`), mirroring `MakeConfigurationMapping`'s `stmt->override` path in `tsearchcmds.c`: every named token type's entire dictionary list is wholesale replaced (delete-then-insert in real PG; a plain overwrite-or-append in goopg's model, since `TSConfigMapping` already holds one `DictOIDs` list per token type rather than one row per dictionary), never raising the 23505 that ADD MAPPING raises on an already-mapped token type. `internal/parser/ast.go`'s `AlterTSConfigStmt` gained an `"altermapping"` action (reusing the existing `Dictionaries` field); `internal/parser/ddl.go`'s `ALTER MAPPING` dispatch branch now checks `len(tokenTypes) > 0 && p.acceptKeyword(KwWith)` after the `REPLACE` check fails (gram.y requires `FOR` for this form, unlike REPLACE's optional `FOR`). `internal/catalog/catalog.go` gained `AlterTSConfigMapping`/`AlterTSConfigMappingDuringRecovery`. `internal/executor/operators_ddl.go` gained `execAlterTSConfigAlterMapping`, reusing the `resolveTSDictOID` helper. Restart persistence landed in the same loop: new WAL record kind 113 (`RecordKindAlterTSConfigMapping`, same wire shape as `RecordKindAddTSConfigMapping`) with `Encode`/`DecodeAlterTSConfigMapping`, wired into `internal/initdb/tsconfig_ddl_recovery.go`'s replay switch. Tests: `internal/executor/tsconfig_altermapping_test.go` (`TestAlterTSConfigAlterMapping`), `internal/wal/tsdict_tsconfig_ddl_test.go` (+3 funcs), `internal/initdb/tsdict_tsconfig_ddl_recovery_test.go` (`TestTSConfigDDLRecoveryReplaysAlterMapping`). Design doc: `docs/design/0110-0001-pg-dump-tap-port.md` new "Slice 446 follow-up: `ALTER MAPPING FOR tok WITH dict` override form" section. This closes every `ALTER TEXT SEARCH CONFIGURATION` sub-form named in `gram.y`'s `AlterTSConfigurationStmt` production. | `OWNER TO` stays a no-op (not expected to need a dedicated path — pg_dump reads `cfgowner` from the catalog row set at CREATE time, not from a replayed ALTER OWNER TO); the `CONFIGURATION = source_config` copy-from-existing-configuration form of `CREATE TEXT SEARCH CONFIGURATION` remains unimplemented (only the bare `PARSER = ...` form is accepted). | `CONFIGURATION = source_config` COPY form: `internal/parser/ddl.go`'s `text search configuration` CREATE-tail parse needs a second accepted key, and `internal/executor/operators_ddl.go`'s CREATE case needs to look up the source configuration by name and deep-copy its `Parser`/`Mappings` into the new one. `OWNER TO`: if ever picked up, would need a dedicated parse branch plus a catalog `Owner` field mutation, mirroring `ALTER TABLE ... OWNER TO`'s unknown-role rejection (loop #107 precedent). | Scoped to the single item explicitly named as the next candidate by the prior loop's `working_set.md` and this row's own predecessor ledger row (which also confirmed no other sub-form of `AlterTSConfigurationStmt` remains unimplemented); COPY-form CREATE and `OWNER TO` are each independently sized follow-ups, not re-litigated here. |
| resolved | 2026-07-06 | M0119-0004 (slice 446 follow-up — `CREATE TEXT SEARCH CONFIGURATION` COPY = source_config form) | `CREATE TEXT SEARCH CONFIGURATION name (COPY = source_config)` is now parsed and applied, mirroring `DefineTSConfiguration`'s COPY branch in `tsearchcmds.c`: PARSER and COPY are mutually exclusive (42601 `cannot specify both PARSER and COPY options`, PG's own `ERRCODE_SYNTAX_ERROR` text verbatim); an unresolvable `source_config` raises 42704; a resolved source's `cfgparser` is copied to the new row and its entire `pg_ts_config_map` mapping list is copied into the new configuration by value (not aliased — verified a post-CREATE `ALTER MAPPING` on the copy does not perturb the source). `internal/parser/ast.go`'s `CompatNoopStmt` gained a `TSConfigCopySource ObjectName` field alongside `TSConfigParser`; `internal/parser/ddl.go`'s CREATE TEXT SEARCH CONFIGURATION option loop gained a `copy` key case (`parseTSObjectNameLoose`, scoped to `tsType == "text search configuration"`) alongside the existing `parser`/`template` cases. `internal/executor/operators_ddl.go`'s `"text search configuration"` case now branches on whether `TSConfigCopySource` is set: if so it resolves the source via `im.ListUserTSConfigs()` (case-insensitive name match, mirroring `resolveTSDictOID`'s schema-oblivious lookup pattern), takes its `Parser`, and after `CreateTSConfig` replays each copied mapping through the existing `im.AddTSConfigMapping` + `wal.EncodeAddTSConfigMapping` path `execAlterTSConfigAddMapping` already uses — no new WAL record kind needed, since a copied mapping list is wire-identical to a sequence of ADD MAPPING calls. Tests: `internal/executor/tsconfig_copy_test.go` (`TestCreateTSConfigCopy` — parser/mapping copy correctness, distinct OID, non-aliased mutation, the 42601 both-options case, and 42704 on an unknown source). Restart persistence for the copy falls out for free from the reused ADD MAPPING WAL path (already restart-durable since the slice 437/446 follow-up landed). This closes the last named-and-deferred sub-form of `CREATE TEXT SEARCH CONFIGURATION`. | `OWNER TO` on `ALTER TEXT SEARCH CONFIGURATION` remains a no-op, unchanged from prior rows (pg_dump reads `cfgowner` from the catalog row set at CREATE time, never from a replayed ALTER OWNER TO — considered fine, not a gap). `CREATE TEXT SEARCH PARSER`/`CREATE TEXT SEARCH TEMPLATE` remain fully unimplemented (C-function-loading features with no analog in goopg). | None — this row closes the CREATE-form deferral named by every predecessor row in this bucket. If `OWNER TO` is ever picked up, it needs a dedicated parse branch plus a catalog `Owner` field mutation (loop #107 `ALTER TABLE ... OWNER TO` unknown-role-rejection precedent). | N/A (resolved, not deferred) — recorded per the ledger's own resolved-row convention (M0119 promotes `-` rows to `resolved` once verified closed). |
| resolved | 2026-07-06 | M0122-0002 (FROM-clause SRF follow-up — comma-join/LATERAL correlation gap) | `FROM tbl, unnest(tbl.arr_col) AS t(m)` / `FROM tbl, LATERAL unnest(tbl.arr_col) AS t(m)` / `FROM tbl, regexp_matches(tbl.col, ...) AS t(m)` no longer raise `XX000: column ref arr/1 on nil slot`. Root cause was one layer above where the earlier ledger row (2026-07-04) guessed: `joinOp.openLateral` (`internal/executor/operators_join_agg.go`) already pushes the left row onto `ctx.OuterRows` per outer iteration, and `fromUnnestOp.Open`/`fromRegexpMatchesOp.Open` already read `ctx.OuterRows[len-1]` as their outer row when evaluating arg expressions — that plumbing was always correct. The actual bug was `internal/planner/planner.go`'s `nodeReferencesOuter`, which decides whether the wrapping `Join` routes through `openLateral` at all: its switch only special-cased `*PgGetPublicationTables`/`*VerifyHeapam`/`*PgGetSequenceData`, falling through to `planHasOuterRef` (which only detects `*OuterColumnRef`, the separate nested-subquery correlation mechanism) for everything else — including `*FromUnnest`/`*FromRegexpMatches`, whose lateral-sibling args resolve to plain `*ColumnRef` instead. The join silently took the non-lateral fast path, `ctx.OuterRows` stayed empty for the whole scan, and the SRF's arg evaluated against a nil row. Fix: added `*FromUnnest`/`*FromRegexpMatches` cases to `nodeReferencesOuter` (reusing the existing `exprContainsColumnRef` helper, mirroring the `PgGetSequenceData` case) plus an `*OrdinalityWrap` unwrap-and-recurse case so `WITH ORDINALITY` doesn't defeat detection. No executor change needed. Tests: `internal/executor/from_srf_lateral_correlation_test.go` (`TestFromSRFLateralCorrelation_UnnestCommaJoin`/`_RegexpMatchesCommaJoin`/`_UnnestWithOrdinality`) — each reproduces the exact `XX000` error against pre-fix code (verified by stashing the `planner.go` hunk and re-running) and passes after. Design doc: `docs/design/0122-0002-pg-relation-size-real-sizes.md` new follow-up section; `docs/design/README.md` row extended. Gates: `go build ./...` clean; `go test ./internal/executor/... ./internal/planner/... ./internal/analyzer/... ./internal/parser/...` PASS (no regressions); `scripts/tpch-spotcheck.sh` PASS (Q12=2 rows, Q13=33 rows). | Item 1 from the same 2026-07-04 row (`WITH ORDINALITY AS t(m, n)` explicit dual-column naming) is unaffected by this fix — it was already independently resolved by a later M0122-0004 loop (`TestAnalyzeWithOrdinalityNamedColumn`), unrelated root cause (analyzer's `tableFuncColumns`, not the planner's lateral-routing switch). No other FROM-clause SRF-correlation gap is known open. | N/A (resolved, not deferred). | N/A — this row closes item 2 of the 2026-07-04 M0122-0002 FROM-clause follow-up row; item 1 was already closed separately (see M0122-0004 ledger rows). |
| resolved | 2026-07-06 | M0122-0005 (`pg_collation_for` array-type support, closes gap (3) of the 2026-07-05 row above) | `pg_collation_for('{a,b}'::text[])` no longer raises `42804` — it now returns `"default"`, matching real PostgreSQL 18.3 (`ARRAY['a','b']::name[]` → `"C"`; `'{1,2}'::int4[]` still correctly errors `42804` since `int4` isn't collatable). `internal/planner/planner.go`'s `foldPgCollationFor` now strips a trailing `"[]"` suffix from `exprType(arg).Name` before the base-type collatable switch — cast-expression array types carry that suffix literally in `Type.Name` (unlike real table-column arrays, which already worked via the separate `Type.IsArray` + unsuffixed-`Name` representation). Verified against a scratch PostgreSQL 18.3 instance. Tests: `internal/planner/pg_collation_for_test.go`'s `TestPgCollationForFolds` gained 3 array-type cases. Design: `docs/design/0122-0005-pg-collation-for-array-types.md`; `docs/design/README.md` new row. | Gaps (1) and (2) from the row above remain fully open and untouched: no real per-expression collation-derivation pass (`parse_collate.c`'s `assign_expr_collations` — a declared column `COLLATE` not restated inline still reports `"default"` rather than its true collation), and `resolveExprAfterAggregate` still has no matching fold (falls through to the executor's coarser Datum-kind runtime approximation). | `internal/planner/planner.go`'s `foldPgCollationFor` (~line 9244) for gap (1)/(2), same resume point named by the row above — unaffected by this loop's array-suffix fix, which only touched the base-type switch. | Scoped strictly to the single array-type sub-gap named as this bucket's "Still open" item in `fix_plan.md`'s M0122-0005 entry; the two larger architectural gaps are independently sized follow-ups already itemized by the predecessor row. |
| resolved | 2026-07-06 | M0122-0004 (interval ordering comparisons) | `compareDatum` (`internal/executor/expr.go`) gained a `KindInterval` case — `<`/`>`/`<=`/`>=`/`ORDER BY`/`MIN`/`MAX` over interval values previously raised `42883` ("comparison not supported for kind ..."), even though interval equality already worked via `datumKey` (`internal/executor/operators_join_agg.go`, GROUP BY/DISTINCT hashing). Fix mirrors upstream's `interval_cmp_value` (`postgres/src/backend/utils/adt/timestamp.c`): months widened to days at a fixed 30-day rate, added to the day field, linearized to a single `int64` and compared; upstream's further widening to microseconds via the interval's `time` field is a no-op since v0's interval Datum has no sub-day component. Verified byte-for-byte against a real PostgreSQL 18.3 instance (tie case `interval '3' month = interval '90' day`, negative months, `ORDER BY`/`MAX`/`MIN` over a `UNION ALL` of interval literals). Tests: `internal/executor/interval_compare_test.go` (`TestIntervalOrderingOperators`, `TestIntervalOrderByAndMinMax`). Design: `docs/design/0003-0006-date-interval-arithmetic.md` new Follow-up section; `docs/design/README.md` row extended. Gates: `go build ./...` clean; `go test -count=1 ./internal/executor/... ./internal/planner/... ./internal/analyzer/... ./internal/parser/...` PASS (no regressions); `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33). | Three independent interval gaps remain, confirmed via direct code audit (not just `unimplemented_feat.json`, which only tracks the first): (1) sub-day interval units (hours/minutes/seconds) — structurally impossible today since `Datum`'s `KindInterval` carrier packs only months/days into `Int` (the `Hi uint64` field is reserved "for future ... interval micros" per its doc comment but unused); (2) no generic string→interval CAST — `evalCast` (`internal/executor/expr.go`) has no `case "interval"`, so `CAST('1 day' AS interval)`/`'1 day'::interval` silently pass the string through unchanged instead of producing a `KindInterval` (the code comment at expr.go:1906, "KindInterval has no text parse path yet", already flags this); (3) `Datum.Format()`'s `KindInterval` case renders `"%d months %d days"` (e.g. `"3 months 0 days"`) instead of PostgreSQL's `intervalout` format (`"3 mons"`, `"1 year 2 mons"`, singular "mon"/"day"/"year", zero components omitted) — verified against a real PostgreSQL 18.3 instance's exact output for 9 interval shapes. | (1): widen `NewIntervalDatum` to take a microseconds/time-of-day third component stored in the existing reserved `Hi` field, add sub-day unit parsing to `IntervalLit`/`parser/select.go:2999-3030`, and extend `interval_cmp_value`-style comparison (this row) plus arithmetic (`addTimeInterval`, `internal/executor/expr.go`) to consume it — a central-Datum-type change, likely its own bounded loop per `m0074_partial_scope_lessons`. (2): add a `case "interval":` to `evalCast` (`internal/executor/expr.go` ~2721) reusing the day/month/year parsing `evalIntervalLit` already has, explicitly deferring sub-day/ISO-8601 forms until (1) lands. (3): rewrite `Datum.Format()`'s `KindInterval` branch (`internal/executor/datum.go:539-540`) to reproduce `EncodeInterval`'s `INTSTYLE_POSTGRES` output (`postgres/src/backend/utils/adt/timestamp.c`) — years/months split, singular/plural, zero-component omission, `IntervalStyle` GUC awareness for the verbose/ISO-8601/SQL-standard variants goopg's `timezone_abbreviations` follow-up (`0097-0032b`) already threads through `intervalstyle`. | Bounded to the single missing `compareDatum` case (ONE task per loop); the three residuals are either a materially larger central-type change (1, blocks 2's full generality) or independently-sized slices (2, 3) with their own resume points, not natural extensions of a comparison-operator fix. |
| resolved | 2026-07-06 | M0119-0004 (per-column `ANALYZE` `SET STATISTICS` runtime enforcement) | `ALTER TABLE ... ALTER COLUMN ... SET STATISTICS n` (`catalog.Column.StatTarget`) previously affected only `pg_dump` fidelity (`attstattarget` round-tripped through `pg_attribute`); `ANALYZE` sampled every column at the table-wide `default_statistics_target` regardless of any per-column override. New `columnStatsTarget` (`internal/executor/operators_analyze.go`) mirrors upstream's `examine_attribute`/`do_analyze_rel` (`postgres/src/backend/commands/analyze.c`): an unset override falls back to the table-wide target, a positive override caps that column's MCV list / histogram bucket count independently of siblings, and `SET STATISTICS 0` excludes the column from `ANALYZE` entirely (no `pg_statistic` row written), matching `attstattarget == 0`'s upstream semantics exactly. Sample size (`targrows`) intentionally stays table-wide, matching upstream's single per-table `acquire_sample_rows` scan. This closes the exact gap `0006-0001-sampling-and-mcv-histograms.md`'s own "Out of scope" section named ("Per-column statistics targets ... full plumbing deferred") and the `unimplemented_feat.json` M0119-0004 entry ("Runtime enforcement of ALTER STATISTICS SET STATISTICS target during DDL operations"). Tests: `TestAnalyzeRespectsPerColumnStatTarget`, `TestAnalyzeSetStatisticsZeroDisablesColumn` (`internal/executor/operators_analyze_test.go`). Design: `docs/design/0006-0005-per-column-stats-target-enforcement.md`; `docs/design/README.md` new row. Gates: `go build ./...` clean; `go test ./internal/executor/...` PASS (full package, no regressions); `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33). | Extended-statistics objects (`CREATE STATISTICS`/`ALTER STATISTICS ... SET STATISTICS`, `pg_statistic_ext.stxstattarget`) remain dump-fidelity only, unchanged by this loop — goopg still has no multi-column NDistinct/dependencies/MCV *computation* at all, so there is no sampling step for that target to influence (a materially larger, separately-tracked feature, not a narrowing of this fix's scope). | If extended-statistics computation is ever built, wire its sampling loop through `StatisticsObject.StatTarget` the same way this loop wired `Column.StatTarget` through `ANALYZE`'s per-column loop — no other resume point needed for the column-level mechanism, which is now fully closed. | The extended-statistics-object target was already a known, separately-scoped, and explicitly-accepted dump-fidelity-only decision (`0119-0004-alter-statistics-set-statistics.md`) predating this loop, not a new deferral this fix introduces — recorded here only for completeness per the ledger's cross-reference convention. |
| resolved | 2026-07-06 | M0119-0004 (`TYPE`/`DATABASE`/`PARAMETER` ACL grantor wiring — follow-up to the 2026-07-06 table-ACL/attacl grantor rows) | `typacl`/`datacl`/`pg_parameter_acl.paracl` no longer hardcode every aclitem's grantor to the object owner under `SET ROLE`/`SET SESSION AUTHORIZATION` impersonation. Unlike `attacl`, these three object kinds were never a structurally separate store — they already share `tableACLs`/`tableACLGrantor` with `relacl` via the common `relaclTextLockedFor` renderer (type/database/parameter ACL OIDs are minted from the same `nextOID` counter as relations), so the grantor-aware `GrantTablePrivilegeAs` primitive was already fully generic. The gap was purely that the three executor call sites — `execTypeACLChange` (`internal/executor/operators_ddl.go`), `execDatabaseACLChange` (`internal/executor/operators_ddl_database_acl.go`), `execParameterACLChange` (`internal/executor/operators_ddl_parameter_acl.go`) — still called the grantor-blind `GrantTablePrivilegeWithGrantOption` wrapper on their GRANT branch. Each now calls `GrantTablePrivilegeAs(oid, role, priv, withGrantOption, o.ctx.NonSuperuserRole)` instead, stamping the session's current effective role as grantor exactly as `tryRecordTableGrant`/`execAttrACLChange` already do. No new grantor map, catalog method, or heap-render change was needed. See `docs/design/0119-0004-table-acl-grantor-tracking.md`'s new "Follow-up: `TYPE`/`DATABASE`/`PARAMETER` ACL grantor wiring" section and `docs/design/README.md`'s updated `0119-0004db` row. Tests: `internal/executor/operators_ddl_acl_grantor_test.go` (`TestExecTypeACLChangeStampsActingRoleAsGrantor`, `TestExecDatabaseACLChangeStampsActingRoleAsGrantor`, `TestExecParameterACLChangeStampsActingRoleAsGrantor`), each mirroring `TestRelaclTextGrantor`'s shape but driving the change through the executor entry point. Gates: `go build ./...` clean; `go test ./internal/catalog/... ./internal/executor/... ./internal/parser/... ./internal/server/...` PASS (no regressions); `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33). | The object-privilege `GRANTED BY <role>` validation (`errGrantorMustBeCurrentUser`) guarding `relacl`/`nspacl`/`proacl`/`srvacl`/`fdwacl` grants is not mirrored here: the parser's shared clause-stripper (`internal/parser/parser.go:283`) discards `GRANTED BY <role>` for `TypeACLChange`/`DatabaseACLChange`/`ParameterACLChange` without capturing it into the AST at all (unlike `RoleMembershipChange`, which does get a `GrantedBy` field) — there is nothing yet for the executor to validate against. No grant-option delegation-chain resolution (`select_best_grantor`) for these three object kinds either, same accepted simplification as the original table-ACL fix. | Resume point for `GRANTED BY` validation: add a `GrantedBy` field to `parser.TypeACLChange`/`DatabaseACLChange`/`ParameterACLChange` (parser.go's grant-statement productions around line 589-650 already do this for role-membership grants — same shape), then check it against `o.ctx.NonSuperuserRole` in each of the three exec functions the same way `tryRecordTableGrant` does via `errGrantorMustBeCurrentUser`. | Scoped to the three mechanical call-site fixes named as the exact resume point in the prior table-ACL-grantor ledger row; `GRANTED BY` validation for these three statement kinds needs new parser AST plumbing first (a distinct, slightly larger follow-up), not a narrowing of this fix's scope. |
| resolved | 2026-07-06 | M0119-0004 (`TYPE`/`DATABASE`/`PARAMETER` `GRANTED BY` validation — follow-up to the 2026-07-06 TYPE/DATABASE/PARAMETER grantor-wiring row) | `GRANT/REVOKE ... ON TYPE|DOMAIN|DATABASE|PARAMETER ... GRANTED BY <role>` naming any role other than the one executing the statement is now rejected `0A000` ("grantor must be current user"), matching real PostgreSQL 18.3's shared `InternalGrant` check (`postgres/src/backend/catalog/aclchk.c:394-412`, verified via the PG-source MCP tools) — the same restriction `relacl`/`nspacl`/`proacl`/`srvacl` already enforce via `grant_ddl.go`'s `errGrantorMustBeCurrentUser`. `parser.TypeACLChange`/`DatabaseACLChange`/`ParameterACLChange` (`internal/parser/ast.go`) each gained a `GrantedBy string` field; the three token-scanning builders in `internal/parser/parser.go` (`buildTypeACLChange`/`buildDatabaseACLChange`/`buildParameterACLChange`), which previously shared one identical inline loop that only located a trailing `GRANTED BY`/`CASCADE`/`RESTRICT` token without ever capturing the role name, now share a new `scanGrantTrailingClause` helper that does. Fixed a latent bug found while generalizing: the original loop's `WITH GRANT OPTION` case unconditionally `break`s, so a `GRANTED BY` clause following it (gram.y's only allowed order, `opt_grant_grant_option opt_granted_by`) was silently unreachable — the new helper `continue`s past a consumed `WITH GRANT OPTION` so a following `GRANTED BY` is still found, mirroring `buildRoleMembershipChange`'s existing post-`WITH` continuation. `internal/executor/operators_ddl.go` gained `checkGrantedByCurrentUser(actingRole, grantedBy string) error` (a duplicate of `grant_ddl.go`'s check, since `executor` cannot import `server`), called first by `execTypeACLChange`, `execDatabaseACLChange` (`operators_ddl_database_acl.go`), and `execParameterACLChange` (`operators_ddl_parameter_acl.go`) — a mismatch aborts before any ACL-store mutation, leaving no partial grant. Tests: `internal/parser/op_grant_typeacl_test.go`/`op_grant_databaseacl_test.go`/`op_grant_parameteracl_test.go` each gained a `GRANTED BY` case plus a combined `WITH GRANT OPTION ... GRANTED BY` case (regression-pins the `WITH`-continuation fix); `internal/executor/operators_ddl_acl_grantor_test.go`'s `TestExecACLChangeGrantedByCurrentUserIsNoop`/`TestExecACLChangeGrantedByOtherRoleErrors`. Design: `docs/design/0119-0004-table-acl-grantor-tracking.md` new "Follow-up: `TYPE`/`DATABASE`/`PARAMETER` GRANTED BY validation" section. Gates: `go build ./...` clean; `go test ./internal/catalog/... ./internal/executor/... ./internal/parser/... ./internal/server/...` PASS (no regressions); `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33). | Column-level `attacl`'s `GRANTED BY` clause is still parsed only far enough to find the end of the role list (`buildAttrACLChange`, `internal/parser/parser.go`) — the named role itself is scanned past but discarded, unvalidated, unlike the three object-privilege paths this row closes. No grant-option delegation-chain resolution (`select_best_grantor`, `acl.c`) for any of the four object kinds — the recorded/validated grantor is simply whichever role executed the statement, not a resolved delegation-chain member (same accepted simplification as every predecessor row in this bucket). | Resume point for `attacl` GRANTED BY: add a `GrantedBy` field to `parser.AttrACLChange`/`buildAttrACLChange` (`internal/parser/parser.go`), then check it against `o.ctx.NonSuperuserRole` in `execAttrACLChange` (`internal/executor/operators_ddl.go`) the same way this row's three exec functions now do via `checkGrantedByCurrentUser`. | Scoped to the three mechanical call-site + parser-capture fixes named as the exact resume point in the prior TYPE/DATABASE/PARAMETER grantor-wiring ledger row; column-level `attacl`'s GRANTED BY is a separate, similarly-shaped follow-up needing its own small AST field addition, not a narrowing of this fix's scope. |
| resolved | 2026-07-06 | M0122-0005 (range-type `ALTER TYPE ... OWNER TO`/`RENAME TO` — follow-up to the 2026-07-05 `ALTER TYPE OWNER TO / composite RENAME TO` row's deferred item 2) | `catalog.RangeType` gained the same `Owner uint32`/`OwnerOrDefault()` pair enum/composite already had (defaulting to bootstrap superuser OID 10); new `SetRangeTypeOwner`/`RenameRangeType` catalog methods mirror `SetCompositeTypeOwner`/`RenameCompositeType` exactly. `execCreateType`'s `IsRange` branch now stamps `rt.Owner = o.currentDDLOwnerOID()` at creation (same "creator becomes owner" convention). `execAlterType`'s RENAME TO and OWNER TO branches each gained a `cat.LookupRangeType(s.Name)` check between the existing composite check and the enum fallback — previously `ALTER TYPE <range> RENAME TO`/`OWNER TO` silently mis-dispatched into `RenameEnum`/`SetEnumOwner`, raising a spurious 42710/42704 "type does not exist" for a range type that does exist (the identical dispatch-by-kind bug class the 2026-07-05 row's composite fix closed). The 4 `bootstrapSuperuserOID` typowner literals in `buildUserPGTypeRowForRange`/`ForMultirange`/`ForRangeArray`/`ForMultirangeArray` (`internal/executor/pg18_user_catalog_rows.go`) now read `rt.OwnerOrDefault()`. Tests: `internal/executor/alter_type_owner_test.go`'s `TestAlterTypeOwnerToRange`/`TestAlterTypeRenameToRange`. See `docs/design/0122-0005-alter-type-owner-rename.md`'s new "Follow-up: range-type `OWNER TO` / `RENAME TO`" section and `docs/design/README.md`'s updated row. Gates: `go build ./...` clean; `go test ./internal/catalog/... ./internal/executor/... ./internal/parser/...` PASS (no regressions); `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33). | (1) `Owner` has no restart persistence: range types already have a WAL record (`wal.EncodeCreateRangeType`/`DecodeCreateRangeType`) for the type definition itself, unlike enum/composite, but the new `Owner` field is not threaded into it — a non-default range-type owner reverts to the bootstrap-superuser default across a restart even though the type survives. (2) Domain typowner remains completely untouched: unlike range types, there is no working `ALTER TYPE`-style dispatch to extend — `ALTER DOMAIN` is a wholly distinct, unparsed statement today (falls through `internal/parser/ddl.go`'s generic "collation/domain/extension/language/operator/system" compat-stub loop, consuming tokens with no AST at all), so this is a materially larger, separate task (parsing `ALTER DOMAIN`'s full grammar: RENAME TO, SET/DROP DEFAULT, SET/DROP NOT NULL, ADD/DROP CONSTRAINT, RENAME CONSTRAINT, OWNER TO, SET SCHEMA), not a bounded mechanical follow-up like this one. | (1): thread a 9th `owner uint32` field through `wal.EncodeCreateRangeType`/`DecodeCreateRangeType` (`internal/wal/recovery.go`) and `RegisterRangeTypeDuringRecovery` (`internal/catalog/catalog.go`), plus emit a new WAL record (or extend the existing one) from `execAlterType`'s new range OWNER TO branch so a post-CREATE `ALTER TYPE ... OWNER TO` survives a restart too — mirrors the pattern used for other in-place catalog field updates. (2): `internal/parser/ddl.go`'s `parseAlter` (~line 7863) would need a real `ALTER DOMAIN` grammar branch (see `postgres/src/backend/parser/gram.y`'s `AlterDomainStmt` production) before any OWNER TO / RENAME TO handling could exist for `catalog.Domain` at all. | Bounded to the one ALTER-TYPE-addressable kind (range) that already had a working, just mis-dispatching, RENAME TO/OWNER TO code path — matches the "ONE task per loop" rule and the design doc's own explicitly-scoped deferral list; the domain half needs new parser grammar first and is independently sized. |
| resolved | 2026-07-06 | M0122-0005 (range-type `Owner` restart persistence — closes resume point (1) from the prior 2026-07-06 row) | `wal.EncodeCreateRangeType`/`DecodeCreateRangeType` gained a 10th `ownerOID uint32` field (fixed-width, right after `collationOID`); `execCreateType`'s range branch now passes `rt.Owner`, and `internal/initdb/range_type_ddl_recovery.go`'s CREATE case threads the decoded value into the replayed `catalog.RangeType`. Two new WAL record kinds, `RecordKindAlterRangeTypeRename`/`RecordKindAlterRangeTypeOwner` (117/118), mirror `RecordKindAlterCollationRename`/`RecordKindAlterCollationOwner` (minus the schema field — range types are keyed by name only); `execAlterType`'s range RENAME TO/OWNER TO branches now call `o.ctx.WAL.Append` after the catalog mutation, and two new idempotent catalog hooks `RenameRangeTypeDuringRecovery`/`SetRangeTypeOwnerDuringRecovery` are wired into `replayRangeTypeDDLRecords`. Both new kinds were also added to the physical-replay skip-list in `internal/wal/recovery.go` alongside `RecordKindCreateRangeType`/`RecordKindDropRangeType` (no page-level effect). Tests: `internal/wal/range_type_ddl_test.go`'s `TestEncodeDecodeAlterRangeTypeRenameRoundTrip`/`OwnerRoundTrip` (+ the CREATE round-trip test now asserts `ownerOID`); `internal/initdb/range_type_ddl_recovery_test.go`'s `TestRangeTypeDDLRecoveryReplaysRenameAndOwner`. See `docs/design/0122-0005-alter-type-owner-rename.md`'s new "Follow-up: range-type `Owner` restart persistence" section and `docs/design/README.md`'s updated row. Gates: `go build ./...` clean; `go test ./internal/catalog/... ./internal/executor/... ./internal/wal/... ./internal/initdb/...` PASS (no regressions; one `TestConcurrentAppendAcrossSegmentBoundariesNoOverflow` flake reproduced identically on a clean pre-change tree under full-suite load, confirmed unrelated); `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33). | Domain typowner remains completely untouched (item (2) from the prior row, carried forward unchanged): unlike range types, there is no working `ALTER TYPE`-style dispatch to extend — `ALTER DOMAIN` is a wholly distinct, unparsed statement today (falls through `internal/parser/ddl.go`'s generic "collation/domain/extension/language/operator/system" compat-stub loop, consuming tokens with no AST at all). | `internal/parser/ddl.go`'s `parseAlter` (~line 7863) would need a real `ALTER DOMAIN` grammar branch (see `postgres/src/backend/parser/gram.y`'s `AlterDomainStmt` production: RENAME TO, SET/DROP DEFAULT, SET/DROP NOT NULL, ADD/DROP CONSTRAINT, RENAME CONSTRAINT, OWNER TO, SET SCHEMA) before any OWNER TO/RENAME TO handling could exist for `catalog.Domain` at all. | Materially larger, separate task from the WAL-persistence mechanical follow-up this row closes — needs new parser grammar first, independently sized, and was already flagged as such (not a narrowing of scope) in the row this one resolves. |
| resolved | 2026-07-06 | M0122-0005 (`ALTER DOMAIN ... SET DEFAULT`/`DROP DEFAULT` — closes the `SET`/`DROP DEFAULT` item from the prior 2026-07-06 row's "Resume point (sub-forms)" list) | `ALTER DOMAIN name SET DEFAULT expr` / `ALTER DOMAIN name DROP DEFAULT` now both work, reusing the exact `Domain.Default parser.Expr`/`DefaultBin()` field and render path `CREATE DOMAIN ... DEFAULT` already populates — no new catalog field, no cross-table scan. `AlterDomainStmt` gained a `DefaultExpr Expr` field; the domain branch's `SET`/`DROP` handling in `parseAlter` (`internal/parser/ddl.go`) gained `DEFAULT`-checking arms, which required restructuring the pre-existing `DROP CONSTRAINT` match from `p.acceptKeyword(KwDrop) && p.acceptKeyword(KwConstraint)` (a flat `&&`) into a nested `if p.acceptKeyword(KwDrop) { if ...CONSTRAINT...; if ...DEFAULT... }` — Go still evaluates and consumes the left `KwDrop` operand even when the right fails to match, so a second flat `p.acceptKeyword(KwDrop) && p.acceptKeyword(KwDefault)` arm added the naive way would have been unreachable (DROP already eaten by the first failed CONSTRAINT check). New `catalog.InMemory.SetDomainDefault(name string, expr parser.Expr) bool` sets or (given a nil expr) clears `Domain.Default` directly. `execAlterDomain` (`internal/executor/operators_ddl.go`) gained `"setdefault"`/`"dropdefault"` cases, both mapping an unknown domain to `42704` (mirrors `"owner"`'s own not-found handling). Tests: `internal/executor/alter_domain_owner_rename_test.go`'s `TestAlterDomainSetDropDefault` (SET DEFAULT populates/renders via `DefaultBin()`, a later SET DEFAULT replaces the prior expression outright, DROP DEFAULT clears it back to nil, both forms 42704 on an unknown domain). See `docs/design/0122-0005-alter-type-owner-rename.md`'s new "Follow-up: `ALTER DOMAIN ... SET DEFAULT` / `DROP DEFAULT`" section and `docs/design/README.md`'s updated row. Gates: `go build ./...` clean; `go test ./internal/catalog/... ./internal/executor/... ./internal/parser/... ./internal/planner/... ./internal/server/...` PASS (no regressions); `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33). | Two things remain open. (1) The three still-unmodelled `ALTER DOMAIN` sub-forms: `SET`/`DROP NOT NULL` (needs a `checkDomainNotNull`-style cross-table scan, feature-sized) and `SET SCHEMA` (domains have no `Schema` field at all — unclear whether to add one or treat as a no-op like the flat catalog does elsewhere), plus domain restart persistence (`CREATE DOMAIN` still has no WAL record, so none of this bucket's catalog mutations survive a restart). (2) A newly discovered, out-of-scope bug found while verifying `SET NOT NULL`/`DROP NOT NULL`/`SET SCHEMA` still parse as no-ops: they don't — they raise a spurious `42P01: relation "" does not exist`, because the shared "unmodelled ALTER DOMAIN form" fallback returns `&AlterTableStmt{pos: t.Pos}` with no `Table` set, and `execAlterTable` immediately fails the empty-name lookup. Confirmed pre-existing at HEAD before this loop (reproduced via `git stash` on an isolated throwaway test), not a regression introduced by this loop's parser restructuring, and not domain-specific — grep finds 12 sites across `internal/parser/ddl.go` using the identical `&AlterTableStmt{pos: t.Pos}` "unmodelled form" fallback pattern for other unimplemented DDL sub-forms, any of which may have the same latent bug if actually reachable by a real client statement. | Resume point (sub-forms/persistence): unchanged from the prior two 2026-07-06 rows — `internal/parser/ddl.go`'s domain branch in `parseAlter` needs `set not null`/`drop not null`/`set schema` arms plus matching `catalog.Domain` mutations, and `CREATE DOMAIN` needs a WAL record before any `ALTER DOMAIN` mutation can be made restart-durable. Resume point (fallback-dispatch bug): audit all 12 `&AlterTableStmt{pos: t.Pos}` fallback sites in `internal/parser/ddl.go` (`grep -n 'AlterTableStmt{pos: t.Pos}' internal/parser/ddl.go`) to determine which are actually reachable by a real client statement (vs. already intercepted by a more specific branch upstream), then either give each a true no-op statement type (e.g. a dedicated `NoopStmt`) or fix `execAlterTable` to tolerate an empty `Table` as a no-op. | (1) is feature-sized (cross-table scan) or needs a design decision (no `Schema` field precedent) — not a bounded mechanical follow-up, consistent with every prior row in this bucket. (2) is a distinct, wider-blast-radius bug (12 call sites, not just domains) discovered as a side effect of this loop's restructuring, not something this loop's SET/DROP DEFAULT scope was meant to fix; recording it here per the "defer-and-record, never close silently" rule rather than expanding this loop's bounded scope to also survey/fix all 12 sites. |
| resolved | 2026-07-06 | M0122-0005 (12-site `&AlterTableStmt{pos}` fallback bug, closes resume point (2)/(fallback-dispatch bug) from the row above) | Surveyed and fixed all 12 sites in `internal/parser/ddl.go` sharing the "unmodelled sub-form, consume tokens, return a no-op" shape (11 inline `return &AlterTableStmt{pos: t.Pos}, nil` sites plus `parseAlterOpFamilyTail`'s `noop` closure at ddl.go:1643, which routed through the shared `parseSkipToSemicolonHelper` instead — the reason it didn't match the other 11's literal grep pattern). Each now returns `parser.CompatNoopStmt{Tag: "<PG command tag>"}` instead of a bare nameless `AlterTableStmt` — `CompatNoopStmt`'s `execCompatNoop` (`internal/executor/operators_ddl.go:14988`) short-circuits at `if s.ObjType == "" { return nil }` (~line 15111) before any relation lookup, and its `Tag` drives the real `CommandComplete` reply tag (`internal/server/dispatch.go:2498`) instead of the misleading `"ALTER TABLE"` the old stub produced. Tags: `ALTER AGGREGATE`, `ALTER INDEX`, `ALTER MATERIALIZED VIEW`, `ALTER VIEW` (x3 call sites), `ALTER OPERATOR`, `ALTER OPERATOR CLASS`, `ALTER OPERATOR FAMILY`, `ALTER PUBLICATION`/`ALTER SUBSCRIPTION` (dynamic, from the loop's `pubSubKind`), `ALTER SCHEMA`, `ALTER DOMAIN`, and the generic collation/extension/language/operator/system compat-stub loop (`"ALTER " + strings.ToUpper(objIdent)`). Fixed 2 pre-existing tests that had pinned the buggy `*AlterTableStmt` shape as the expected result (`TestParseAlterOperatorOwnerToIsNoop` in `internal/parser/op_compat_test.go`, `TestParseAlterPublicationOtherFormsStillNoop` in `internal/parser/alter_pubsub_owner_test.go`) — both now assert `*CompatNoopStmt`. New test: `internal/executor/alter_domain_owner_rename_test.go`'s `TestAlterDomainUnmodelledFormsAreNoop` pins the concrete executor-level regression end to end (`SET NOT NULL`/`DROP NOT NULL`/`SET SCHEMA` on a real domain now succeed instead of raising `42P01`). See `docs/design/0122-0005-alter-type-owner-rename.md`'s new "Follow-up: the 12-site `&AlterTableStmt{pos}` fallback bug" section and `docs/design/README.md`'s updated row. Gates: `go build ./...` clean; `go test ./internal/parser/... ./internal/catalog/... ./internal/executor/... ./internal/planner/... ./internal/server/...` PASS (no regressions); `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33). | None — this closes the fallback-dispatch bug cleanly for all 12 sites. The 3 genuinely feature-sized `ALTER DOMAIN` sub-form gaps ((1) from the row above: `SET`/`DROP NOT NULL` cross-table scan, `SET SCHEMA` needing a `catalog.Domain.Schema` field decision, domain restart persistence) remain open and unaffected — this follow-up only fixed the wrong-error no-op mechanism, not any feature scope. | n/a (resolved) | n/a (resolved) |
| resolved | 2026-07-06 | M0122-0005 (domain restart persistence — closes the "domains have no restart persistence at all" resume point carried across every prior 2026-07-06 row in this bucket) | Two new WAL record kinds, `RecordKindCreateDomain`/`RecordKindDropDomain` (119/120), mirror `RecordKindCreateRangeType`/`RecordKindDropRangeType` but use a `bytes.Buffer`-based encoding (mirroring `EncodeColumnDefaults`'s style) instead of a fixed-offset layout, since `Domain.Checks`/`DomainCheck.InValues` are nested variable-length lists that don't fit a flat offset scheme. `wal.EncodeCreateDomain`/`DecodeCreateDomain` carry the full domain definition: OID/ArrayOID, base type name + typmod args, resolved enum `BaseOID`/`BaseIsEnum`, `NotNull`, owner, the `DEFAULT` expression as SQL text (`catalog.FormatExprForAttrdef` — the same base, non-decorated form `RecordKindColumnDefaults` already uses for column defaults, re-parsed via `parser.ParseExpr` on replay; deliberately NOT `Domain.DefaultBin()`'s decorated output, since that method re-derives the `::type` cast suffix dynamically from `Base.Name` at render time and baking it into the persisted text would double it up after a reparse), and every `CHECK` constraint (name, expr, OID, `InValues`). New `catalog.InMemory.RegisterDomainDuringRecovery`/`DropDomainDuringRecovery` mirror `RegisterRangeTypeDuringRecovery`/`DropRangeTypeDuringRecovery` (the register hook also advances `nextOID` past the domain's OID/ArrayOID/every CHECK OID). New `internal/initdb/domain_ddl_recovery.go`'s `replayDomainDDLRecords`, wired into `Open` (`internal/initdb/open.go`) right after `replayRangeTypeDDLRecords`. Both new kinds added to the physical-replay no-op skip-list (`internal/wal/recovery.go`). `execCreateDomain`/`execDropDomain` (`internal/executor/operators_ddl.go`) now append the records. Verified against a real running server (not just unit tests): `CREATE DOMAIN us_zip AS varchar(10) NOT NULL DEFAULT '00000' CHECK (length(VALUE::text) = 5)`, full `stop`/`start` restart, `\dD`/`pg_get_expr(typdefaultbin,0)`/`pg_get_constraintdef` all identical before/after. Tests: `internal/wal/domain_ddl_test.go` (`TestEncodeDecodeCreateDomainRoundTrip`/`TestEncodeDecodeDropDomainRoundTrip`/`TestDecodeDomainRejectsWrongKindAndTruncatedPayload`), `internal/initdb/domain_ddl_recovery_test.go` (`TestDomainDDLRecoveryReplaysCreate`/`TestDomainDDLRecoveryReplaysDropAfterCreate`/`TestReplayDomainDDLRecordsHandlesMissingWalDir`/`TestReplayDomainDDLRecordsHandlesNilCatalog`). See `docs/design/0122-0005-alter-type-owner-rename.md`'s new "Follow-up: domain restart persistence" section and `docs/design/README.md`'s updated row. Gates: `go build ./...` clean; `go test ./internal/catalog/... ./internal/executor/... ./internal/parser/... ./internal/planner/... ./internal/wal/... ./internal/initdb/... ./internal/server/...` PASS (no regressions); `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33). | (1) `SET`/`DROP NOT NULL` still needs the `checkDomainNotNull`-style cross-table scan (feature-sized); `SET SCHEMA` still needs a `Domain.Schema` field decision — both unchanged from every prior row in this bucket. (2) None of the later `ALTER DOMAIN` sub-forms (RENAME TO/OWNER TO/RENAME CONSTRAINT/ADD-DROP CONSTRAINT/SET-DROP DEFAULT) got their own WAL records in this follow-up — only the domain's state as of `CREATE DOMAIN` survives a restart; a post-CREATE `ALTER DOMAIN` mutation is still silently lost on restart. (3) A newly discovered, unrelated gap found while verifying this fix end to end (see next row): domain `NOT NULL`/`CHECK` constraints are not enforced at all on table columns. | Resume point (1)/(sub-forms): unchanged, see the prior 2026-07-06 rows in this bucket. Resume point (2)/(ALTER DDL persistence): mirror `RecordKindAlterRangeTypeRename`/`RecordKindAlterRangeTypeOwner` (117/118) for domains at the next free kind numbers (121+), one record kind per `AlterDomainStmt.Action` value (`rename`/`owner`/`renameconstraint`/`addconstraint`/`dropconstraint`/`setdefault`/`dropdefault`), each wired into `execAlterDomain`'s matching case and `replayDomainDDLRecords`. Resume point (3): see the next ledger row (fresh item, not this row's scope). | (1)/(2) are unchanged/feature-sized precedent already recorded in prior rows — this loop's bounded scope was specifically "give the domain *definition* a WAL record", matching the exact resume point named by every prior 2026-07-06 row in this bucket. (3) is a distinct, out-of-scope discovery (DML-path constraint enforcement, not persistence) surfaced only while manually verifying the restart fix end to end against a real server — recording it per the "defer-and-record, never close silently" rule rather than expanding this loop's scope. |
| resolved | 2026-07-06 | M0122-0005 (newly discovered while verifying domain restart persistence — domain constraint enforcement gap) | (nothing landed this loop for this item — pure discovery) | Domain `NOT NULL` and `CHECK` constraints are never enforced on table columns of a domain type. Reproduced against a *freshly created* domain in the same live session with no restart involved at all (so this is not a manifestation of the restart-persistence gap the same-dated row above closes — it is a DML-path validation gap that predates this loop entirely): `CREATE DOMAIN nn_int AS int NOT NULL; CREATE TABLE t_nn (p nn_int); INSERT INTO t_nn VALUES (NULL);` succeeds (should raise `23502`/`domain_constraint_violation`-style error); `CREATE DOMAIN pos_int AS int CHECK (VALUE > 0); CREATE TABLE t_pos (p pos_int); INSERT INTO t_pos VALUES (-5);` also succeeds (should raise `23514`). | Real PG wraps every domain-typed column's INSERT/UPDATE value in a `CoerceToDomain` execution node (`postgres/src/backend/executor/execExprInterp.c`'s `ExecEvalConstraintNotNull`/`ExecEvalConstraintCheck`, built by `ExecInitExprRec`'s `T_CoerceToDomain` case) that runs the domain's NOT NULL/CHECK constraints on every assignment, not just at `CREATE TABLE`/`ALTER TABLE ADD COLUMN` time. goopg's expression evaluator (`internal/executor/expr.go`) and INSERT/UPDATE value-coercion path (`internal/executor/operators_storage.go`, `operators_upsert.go`) have no equivalent node/hook — a domain-typed column is only ever resolved to its base type for storage, with no constraint-carrying wrapper. Resume point: find where goopg resolves a column's declared type to a domain (`catalog.LookupDomain` call sites in `internal/executor/operators_storage.go`/`operators_ddl.go`'s CREATE TABLE column-type resolution) and add a per-INSERT/UPDATE-value domain-constraint check there — likely a new `checkDomainConstraints(cat, domainName, value) error` helper reusing `Domain.Checks`' stored expr text (`parser.ParseExpr` + eval against a synthetic `VALUE` binding) and `Domain.NotNull`, called from both the plain INSERT/UPDATE row-build path and any COPY/upsert path that assigns a domain-typed column. | Feature-sized (new expression-evaluation hook shared across every DML entry point that can write a domain-typed column, not a bounded mechanical follow-up) — discovered as a side effect of manually verifying the same-dated restart-persistence fix against a real server, not something this loop's WAL-persistence scope was meant to fix; recording per the "defer-and-record, never close silently" rule. |
| resolved | 2026-07-06 | M0122-0005 (domain constraint enforcement on INSERT — closes the row above's "domain NOT NULL/CHECK are never enforced on table columns" discovery) | New `checkDomainConstraintsForRow(ctx, cols, row)` (`internal/executor/operators_fk.go`, next to the existing table-level `checkConstraints`) enforces a domain-typed column's (`Column.DeclaredTypeName != ""`) NOT NULL and every CHECK constraint on INSERT: `dom.NotNull` raises `23502` (`"domain %s does not allow null values"`, matching `domains.c`'s wording exactly), and each `dom.Checks` entry is validated either via the existing fast-path string-membership test (`ck.InValues`, for `CHECK (VALUE IN (...))`) or, for the first time, a **generic** predicate evaluator (`evalDomainCheckExpr`) that mirrors `checkConstraints`' mini-query trick: `SELECT (<ck.Expr>) FROM (VALUES (val::basetype)) AS _chk(value)`, planned/built/run through the full executor stack (the CHECK expression's only reference is the lexer-folded `value` identifier, which the synthetic column name matches with no VALUE-specific rewriting needed) — this closes the second half of the gap the row above's `CHECK (VALUE > 0)` repro needed (the pre-existing cast-time-only enforcement in `expr.go` only ever handled the `IN (...)` fast-path). Wired into `insertOp.Next()` (`internal/executor/operators_storage.go`) at two sites: the non-partitioned path (gated `!isPartitioned`, alongside the existing NOT NULL/CHECK block) and the partitioned leaf path (after `remapRowForPartition`, alongside the existing leaf-level NOT NULL check) — domain error messages don't name the table, so no parent-vs-leaf wording distinction applies, unlike the plain NOT NULL check. Also fixed a **latent bug in the pre-existing cast-time enforcement** discovered while building this: both the old `expr.go` CastExpr arm and this loop's first draft used `Datum.StringValue()` to stringify the value for `ck.InValues` comparison, but `StringValue()` only extracts `KindString`'s `Buf` payload — for a `KindInt` datum (e.g. `CHECK (VALUE IN (1,2,3))` on an int domain) it silently returns `""`, so every non-string/non-enum `IN (...)` domain CHECK was being evaluated against an empty label and would incorrectly reject *every* value including valid ones (caught by `TestDomainColumnInCheckEnforced` failing on the valid case `INSERT INTO t_small VALUES (2)` during this loop's own testing, not by any pre-existing test — the only prior IN-form test, `TestDomainCheckConstraintEnforced`, happened to use an enum-backed domain, whose `Kind == KindEnum` branch worked correctly). Fixed both call sites to use `Datum.Format()` instead, which renders every `Kind`'s canonical text form (including `KindEnum`, so the `KindEnum` special-case became unnecessary and was dropped). Tests: `internal/executor/domain_column_constraint_test.go`'s `TestDomainColumnNotNullEnforced`/`TestDomainColumnGenericCheckEnforced`/`TestDomainColumnInCheckEnforced` (new), plus the pre-existing `TestDomainCheckConstraintEnforced` (`enum_seqscan_test.go`) re-verified green after the `StringValue()`→`Format()` fix. Verified against a real running server (not just unit tests): all three of the row-above's exact repro statements (`nn_int`/`pos_int`/`small_num`) now raise the correct SQLSTATE/message on the invalid value and still accept the valid one. Gates: `go build ./...` clean; `go test ./internal/executor/... ./internal/catalog/... ./internal/planner/... ./internal/parser/...` PASS (no regressions); `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33). | UPDATE does not call `checkDomainConstraintsForRow` at all — verified live: `UPDATE t_pos SET p = -1` (a `CHECK (VALUE > 0)` domain column) silently succeeds. This is because **UPDATE in goopg does not enforce table-level NOT NULL or CHECK constraints at all**, not just domain ones — a `grep` for `checkConstraints(` and `"violates not-null constraint"` across `internal/executor/` turns up call sites only in `insertOp.Next` (both the plan-table and partitioned-leaf branches), none in `updateOp.Next`/`updateWithFrom`/`upsertOp.applyUpdate`. Domain enforcement was scoped to INSERT (mirroring where the *existing* table-level CHECK/NOT NULL enforcement already lived) rather than expanding into UPDATE's separate, larger, pre-existing gap. | Two independent resume points, ordered by leverage: (1) **general UPDATE constraint enforcement** (bigger, unblocks both table-level and domain-level in one pass) — add the same NOT NULL + `checkConstraints` + `checkDomainConstraintsForRow` sequence to `updateOp.Next` (`internal/executor/operators_storage.go` ~4086) and `updateWithFrom` (~5419) after the new row is built and before the heap write, mirroring `insertOp.Next`'s ordering exactly; PG oracle: `execExprInterp.c`'s `ExecConstraints`, called from `ExecUpdateAct`/`ExecInsert` identically (`execMain.c`), i.e. upstream already treats INSERT and UPDATE symmetrically here, so this is a real, not cosmetic, gap. (2) once (1) lands, `operators_upsert.go`'s `applyInsert`/`applyUpdate` (the `INSERT ... ON CONFLICT DO UPDATE` path) need the same two calls threaded in, since they bypass `insertOp`/`updateOp` entirely. | Not a mechanical follow-up to this loop's fix: UPDATE's missing CHECK/NOT-NULL enforcement is a distinct, pre-existing, cross-cutting gap (affects every table, not just domain-typed columns) whose fix touches a different operator (`updateOp`) and would double this loop's diff for a change unrelated to what the row above asked to close; recording per the "defer-and-record, never close silently" rule rather than expanding scope. |
| resolved | 2026-07-06 | M0122-0005 (general UPDATE constraint enforcement — resolves resume point (1)/(2) from the row above: "UPDATE does not enforce table-level NOT NULL or CHECK constraints at all") | New shared `checkRowConstraintsForWrite(ctx, tbl, cols, row)` (`internal/executor/operators_fk.go`, right after `checkDomainConstraintsForRow`) bundles the NOT NULL + `checkConstraints` + `checkDomainConstraintsForRow` trio insertOp.Next already ran, for reuse by every UPDATE write path. Wired into all three `updateOp` write paths in `internal/executor/operators_storage.go` — `Next` (SeqScan fallback), `updateViaIndex` (B-tree point-lookup fast path), and `updateWithFrom` (`UPDATE ... FROM`) — plus `operators_upsert.go`'s `applyInsert`/`applyUpdate` (the `INSERT ... ON CONFLICT` insert and DO-UPDATE arms, which bypass `insertOp`/`updateOp` entirely). Each call site had to check the row **before** attempting `tryApplyHOTUpdate` (not only in the non-HOT fallback branch), since `hotUpdateEligible` is a plan-level decision (no indexed column is in the SET list) independent of whether the new value violates NOT NULL/CHECK/domain constraints — a check placed only in the "!used" branch would be silently skipped for every HOT update, which is the common case. Also fixed a **latent, independent bug found via this loop's own index-path test** (`TestUpdateViaIndexEnforcesNotNull` initially failed with no error at all): `updateViaIndex`'s two write sites (main scan + its EPQ-retry re-evaluation) carried an "M0111-0001: restore columns that became null during decode→rebuild" loop that, for any column with an explicit SET expression, unconditionally overwrote a legitimately-computed NULL back to the old (pre-update) value — by construction it could only ever fire on an explicit SET (a no-SET column's new/old values are literally the same variable, so they can never differ), meaning `UPDATE t SET col = NULL WHERE <indexed-column> = x` silently no-op'd on the indexed fast path only (the SeqScan and `UPDATE ... FROM` paths never had this workaround and were unaffected). Removed both occurrences; the decode-format drift it was presumably compensating for was independently fixed by the later 0111-0002..0111-0007 codec-parity passes per `docs/design/README.md`. Tests: `internal/executor/update_constraint_enforcement_test.go` (new) — `TestUpdateSeqScanEnforcesNotNull`, `TestUpdateSeqScanEnforcesCheckConstraint`, `TestUpdateViaIndexEnforcesNotNull`, `TestUpdateFromEnforcesNotNull`, `TestUpsertInsertArmEnforcesCheckConstraint`, `TestUpsertUpdateArmEnforcesNotNull`, one per independent write path. Design: `docs/design/0122-0005-alter-type-owner-rename.md`'s "Follow-up" section extended; `docs/design/README.md` row updated. Gates: `go build ./...` clean; `go test ./internal/executor/...` full package PASS (no regressions, including the removed M0111-0001 workaround); `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `scripts/ralph-precommit-test.sh` PASS (full unit suite + pgbench TPC-B/simple-update/select-only smoke, 0 failed transactions). | A partition-key-changing (cross-partition-move) UPDATE's constraint check runs against the **source** scan-time table (`pu.scanTbl`/`idxTbl`/`pu.tbl`), not the destination leaf partition (`destPart`), since the destination isn't resolved until after the HOT-eligibility gate this check must precede — this mirrors how `insertOp.Next` already checks table CHECK constraints against the parent (pre-routing) table for every insert, so it is not a regression, but a domain/CHECK constraint that exists only on a destination leaf partition (not the source/parent) would still go unenforced on a moving UPDATE. Narrow, pre-existing-class gap (same as insertOp's parent-level CHECK check), not re-derived from scratch here. | Bounded scope: resolving the destination-leaf-aware variant requires restructuring the HOT-eligibility gate to resolve partition routing first in all three write paths, a larger, independently-scoped change; the common (non-partitioned, non-cross-move) case — the vast majority of UPDATEs — is now fully covered, which was this loop's stated goal. |  |
| resolved | 2026-07-06 | M0122-0008 (view-owner privilege check — closes the "views inline with no view-owner identity" gap noted under the 2026-07-05 SELECT-enforcement row) | `catalog.InMemory.CreateView` never set `Owner` on a newly created view (`execCreateView` never stamped it either), so every view was silently "owned" by the bootstrap superuser regardless of who ran `CREATE VIEW` — `execCreateView` (`internal/executor/operators_ddl.go`) now stamps `vt.Owner = o.ctx.NonSuperuserRole` at creation, except on `CREATE OR REPLACE VIEW`, which keeps the replaced view's existing owner. New `planner.SeqScan`/`IndexScan`/`IndexOnlyScan.PrivilegeCheckRole`/`PrivilegeCheckRoleSet` fields plus `tagViewOwnerScans` (`internal/planner/view_privilege.go`) walk an inlined view's plan tree and tag every scan whose role is still unset with the view's owner — skipped when `WITH (security_invoker = true)` is set (PG 15+'s escape hatch, `catalog.Table.SecurityInvoker`/`SecurityInvokerSet` already existed but had never been enforced anywhere before this loop). `dmlPrivilegePermitted` is now a thin wrapper around new `dmlPrivilegePermittedAs(ctx, tbl, priv, checkRole)`; the three SELECT-gated scan operators resolve `checkRole` via new `selectPrivilegeCheckRole` (tagged role when set, else the querying session's own role) — the querying session's own superuser bypass always wins first, matching real PG (a superuser session runs everything as itself regardless of view-owner semantics). Nested views keep their own owner: `Plan()`'s existing depth-first recursion means a nested view's own `tagViewOwnerScans` call completes and stamps its immediate scans before the outer frame tags whatever is still untagged. Tests: `internal/planner/view_privilege_test.go` (`TestPlanViewInliningTagsScanWithOwnerRole`, `TestPlanViewInliningSecurityInvokerSkipsOwnerTag`), `internal/executor/storage_dml_test.go`'s `TestScanOperatorsUseViewOwnerPrivilegeOverride` (all three scan operators, allow+deny+grant-vs-ownership directions), `internal/executor/view_owner_privilege_test.go` (`TestCreateViewStampsCreatingRoleAsOwner`, `TestViewOwnerPrivilegeGrantsThroughView` — the reported bug's exact end-to-end scenario). Design: `docs/design/0118-0039-truncate-conflict-privilege-model.md`'s new "Follow-up (2026-07-06): view-owner privilege check" section; `docs/design/README.md` row extended. Gates: `go build ./...` clean; `go test ./internal/executor/... ./internal/planner/... ./internal/catalog/... ./internal/parser/... ./internal/server/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `scripts/ralph-precommit-test.sh` PASS (full suite + pgbench TPC-B/simple-update/select-only smoke, 0 failed). | The view's *own* ACL is never checked against the querying role at all — no plan/operator node represents "scan the view itself" (it disappears entirely into its inlined expansion), so a role with zero grants anywhere (not even on the view) can still read through a view whose owner happens to have base-table access. Not a regression from this loop (the pre-fix base-table-only check never consulted the view's ACL either), but `GRANT SELECT ON view` is not yet a hard prerequisite for view access the way real PostgreSQL's `ExecCheckRTPerms` (which walks the view's own un-inlined RTE too) makes it. Also, `Table.Owner` changes (both this fix's `CREATE VIEW` stamp and the pre-existing `ALTER ... OWNER TO` path) are in-memory only — no WAL persistence — so a view's owner reverts to the bootstrap superuser after a restart until `ALTER VIEW OWNER TO` is reissued; this applies uniformly to tables and views and is a separate, broader, pre-existing gap. | Fixing the view's-own-ACL gap needs a preliminary per-statement permission pass over the plan tree checking every inlined view's OID against the querying role — planning currently has no `Context`/session-role visibility (only the executor operators do), so this is a materially larger, separately-scoped change, not a bounded follow-up. Owner-restart-persistence needs a broader `Table.Owner`-via-WAL design shared across every object kind that supports `OWNER TO`, also out of this loop's bounded scope. |  |
| resolved | 2026-07-06 | M0122-0005 (`userTypeNameForOID` schema-visibility — closes the row above's "newly observed, pre-existing, unfixed" gap) | `userTypeNameForOID(cat, oid, qualify bool)` (`internal/executor/expr.go`) gained a `qualify` parameter — the `"public."` prefix is now added only when the caller determines `"public"` is not visible on the effective search_path, instead of unconditionally. All three callers (the `::regtype` cast's `KindString`/`KindInt` OID→name branches, `format_type`'s built-in-fallback path) now pass `!regObjectSchemaVisible(ctx, "public")`, mirroring the exact pattern the `regproc`/`regoperator` casts already use. `RegtypeName(cat, oid, qualify bool)` (used by the wire-output layer for a plain `SELECT pg_typeof(...)`/regtype-typed column) gained the same parameter. `internal/server/dispatch.go`'s `appendTypedCellText` has no `executor.Context` (only per-connection session state), so it gained a new `publicSchemaVisible(getSetting func(name string) (string, bool)) bool` helper mirroring `regObjectSchemaVisible`'s search_path-parsing logic — threaded in as `ctx.GetSetting` (simple-query path) / `ectx.GetSetting` (extended-query path) at the two real call sites, replacing the previous no-parameter form. Verified live against a real running PostgreSQL 18.3 instance side-by-side (both on throwaway data dirs, torn down after the session): for a public-schema `CREATE TYPE mood AS ENUM (...)`, both `'mood'::regtype` and `format_type(<oid>, -1)` now render bare `mood` under the default search_path, `public.mood` under `search_path=''`, and `public.mood` under a search_path without `public` on it — byte-identical to real PG in all three scenarios (previously goopg always rendered `public.mood` regardless of search_path). Tests: `internal/executor/user_type_oid_name_test.go`'s `TestUserTypeNameForOIDAllKinds` extended to both qualify states across all ten OID forms; new `internal/executor/regtype_format_type_schema_visibility_test.go`'s `TestRegtypeFormatTypeSchemaVisibility` (live query execution, three search_path scenarios); new `internal/server/regtype_output_test.go`'s `TestAppendTypedCellTextRegtypeSchemaQualification`; `TestAppendTypedCellTextRegtypeRendersName`'s pre-existing user-enum case updated from `"public.mood"` to `"mood"` (correct default-search_path behavior). Design: `docs/design/0122-0005-char-oid18-disambiguation.md`'s new "Follow-up: `userTypeNameForOID` schema-visibility" section; `docs/design/README.md` row extended. Gates: `go build ./...` clean; `go vet ./...` clean on touched packages; `go test ./internal/executor/... ./internal/server/... ./internal/catalog/... ./internal/planner/... ./internal/parser/...` all PASS (no regressions); `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); live side-by-side verification against real PostgreSQL 18.3. | (nothing left open — this closes the specific gap the row above recorded) | n/a | n/a |
| resolved | 2026-07-06 | M0119-0004 (`verify_dictoptions` CREATE/ALTER TEXT SEARCH DICTIONARY option validation — closes the slice-437 CREATE-side deferral and the slice-437-follow-up ALTER-side deferral, both of which read "any option name/value is accepted verbatim") | `CREATE`/`ALTER TEXT SEARCH DICTIONARY` now reject an option key the chosen template's own init function doesn't recognize, mirroring real PG's `verify_dictoptions` (`tsearchcmds.c`), which calls the template's init method (`dsimple_init`/`dsynonym_init`/`dispell_init`/`thesaurus_init`) and propagates its `ERRCODE_INVALID_PARAMETER_VALUE` (22023) rejection. Read all four `*_init` functions directly from `./postgres/src/backend/tsearch/` to pin the exact allowed-key sets (simple: stopwords/accept; synonym: synonyms/casesensitive; ispell: dictfile/afffile/stopwords; thesaurus: dictfile/dictionary) and their non-shared message text. New `catalog.tsDictTemplateOptionSpec`/`ValidateTSDictOptions`/`builtinTSTemplateNameForOID` (`internal/catalog/catalog.go`); `AlterTSDictOptions` validates the post-merge option list (not just the incoming ALTER directives) before persisting, matching `AlterTSDictionary`'s own validate-then-persist order — a bare delete-only directive naming a never-real key is correctly a silent no-op, not an error, since it never re-enters the merged list. `internal/executor/operators_ddl.go`'s CREATE case calls the validator before `im.CreateTSDict`; `execAlterTSDict`'s `"options"` case maps a validation-failure error to 22023 via `strings.Contains(err.Error(), "unrecognized")` (same dispatch shape `execAlterDomain`'s `RenameDomainConstraint` call already uses), keeping 42704 for the pre-existing "dictionary does not exist" case. Tests: `internal/executor/tsdict_option_validation_test.go`'s new `TestCreateTSDictOptionValidation` (table-driven, all 4 templates) and `TestAlterTSDictOptionValidation`. Design: `docs/design/0110-0001-pg-dump-tap-port.md`'s new "Slice 437 follow-up: `verify_dictoptions` template-specific option validation" section; `docs/design/README.md` row extended. Gates: `go build ./...` clean; `go test ./internal/catalog/... ./internal/executor/... ./internal/parser/... ./internal/server/... ./internal/initdb/... ./internal/wal/...` all PASS (no regressions); `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33). | (nothing left open — this closes both named deferrals for TEXT SEARCH DICTIONARY option validation; `OWNER TO` remains a no-op for the unrelated, already-accepted reason CONFIGURATION's is) | n/a | n/a |
| resolved | 2026-07-06 | M-NIGHTLY (AI-20260706-201855-001, `pgbench/nightly` — investigation of the recurrence of M0118-0130's "btree: item length mismatch keyLen=9 total=37" at nightly scale c=100, NOT yet fixed) | Confirmed the nightly failure is the SAME error class M0118-0130 (2026-06-26) partially fixed, and found a fast, fully local, in-process repro that reuses the already-skipped `TestMultiWriterStress_M0055_Phase_C` (`internal/access/btree/multi_writer_stress_test.go:40`, disjoint-key 32-writer×1000-insert stress, no VACUUM/deletes involved) — removing its unconditional `t.Skip` and running `go test -run TestMultiWriterStress_M0055_Phase_C -count=1 ./internal/access/btree/...` in a loop reproduces a sibling symptom ("btree: empty internal page" from `findChildBlockDirect`, btree.go:1227) in ~2/40 single-process runs (seconds each, vs. the nightly gate's 180s×3 pgbench run). Added a temporary diagnostic (`fmt.Errorf` in `descendToLeaf`'s internal-page branch, btree.go:1321, printing `cur`/`op.Level`/`op.IsRoot()`/`meta.Root`) and captured two failures: both showed the FAILING block's opaque as `level=0 isRoot=false` while `meta.Root` was a DIFFERENT, unrelated block — i.e., the reader landed on a page whose `BTPageOpaque` reads as the zero value, the exact signature of a page that has been through `storage.InitPage` (via `Pool.PinNew`, bufpool.go:1048) but not yet through the btree layer's own `initPage`/item-population step. All temporary edits were reverted (not committed) after capturing the evidence — no functional change landed this loop. | The buffer-pool-level root cause itself: reviewed `Pool.PinNew` (bufpool.go:1029-1104) and confirmed it publishes the new block's tag into the buffer map AND flips `slotValidBit` (line 1085, `bm.Insert` line 1088) BEFORE returning the `*Slot` to its btree-layer caller (`pinNewOrRecycled`/`createNewRoot`), which only THEN calls `.Lock()` to populate real content — i.e., `PinNew` hands back an already-publicly-pinnable, unlocked slot whose content is merely `storage.InitPage`'d (a structurally valid but logically empty page). Verified (via the Go memory-model happens-before chain through the per-page `contentMu` mutexes) that ordinary content visibility between a split's rightSlot-population and its later parent-downlink-publish IS correctly ordered for a normal reader — so the reproduced case is NOT simply "reader saw stale/torn bytes on a page it legitimately reached"; something in the buffer pool's slot/tag bookkeeping (a stale slot handle, a generation/ABA edge case in `tryPinSlot`/`claimVictim`/`bm.Insert`'s races, or block-number reuse) is letting a concurrent reader reach this fresh, not-yet-populated slot via a path that should not yet be able to name it. Root cause NOT yet isolated further — needs focused, dedicated investigation of `internal/storage/bufpool.go`'s Pin/PinNew/claimVictim/evictVictim state machine, exactly as ledger item M0118-0130's blocker (4) already flagged. | Resume point: (1) re-apply the reverted diagnostic patch (or better, add a permanent debug-build-only assertion) to `descendToLeaf` (btree.go:~1321) and `findChildBlockDirect` (btree.go:1227) to capture the failing block number directly; (2) drive the now-known-fast repro (`TestMultiWriterStress_M0055_Phase_C` with its `t.Skip` removed, `go test -run ... -count=1` looped — do NOT re-enable it as a real gate until the flake is fixed, it will break CI) under `go test -race` AND under a bufpool-level tracer that logs every `claimVictim`/`evictVictim`/`bm.Insert`/`bm.Delete`/`bm.Lookup` call with tag+slotIdx+gen, to catch the exact moment two different tags observe the same slotIdx; (3) once isolated, the likely fix shape is publishing `PinNew`'s new tag into `bm` only AFTER the caller's content-population Lock/Unlock completes (may require restructuring `PinNew`'s contract so callers populate content BEFORE the slot becomes bufmap-visible, mirroring how `pinNewOrRecycled`'s OWN recycled-block path already zeroes-then-relies-on-a-separate-Lock, i.e. push the "populate before publish" invariant down into `PinNew` itself rather than leaving it to each caller). | Same keystone blocker M0118-0130 already named ("Buffer-pool eviction protocol concurrency... prerequisite for (2) and (3)") — distinct subsystem (`internal/storage/bufpool.go`, not `internal/access/btree`), high blast radius (shared by every relation, not just btree), so scoped as investigation-only this loop per the hard-won rule against rushed structural buffer-pool changes (the M0055-0004-followup-stage2-splitmu-removal comment in btree.go:601-613 already documents one prior rushed attempt causing NEW panics). This loop's contribution is turning a "needs a 180s×3 nightly pgbench run to observe" bug into a "reproduces in seconds, locally, with a known code-level signature" bug — a large investigation-cost reduction — without risking a silent regression by attempting the buffer-pool fix itself under time pressure. |
| resolved | 2026-07-07 | M-NIGHTLY (AI-20260706-201855-001, `pgbench/nightly` — continued investigation of the recurrence of M0118-0130's "btree: item length mismatch keyLen=9 total=37" at nightly scale c=100, NOT yet fixed) | Dynamically REFUTED the prior loop's leading hypothesis and narrowed the search space substantially, via temporary instrumentation (all reverted, nothing committed): (1) added `bufmap.debugCountLive(tag)` (full non-early-terminating bucket scan) called after every successful `bm.Insert` in both `PinNew` and `pinLoad` (`internal/storage/bufpool.go`), panicking if a tag ever has >1 live bufmap entry at insert time — reproduced the "btree: empty internal page" failure twice (150- and 300-iteration loops of the already-known-fast repro, `TestMultiWriterStress_M0055_Phase_C` with its `t.Skip` removed) with this check ACTIVE and it never fired, refuting "two concurrent PinNew publish the same tag" as the mechanism (also independently confirmed unreachable by re-reading `relFile.extend`/`Manager.relFile`, both fully serialized under `relFile.mu`/`Manager.mu`, so PinNew's own defensive "another goroutine published this block while we were in Extend" branch in bufpool.go is dead code in practice). (2) added a tag-match assertion (`s.tag != tag → panic`) at all 3 successful-pin return sites (`Pin`'s fast-path CAS, `pinSlow`'s `tryPinSlot` call, and inside `tryPinSlot` itself so `pinLoad`'s internal re-check branch is covered too) — reproduced the failure repeatedly (multiple 200–500-iteration runs, both single-process and 4-way-parallel) with this check ACTIVE and it never fired either, refuting "reader resolves to the wrong physical slot for its tag" (a stale-slotIdx/gen ABA) as the mechanism. (3) added an unbounded (not ring-buffer-capped) cross-layer event trace (`storage.DebugTraceEvent`/`DumpDebugTrace`, plus call sites in `claimVictim`/`evictVictim`/`PinNew`/`pinLoad`/`tryPinSlot` and in `btree.go`'s `insertIntoBlock`/`createNewRoot`/`descendToLeaf`'s failure branch) and captured the FULL lifecycle of the actual failing block number in 3 separate single-process reproductions (ruling out cross-process log contamination, which corrupted an earlier parallel-process attempt): each time, the failing block was legitimately created via `PinNew`, read successfully ~50-200 times via the traced fast path (content genuinely valid and tag-matched throughout), then evicted DIRTY (flushed to disk), reloaded cleanly into a new slot, then evicted a SECOND time CLEAN (`dirty=false`, no flush) — and after that second eviction, NO further `PinNew`/`pinLoad`/`tryPinSlot`/`Pin`-fast-path trace event for that tag ever appears before the dump fires, even though `descendToLeaf`'s own failure-branch trace (fired from inside the SAME failing call) proves `bt.pinR()` on that exact block DID return successfully with zero content moments later. All temporary instrumentation reverted via `git checkout --` before this loop ended (`internal/storage/bufpool.go`, `internal/storage/bufmap.go`, `internal/access/btree/btree.go`, `internal/access/btree/multi_writer_stress_test.go`); baseline `go test -count=1 ./internal/storage/... ./internal/access/btree/...` PASS after revert. | The exact mechanism by which the reader's successful, tag-matched `Pin()` call (proven to happen, since it's the caller of the failing `descendToLeaf` branch) evades ALL FOUR instrumented "successful pin" return points (`Pin` fast-path CAS, `pinSlow`'s `tryPinSlot`, `pinLoad`'s internal re-check `tryPinSlot`, `pinLoad`'s final post-`ReadBlock` publish) is still unexplained — either there is a 5th, un-instrumented return path in the buffer-pool Pin machinery, or (more likely given how exhaustively `Pin`/`pinSlow`/`pinLoad`/`tryPinSlot` were re-read this loop) the corrupted read is being serviced by a STALE, ALREADY-HELD `*storage.Slot`/`storage.Page` reference from an EARLIER, legitimately-successful pin — i.e. a caller-side (btree.go) bug where a slot handle or page byte-slice obtained several loop iterations/calls earlier is read again without a fresh `Pin()` round-trip, rather than a bufmap/eviction-protocol bug per se. This REDIRECTS the prior loop's "buffer-pool bufmap/eviction protocol" hypothesis (M0118-0130 blocker (4)) toward "btree.go caller-side stale-reference reuse" as the new leading hypothesis. | Resume point: (1) re-apply the same instrumentation (this row documents the exact diff shape: `debugCountLive` in `bufmap.go`; tag-match panics + unbounded `debugTraceEvent`/`DebugTraceEvent` trace in `bufpool.go`; `storage.DebugTraceEvent` calls bracketing `insertIntoBlock`/`createNewRoot`'s populate step and `descendToLeaf`'s failure branch in `btree.go`; un-skip `TestMultiWriterStress_M0055_Phase_C`) — reproducing takes 1 single-process `go test -run TestMultiWriterStress_M0055_Phase_C -count=400..500 ./internal/access/btree/...` run (~200-270s, ~1/150-500 failure rate, HIGHLY flaky run-to-run — some 400-iteration runs show zero failures). (2) Add ONE more instrumentation point this loop ran out of time for: a trace/assertion INSIDE `bt.pinR` itself (btree.go:907) logging every call with its `blk` argument, cross-referenced against a trace at the TOP of `descendToLeaf`'s loop body (before `bt.pinR(cur)`) — this will show definitively whether `pinR`/`Pin` is even being re-invoked for the failing block on its final access, or whether the failing read is reusing an old `*storage.Slot`/page-byte-slice handle without a fresh `Pin()` call (the new leading hypothesis). (3) If a stale-handle reuse is confirmed, audit every `slot`/`op`/`.Page()` variable in `descendToLeaf`, `insertIntoBlock`, and `finishSplit`/`CompleteDeferredSplits` for a code path that reads page content after the page's own `unpinR`/`Unpin` call (or across a `continue`/loop-iteration boundary without re-pinning) — the right-link-recovery `for` loop in `descendToLeaf` (btree.go:1278-1328) reassigns `cur` and `continue`s without an obvious staleness risk on first read, but `finishSplit`/`CompleteDeferredSplits`'s more complex multi-pin choreography (not reviewed this loop, time-boxed out) is a more probable location for such a bug given its higher structural complexity. | Scoped as investigation-only per the hard-won rule against rushing a buffer-pool fix under time pressure (a rushed attempt at this exact class of fix already caused a NEW panic class once, per the `M0055-0004-followup-stage2-splitmu-removal` comment in btree.go:601-613) — this loop's value is in DISPROVING two plausible-looking hypotheses with direct dynamic evidence (saving a future loop from re-deriving and re-testing them) and producing a reusable, well-tested instrumentation harness plus a concrete, narrower next step, rather than attempting a fix blind. |
| resolved | 2026-07-07 | M-NIGHTLY (AI-20260706-201855-001, `pgbench/nightly` — 6th loop; LANDED a real concurrency fix, but it does NOT fully close the nightly failure) | Made a methodological pivot from the prior 3 loops' buffer-pool/bufmap tracing (which never isolated a call site) to auditing `internal/access/btree/btree_vacuum.go`'s structural mutations for missing `splitMu` coverage — the file that removes a vacuumed-empty leaf's downlink from its parent internal page. Found and fixed a genuine logic race: `unlinkEmptyLeaf`'s WAL-emitter branch calls `resolveParentDownlink(leaf)` to capture a parent block + **1-based slot index** of the leaf's downlink, THEN (several statements later — WAL record emission, two sibling-relink `applyOpaqueMutation` calls) calls `applyParentDownlinkRemoval(parentBlk, capturedSlot, lsn)`, which removes-by-INDEX with no re-validation that slot `capturedSlot` still holds `leaf.blk`'s downlink. Neither `resolveParentDownlink` nor `applyParentDownlinkRemoval` (nor anything else in `btree_vacuum.go`, confirmed via `grep splitMu btree_vacuum.go` → no matches) takes `bt.splitMu`, the mutex `Insert`'s split path and `finishSplit` hold for their ENTIRE structural-change window. So a concurrent Insert-driven split on the SAME parent page — inserting a new downlink ahead of the captured slot — shifts every later index right; the vacuum goroutine then deletes whatever now sits at the stale index (some unrelated LIVE child's downlink), while `leaf.blk`'s own downlink survives in the parent even after `recycleBlock(leaf.blk)` returns it to the free list for a different split to reuse/zero — a later reader following the parent's stale downlink lands on the reused block's new content, surfacing as "item length mismatch". Note `unlinkEmptyLeafFPI` (the no-WAL-writer fallback, `removeDownlinkFromParent`) is NOT affected — it re-derives the target item by matching `it.ptr.Block == childBlk` at removal time, immune to index drift. Fix: wrap `unlinkEmptyLeaf`'s entire body (both the emitter and FPI-fallback branches) in `bt.splitMu.Lock()`/`defer Unlock()` (`internal/access/btree/btree_vacuum.go` top of `unlinkEmptyLeaf`), closing the resolve-to-remove window against concurrent splits — verified no reentrancy risk (`VacuumIndexPages`'s only production caller, `internal/executor/operators_vacuum.go`'s `vacuumIndexes`, never runs from within `Insert`/`finishSplit`, so no self-deadlock). Gates: `go build ./...` clean; `go test -count=1 ./internal/access/btree/... ./internal/amcheck/...` PASS; `go test -count=1 ./internal/executor/... -run Vacuum` PASS. | **This fix alone does NOT resolve the nightly repro.** Re-ran the authoritative repro post-fix (`REPO_ROOT=$PWD RUN_DIR=$(mktemp -d) NIGHTLY_PGBENCH_PORT=5570 bash ci/batch/stages/stage-pgbench.sh`, s=50/c=100/j=20/T=180): still fails, dozens of clients hit "item length mismatch keyLen=0/9 total=37" — but notably now failing on command 1 (the `pgbench_accounts` UPDATE) instead of command 5, immediately at the start of the `standard` workload. Built a small isolated manual repro to test a NEW hypothesis (is the corruption baked in at `pgbench -i` bulk-load time, given `keyLen=9 total=37` exactly matches a 4-TID posting item per M0118-0130's own math, and `btree.BulkCreate`/`deduplicateToRawItems` — called from `internal/executor/operators_ddl.go:9554` for every `CREATE INDEX`/`ADD PRIMARY KEY` — is the ONLY place that ever calls `marshalPosting` for a live btree, since the steady-state insert path's `appendTIDToPosting`/`promoteSingleToPosting` are confirmed dead code): loaded scale=10 fresh, ran a single-client `SELECT`-only check across the whole `pgbench_accounts` PK index immediately post-load — clean, zero corruption, ruling out a deterministic bulk-load-time bug for THIS schema/scale. Then ran `pgbench -c 20 -j 8 -T 30` (modest concurrency, scale=10, no autovacuum-scale requirement) against the SAME freshly-loaded, still-running server: reproduced within 30s, but with a DIFFERENT, arguably more fundamental symptom — "btree: empty internal page" (`findChildBlockDirect`'s `count == 0` check, btree.go) on the `pgbench_branches` UPDATE (10 rows, matching the original "no-HOT → constant duplicate-key churn on tiny tables" theory). Audited `btree_vacuum.go` for any internal-page cascade-deletion logic when a page's own downlink count reaches zero (mirroring PG's `_bt_pagedel` recursing up when a parent's last child is removed) — found NONE: `applyParentDownlinkRemoval`/`removeDownlinkFromParent` both happily leave a parent internal page with 0 items once all its leaf children have been individually vacuum-unlinked (plausible for a 10-row branches index once no-HOT churn has driven enough splits to create a multi-level tree), and no code anywhere re-checks/deletes/merges an internal page whose downlink count drops to 0 — so once ALL of an internal page's children are eventually vacuumed away (across one or more `VacuumIndexPages` passes), the very next descent through that now-empty internal page raises "empty internal page" deterministically, independent of any race. This is a plausible, evidence-backed SECOND root cause (a missing feature, not a bug) fully separate from the slot-index race this loop fixed. | (1) For the fixed slot-index race: none — closed. (2) For "empty internal page": implement recursive internal-page deletion in `internal/access/btree/btree_vacuum.go` — when `VacuumIndexPages`/`unlinkEmptyLeaf` removes an internal page's last downlink (item count → 0), that internal page must itself be unlinked from ITS parent (recursively, up to but not including the root, mirroring PG's `_bt_pagedel`'s parent-walk-up in `postgres/src/backend/access/nbtree/nbtpage.c`) rather than being left as a permanent dead end; `applyParentDownlinkRemoval`/`removeDownlinkFromParent` are the two call sites that would need a post-removal `len(newItems) == 0 && parentBlk != root` check triggering a recursive unlink. Root-becoming-childless is already handled separately (`isTreeEmpty`/`resetToEmptyRoot` in `VacuumIndexPages`). Next loop: reproduce with the cheap scale=10/c=20/T=30 recipe recorded here (much cheaper than the nightly s=50/c=100/T=180×3 recipe), confirm via a focused unit test that an internal page can legitimately reach 0 downlinks under repeated leaf-level vacuum unlinking, then implement the cascade. | Time-boxed: the slot-index race was a concrete, low-risk, independently-verifiable fix worth landing immediately once found (this loop's primary deliverable). The internal-page cascade-deletion gap is a materially larger feature (recursive multi-level page deletion protocol) discovered only in this loop's final verification pass, with real deadlock/correctness risk if implemented hastily under time pressure — matches the project's hard-won rule against rushing structural btree changes (the `M0055-0004-followup-stage2-splitmu-removal` comment in btree.go:601-613 already documents one prior rushed attempt causing a new panic class). Deferred to a dedicated loop with a fresh, cheap (scale=10, ~1 min) repro recipe in hand. |
| resolved | 2026-07-07 | M-NIGHTLY (AI-20260706-201855-001, `pgbench/nightly` — continued investigation of the recurrence of M0118-0130's "btree: item length mismatch keyLen=9 total=37" / "btree: empty internal page", NOT yet fixed) | Executed the prior loop's planned next step (instrument `bt.pinR` call/return plus `descendToLeaf`'s loop-top, cross-referenced against ALL FIVE `Pool.Pin`/`pinSlow`/`pinLoad`/`tryPinSlot`/`PinNew` success-return paths — not just the 4 from the prior loop, adding the previously-untraced `pinLoad` final post-`ReadBlock` return and `PinNew`'s two returns) via a temporary `internal/storage/debugtrace_temp.go` (env-gated `GOOPG_PINTRACE=1`, all reverted, nothing committed). Found the repro fails far more often under instrumentation (~every run at `-count=200`, vs. the prior loop's "~1/150-500") — almost certainly the trace itself perturbing timing (Heisenbug), but this made capture trivial. Got a clean single-process capture (`/tmp/pintrace-<pid>.log`, block 83): `bt.pinR(83)` returned successfully with `allzero=false` (verified via `DebugAllZero(s.Page())` computed WHILE STILL HOLDING `s.RLock()`, i.e. `contentMu.RLock()`, taken inside `pinR` itself, never released before the next statement) at trace seq 75631224; the VERY NEXT statement in the SAME goroutine, SAME call, under the SAME never-released RLock (`descendToLeaf`'s `op := readOpaque(slot.Page())`, executed immediately after `pinR` returns, with `unpinR` not yet called) read `Flags:0`/`Prev:0`/`Next:0`/all-zero opaque only 23 trace-sequence-numbers later — i.e. the page's bytes changed from valid, non-zero content to fully zeroed WHILE a single goroutine held a continuous `contentMu.RLock()` on that exact slot. This is only possible if some OTHER goroutine mutated `slot.page`'s backing array WITHOUT ever acquiring `contentMu` (a `sync.RWMutex` cannot let a `Lock()` holder run concurrently with an active `RLock()` holder, so a lock-respecting writer is provably excluded). Audited every locked mutation path this loop (`pinNewOrRecycled`'s recycle-zero-fill — confirmed NOT exercised by this test at all, since `TestMultiWriterStress_M0055_Phase_C` only inserts, never deletes/vacuums, so `bt.freeList` stays empty and `pinNewOrRecycled` always falls through to fresh `PinNew`; `PinNew`'s `InitPage`/`Extend`/publish sequence; `relFile.extend`'s `r.mu`-serialized block-number assignment; `evictVictim`/`claimVictim`'s `statePin(old) != 0` pinned-slot exclusion; `InvalidateBlock`/`InvalidateRel`'s same pinned-slot exclusion; `bufmap.Delete`/`Lookup`'s seqlock-style bucket protocol) and found NO locked-mutation-path bug — the unlocked writer's location is still unidentified. This REDIRECTS the investigation a 3rd time: away from "stale/reused `*storage.Slot` handle" (the last loop's hypothesis, which is now moot — the SAME slot's live content changed, not a stale reference to old content) and toward "some code path writes into a pinned, RLock'd slot's `.page` byte array without acquiring `contentMu` at all" — a genuine data race distinct from every eviction/ABA/reuse hypothesis explored across the 3 prior loops. All instrumentation reverted (`rm internal/storage/debugtrace_temp.go`; `bufpool.go`'s `Pin`/`pinSlow`/`pinLoad`/`tryPinSlot`/`PinNew` and `btree.go`'s `pinR`/`descendToLeaf` restored via `replace_symbol_body` to their pre-loop bodies, confirmed byte-identical via `git diff` showing no changes); `multi_writer_stress_test.go`'s `t.Skip` re-added. Baseline `go build ./...` clean and `go test -count=1 ./internal/storage/... ./internal/access/btree/...` PASS after revert. | The exact unlocked-write call site is still unidentified — this loop proved WHAT class of bug it is (an unlocked mutation of a live, RLock'd slot's page bytes) but not WHERE. Also newly noted but NOT the cause of this specific repro (page-deletion/vacuum never runs in this insert-only test): `pinNewOrRecycled` (btree.go:645-667) calls `slot.Lock()` / zero-fills / `slot.Unlock()` and then RETURNS to its caller (`insertIntoBlock`), which only re-acquires the lock (`rightSlot.Lock()`) several lines later before calling `initPage` — a real lock-release/re-acquire gap during which a concurrent reader with a stale-but-not-yet-invalidated downlink to a just-recycled block could observe an all-zero page, IF the recycle path were ever exercised concurrently with such a reader. Additionally, `recycleBlock`'s own doc comment ("The block must have been unlinked from the tree's logical structure before it lands here") has no visible safe-recycle deferral (no PG-style `RecentGlobalXmin`/safe-XID horizon before a page-deleted block re-enters `bt.freeList`), unlike real PostgreSQL's btree page-deletion + FSM recycling (`_bt_pendingfsm_add`/`_bt_pendingfsm_finalize` in `postgres/src/backend/access/nbtree/nbtpage.c`), which defers recycling until no possible concurrent scan could still hold a stale downlink to the deleted page. | Resume point for the unlocked-write race: instrument with the SAME env-gated `GOOPG_PINTRACE=1` approach (this row documents the exact shape: a `DebugPinTrace`/`DebugAllZero` pair added at each of the 5 `Pool.Pin`/`pinSlow`/`pinLoad`/`tryPinSlot`/`PinNew` success returns, PLUS `pinR`-call/`pinR`-return/`descendToLeaf`-loop-top markers in btree.go), but this time ALSO trace every WRITE-side mutation of `slot.page` (`insertItemSorted`/`resetPageItems`/`writeOpaque`/`initPage`/`InitPage`/the `pinNewOrRecycled` zero-fill loop) with the calling goroutine + slotIdx + tag, to find which one fires against block 83's slot WITHOUT a preceding `Lock()` in the same call stack — cross-reference by slotIdx+timestamp against the confirmed zeroing window (trace seq 75631224–75631247 in this loop's now-deleted `/tmp/pintrace-333145.log`, not preserved — re-capture needed). Note the repro rate is dramatically higher WITH tracing enabled (near 100% at `-count=200` vs. the untraced ~1/150-500), so budget only ~1-2 minutes of `-count=50` runs, not the full 200-500 iterations, to keep the loop's time budget bounded. Separately (lower priority, not this bug): give `pinNewOrRecycled` a safe-recycle deferral before its lock-gap gets exercised by a future VACUUM-concurrent-with-insert test, and close the `slot.Unlock()`-then-caller-re-`Lock()` gap by having `pinNewOrRecycled` return the slot still locked (caller unlocks after populating), mirroring `PinNew`'s own contract more closely. | Scoped as investigation-only per the hard-won rule against rushing a buffer-pool/btree-concurrency fix under time pressure (a rushed attempt at this exact class of fix already caused a new panic class once, per the `M0055-0004-followup-stage2-splitmu-removal` comment in btree.go:601-613) — this loop's value is DEFINITIVELY narrowing the bug class (unlocked write, not stale handle/ABA/eviction-protocol) via direct dynamic evidence, ruling out the previous loop's leading hypothesis, and flagging a separate real recycle-safety gap for later, rather than attempting a blind fix. |
| resolved | 2026-07-07 | M-NIGHTLY (AI-20260706-201855-001, `pgbench/nightly` — 5th consecutive investigation loop on "btree: item length mismatch keyLen=9 total=37"/"btree: empty internal page", NOT yet fixed) | This loop's contribution is a MAJOR redirection away from all 4 prior loops' working assumption, backed by fresh dynamic evidence rather than static code audit. (1) Ran the exact fast repro the last 3 loops relied on (`TestMultiWriterStress_M0055_Phase_C`, `t.Skip` removed) for ~1180 total iterations this session — 100+300+500 via `go test -race -run ... -count=N` (in-process repeats) plus 80 separate-process invocations of a `-race`-built test binary (`go test -c -race`, matching the exact "loop `go test -count=1`" recipe cited by earlier ledger rows) plus 200 plain (non-race) `-count=200` iterations — and got ZERO failures and ZERO race-detector reports across all of them, in an environment with 16 real cores (ruling out a CPU-starvation explanation). This directly contradicts the previous loop's claim of a confirmed, reproducible "unlocked write to a live RLock'd slot" — either that capture was itself an artifact of the custom trace instrumentation (a bug in `DebugPinTrace`/`DebugAllZero`'s own synchronization, not a real product race), or the bug is real but far too rare to hit even at 1180 iterations while the custom tracing's "near-100% at -count=200" claim was a Heisenbug specific to that exact instrumentation's overhead pattern (not reproduced by -race's own, differently-shaped overhead). (2) Re-ran the ACTUAL authoritative repro from `ci/logs/action-items.md` (`REPO_ROOT=$PWD RUN_DIR=$(mktemp -d) bash ci/batch/stages/stage-pgbench.sh`, s=50 c=100 j=20 T=180) at HEAD per fix_plan rule 3 — CONFIRMED it still fails immediately and reliably (dozens of clients hit `btree: item length mismatch keyLen=9/keyLen=0 total=37` within the first ~30s of the very first TPC-B workload, every time). (3) Built `bin/goopg-race` (`go build -race -o bin/goopg-race ./cmd/goopg`), ran it manually against a fresh scale=50 pgbench dataset (matching the real repro's scale — a scale=5 dataset was tried first and did NOT reproduce in 60s, most likely because it fits entirely in the buffer pool and never exercises `claimVictim`/`evictVictim` eviction at all) under the real TPC-B workload (`pgbench -T 80 -c 100 -j 20`) for 80s: the corruption reproduced again, reliably (25 client aborts, `keyLen=47460`/`keyLen=0 total=37`), but the server's race-detector log (`GORACE="log_path=... exitcode=0 halt_on_error=0"`) recorded **zero "DATA RACE" reports** for the entire run. Go's race detector reliably catches a genuine unsynchronized concurrent read/write to the same memory (including syscall-filled buffers via `os.File` methods, which use `runtime.RaceWriteRange` internally) — a clean run through an actual, repeated reproduction of the real bug is strong evidence this is NOT a classic data race at all, but a LOGIC bug in code that IS properly synchronized (every write happens under the right lock, but the wrong bytes/offsets are computed or a stale-but-lock-consistent read occurs). All temporary artifacts removed (`bin/goopg-race`, race-mode data dirs, `/tmp/race_pgbench/*`, temp `-race` test binary); `multi_writer_stress_test.go`'s `t.Skip` re-added via `git checkout --`; `go build ./...` clean and `go test -count=1 ./internal/storage/... ./internal/access/btree/...` PASS after revert. | The real mechanism is still unidentified, but the search space has fundamentally changed: (a) the pure-insert, disjoint-key `TestMultiWriterStress_M0055_Phase_C` repro the last 4 loops relied on is now suspect as either a different, much rarer bug (the "empty internal page" symptom) or simply not a reliable proxy for the real nightly failure at all (0/1180 this session vs. near-instant, reliable reproduction via the real pgbench TPC-B workload); (b) the real bug requires the FULL heap+index+MVCC stack under UPDATE-heavy contention on SMALL, heavily-shared tables — TPC-B's `pgbench_branches` (50 rows at scale=50) and `pgbench_tellers` (500 rows) are hammered by all 100 concurrent clients, meaning every transaction takes a row-level UPDATE (HOT-update path, index maintenance, MVCC visibility under heavy xmax/xmin churn on a handful of index leaf pages) that `TestMultiWriterStress_M0055_Phase_C`'s 32-writer/disjoint-key/insert-only workload structurally cannot exercise (no updates, no deletes, no VACUUM, no shared-row contention — see `internal/access/btree/multi_writer_stress_test.go:45-66`); (c) since -race found nothing across a real reproduction, the bug is very likely a pure logic error (wrong length/offset computed under a correctly-held lock) rather than a synchronization bug — candidates worth checking first: `dedupConsolidate`/posting-list consolidation (`internal/access/btree/posting.go`, `appendTIDToPosting`/`promoteSingleToPosting` — flagged as currently *unused* by `go vet`'s new diagnostics this loop, meaning the dedup-consolidation branch in `insertIntoBlock` (btree.go:1490-1515) may not actually be calling them, worth checking whether `dedupConsolidate` is dead/no-op on the exact duplicate-heavy access pattern TPC-B produces), the item/line-pointer length encoding in `insertItemSorted`/`pageItems`/`findChildBlockDirect` (btree.go), and MVCC-driven HOT-update index-entry rewriting (`goopg_no_hot_update_index_reeval` memory: goopg has no HOT update, so every UPDATE re-inserts a new index entry — meaning TPC-B's per-transaction UPDATE on `pgbench_branches`/`pgbench_tellers` produces a NEW btree insert on every single transaction into the SAME few pages, a duplicate-key-heavy, page-thrashing insert pattern the old repro never modeled). | Resume point: (1) do NOT keep chasing `TestMultiWriterStress_M0055_Phase_C` — it has not reproduced in 1180 attempts this session across 3 repro styles (in-process -count=N, separate-process loop, plain); either replace it with a repro that models UPDATE-heavy small-table contention (e.g. N writers doing INSERT+conflicting-key overwrite or repeated UPDATE-style delete+insert on a tiny key range, mirroring `pgbench_branches`'s 50-row hot set) or retire it from the investigation entirely. (2) Reproduce via the SAME manual recipe this loop used and verified works (build `go build -race -o bin/goopg-race ./cmd/goopg`; `./bin/goopg-race init -D <dir>`; start with `GORACE="log_path=<path> exitcode=0 halt_on_error=0"`; `pgbench -i -s 50`; `pgbench -T 80 -c 100 -j 20 -P 5`) — this is CHEAP (~15 min total: ~5 min build+init, ~11 min load at s=50, ~80s workload) and reliably hits the bug on the very first attempt, a much faster loop than the btree-unit-test's rare flake. (3) With a live failing server, instead of more -race iterations (already shown fruitless), attach a debugger/print-based dump of the actual corrupted page's raw bytes + line-pointer array at the moment `pageItems`/`findChildBlockDirect` raises the "item length mismatch" error (the error's own call site — grep `"item length mismatch"` in btree.go — already has `keyLen`/`total` in scope; dump the full page's line-pointer directory and the specific item's raw byte range to see whether the recorded length is genuinely wrong at rest (a logic bug) vs. right-at-write-but-wrong-at-read (still points to a race, just one -race can't see, e.g. via `unsafe`/non-instrumented memory — but grep confirmed no `unsafe.Pointer` byte-writing exists in `internal/storage` or `internal/access/btree` outside the arena's alignment probe, so this is unlikely). (4) Check specifically whether `dedupConsolidate`/posting-list code is reached at all on the TPC-B workload's UPDATE pattern (`internal/access/btree/posting.go`'s `appendTIDToPosting`/`promoteSingleToPosting` showed as `unusedfunc` in `go vet`'s new-diagnostics output during this loop's `-race` test run — verify whether that's just dead code in THIS file, or evidence the whole posting-list dedup path is unreachable/broken, which combined with goopg's "no HOT update, re-inserts a new index entry per UPDATE" behavior would explain rapid page bloat/thrashing on the 3 tiny hot tables). | Scoped as investigation-only per the hard-won rule against rushing a buffer-pool/btree-concurrency fix under time pressure. This loop's value is falsifying the leading hypothesis from the last 3 loops (unlocked-write data race) with strong negative evidence from the actual race detector run against a REAL, live reproduction (not just the isolated unit test), and redirecting the investigation from "buffer-pool concurrency" toward "UPDATE-driven small-table index-maintenance logic bug, not a data race" — a fundamentally different, more tractable bug class with a much cheaper (~15 min, first-try) repro recipe for the next loop to use instead of the unreliable btree-only unit test. |
| resolved | 2026-07-07 | M-NIGHTLY (AI-20260706-201855-001) | (No code landed for this row — pure investigation.) Re-ran the full authoritative repro (`ci/batch/stages/stage-pgbench.sh`, s=50 c=100 j=20 T=180x3) after landing BOTH the splitMu fix (previous loop) and the internal-page cascade fix (this loop's other row above). | It STILL FAILS, with the SAME original signature (`btree: item length mismatch keyLen=9 total=37`, occasionally keyLen=0 or keyLen=7460) on the very first workload (`pgbench_accounts` UPDATE), ~78 of 100 clients aborting within roughly the first 30s. This proves a THIRD, still-unidentified root cause exists at the full c=100/j=20/s=50 scale that the cheap c=20/j=8/s=10 manual repro (clean for 90s, twice, post-fix) does not exercise. | `ci/batch/stages/stage-pgbench.sh` reproduces reliably; RUN_DIR=$(mktemp -d) NIGHTLY_PGBENCH_PORT=5570 bash ci/batch/stages/stage-pgbench.sh, inspect `$RUN_DIR/pgbench/pgbench.log` + `server.log`. Resume the 5th/6th-loop investigation thread already in `fix_plan.md`'s M-NIGHTLY item (2026-07-07 update #3): that thread already refuted "unlocked write"/data-race via `-race` (zero reports across ~1180 iterations) and redirected toward "a logic bug in properly-synchronized code, triggered only by the FULL heap+index+MVCC stack under UPDATE-heavy contention on tiny shared tables" — build `bin/goopg-race`, `pgbench -i -s 50` then `pgbench -T 80 -c 100 -j 20 -P 5` for a ~15 min repro; candidates flagged there but not yet inspected: `posting.go` dedup/posting-list code, and the item/line-pointer length encoding in `insertItemSorted`/`pageItems`/`findChildBlockDirect`. | Scope for this loop was the specific next-step handed off by the previous loop (the internal-page cascade); the c=100 recurrence is a distinct, deep investigation already flagged as needing its own dedicated non-rushed loop, not something to rush into the tail of this one. |
| resolved | 2026-07-07 | M-NIGHTLY (AI-20260706-201855-001, 8th consecutive investigation loop) | (1) LANDED: closed a real, previously-flagged race window in `internal/access/btree/btree.go`'s `pinNewOrRecycled` — it used to zero a recycled block's page bytes under `slot.Lock()` then `Unlock()` *before* the caller (`insertIntoBlock`'s split path) re-`Lock()`'d to stamp real opaque/header content, leaving a window where any other `pinW`-based writer targeting that same now-tagged, now-reachable block could observe/insert into the transitional all-zero page just before `initPage` silently overwrote it. Now both the recycled-block branch and the fresh-`PinNew` branch (added `pinNewLocked` wrapper) return the slot still content-locked; the single call site no longer re-`Lock()`s. Verified: `go build ./...` clean, `go test -count=1 ./internal/access/btree/... ./internal/amcheck/... ./internal/executor/... -run Vacuum` PASS, `go test -race -count=1 ./internal/access/btree/...` PASS (18.2s, zero race reports). (2) RULED OUT two hypotheses empirically (not just by static read): (a) posting-list re-encoding in the live insert/split path — `appendTIDToPosting`/`promoteSingleToPosting` (posting.go) are dead code, never called outside tests; `dedupConsolidate` (btree.go) only drops exact (key,ptr) duplicates and never re-marshals as posting-format bytes, so the split/no-split insert paths never write posting-encoded items — posting format only exists from `BulkCreate`. (b) the `rightmostLeafBlk` insert fast-path cache (`tryInsertOnCachedRightmost`) — found its populate-condition (`btree.go:1299` `op.Next == 0`) and staleness-check (`btree.go:1998` `op.Next != 0`) both compare against the wrong sentinel: every real "no right sibling" page in this codebase is initialized with `Next: storage.InvalidBlockNumber` (0xFFFFFFFF), never `0` (confirmed via every `BTPageOpaque{Next: ...}` write site in btree.go/bulkload.go/btree_vacuum.go). Wrote a throwaway probe test (`bt.Insert` 5000 sequential keys, forcing many splits, then read `bt.rightmostLeafBlk.Load()`) — confirmed it stays `0` forever, i.e. this entire fast path is currently 100% dead/inert. NOT fixed this loop (activating a currently-dormant path deserves its own dedicated verification loop, not a tail-end change); left as-is. (3) MAJOR NEW FINDING — a much cheaper repro AND proof the corruption is concurrency-triggered, not load-time: ran a single-threaded `pgbench -i -s 50` (no concurrent clients at all) against a fresh local server, then `bt_index_check`/`bt_index_parent_check` on all 3 pkey indexes — ALL CLEAN. Then ran `pgbench -c 100 -j 20 -T 25 -P 5` (just 25s!) against that SAME already-loaded database — reproduced the exact `btree: item length mismatch keyLen=9 total=37` failure within ~10s (compare: the full authoritative `stage-pgbench.sh` gate takes 15-30+ min). Post-failure `bt_index_check('pgbench_accounts_pkey')` shows WIDESPREAD "item order invariant violated" across several hundred distinct leaf blocks (starting as low as block 5), plus one genuinely byte-corrupt page at block 1096 with the familiar keyLen=9/total=37 signature — i.e. this is not one isolated glitch but either one structural mis-split whose effects cascade across the whole sibling chain (amcheck's order check compares HighKey/sibling links, so one bad splice can flag many downstream pages) or multiple independent occurrences. | The THIRD root cause (the one this whole 2026-07-07 investigation thread has been chasing since loop 5) is STILL not identified — my pinNewOrRecycled fix did not resolve it (confirmed via a fresh authoritative `stage-pgbench.sh` run post-fix: still fails identically, `btree: item length mismatch keyLen=9 total=37` etc. on the very first workload). The mechanism itself remains open. | **Use the new cheap repro for the next loop, not the 15-30 min authoritative gate or the unreliable btree-only unit test**: (1) build the tree once (`go build -o /tmp/goopg-diag ./cmd/goopg`; `init`; `start -D <dir> --listen 127.0.0.1:<port>`; `PATH` must include `postgres/local_install/bin` + matching `LD_LIBRARY_PATH`); (2) `pgbench -i -s 50 -h 127.0.0.1 -p <port> <db>` ONCE (~4 min, single-threaded, confirmed clean via amcheck — reuse this loaded DB across attempts, no need to re-init each time); (3) `pgbench -c 100 -j 20 -T 25 -P 5 -h 127.0.0.1 -p <port> <db>` reproduces in ~10s; (4) immediately run `SELECT bt_index_check('pgbench_accounts_pkey'::regclass);` and `bt_index_parent_check(...)` against the now-corrupted DB for structural detail before tearing down. Next investigative step: since corruption is widespread ("item order invariant violated" across hundreds of blocks, not a single page), instrument the SPLIT path specifically (`insertIntoBlock`, btree.go:~1420-1660, under `bt.splitMu`) rather than the no-split fast path (`tryInsertNoSplit`) — pgbench_accounts has 5M rows at scale=50 so splits are frequent and constant during the workload, unlike the tiny branches/tellers tables; a single mis-split (wrong byteAwareSplitLoc, wrong HighKey stamp, or a race between a split's `bt.unpinW(slot)` at line ~1638 and the *separate* parent-downlink insert a few lines later while `BTIncompleteSplit` is set but NOT checked by `tryInsertNoSplit`'s fast path — confirmed via grep that `tryInsertNoSplit`/`tryInsertOnCachedRightmost` never call `HasIncompleteSplit()` or `finishSplit`, unlike the documented crash-recovery contract) could plausibly explain a cascade of downstream order violations across many sibling pages. This BTIncompleteSplit-not-checked-by-fast-path gap was flagged but NOT yet proven exploitable this loop — worth checking first next loop. | Time-boxed to one loop; chose to (a) close a small, clearly-safe, already-flagged gap with full test verification, (b) rule out two hypotheses definitively via cheap empirical probes rather than more static reading, and (c) invest the rest of the budget in finding a 10s repro (vs. 15-30 min) since every future loop on this item benefits from that speedup — over trying to land a fix for the still-unidentified third root cause under time pressure in a subsystem with a documented history of rushed-fix regressions. |
| resolved | 2026-07-07 | M-NIGHTLY (AI-20260706-201855-001, 10th consecutive investigation loop) | (No code landed — pure investigation.) Audited the FULL split/root-lift/parent-insert call graph (`insertIntoBlock`, `createNewRoot`, `clearRootFlag`, `clearIncompleteSplit`, `finishSplit`, `descendToLeaf`, `tryInsertNoSplit`, `tryInsertOnCachedRightmost`) against the two candidate mechanisms flagged by loop 9 and confirmed: (a) `tryInsertOnCachedRightmost` is provably 100% dead code, re-confirmed independently — `rightmostLeafBlk.Store` only fires on `op.Next==0`, which never happens per loop 8's finding, so `bt.rightmostLeafBlk.Load()` always returns 0 and `tryInsertNoSplit`'s `if cached != 0` guard never lets it run; (b) ALL internal-(non-leaf)-page mutations provably route through `insertIntoBlock` under `bt.splitMu` — the only two writers of internal pages are the recursive parent-insert (`bt.insertIntoBlock(parentBlk, ...)`, itself always called from within a splitMu-held call chain: `Insert`, `finishSplit`, or `createNewRoot`'s fallback branch) and `createNewRoot`'s fresh-root population (also splitMu-held); no fast path ever pins an internal page for write, only leaves — so the hypothesis floated at the end of loop 9 ("does an internal page ever get a fast, non-splitMu insert path") is REFUTED. Also confirmed `byteAwareSplitLoc` can never return a split point leaving either half of a split page empty (explicit clamps `split<1→1`, `split>len-1→len-1`), so an ordinary split cannot itself produce a 0-item page. Re-tested the buffer-pool "reader resolves to the wrong physical slot for its tag" hypothesis (loop 9's row already refuted this for the DIFFERENT pgbench keyLen-mismatch symptom) specifically against THIS bug's symptom/repro: added a temporary tag-match assertion (`s.Tag() != BufferTag{Rel,blk}` → panic) at the two lowest-level call sites shared by every reader and writer, `pinR`/`pinW` (`internal/access/btree/btree.go`); ran `go test -run TestMultiWriterStress_M0055_Phase_C -count=300 ./internal/access/btree/...` (reproduced the failure once — `writer 3 insert 253: btree: empty internal page`, at 139s) — assertion never fired; also ran `go test -race -timeout 25m -run TestMultiWriterStress_M0055_Phase_C -count=250 ./internal/access/btree/...` (770s) — zero failures, zero race reports. All temporary instrumentation reverted (`pinR`/`pinW` restored verbatim, `t.Skip` restored) before finishing; `git diff --stat` on both files shows zero diff. `go build ./...` clean; `go test -count=1 ./internal/access/btree/... ./internal/amcheck/...` PASS. | The "empty internal page" bug in the pure disjoint-key insert-only stress test is STILL not identified. Two more candidate mechanisms are now conclusively refuted for this specific symptom (dead rightmost-cache path; internal pages always splitMu-guarded; tag/slot misresolution). Since the tag genuinely matches and the content lock genuinely excludes concurrent writers, the corruption is either (a) a genuine logic bug that legitimately writes/leaves 0 items on a page reachable via a valid downlink — not yet located despite auditing every mutation site — or (b) a lower-level memory-safety issue that bypasses the tag/lock model entirely (e.g. a stale `[]byte` alias into a slot's backing array retained across an unpin from an earlier call, written into after the slot is reused for a different block) — this exact hypothesis was the leading redirect in loop 9's row for the OTHER (pgbench) symptom and has never actually been tested for either symptom. | Test the stale-`[]byte`-alias-across-reuse hypothesis directly: audit every `slot.Page()` call in `internal/access/btree/btree.go` (particularly `insertIntoBlock`'s dedup-avoid-split branch, `pageItems`, `dedupConsolidate`, `compactRawSize`) for a captured byte-slice or `item.key` NOT defensively copied (`append([]byte(nil), ...)`) that could be read/written after the originating `unpinW`/`unpinR` call — `pageItems`/`parseItem` were checked this loop and DO copy (ruled out for that call site), but `parseItemNoCopy` (used only by `RangeScan`, not exercised by this test) and other `slot.Page()`-derived slices were not exhaustively enumerated. Alternatively, stamp a per-pin generation sentinel into unused page-trailer bytes on every `PinNew`/recycle (independent of B-tree content) and assert it is unchanged immediately before/after every `pinR`/`pinW` critical section — this would catch physical buffer aliasing that a tag/lock check cannot, since tag/lock correctness doesn't guarantee no other code path writes to the same backing array without going through `Pin()` at all. | Time-boxed per the Hard-won Rule against rushing a fix in this exact subsystem (documented history of rushed-fix regressions, `M0055-0004-followup-stage2-splitmu-removal` comment). This loop's value is ruling out 2 more concrete hypotheses and narrowing toward a buffer-aliasing-vs-genuine-logic-bug fork for the next loop, rather than attempting a blind fix. |
| resolved | 2026-07-07 | M-NIGHTLY (AI-20260706-201855-001, 11th consecutive investigation loop) | (No code landed — pure investigation, all instrumentation reverted.) Built a temporary diagnostic (ring-buffer trace + enriched error) directly testing the stale-buffer hypothesis loop 10 handed off, and it worked: reproduced the "empty internal page" failure THREE times across two runs (`go test -run TestMultiWriterStress_M0055_Phase_C -count=500` and `-count=600`, ~180-220s each) with full forensic detail attached to the error. Instrumentation (all in `internal/access/btree/btree.go`, reverted before finishing): (1) a `debugTrace(format, args...)`/`debugDumpTraceFor(blk)` package-level ring buffer gated by `os.Getenv("BTREE_DEBUG_TRACE")`, reset per-test-iteration via a `debugTraceLog = nil` line added at the top of `TestMultiWriterStress_M0055_Phase_C` (essential — without the per-iteration reset, `-count=N` reruns the same test function N times in one process and block numbers collide across iterations, making the dump unreadable); (2) a `debugTrace("SPLIT blk=%d leftBlk=%d level=%d leftN=%d blk=%d rightBlk=%d rightN=%d oldNext=%d", ...)` call in `insertIntoBlock`'s split path right after the rightItems insertion loop (btree.go, immediately before the "Stamp the new high key" comment); (3) a matching `debugTrace("NEWROOT blk=%d level=%d leftBlk=%d rightBlk=%d", ...)` call in `createNewRoot` right after the two `insertItemSorted(rootSlot.Page(), ...)` calls; (4) enriching `descendToLeaf`'s internal-page branch (btree.go, the `findChildBlockDirect(slot.Page(), key)` call) to wrap a non-nil error with `blk=%d level=%d flags=%#x lower=%d upper=%d next=%d prev=%d trace=%s` (reading `op` already in scope plus `storage.MustHeader(slot.Page())` and `debugDumpTraceFor(cur)`) before unpinning. | THE decisive new fact: every reproduction's failing page shows the EXACT byte signature of a page fresh out of `storage.InitPage` that has never received real B-tree opaque content — `flags=0x0 lower=24 upper=8192 next=0 prev=0` (0 items, no BTLeaf/BTRoot/BTHasHighKey bit, Next/Prev both 0 rather than `storage.InvalidBlockNumber`=4294967295) — NOT a page that was once correctly populated and later corrupted/wiped. This held for TWO qualitatively different failing blocks: (a) `blk=25`, a brand-new split-right-page created exactly once (`SPLIT blk=1...blk=25 rightBlk=25 rightN=197 oldNext=21`) and immediately found empty by a concurrent reader; (b) `blk=1`, the ORIGINAL first leaf, which had already been legitimately split-and-repopulated with ~198 items FIVE separate times (`blk=1→2,4,6,16,31`) before a later reader found it fully blank. Case (b) rules out "the write path sometimes emits a bad rightOpaque/leftOpaque" (audited clean in loops 9-10 and again this loop: `rightOpaque.Flags = op.Flags & ^BTRoot` always preserves BTLeaf; `writeOpaque` on the left page never clears BTLeaf either) and instead points at the READ side resolving to the WRONG PHYSICAL BUFFER for a tag it believes it pinned correctly — i.e. two different `*storage.Slot` objects transiently satisfying the same `BufferTag` in `internal/storage/bufpool.go`'s `p.bm` (bufmap) / `Pool.Pin`/`pinLoad`/`claimVictim`/`evictVictim` machinery, OR a slot recycled out from under a tag while some other path still believes it holds the old content. This loop's static audit of `Pool.PinNew` (bufpool.go:1028), `pinSlow`/`pinLoad` (bufpool.go:1194/1239), `claimVictim`/`evictVictim` (bufpool.go:939/994), and `relFile.extend` (smgr.go:719, correctly serialises block-number allocation under `r.mu`, ruling out duplicate-blk-from-Extend) did not find the exact gap, but did newly correct loop 9's claim that no crash-recovery split-completion pass exists — `BTree.CompleteDeferredSplits` (btree.go:1724) DOES exist and is wired (just not called from the live insert path, only from maintenance/crash-recovery, so it does not explain this test's failure). | Recreate the instrumentation verbatim from the `landed` column (all in `internal/access/btree/btree.go` + one `debugTraceLog = nil` line at the top of `TestMultiWriterStress_M0055_Phase_C` in `multi_writer_stress_test.go`, after un-skipping it), then instead of reasoning further about the B-tree layer, move DOWN a layer into `internal/storage/bufmap.go`'s `Insert`/`Lookup`/`Delete` (named as "audited, no changes" in loop 10's working_set but never exercised against this exact failure signature) and `bufpool.go`'s slot lifecycle: add a SECOND diagnostic that records, per slot index, the sequence of (tag, gen, unique pointer identity of the returned `*storage.Slot`) every time `Pool.Pin`/`PinNew` returns one, and when `findChildBlockDirect` detects `count==0`, cross-reference whether any OTHER trace entry shows a DIFFERENT physical slot index having ALSO briefly held this exact tag around the same time — that directly confirms or refutes the duplicate-mapping hypothesis instead of further static reading. Cheap repro unchanged: `BTREE_DEBUG_TRACE=1 go test -run TestMultiWriterStress_M0055_Phase_C -count=500 -v ./internal/access/btree/...` (un-skip line ~40 first), ~180-220s wall time, observed 1-2 failures per 500-600 iterations. | Time-boxed per the Hard-won Rule against rushing a fix in this exact subsystem; this loop's mandate was specifically to test the stale-buffer/aliasing hypothesis loop 10 handed off (working_set.md), which it did, empirically, for the first time in this 11-loop thread — but confirming the failure signature is not itself a fix, and the exact bufmap/slot mechanism responsible is still unidentified, so no code change is warranted yet. |
| resolved | 2026-07-07 | M-NIGHTLY (AI-20260706-201855-001, 12th consecutive investigation loop) | (No code landed — pure investigation, all instrumentation reverted, `git diff --stat` confirmed clean.) Executed loop 11's handed-off next step and went one level further. Built a richer diagnostic than any prior loop: per-event trace calls in `internal/storage/bufmap.go`'s `Insert`/`Delete` (recording blk/slot/gen/bucket on every publish and tombstone) plus `internal/storage/bufpool.go`'s `claimVictim` (blk/wasValid/wasDirty/newGen at the moment a slot is claimed as a victim), `evictVictim` (implicitly covered via the `BM_DELETE` trace), `PinNew`/`pinLoad` (every publish, re-check-hit, and final valid-transition), and `Pin`'s fast-path CAS plus `pinSlow`'s `tryPinSlot` success (each asserting/logging a tag-mismatch if the pinned slot's `s.tag` differs from the caller's intended tag) — all gated behind `GOOPG_SLOTTRACE=1` env var, all in a new temporary `internal/storage/debugtrace_temp.go` (ring buffer + `DumpSlotTraceFor(blk)`), reverted via `rm` + `git checkout --` before finishing. Also added two call-site attribution markers in `internal/access/btree/btree.go` (`SITE_NEW_ROOT` in `createNewRoot`, `SITE_SPLIT_RIGHT` in `pinNewLocked`, the latter shared by both `pinNewOrRecycled`'s fresh-allocation branch and any other `pinNewLocked` caller) so a failing block's ORIGIN call site is directly visible in its trace, plus enriched `descendToLeaf`'s `findChildBlockDirect` error branch to attach `storage.DumpSlotTraceFor(uint32(cur))`. Un-skipped `TestMultiWriterStress_M0055_Phase_C` and ran it with `GOMAXPROCS=4` (constraining P count sharply increased the repro rate vs. loop 11's unconstrained-GOMAXPROCS runs: 0 failures in 850 iterations unconstrained, but 3 failures in 400 iterations and then 8 failures in 400 iterations once GOMAXPROCS=4 was added — full logs preserved this loop's session at `/tmp/slottrace_run1.log`, `/tmp/slottrace_run2.log`, `/tmp/slottrace_run3.log`, not committed). Captured 11 total forensic failures across 3 runs, EVERY ONE showing the IDENTICAL 3-phase signature for the failing block: (1) `BM_INSERT`+`PINNEW` (or, for blk=1, the tree's original root, created pre-concurrently by `CreateWithOptions`) — the block is created fresh; (2) `CLAIM slot=N blk=<X> wasValid=true wasDirty=true newGen=<G+1>` followed immediately by `BM_DELETE` — the SAME block gets evicted from the buffer pool by an unrelated `claimVictim` call, with the state machine reporting it as dirty (expected, since `PinNew` always stamps `slotDirtyBit` unconditionally at creation, before real content exists, so `wasDirty=true` alone does NOT prove real content was ever written — see caveat below); (3) `BM_INSERT`+`PINLOAD_PUBLISH_IOINFLIGHT`+`PINLOAD_VALID` at a NEW slot index — a cache-miss reload from disk, and THIS reload's content is what the failing reader observes as `flags=0x0 lower=24 upper=8192 next=0 prev=0` (a raw, never-populated `storage.InitPage` signature). The NEW, decisive finding this loop: of the 10 failures with a call-site marker (the 11th, blk=1, predates the markers since it's created by the single-threaded `CreateWithOptions` before the concurrent phase), ALL 10 show `SITE_SPLIT_RIGHT` — ZERO show `SITE_NEW_ROOT`. This conclusively REFUTES a `createNewRoot`-side mechanism (including the specific gap this loop's static audit flagged and then set aside — see `deferred` column) and CONFIRMS the corruption originates specifically in `insertIntoBlock`'s split-right-page allocation (`bt.pinNewOrRecycled()` → `pinNewLocked()`, btree.go:1493/692), which per loop 8's fix already returns the slot pre-content-locked and — per direct code re-audit this loop — is ALWAYS fully populated (`initPage` unconditionally at line 1513, items inserted, left page's `Next`/`HighKey` stamped, both `MarkDirty`'d, WAL-logged if wired) BEFORE either page is unlocked/unpinned, in the correct unlock order (right before left) that prevents a reader from following the left page's updated `Next` link before the right page's content is actually safe to read. Since `claimVictim` provably cannot select a slot while `statePin>0`, the observed eviction proves the split writer's own `Unpin` had already run (i.e., population legitimately completed, by this code's own logic) before the slot was reused — meaning the CORRECT, POPULATED content should have been flushed to disk by `evictVictim`'s `wasDirty` branch (`internal/storage/bufpool.go`'s `flushSlot`→`Manager.WriteBlock`→`relFile.writeBlock`, all serialized under the SAME `relFile.mu` as the original `relFile.extend` call from `PinNew`, so no write-reordering-on-the-same-fd explanation survives a first pass) — yet the reload reads back virgin/empty bytes. | The exact mechanism by which a demonstrably-populated, correctly-unlocked, dirty-marked split-right page reads back as a raw uninitialized page after an evict+reload cycle is STILL not identified — this loop narrowed the search space from "the whole buffer pool + both allocation call sites" (loops 9-11's scope) down to specifically "does insertIntoBlock's split-right-page content actually survive its own eviction+reload roundtrip", but did not instrument the CONTENT itself (line-pointer count / byte sample) at the two decisive checkpoints: (a) immediately before `evictVictim`'s `flushSlot` call (does the in-memory `s.page` the flush is about to write already read as empty at that instant — meaning the corruption happened BEFORE eviction, inside the split's own populate-then-unlock window, e.g. a stale-`[]byte`-alias write clobbering it after population but before/during unlock) vs. (b) immediately after `relFile.writeBlock`'s `WriteAt` returns and again immediately after the SUBSEQUENT `relFile.readBlock`'s `ReadAt` returns (does the disk round-trip itself lose the bytes — a smgr/relFile bug, not a btree-layer bug). Also flagged but NOT tested: the `insertIntoBlock` "dedup avoids the need for a split" early-exit (btree.go:1531-1547) legitimately abandons a freshly-`initPage`'d (but zero-item) `rightSlot` without ever linking it into the tree — a genuine leaked/orphaned block (never reachable via any downlink, so not a candidate for THIS bug, but wastes a block number every time dedup avoids a split — worth a cheap follow-up fix independent of this investigation). Also re-confirmed but explicitly set aside as unproven: `createNewRoot` (btree.go:1889-1893) still calls the raw `bt.pool.PinNew(bt.rel)` followed by a SEPARATE `rootSlot.Lock()` two lines later, instead of `bt.pinNewLocked()` like `pinNewOrRecycled` uses — the exact "populate-before-lock" gap loop 8's own doc comment on `pinNewLocked` warns a fresh PinNew allocation would reopen — but static re-audit this loop (and the 0-of-10 `SITE_NEW_ROOT` hit rate) confirms this gap is NOT exploitable for THIS bug: `PinNew`'s caller (createNewRoot itself) holds the ONLY pin on the new root block throughout the gap (pinCount=1, so `claimVictim` cannot select it), and no downlink/meta.Root makes the new root reachable to any OTHER goroutine until strictly after full population+unlock+unpin, per code read at btree.go:1919-1941. Worth a cheap, zero-risk consistency fix (`createNewRoot` → `bt.pinNewLocked()`) independent of this investigation, but confirmed not the cause. | Resume point: reuse this loop's exact instrumentation (recipe fully specified above: `bufmap.go` Insert/Delete traces, `bufpool.go` claimVictim/PinNew/pinLoad/Pin/pinSlow traces, `btree.go`'s `SITE_SPLIT_RIGHT`/`SITE_NEW_ROOT` markers and `descendToLeaf`'s `DumpSlotTraceFor` error-enrichment, all gated on `GOOPG_SLOTTRACE=1`, run via `GOMAXPROCS=4 GOOPG_SLOTTRACE=1 go test -run TestMultiWriterStress_M0055_Phase_C -count=400 -timeout 20m ./internal/access/btree/...` — this GOMAXPROCS=4 constraint measurably raised the repro rate this loop, worth keeping for future attempts), but ADD two more trace points: (1) inside `internal/storage/bufpool.go`'s `evictVictim`, immediately before the `flushSlot` call, sample `storage.PageLinePointerCount(s.page)` (or a raw first-32-bytes hex dump) and log it — if this ALREADY reads as empty/zero at flush time, the bug is upstream in the btree split's populate-then-unlock sequence (next audit target: `insertIntoBlock`'s right-item insertion loop at btree.go:1564-1567 and everything between `initPage` (1513) and `Unlock` (1668) for a write that gets silently reverted, e.g. a `slot.Page()` call returning a stale/aliased backing array — re-examine `pageItems`/`dedupConsolidate`/`compactRawSize`'s handling of `rightItems`, none of which were the focus of loops 9-10's `pageItems`/`parseItem` copy-safety audit, which only covered the LEFT-side dedup path); (2) inside `relFile.writeBlock`/`extend` (smgr.go) and `relFile.readBlock` (smgr.go:~680-701), log a checksum/hash of the buffer just written/read (e.g. a simple sum of the first 64 bytes) keyed by blk — if the WRITE's logged content is non-empty but the SUBSEQUENT READ's logged content is empty for the SAME blk, the loss is in the disk I/O layer itself (candidates: `r.f` file-handle coherency, a stale `r.nblocks` short-circuiting a read before the real write lands, or an `O_DIRECT`/buffering assumption violated by the test's tmp-dir filesystem). This dichotomy (in-memory-already-empty vs. disk-roundtrip-loses-it) is the single most valuable measurement the next loop can take — it was time-boxed out of this loop by the effort already spent building and running the slot/tag-level instrumentation. | Time-boxed per the Hard-won Rule against rushing a fix in this exact subsystem (documented history of rushed-fix regressions, `M0055-0004-followup-stage2-splitmu-removal` comment in btree.go:601-613). This loop's mandate was specifically loop 11's handed-off next step (bufmap/slot-level tracing to confirm/refute a duplicate-mapping bug) — that specific hypothesis was REFUTED (no `BM_INSERT_DUP_REFUSED` ever fired, no tag-mismatch ever fired across all 3 runs), but the SAME instrumentation, extended with a call-site marker, produced a much more valuable and unexpected finding (conclusive `SITE_SPLIT_RIGHT` attribution + proof the corruption survives a real evict+reload roundtrip) that redirects the investigation toward a narrower, more mechanical next question (in-memory vs. on-disk content loss) rather than the broad "which buffer-pool subsystem" framing every prior loop used. |
| resolved | 2026-07-07 | M-NIGHTLY (AI-20260706-201855-001, 13th consecutive investigation loop) | Executed loop 12's exact next step (checksum/line-pointer sampling immediately before `evictVictim`'s `flushSlot` call, and after `relFile.writeBlock`/`readBlock`'s `WriteAt`/`ReadAt`) via slot-index-keyed tracing (`CLAIM_VICTIM`/`BM_DELETE`/`BM_INSERT`/`PIN_FAST_HIT`/`PIN_SLOW_HIT`/`READ_AFTER_READAT`/`WRITE_AFTER_WRITEAT`, all gated `GOOPG_SLOTTRACE=1`, `internal/storage/debugtrace_temp.go` + call sites in `bufmap.go`/`bufpool.go`/`smgr.go`). Isolated single-process repros (build the test binary once via `go test -c`, loop invoking it standalone so each failure's trace is uncontaminated by other test iterations) caught the exact race on the first two attempts: for the failing block, `BM_DELETE` (removing the tag from bufmap) was followed immediately by a DIFFERENT goroutine's `BM_INSERT` (same tag, a NEW slot index) and `READ_AFTER_READAT` (empty/virgin checksum) — all of which landed BEFORE the ORIGINAL slot's own `WRITE_AFTER_WRITEAT` (valid, real content checksum) completed. Root cause: `evictVictim` (`internal/storage/bufpool.go`) called `p.bm.Delete(oldTag, victimIdx)` BEFORE `flushSlot`'s `WriteAt`, so a concurrent `Pin(oldTag)` racing in during the flush saw a bufmap miss (instead of correctly blocking on the slot's IO-inflight semaphore, since the tag was already gone) and took its own unordered disk read via `pinLoad`, sometimes winning the race against the still-in-flight write and permanently caching pre-flush (virgin) content under a different slot. **FIXED and LANDED**: moved `bm.Delete`+tombstone accounting to AFTER `flushSlot` returns, and added a semaphore-release loop for any waiters that queued during the flush (reuses the existing IO-inflight-wait mechanism `pinLoad` already relies on — no new synchronization primitive). Also permanently un-skipped `TestMultiWriterStress_M0055_Phase_C` (previously gated behind `t.Skip("M0056-followup-multiwriter-flake...")`) as this bug's regression guard, and added blk/rel context to `descendToLeaf`'s "empty internal page" error (`btree.go`) for future diagnosis. All forensic tracing (`debugtrace_temp.go` + call sites) reverted after capture; `git diff --stat` shows only the real fix + the permanent test un-skip + the one-line error enrichment. | The nightly-reported symptom itself (`btree: item length mismatch keyLen=9 total=37` under `pgbench -c 100 -j 20` at scale=50) — manually re-ran the authoritative repro (fresh datadir, `pgbench -i -s 50`, `pgbench -T 120 -c 100 -j 20`) against the SAME fixed binary post-fix and it failed identically (~100/100 clients aborted within seconds, same signature plus a `keyLen=2272/2265/2267 total=37` variant and one `short read at block`). This confirms (4th time now, after update #6/#7/#8's fixes) that the empty-internal-page bug just fixed is a genuinely separate root cause from the pgbench-scale keyLen-mismatch flake. | `internal/access/btree/btree.go`'s split path (`insertIntoBlock`, ~1420-1660, under `bt.splitMu`) — instrument it using the cheap repro from the 2026-07-07 update-#6 ledger row (`pgbench -i -s 50` once, reuse that DB, then `pgbench -c 100 -j 20 -T 25 -P 5`, reproduces in ~10s) rather than the insert-only unit test (which only reproduces the now-fixed empty-internal-page symptom, unrelated to this one). | Validated the fix is real and general (400+300 plain-run + 60+40 race-detector-run zero-failure samples vs. a clear pre-fix ~1-in-10-to-20 failure rate on the identical recipe, full targeted `go test` PASS, `tpch-spotcheck.sh` PASS) but is architecturally unrelated to the nightly item's actual failure mode — TPC-B's UPDATE-heavy contention on tiny shared hot tables (`pgbench_branches`/`pgbench_tellers`, per `[[goopg_no_hot_update_index_reeval]]`) drives split-path churn the insert-only disjoint-key stress test structurally cannot reach, so a fresh, split-path-focused instrumentation pass is needed rather than continuing the buffer-pool-eviction thread this loop just closed out. |
| resolved | 2026-07-07 | M-NIGHTLY (AI-20260706-201855-001, 14th consecutive investigation loop) | (No code landed — pure investigation, all instrumentation reverted, `git status`/`git diff` confirmed clean of the debug files.) Executed update #11's exact handed-off next step (instrument `insertIntoBlock`'s split path using update #6's cheap pgbench repro) and got a genuinely new, concrete finding. Built a forensic dump helper (`internal/access/btree/parse_err_dump.go`, `maybeDumpPageOnParseErr(p storage.Page, ctx string)`, gated on `GOOPG_BTREE_PARSE_ERR_DUMP=1`) wired into every `parseItem`/`readPageItem` error branch in `btree.go` (`pageItems`, `PageItemKeys`, `PageLeafEntries`, `PageDownlinks`, `findChildBlockDirect`, `readPageItem`) — on a parse failure it writes the page's decoded opaque header, the full decoded line-pointer table (slot/len/raw-hex for every entry), and a full page hex dump to `/tmp/btree-parse-err-<pid>-<seq>.dump`. Ran update #6's exact cheap repro fresh (own build/datadir/port, not the shared stage script): `pgbench -i -s 50` once (confirmed the load itself completes cleanly), then `pgbench -c 100 -j 20 -T 25 -P 5` — reproduced `btree: item length mismatch keyLen=9 total=37 (blk=<N> rel={DBOid:5 RelOid:16450 Fork:0})` within ~10s, ~26 aborted clients, capturing 96 forensic dumps. Cross-verified `RelOid:16450` against the live catalog (`select oid,relname from pg_class where oid=16450` → `pgbench_accounts_pkey`, an int4-keyed index whose real item size should be `itemPrefixSize(8)+4=12` bytes) and independently `dd`'d the raw on-disk bytes of several distinct reported blocks (8766, 3328, 5365, 15544) directly from `base/5/16450` on disk — **the on-disk bytes exactly match the buffer pool's decoded content byte-for-byte**, proving the corruption is genuinely persisted (not a read-time/buffer-aliasing artifact re-confirmed in update #11's now-fixed bug). Manually unpacked several raw `ItemID` entries from the hex dump (`unpackItemID`'s bit layout: offset\|flags\<\<15\|length\<\<17) and found every single line pointer on these pages reports `Length=37`, with successive item offsets spaced exactly 40 bytes apart (8152, 8112, 8072, ... i.e. a constant 40-byte stride) — that is precisely `MAXALIGN(37)=40` (round up to the next multiple of 8), which is the alignment scheme `internal/storage/heap.go`'s `PageAddHeapTuple` explicitly documents applying to HEAP tuples (`alignedSize = MAXALIGN(size)`), NOT the btree package's own item layout, whose own source comment (`btree.go` `MaxItemsPerPage` doc) states items are deliberately stored unaligned with no MAXALIGN term. The decoded opaque header for every one of these corrupted pages, regardless of which distinct on-disk block was reported, is near-identical and structurally impossible for this tree: `BTRoot` flag set + `Next=storage.InvalidBlockNumber` (plausible for a root) but `Level=262144` (impossible — a 5M-row tree's root level is a small single-digit number) and `Flags=0x802`, where bit `0x800` is not a defined value anywhere in the `BTPageOpaque` flag const block (`BTLeaf..BTHalfDead` only span `0x0001..0x0020`). Manually decoding one 37-byte "item"'s bytes (`keyLen=9,block=0,offset=0` per the `item.marshal` layout) against the item's own remaining 29 "key" bytes found something decisive: bytes 9-24 of that key (16 bytes) are **byte-for-byte identical** to the page's own decoded `Prev(0)/Next(0xFFFFFFFF)/Level(262144)/Flags(0x802)/HighKeyLen(24)` quintuple — i.e. the item content and the page's opaque-header content share the exact same anomalous bytes. | The genuine, still-unidentified root cause of the pgbench keyLen-mismatch flake is very likely **heap-relation content landing in the index relation's on-disk blocks** (or some other foreign, MAXALIGN'd structure being written where btree item bytes belong) rather than a plain buffer-pool tag/eviction race (all of which were already audited/refuted or fixed across updates #6-#11 for the DIFFERENT empty-internal-page symptom) — this is a materially narrower and more specific hypothesis than any prior row in this 14-loop thread, but the exact WRITE-PATH mechanism that could place heap-shaped bytes into the pkey index's `PinNew`/`Extend`/flush path was not located this loop. Audited-and-cleared-as-NOT-the-mechanism this loop: `internal/storage/smgr.go`'s `Manager.relFile` (keyed by the full `RelFileNode` struct including `Fork`, opens the correct per-relation `path` unconditionally — no cross-relation file-handle sharing possible); `internal/storage/bufmap.go`'s `Lookup`/`Insert`/`Delete` (every match requires an exact `key0==wantKey0 && key1==wantKey1` check under a seqlock-style snapshot — no hash-collision false-positive path found); `PinNew`'s `pinMu`-released window between `Extend()` returning and the caller's `pinNewLocked`'s `slot.Lock()` (content during that window is a legitimate blank `InitPage`'d page, per-code-read, not heap-shaped, so it does not by itself explain heap-shaped bytes appearing later). NOT yet audited: the executor-level code path that performs a heap INSERT plus its primary-key index-maintenance INSERT together for `pgbench_accounts` — specifically whether any per-statement/per-connection `*btree.BTree` instance, `RelFileNode`, or buffer-pool target reference could be shared or reused across concurrent backends incorrectly under contention; also not yet audited: whether `MarkDirtyChangeRecord`'s logical "change record" WAL emission (`logHeapInsert`/`logBtreeInsert` etc.) or its potential replay path could ever route a heap-insert's payload into a call that mutates a btree page's bytes. | Reconstruct `internal/access/btree/parse_err_dump.go` (full source given in this row's `landed` column: a `maybeDumpPageOnParseErr(p storage.Page, ctx string)` helper gated on `GOOPG_BTREE_PARSE_ERR_DUMP`, dumping decoded opaque + full decoded line-pointer table + hex page dump to `/tmp/btree-parse-err-<pid>-<seq>.dump`) and its 6 one-line call-site wires (`pageItems`/`PageItemKeys`/`PageLeafEntries`/`PageDownlinks`/`findChildBlockDirect`/`readPageItem` in `btree.go`, each on the `perr != nil` branch immediately before `return ..., perr`), then re-run update #6's exact cheap repro (`pgbench -i -s 50` once against a fresh server on an isolated port — 5433/5434/5544/5561/5599 were all occupied by other live goopg/PG instances this loop, pick a genuinely free port via `ss -ltn`; then reuse that DB for `pgbench -c 100 -j 20 -T 25 -P 5`, reproduces in ~10-25s) with the dump env var set. With a fresh dump in hand: (1) byte-compare the item's embedded 16-byte "fake opaque" fragment (and the rest of the 37-byte item) against what `internal/storage/heap.go`'s actual heap-tuple marshal function produces for a real `pgbench_accounts` row (aid/bid/abalance/filler columns) — an exact byte match is the smoking-gun confirmation this is genuinely heap content, not coincidental; (2) if confirmed, audit the executor's combined heap-insert-plus-index-maintenance call site (likely in `internal/executor/`, search for where `pgbench_accounts`' `INSERT` triggers both a heap `PageAddHeapTuple`-style write and a `btree.BTree.Insert` call in the same statement) for any shared/reused relation-identity or buffer-pool-target state that could leak across concurrent backends; (3) if NOT a byte-for-byte heap-tuple match, reconsider whether the 37-byte/MAXALIGN(40)-stride structure instead matches some OTHER heap-adjacent format (FSM, a WAL record, a different table's row shape) before ruling heap-content out entirely. | Time-boxed per the Hard-won Rule against rushing a fix in this exact subsystem (documented history of rushed-fix regressions). This loop's mandate was update #11's handed-off next step (instrument the split path with the cheap pgbench repro, never actually executed across loops 9-13 because they pivoted onto the now-resolved empty-internal-page thread) — executed it for the first time this loop, and rather than open-endedly re-tracing the same buffer-pool machinery already exhaustively audited for the OTHER symptom in updates #9-#11, took the forensic-dump approach (capture the actual corrupted page bytes, not just event traces) which surfaced the MAXALIGN/heap-tuple-shape signature — a materially different and more specific class of hypothesis than "some buffer-pool race", worth handing to the next loop with a precise byte-level verification step rather than continuing to guess at the mechanism under this loop's remaining time budget. |
| resolved | 2026-07-07 | M-NIGHTLY (AI-20260706-201855-001, 15th consecutive investigation loop) | Reconstructed update #12/loop 14's forensic dump helper (`internal/access/btree/parse_err_dump.go`'s `maybeDumpPageOnParseErr(p storage.Page, ctx string)`, gated `GOOPG_BTREE_PARSE_ERR_DUMP=1`, wired into all 6 `perr != nil` branches: `pageItems`/`PageItemKeys`/`PageLeafEntries`/`PageDownlinks`/`findChildBlockDirect`/`readPageItem` in `btree.go`) — and this time **KEPT it committed rather than reverting** (judgment call, see `why`). Ran the cheap repro fresh (own isolated build/datadir/server on port 5561, confirmed free via `ss -ltn`; `pgbench -i -s 50` once ~4min, then `pgbench -c 100 -j 20 -T 25 -P 5`) — reproduced `btree: item length mismatch keyLen=9 total=37 (blk=<N> rel={DBOid:5 RelOid:16412 Fork:0})` within ~10s (note: RelOid 16412 this run vs. 16450 in loop 14 — confirms the bug is not tied to one specific relation OID/file), captured 100 forensic dumps. Executed update #12's exact next step: byte-compared the full 37-byte raw item content (not just the 20-byte "fake opaque" fragment loop 14 had time for) against `internal/storage/heap.go`'s `HeapTupleHeader`/`MarshalBinary` on-disk layout for a real `pgbench_accounts` row. **CONCLUSIVE MATCH, upgrading loop 14's "consistent with" to "confirmed"**: decoding slot 1's raw bytes as a `HeapTupleHeader` gives `Xmin=9` (a small, entirely plausible transaction ID), `Xmax=0`, `Xvac=0`, `CTID={Block:4294967295(InvalidBlockNumber), Offset:0}` (exactly `NewHeapTuple`'s pre-insert default), `Infomask2&HeapNattsMask=4` (matches pgbench_accounts's 4 columns: aid/bid/abalance/filler), `Infomask=0x0802`, and `Hoff=24` — **exactly** `DefaultHeapTupleHoff`=`MAXALIGN(SizeOfHeapTupleHeaderData=23)`=24. The bytes immediately following (the data region starting at `hoff=24`) decode as `aid=3706034` (int4 LE) — within scale-50's 1..5,000,000 range — then `bid=38` — within scale-50's 1..50 range. Five independently-checkable structural fields (the `Xvac`=0 sentinel, `CTID`'s exact `InvalidBlockNumber` pre-insert default, `natts=4`, `Hoff` landing exactly on `MAXALIGN(23)`, and two in-range column values) all decoding correctly simultaneously rules out coincidence. Also newly noted: the corrupted line pointer's own reported offset (8152) plus length (37) place this "item" entirely INSIDE the page's special/opaque region (`btSpecialOffset`=7920..8192, `SizeOfBTPageOpaque`=272) — the writer that put heap bytes here did not respect this btree page's `Special()` boundary at all, consistent with a heap-insert routine (`PageAddHeapTuple`, which only knows about `h.Special()`, never a btree-specific reservation) writing into a page whose `pd_special` was 8192 (a bare/heap-shaped page, PG's own default) rather than btree's 7920 — i.e. the page's TYPE/IDENTITY was wrong at the moment of the write, not just misread later. Verified `go build ./...` clean and the full `go test ./internal/access/btree/...` package suite (2.0s) PASS after the wiring change; cleaned up the isolated test server/datadir/binary and all dump files before finishing. | Still not located: the exact executor/storage call site where a heap-relation write lands on a btree-index-relation's block. NOT yet audited this loop (time-boxed): `internal/executor/operators_storage.go` and `operators_upsert.go` (located as the files that call both a heap mutation and `btree.BTree` methods) were identified as the next audit target but not read in detail — need the exact sequence by which the INSERT execution path obtains the `*storage.Pool`/`RelFileNode` handle for the heap file vs. the pkey index file, and whether a per-statement/per-connection cached handle or variable meant for one could ever be passed to the other's write call under concurrency. | `internal/executor/operators_storage.go` / `operators_upsert.go`: read the INSERT execution path end-to-end for (1) where the heap-file and pkey-index-file `RelFileNode`/handle are each obtained — two genuinely independent derivations, or is there a route where a heap-target reference leaks into the `bt.Insert` call (or vice versa)? (2) reproduce again with the now-kept `parse_err_dump.go` (`GOOPG_BTREE_PARSE_ERR_DUMP=1`, same cheap repro) and additionally log, from the executor side, every `(heap RelOid, index RelOid, blk)` triple passed into any storage-layer write during the same 10-25s window, to directly catch the moment a write meant for RelOid X lands on RelOid Y's file. | Time-boxed per the Hard-won Rule against rushing a fix in this exact subsystem (documented history of rushed-fix regressions). This loop's mandate was update #12's exact handoff (byte-compare against heap.go's tuple format) — executed it more thoroughly than time allowed for in loop 14 (a full 37-byte structural decode against 5 independent header fields, not just the 20-byte fragment), upgrading the finding from "consistent with heap content" to "conclusively confirmed heap content" — a strictly stronger evidentiary basis for the next loop's executor-side audit. Also made the judgment call to KEEP `parse_err_dump.go` committed (a deliberate deviation from the loops-9-14 "revert all instrumentation" pattern): two consecutive loops (12, then 14) had to rebuild an equivalent forensic-dump tool from scratch after the prior instance was reverted, a real and now-measured cost; the tool is env-gated and inert by default, so keeping it is low-risk and saves the reconstruction step for whichever future loop needs it again. |
| resolved | 2026-07-07 | M-NIGHTLY (AI-20260706-201855-001, 16th consecutive investigation loop) | (No code landed — pure investigation; no instrumentation built or run this loop, so no build/test gates needed re-running beyond a final `go build ./...` sanity check, which stayed clean.) Executed loop 15's exact handoff: read `internal/executor/operators_storage.go`'s `insertOp.Next` (INSERT path, lines ~1824-2153) and `updateOp.updateViaIndex` (the index-driven UPDATE path pgbench's `UPDATE ... WHERE aid=:aid` actually takes, lines ~3576-4132) end-to-end, tracing every `rel`/`idxRel`/`targetWriteRel` derivation and every `Pin`/`RLock`/`RUnlock`/`Unpin`/`Lock`/`Unlock` pairing. Result: **the heap-vs-index RelFileNode-crossing hypothesis is REFUTED for these two call sites** — `rel := ctx.Catalog.RelFileNode(tbl)` (heap) and `idxRel := ctx.Catalog.IndexRelFileNode(idx)` (each index) are freshly, independently re-derived from distinct `table.OID`/`index.OID` values on every call (confirmed `catalog.go:17101-17116`: no caching, no shared mutable field); `updateViaIndex`'s `tree.RangeScan` callback only COLLECTS matches into a `pendingUpdate` slice (blk/slot/newRow/oldRow) and defers all writes (`writeHeapRowReturning`, `maintainUniqueIndexesForInsert`) to AFTER `RangeScan` fully returns (all its pins released) — so the RangeScan contract comment at btree.go:2358 ("none re-enter the btree") is honoured, not violated, by this caller; every `slot`/`s`/`s2` obtained via `Pool.Pin` in both operators is locked, used, and `Unlock`+`Unpin`'d within a tightly-scoped block with no retained byte-slice or `*Slot` escaping across an Unpin boundary. Given this ruled out the assigned hypothesis, spent the remainder of the loop pushing one level down per the natural next question ("if not the executor, is it the buffer pool's own tag/pin bookkeeping, re-verified from scratch rather than trusting loops 9-13's conclusions") and re-derived, independently, that: `claimVictim`/`tryPinSlot`/`Unpin`/`PinNew`/`pinLoad` (`internal/storage/bufpool.go`) are all correct lock-free CAS state machines (IO-bit gates concurrent claims, gen field guards ABA, `Unpin` panics on underflow) with `storage.InitPage` unconditionally zeroing all 8192 bytes before any btree-specific opaque stamping — confirming fresh (non-recycled) index pages cannot carry stale bytes at allocation time. Separately and newly PROVEN (not merely assumed, as loop 6/13's "confirmed NOT exercised" note implied): `BTree.freeList`/`pinNewOrRecycled`'s recycle branch is structurally UNREACHABLE for the pgbench workload — every `btree.Open()` call site in the executor allocates a brand-new `*BTree` Go struct (`catalog.go`-derived `idxRel` passed fresh each time; confirmed via every call site of `btree.Open(ctx.Pool, idxRel)` in `operators_storage.go`/`operators_upsert.go`), so `bt.freeList` starts nil/empty on every single call and can only be populated+drained within the SAME long-lived `*BTree` handle — which only `VacuumIndexPages`'s single call bridges; no VACUUM runs during the 25s pgbench window, so `pinNewOrRecycled` always takes the fresh-`PinNew` branch (fully zeroed, per above) for this repro, closing off the recycle-path hypothesis with certainty rather than probability. Also confirmed `BTree.rel` is set once at `Open`/`Create` time from the caller's own freshly-derived `RelFileNode` and threaded unchanged through every `pinR`/`pinW` call for that instance's lifetime — no cross-instance aliasing possible since instances are never shared or pooled. Also confirmed `upsertOp.leafTrees` (a per-statement `map[uint32]*btree.BTree` cache flagged as a superficially similar risk) is operator-instance-scoped (one `upsertOp` per statement execution, never shared across connections) and irrelevant to plain pgbench TPC-B anyway (no `ON CONFLICT` in the standard script). | The exact mechanism placing heap-shaped bytes into an index page's on-disk special/opaque region (loop 15's conclusive finding) is STILL not identified. This loop closes off the two hypotheses it was assigned to test (executor-level RelFileNode/handle crossing; buffer-pool tag/pin/zeroing correctness) with high confidence via direct code re-derivation, and additionally closes the recycle-path question from "assumed inert" to "proven structurally unreachable" — but lands no fix and identifies no new candidate mechanism beyond what loop 14 already flagged as unaudited (`MarkDirtyChangeRecord`'s logical WAL emission/replay path) and what loop 12 flagged as the more mechanical open question (does the split-right-page's content genuinely survive its own evict+reload roundtrip, or is the loss upstream in `insertIntoBlock`'s populate-then-unlock window). | Two concrete options for the next loop, in priority order: (1) Execute loop 12's still-outstanding checksum/line-pointer-sample instrumentation (recipe fully specified in the 12th-loop ledger row: sample `storage.PageLinePointerCount`/a byte hash of `s.page` immediately before `evictVictim`'s `flushSlot` call, and again immediately after `relFile.writeBlock`'s `WriteAt`/`relFile.readBlock`'s `ReadAt`, keyed by blk) — this dichotomy (in-memory-already-wrong vs. disk-roundtrip-loses-it) was never actually executed across loops 13-15 (they pivoted onto the empty-internal-page thread, now resolved, then the byte-shape confirmation, now also resolved) and remains the single most decisive unrun measurement. (2) Audit `internal/executor/operators_storage.go`'s canonical-WAL emission helpers (`emitCanonicalHeapInsert`/`emitCanonicalHeapDelete`, called from both `insertOp` and `updateViaIndex` right after every heap write) and their shared logical-record plumbing (`ctx.LogCanonical`, `MarkDirtyChangeRecord` in btree.go, `MarkDirtyLogicalChange` in bufpool.go) for a path where a heap-insert's logical payload could be mis-routed to a btree page during WAL replay or change-record application — flagged as unaudited by loop 14 and still untouched. Use loop 15's now-committed `GOOPG_BTREE_PARSE_ERR_DUMP=1` dump tool (`internal/access/btree/parse_err_dump.go`) plus the same cheap repro (`pgbench -i -s 50` once on a freshly-confirmed-free port via `ss -ltn`, then `pgbench -c 100 -j 20 -T 25 -P 5`, ~10-25s to failure) to correlate either investigation's findings against a fresh forensic dump. | Time-boxed per the Hard-won Rule against rushing a fix in this exact subsystem (documented history of rushed-fix regressions, `M0055-0004-followup-stage2-splitmu-removal` comment). This loop's mandate was loop 15's exact handoff (audit the executor INSERT/UPDATE path for a stale-handle/RelFileNode-crossing bug) — executed thoroughly and refuted with specific evidence (not just "looked fine"), then used remaining budget to re-derive (rather than merely re-cite) the buffer-pool correctness loops 9-13 established, closing the recycle-path question from probable to certain — a smaller but real narrowing of the search space, in a subsystem where premature/rushed fixes have previously caused regressions, so no code change was attempted without a concrete located mechanism. |
| resolved | 2026-07-07 | M-NIGHTLY (AI-20260706-201855-001, 17th consecutive investigation loop) | **ROOT CAUSE FOUND AND FIXED.** Executed loop 16's handoff option (1): built `internal/storage/io_trace.go` (`GOOPG_IO_TRACE=1`-gated, kept committed like the loop-13/15 dump tool precedent), recording a content hash + `storage.PageLinePointerCount` at three points — `evictVictim`'s pre-flush (`bufpool.go`), and immediately after `relFile.writeBlock`/`readBlock`'s `WriteAt`/`ReadAt` (`smgr.go`) — and wired the btree parse-err dump tool (`parse_err_dump.go`) to look up any trace event matching the corrupted page's own content hash. First fresh repro (own isolated build/datadir/port 55611; `pgbench -i -s 50` once, `pgbench -c 100 -j 20 -T 25 -P 5`, reproduced in ~10s, 96 dumps): every one of 86 unique corrupted-page hashes had exactly ONE matching trace event — a `postRead` under the pkey index's own RelOid at the exact reported block — and NEVER a matching `preFlush`/`postWrite`, meaning whatever wrote the corrupted bytes bypassed both instrumented paths. Extended tracing to `relFile.extend`/`extendBatch` (also never matched for the corrupted hash) and added an uncapped-by-hash "last 2s, all tags" recent-activity dump. Re-ran fresh (full reinit after the mid-investigation server had to be `kill -9`'d once when a stale/dirty-shutdown data dir hung on restart — discarded, not reused): block 2464 of `pgbench_accounts_pkey` showed `preFlush` (valid btree content, 247 line pointers, t=28.198) → `postWrite` (same content, checksummed copy, t=28.211) → **270ms gap with zero recorded write/extend/postRead events for ANY tag matching this content** → `postRead` (corrupted content, 185 line pointers, t=28.481, matching the crash). The only write path never instrumented was `Pool.flushBatch`'s `Manager.WriteBlockAIO` call (`bufpool.go:1693`) — the checkpointer/bgwriter's batched-flush path invoked from `FlushAllPaced` (`bufpool.go:1619`). Read `FlushAllPaced` end-to-end: it scans every slot ONCE up front via an **unlocked** `state.Load()` + `p.slots[i].tag` read, collecting `(idx, tag)` pairs into a worklist, then processes that worklist in later batches via `flushBatch`. Between the scan and a given slot's batch actually running, ordinary buffer-pool churn (`claimVictim`/`evictVictim`/`PinNew`/`pinLoad`) can fully evict and repurpose that exact slot for a DIFFERENT relation (e.g. a heap page) before its batch turn comes up; `flushBatch` then takes `contentMu.RLock()` (correctly synchronized — no data race, which is why 1180+ race-detector iterations across loops 9-15 never caught this) and writes whatever the slot NOW holds under the STALE captured tag via `WriteBlockAIO` — silently writing one relation's page bytes into a different relation's file at a page-aligned offset with a validly-recomputed checksum (explaining why checksums, enabled by default, never flagged it: the checksum matches the wrong-but-real content it was stamped over). The pre-existing `if s.tag == tags[i] { clear dirty bit }` guard immediately after the write already silently acknowledged this staleness window (to avoid corrupting dirty-bit bookkeeping for a since-repurposed slot) but did nothing to stop the wrong-content write itself. **FIXED**: `flushBatch` now computes `stale[i] := s.tag != tags[i]` once, immediately after all slots' `contentMu.RLock()`s are held (race-free for the remainder of the call, since any write-lock repopulation would block on that RLock), and skips `WriteBlockAIO`/WAL-LSN accounting/dirty-bit-clear for every stale slot instead of writing its new content to the old tag's (rel, block). Gates: `go build ./...` clean, `go vet ./...` clean, `go test ./internal/storage/... ./internal/access/btree/... ./internal/wal/...` PASS. Verification beyond unit tests: fresh `pgbench -i -s 50` init against the fixed binary, then **two consecutive** `pgbench -c 100 -j 20 -T 25 -P 5` runs (the exact authoritative nightly repro, previously ~100% reproducing within seconds every single time across all 17 loops) — **0 failed transactions, 0 errors, both runs** (25019 then 34169 transactions), with server-log-confirmed checkpoints firing during the run (one spanning 4061ms — exactly the scan-then-batch window the bug needed). Mandatory `scripts/ralph-precommit-test.sh` (`RALPH_PRECOMMIT_SCOPE=smoke`) also PASS across all three CI workloads. All temporary datadirs/binaries/dump files cleaned up; `internal/storage/io_trace.go` kept committed (env-gated, single cached bool check when unset, same precedent as the loop-13 dump tool). Also added `TestFlushBatchSkipsStaleTag` (`internal/storage/storage_test.go`) — a white-box (`package storage`) test that directly repurposes a dirtied slot's tag/content mid-flight (bypassing the clock-sweep's non-deterministic victim selection for a deterministic repro) and calls `pool.flushBatch` with the stale pre-repurpose tag, asserting both that the wrong-relation write never lands on disk (verified via a second `Manager` opened on the same data dir, bypassing the pool cache) and that the dirty bit stays set for the skipped slot. Confirmed this is a real, non-vacuous regression guard: `git stash push -- internal/storage/bufpool.go` (reverting only the fix) makes the test fail exactly as predicted (`relOld block 0 byte 100 = 0xbb, want 0x00`), and `git stash pop` restores the pass. | This fix mirrors PG's `SyncOneBuffer` re-validate-under-lock spirit but is a narrower, goopg-specific patch, not a full port of PG's `BM_CHECKPOINT_NEEDED`/refcount checkpoint protocol (`bufmgr.c`) — worth a closer diff against upstream if checkpoint-path bugs recur. | Diff `flushBatch`/`FlushAllPaced` against upstream `SyncOneBuffer`/`BufferSync` (`postgres/src/backend/storage/buffer/bufmgr.c`) for any other checkpoint-scan-vs-flush hazard the goopg-specific rewrite may have missed (e.g. PG's `BM_CHECKPOINT_NEEDED` flag semantics vs. goopg's plain per-slot dirty bit). | Not a blocker for this fix — the stale-tag hazard itself is now closed and regression-tested; this is a follow-up hardening pass, not required to resolve the nightly item. |
| resolved | 2026-07-07 | AI-20260707-000712-005 | `internal/planner/bushy.go`: `remapByPosMap` now remaps `*ExistsExpr`/`*SubqueryExpr`/`*ArraySubqueryExpr` outer refs (new `remapOuterRefsInSubplan`) so a correlated EXISTS/scalar-subquery's `OuterColumnRef.Index` tracks the `MultiHashJoin` rewrite's column reordering (was reading the wrong outer column — TPC-H Q21's `l1.l_suppkey` landed on `l_comment`, causing a numeric-cast error). Verified via minimal repro, pg-oracle-diff byte match, full Q21 run (370 rows), `go test ./internal/planner/... ./internal/executor/...`, pgbench smoke gate. | While validating via `scripts/tpch-spotcheck.sh`, found Q13 fails independently (`operator NOT LIKE requires string operands (got left.Kind=5 right.Kind=3)`, i.e. `o_comment` resolves to `o_orderdate`'s column index in a `customer LEFT OUTER JOIN orders ON ... AND o_comment NOT LIKE ...` plan whose ON-conjunct filter is pushed onto the bare `orders` seq-scan) — confirmed via `git stash` this is pre-existing/unrelated to this fix. | see new `tpch/Q13-regression` item in `.ralph/fix_plan.md` (filed same loop): resume by bisecting the 2026-07-06 commits touching `internal/planner/pushdown.go`/`planner.go`'s `shiftColumnRefsBy`/`classifyConjunctSide` (cf. M0110-0003's sibling fix for the same LEFT JOIN inner-only pushdown class of bug), then trace the ON-clause pushdown for the `customer`/`orders` plan shape. | kept this loop's scope to the assigned Q21 item (ONE task per loop); Q13 is a distinct planner pushdown bug requiring its own investigation, not a follow-on of the MHJ/EXISTS fix landed here. |
| resolved | 2026-07-07 | tpch/Q9-timeout (AI-20260707-000712-007) | **NO CODE LANDED — fix found, prototyped, and REVERTED after a correctness hazard was proven; investigation only.** Root cause of the timeout: Q9's `partsupp` join is planned as a Nested-Loop-Index-Join using only the single-column FK index `partsupp_supplier_fkidx (ps_suppkey)`, even though the WHERE clause also has `ps_partkey = l_partkey` and `partsupp_pk (ps_partkey, ps_suppkey)` exists — `internal/planner/bushy.go`'s DP (`enumerateBushyPlans`) only ever wires ONE equality edge per table-pair into a join's canonical `LeftKey`/`RightKey`/`Predicate` (deliberately, per the existing "Q9's partsupp<->lineitem" comment at bushy.go:590-595); the second edge (`ps_partkey=l_partkey`) surfaces only as a single whole-plan residual Filter far above the join, so the NLI index-picker (`nl_index_join.go`'s `collectCrossSideEquiKeys`/`pickIndexCoveringAllLeadingColumns`) never sees it and can't choose the composite PK — each of the ~6M lineitem-driven probes fans out to ~80 partsupp rows (all rows for that supplier) instead of 1, before the missing condition is ever checked. Prototyped a fix (`attachCrossConjunctsToTree` in bushy.go, walking the FINAL winning bushy tree post-DP and ANDing any not-yet-consumed cross-table edge onto the lowest join that first co-locates both its tables, using a new `remapEdgeToJoinPredicate` mirroring `buildJoinFromDP`'s own local-subset column remap) — this WORKED for the actual SF1 Q9 shape: EXPLAIN showed `Index Scan using partsupp_pk ... Index Cond: (ps_partkey = l_partkey AND ps_suppkey = l_suppkey)`, `tmp/tpch-runner -queries 9` went from a 280s+ timeout to `OK elapsed=87.19s rows=175` (175 = the correct 25-nation×7-year structural anchor, confirmed against a fresh upstream-PostgreSQL-18.3 run on the same schema shape), and `go test ./internal/planner/...` stayed green. **Then found it corrupts results when the SAME join instead gets folded into a `*planner.MultiHashJoin` chain** (which DP prefers over NLI whenever the NLI cost gate rejects — happens for small/tied row-count subsets, reproduced with a 3-row toy dataset): `internal/planner/bushy.go`'s `collectMultiHashTables` captures any AND'd-on "extra" conjunct verbatim into `mh.Filters` assuming it is still in "pre-rewrite global FROM-order" coordinates (per its own comment, and per `applyJoinTreePosMap`'s `*MultiHashJoin` case, which later applies a GLOBAL-FROM-order→MHJ-output `posMap` unconditionally to every `mh.Filters[i]`) — but my new attachment produces the conjunct in the 2-way join's OWN LOCAL bushy-DP-subset coordinates (the same convention `LeftKey`/`RightKey`/`Predicate` already use, which `nl_index_join.go`'s `collectCrossSideEquiKeys` REQUIRES for its inner/outer classification). Patching `rewriteMultiWayChain` to re-resolve `extras` by NAME against the final OID-sorted `mh.schema` (mirroring how `keys` are already looked up via `findScanByColName`) fixed the *first* misalignment but then got double-shifted by `applyJoinTreePosMap`'s later unconditional global-coords remap — confirmed via a minimal 3-table repro (`zz_part ⋈ zz_lineitem ⋈ zz_partsupp` where `partsupp` has 3 rows sharing one supplier but different parts): `SELECT ... WHERE ps_suppkey=l_suppkey AND ps_partkey=l_partkey AND p_partkey=l_partkey` returned **0 rows instead of the correct 1**, with `evalFilters` tracing showing the `ps_partkey=l_partkey` comparison reading the wrong physical columns (off by exactly partsupp's own column width, `+3`) after the double-remap. Reverted all three touched files (`internal/planner/bushy.go`, `internal/planner/nl_index_join.go`, `internal/executor/multi_hash_join.go`) to HEAD via `git checkout --`; confirmed `git status`/`git diff --stat` clean except `.ralph/progress.json`; `go build ./...` clean; `scripts/tpch-spotcheck.sh` PASS (Q12=2, Q13=33) on the reverted tree. | Same as landed: the fix that makes Q9 fast is fully designed and was empirically validated on the real query shape, but is unsafe to land until the two downstream consumers of a join's extra AND'd Predicate (`nl_index_join.go`'s local-subset-coordinate NLI index picker vs. `bushy.go`'s `collectMultiHashTables`+`applyJoinTreePosMap` global-coordinate MHJ-Filters pipeline) are reconciled to a SINGLE coordinate convention, or the new attachment pass is scoped to skip joins that end up folded into an MHJ. | Two options, either is a bounded, well-specified next step: **(A)** Make `nl_index_join.go`'s `collectCrossSideEquiKeys` robust to a GLOBAL-FROM-order-coordinate extra conjunct (fall back to classifying a ColumnRef's side by checking which of `j.Left.Output()`/`j.Right.Output()` contains its Name, only when its `Index` falls outside `[0, leftWidth+rightWidth)` — i.e. is clearly not a valid local-subset index — so ordinary local `LeftKey`/`RightKey` classification for existing self-join cases is untouched), THEN change `attachCrossConjunctsToTree` to AND the edge's RAW, unremapped `e.leftKey`/`e.rightKey` (true global FROM-order coordinates, no `remapKeyToSubset` call) onto `j.Predicate` — this makes the new mechanism produce exactly the coordinate convention `collectMultiHashTables`/`applyJoinTreePosMap` already expect and handle correctly for MHJ-folded joins, while the new name-based fallback lets the NLI path keep working. **(B)** Simpler but more invasive: run `attachCrossConjunctsToTree` as a separate tree-walk AFTER `rewriteMultiWayChain` (so already-MHJ-folded joins are structurally gone and can't be touched) but BEFORE `rewriteJoinsToNLI` (so NLI index-selection still sees the extra predicate) — requires threading `g`/`edgeUsed` out of `tryBushyDP`'s scope to planner.go's `planSelect`, and auditing whether `collectMultiHashTables`'s OWN independent key-consumption (via `findScanByColName`, not `edgeUsed`) could still race with which edges this later pass considers "available" for a *different*, not-yet-MHJ-folded join sharing a table with the MHJ. Whichever option is picked, add a permanent regression test pinning the toy 3-table repro (part/lineitem/partsupp, 2 join conditions on the same table pair, one side non-unique) alongside the existing Q9 EXPLAIN/row-count checks, so a future attempt is caught by CI rather than manual toy-query archaeology. | A wrong query result is this project's worst failure mode (`.ralph/PROMPT.md`'s "Silent row-count regressions are this project's most expensive failure mode"); landing a fix that measurably helps one plan shape while corrupting another is not an acceptable trade within a single loop's remaining time budget, especially with no regression test yet proving the MHJ-interaction is fixed. Investigation-only, but the root cause, the two candidate fixes, and a concrete repro are now fully specified so the next loop does not need to re-derive any of this. |
| resolved | 2026-07-07 | tpch/Q20-timeout (AI-20260707-000712-008) | **FULLY LANDED — two independent planner bugs found and fixed, both with permanent regression tests.** (1) `internal/planner/planner.go`'s `planHasOuterRef` (used to compute `InExpr`/`SubqueryExpr`/`ExistsExpr.IsNonCorrelated` at bind time) treated ANY `OuterColumnRef` found anywhere inside a nested subquery's plan as "escapes the tested plan", without checking `OuterColumnRef.Level` (the hop-count field `resolveColumnRefAt` already populates, and that `bushy.go`'s `remapOuterRefsInSubplan` already consumes correctly with a depth-tracked convention). Q20's shape — `s_suppkey IN (SELECT ps_suppkey FROM partsupp WHERE ps_partkey IN (...) AND ps_availqty > (SELECT 0.5*sum(l_quantity) FROM lineitem WHERE l_partkey=ps_partkey AND l_suppkey=ps_suppkey ...))` — has a scalar subquery whose `Level=1` `OuterColumnRef`s (ps_partkey/ps_suppkey) resolve fully within the MIDDLE (partsupp) scope, one nesting level below the point `planHasOuterRef` was asked to test. The old code counted this as "partsupp-subquery is correlated", so `unnestNonCorrelatedInExpr`'s non-correlated-IN → SemiJoin fast path (M0069-0005) never fired for the OUTERMOST `s_suppkey IN (...)`, leaving it as a raw per-row-evaluated `InExpr` in the outer Filter — EXPLAIN showed literally `Filter: (<*planner.InExpr> AND (n_name = 'CANADA'))` — forcing the executor to re-run the ENTIRE partsupp+lineitem-aggregate subtree once per `supplier` row (10000 rows at SF1) instead of building it once as a hash-join probe side. Fixed by threading a `depth` counter through a new `planHasEscapingOuterRef(node, depth)` worker (depth starts at 1, +1 per subquery nesting level recursed into, mirroring `remapOuterRefsInSubplan`'s existing convention) and only counting `Level >= depth` as escaping. (2) Independently, `splitEqualityForHash` (used by `planFromClause`'s explicit `JOIN ... ON` handling, a different code path than bushy DP) only recognised a BARE single equality predicate; an AND-of-equalities (e.g. `partsupp JOIN (SELECT ... GROUP BY l_partkey, l_suppkey) agg ON ps_partkey=agg.l_partkey AND ps_suppkey=agg.l_suppkey`) fell through to `(nil,nil,false)`, leaving the join on the default Nested-Loop algorithm — which, against an expensive derived-table side with no usable index, recomputed the GROUP BY aggregate once per outer row. Fixed by iterating `splitAnd(pred)`'s conjuncts and hashing on the FIRST one that decomposes into disjoint sides, leaving `jn.Predicate` (all conjuncts) unchanged for the executor's existing residual-recheck-per-hash-match mechanism (`joinPredicateMatchSlot`, the same mechanism TPC-H Q9's bushy DP already relies on) to catch the rest. End-to-end verified against `bench/tpch/runtime_goopg/data` (fresh server restart): `tmp/tpch-runner -queries 20` went from a 1200.13s cancel (57014) to `OK elapsed=2.55s rows=92`; row count and full row content cross-checked against a freshly-started real PostgreSQL 18.3 instance on an independently-generated SF1 dataset (`bench/tpch/runtime/pgdata`) — both return exactly 92 rows (differing supplier keys are expected, since the two SF1 datasets were generated with different RNG seeds; the canonical TPC-H Q20 verification method is row-count parity, same as this project's existing Q12/Q13 spot-check). `go test ./...` full suite green; `scripts/tpch-spotcheck.sh` PASS (Q12=2, Q13=33) on the same fresh restart. Regression tests added: `internal/planner/non_correlated_subquery_test.go`'s `TestPlanHasOuterRef_NestedSubqueryResolvesLocally` (fails without the `planHasOuterRef` fix, passes with it — the precise minimal repro), its sibling `TestPlanHasOuterRef_NestedSubquery` updated to use `Level=2` (a genuinely escaping reference) instead of the `Level=1` value it previously used to pin the OLD, over-eager behaviour; `internal/planner/multikey_hash_join_test.go`'s `TestSplitEqualityForHashMultiKey` (fails without the `splitEqualityForHash` fix); `internal/planner/q20_unnest_test.go`'s `TestPlanQ20OuterInFullyUnnested` (asserts no raw `*InExpr` survives anywhere in Q20's planned tree — does NOT reproduce the bug against a minimal synthetic catalog, see `why`, but guards the desired end state going forward). | Nothing deferred — both root causes are fixed with no remaining PG-semantics gap for this query shape. | N/A — fully resolved this loop. | Minor unresolved curiosity (not a functional gap): the `planHasOuterRef` bug reproduces reliably against the REAL `bench/tpch/runtime_goopg/data` HammerDB-loaded schema (confirmed via live-server EXPLAIN + targeted stderr tracing added and removed this loop) but could NOT be reproduced end-to-end against a minimal synthetic `catalog.NewInMemory()` Q20 test with the same table/column shape (tried: bare columns, added PK/composite-PK indexes — `TestPlanQ20DumpsTree`/`TestPlanQ20OuterInFullyUnnested` both show correct unnesting even with `planHasOuterRef` un-fixed). Some structural difference in the real HammerDB-generated schema (extra columns, FK constraints, column ordering, or something else not yet isolated) changes which code path handles the outer IN. Time-boxed given the root cause was independently, precisely pinned at the expression level (`TestPlanHasOuterRef_NestedSubqueryResolvesLocally`) and the real-world fix is verified end-to-end; resume point if ever revisited: diff the live server's `\d supplier`/`\d partsupp` output (captured this loop, see this loop's session transcript) against the minimal test catalog column-by-column, adding one real-schema feature at a time (FK constraints via `c.CreateForeignKey`-equivalent, then column reordering) until the minimal repro flips. |
| resolved | 2026-07-07 | tpch/Q9-timeout (AI-20260707-000712-007) | **FULLY LANDED — option (A) from the prior loop's revert, implemented as specified.** New `attachUnusedCrossEdges` (`internal/planner/bushy.go`), called from `enumerateBushyPlans` right after the winning DP tree (`fullEntry.plan`) is selected, walks that tree bottom-up computing each node's covered-table bitmask (matching leaves by pointer identity against `g.scans`) and, for every still-unused edge (`!edgeUsed[i]`) whose two tables split across a `*Join`'s Left/Right children, ANDs the edge's RAW `joinEdge.predicate` (the untouched original `*BinaryOp` — global FROM-order ColumnRef indices, exactly as `buildJoinGraph` captured it from the WHERE clause, no `remapKeyToSubset` call) onto that Join's `Predicate` and marks the edge used so the residual-Filter computation right below it excludes it. This is the key difference from the reverted prototype: that version remapped the extra edge into the Join's own LOCAL bushy-DP-subset coordinates (the `LeftKey`/`RightKey` convention) before attaching, which is exactly what corrupted the `*MultiHashJoin`-folded case — `collectMultiHashTables`'s pre-existing `extraInScans`-guarded "extras" capture (its own comment already names this precise Q9 shape) and `applyJoinTreePosMap`'s MHJ-Filters remap both already assume any AND'd-on extra conjunct is in GLOBAL coordinates, matching how a residual top-level Filter conjunct looks. By keeping the RAW global-coordinate form, a join later folded into an MHJ needs zero changes on that side. The only remaining gap was the *other* consumer: `collectCrossSideEquiKeys` (`internal/planner/nl_index_join.go`), used by the NestedLoopIndexJoin rewrite, classified an AND-conjunct's side purely by `ColumnRef.Index < leftWidth` against the Join's own LOCAL width — which fails for a global-coordinate extra. Gave it a name-based fallback (via the existing `collectScanOutputNames` helper from `pushdown.go`) that classifies a conjunct's ColumnRef by which side's actual output schema contains its `.Name`, used only when the index-based verdict is inconclusive — ordinary local-coordinate conjuncts (the common case) are untouched. Also hardened `tryBuildNLI`'s final "Fallback rebind" loop (previously only name-rebound a key when its `.Index` was out of range) to also catch an in-range-but-WRONG-name `.Index` — the same defensive check its MHJ/NLI/Join-outer sibling block already had — closing a latent gap the new global-coordinate keys could otherwise hit for a plain (non-MHJ/NLI/Join) outer node. End-to-end verified against `bench/tpch/runtime_goopg/data` (fresh server restart, superuser@postgres): `EXPLAIN` for Q9 now shows `Index Scan using public.partsupp_pk on public.partsupp ... Index Cond: (ps_partkey = l_partkey AND ps_suppkey = l_suppkey)` (previously the single-column `partsupp_supplier_fkidx`); `tmp/tpch-runner -queries 9 -per-query-timeout 280s` → `OK elapsed=88.32s rows=175` (was a 1200s+ 57014 cancel; 175 matches the reverted prototype's row count, independently cross-checked back then against a fresh real-PostgreSQL-18.3 run on the same schema shape). Full 22-query `-explain` sweep clean (zero errors). `scripts/tpch-spotcheck.sh` PASS (Q12=2, Q13=33) on the same fresh restart. `go build ./...` clean; `go test ./internal/planner/... ./internal/executor/...` PASS; targeted re-run of `internal/testutil/tpch/...` (all NLI/parity/index-utilisation tests, the package most likely to catch a join-planning regression) PASS except `TestTPCHScaleLoadAndQueryRun`, confirmed via `git stash` to hang identically on HEAD with this loop's planner changes removed (a pre-existing 9-minute `os/exec`/cluster-startup timeout unrelated to this change, not investigated further per the standing ONE-task-per-loop rule). A broader `go test ./internal/...` run also surfaced `internal/initdb`, `internal/testport` (`TestE2E_FailoverPGtoGoopg`), and `internal/testutil/cluster` (`TestKillKillRecovery`) as failing — all three independently confirmed via the same stash-and-rerun method to fail identically without this loop's changes (resource-contention/timing flakiness under full-suite parallelism in this environment, not regressions). Added a permanent regression test, `internal/executor/bushy_dp_cross_conjunct_test.go`'s `TestBushyDPAttachesUnusedCrossEdge` (a 3-way comma-FROM query mirroring Q9's partsupp/lineitem two-equality shape, executed end-to-end via the `newDDLFixture`/`runDDL`/`runQuery` in-process harness), plus a bushy-DP structural probe confirming `attachUnusedCrossEdges` fires and attaches the edge with correct global-coordinate indices. | Nothing deferred — both consumers (`collectMultiHashTables`'s MHJ-Filters path and `collectCrossSideEquiKeys`'s NLI path) now correctly handle a global-coordinate extra conjunct attached post-DP; the query returns PG-matching results with no remaining correctness gap for this shape. | N/A — fully resolved this loop. | One honest caveat, not a functional gap: `TestBushyDPAttachesUnusedCrossEdge` does NOT by itself prove `attachUnusedCrossEdges` is necessary — at 3-4 table toy scale, bushy DP's chosen tree happens to keep LOCAL subset coordinates numerically identical to GLOBAL FROM-order coordinates (confirmed by instrumenting both `attachUnusedCrossEdges` and `pushdown.go`'s pre-existing `pushOneConjunct` with temporary debug prints, reverted before commit), so `pushOneConjunct`'s own width-based re-attachment already produces an identical plan even with `attachUnusedCrossEdges` stashed out. The two mechanisms are provably NOT redundant only at real-Q9 scale (6-way join, genuine ANALYZE-driven cost estimates bias DP toward a non-FROM-order-preserving split), which is exactly why this bug survived as a live timeout despite `pushOneConjunct`'s pre-existing (and, per its own comments, already Q9-aware) safety net — a future loop wanting a toy-scale test that fails without `attachUnusedCrossEdges` specifically would need to engineer `catalog.Table.Stats.RowCount` values that force bushy DP's cost-based tie-breaking into a genuinely non-contiguous (relative to FROM order) partition; not attempted here given the real-scale verification already fully confirms the fix. |
| resolved | 2026-07-07 | M0119-0006 (AC-002 residual #1, `datconnlimit = -2` invalid-database filter — M-NIGHTLY AI-20260707-000712-004 follow-up) | Runtime `pg_database.datconnlimit` write + read now work end-to-end: `catalog.InMemory.databaseConnLimit map[string]int32` + `SetDatabaseConnLimit`/`DatabaseConnLimit` (catalog.go), consumed by `pg_database`'s `VirtualRows` builder; `UPDATE pg_database SET datconnlimit = ... WHERE datname = ...` persists via a new `updateOp.nextVirtualPgDatabase()` path (internal/executor/operators_storage.go) that bypasses the ~1300-line physical-heap `Next()` machinery for this one Virtual, no-RelFileNode table — reads current rows from `tbl.VirtualRows()`, evaluates the already-extracted `o.pred`/`o.plan.Set` against them (same `planner.TypedVirtualCell` decode the SELECT-side Values/VirtualSource path uses), and calls `SetDatabaseConnLimit` for matches. Ported 002_nonesuch.pl's "Invalid / partially dropped database" section (`internal/testport/pgamcheck002_port_test.go`) — both `command_checks_all` cases now pass against the real `pg_amcheck` binary; confirmed non-vacuous via `git stash` on the two impl files (fails with the pre-fix "amcheck is not installed" symptom). AC-002 (002_nonesuch.pl) has no remaining residuals. | (1) No connect-time enforcement: a real client CAN still open a session against a `datconnlimit = -2` (or any negative/exceeded-limit) database — goopg's connection-startup path only checks `HasDatabase`/`datallowconn`-style registration, never consults `DatabaseConnLimit`. Real PG rejects the connection itself (`FATAL: ... cannot connect to invalid database` for -2; `FATAL: too many connections for database "%s"` for a reached positive limit). (2) Positive `datconnlimit` values (actual per-database connection-count throttling) are entirely unimplemented — only the -2 sentinel's SQL-visibility half was needed for AC-002 and is what's covered by a test. | (1) resume in the session/connection-startup path (wherever `HasDatabase`/`datallowconn` are already checked at auth time — grep for that call site) — add a `DatabaseConnLimit(name) == -2` check there, erroring FATAL before the session is fully established; (2) a real active-connection counter keyed by database name, checked against a positive `DatabaseConnLimit` at the same startup gate. | Scoped this loop to exactly what AC-002's `command_checks_all` cases assert (pg_amcheck's own SQL-side `datconnlimit != -2` filter, which never attempts the actual connection) — connect-time enforcement and positive-limit throttling are both separate, untested-here behaviors; adding them speculatively would risk the connection-startup hot path without a driving test. |
| resolved | 2026-07-07 | M0119-0006 (AC-002 residual #1 follow-up, `datconnlimit = -2` connect-time enforcement) | Item (1) from the row above is now landed: a real client connection to a database whose `datconnlimit == -2` is rejected at connect time. `catalog.DatconnlimitInvalidDB` (exported `-2` constant, catalog.go) + new `databaseConnLimitRegistry` interface (`internal/server/database_ddl.go`, single method `DatabaseConnLimit(name string) int32`, kept separate from `databaseRegistry` so a catalog fake missing this method doesn't silently lose the unrelated role/database-existence gates) + a new check in `Server.handleStartup` (`internal/server/server.go`) right after the existing `HasDatabase` gate, writing the real PG FATAL `55000` `cannot connect to invalid database "%s"` (mirrors `postinit.c`'s `database_is_invalid_form`/`InitPostgres` check). Test `TestConnectInvalidDatconnlimitDatabaseRejected` (`internal/server/database_exists_test.go`), confirmed non-vacuous via `git stash` on `server.go` alone. See `docs/design/0110-0003-pg-amcheck-tap-port.md`'s new "Follow-up: connect-time `datconnlimit = -2` rejection" section. | Item (2) from the row above remains fully open: positive `datconnlimit` values (real per-database connection-count throttling, PG's `too many connections for database "%s"` / `ERRCODE_TOO_MANY_CONNECTIONS`) are still entirely unimplemented — goopg has no live per-database active-connection counter at all. | Add a live connection counter keyed by database name (increment on successful `handleStartup`, decrement on connection teardown — the activity registry (`internal/activity`) already tracks per-backend state and is the natural home), then in the same `handleStartup` gate check `DatabaseConnLimit(db) >= 0 && count > limit` and reject non-superuser connections over the limit, mirroring `postinit.c`'s `CountDBConnections`/`AmRegularBackendProcess`/`am_superuser` guards. | Scoped this loop to exactly the `-2` sentinel (residual #1's remaining half) per the prior row's own rationale — positive-limit throttling needs new cross-connection counting state and a superuser-bypass check, a materially larger and differently-tested change, not attempted here. |
| resolved | 2026-07-07 | M0119-0006 (AC-002 residual #2, positive `datconnlimit` connection-count throttling) | Item (2) from the two rows above is now landed: a non-superuser connection to a database whose live connection count exceeds a positive `datconnlimit` is rejected at connect time. New `activity.ActivityRegistry.CountByDatName(name string) int32` (`internal/activity/registry.go`) scans live backend slots for a matching `DatName` — no new mutable counter/lock needed (an earlier design draft proposed one; an agent design review flagged it as unnecessary extra state, since `Server.handleStartup` already registers each backend via `reg.Register` before this check can run, making a slot-scan self-inclusive by construction, exactly like PG's `CountDBConnections`). New check in `Server.handleStartup` (`internal/server/server.go`), placed right after the existing `reg.Register`/`SetCurrentGoroutine` block: if `DatabaseConnLimit(db) >= 0 && !isSuperuserRoleName(user) && CountByDatName(db) > limit`, write the real PG FATAL `53300` (`sqlstate.TooManyConnections`) `too many connections for database "%s"` (mirrors `postinit.c`'s `CheckMyDatabase`). Superuser connections are still counted (matching `CountDBConnections` having no role filter) but never rejected. Tests: `TestActivityRegistryCountByDatName` (`internal/activity/registry_test.go`, direct multi-database counter checks incl. post-`Unregister` drop); `TestConnectExceedsPositiveDatconnlimitRejected`/`TestConnectPositiveDatconnlimitSuperuserBypasses` (`internal/server/database_exists_test.go`, real wire-protocol round trip via a new `startServerWithCatalogAndActivity` helper variant). Confirmed non-vacuous via `git stash` on `server.go`+`registry.go` (the rejection test fails with "unexpected frame S before ErrorResponse" — connection proceeds to AuthenticationOk instead of rejecting — without the fix). M0119-0006 (AC-002) now has zero remaining residuals. Design: `docs/design/0110-0003-pg-amcheck-tap-port.md`'s new "Follow-up: residual #2 — positive `datconnlimit` connection-count throttling" section (design doc reviewed by an independent agent before implementation per the M0119 per-task rule — the review caught and corrected an unnecessary-state issue in the first draft, folded into the doc before coding began); `docs/design/README.md` row extended. | Per-role connection limits (`pg_authid.rolconnlimit`, a separate PG mechanism from per-database `datconnlimit`) remain entirely untracked — goopg has no `rolconnlimit` column/enforcement at all. | `internal/catalog/catalog.go`'s role-attribute store (`RoleAttrs`) would need a `ConnLimit int32` field plus a parser/executor path for `CREATE/ALTER ROLE ... CONNECTION LIMIT n`, then a role-keyed sibling of `CountByDatName` (e.g. `CountByUserName`) checked alongside the database-level gate in the same `handleStartup` block. | Out of scope for this loop — a materially separate feature (role-level vs. database-level limiting, its own grammar surface, its own catalog column) from the datconnlimit throttling this row closes; not part of AC-002/residual #2's stated scope. |
| resolved | 2026-07-07 | M0119-0006 (AC-003 cluster, synthetic TOAST relation open-failure — LANDED, unblocks 3 of 7 skipped tests) | `verifyHeapamResolveTable`'s call site (`internal/executor/operators_verify_heapam.go`) and `btIndexResolve`'s call site (`internal/executor/operators_bt_index_check.go`) now fall back to `catalog.InMemory.ToastParentTable(oid)` when a `regclass` OID fails to resolve to a real table/index: a synthetic `pg_toast_<oid>`/`pg_toast_<oid>_index` OID (no real backing heap/index file by design — see `tableHasToastRelation`'s doc comment) now reports zero findings instead of raising `42P01 could not open relation`. Real `pg_amcheck`'s default whole-table walk always checks a table's TOAST relation alongside it, so every one of the 7 AC-003/AC-004 tests was permanently failing its "confirm a clean baseline before injecting corruption" gate purely because of this TOAST-OID artifact, with zero real corruption ever having been injected. `TestPort_PgAmcheck004VerifyHeapam`, `TestPort_PgAmcheckAllTables`, `TestPort_PgAmcheckBtreeIndexCheck` now genuinely PASS. Gates: `go build ./...` clean; `go test ./internal/executor/... ./internal/catalog/... ./internal/amcheck/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke scripts/ralph-precommit-test.sh` PASS (0 failed, all 3 workloads). Design: `docs/design/0110-0003-pg-amcheck-tap-port.md` new "Follow-up (2026-07-07): AC-003 cluster unblocked" section; `docs/design/README.md` row updated. | The remaining 4 AC-003 tests (`TestPort_PgAmcheck003CombinedCorruption`, `003MissingIndexFork`, `003MissingHeapFile`, `003SchemaScoped`) — the ones that manually remove a heap or index main-fork file to simulate corruption, matching real pg_amcheck's `003_check.pl` — now advance past the TOAST gate but hit a NEW, previously-undiscovered blocker: restarting the goopg cluster after the file removal times out after 20s (`restart cluster after removal: start timeout after 20s`), so `pg_amcheck` never even runs against the corrupted cluster. Confirmed uniform across all 4 (same symptom whether a heap file, an index file, or both are removed). Not yet localized to a specific startup code path — candidates not yet ruled out: (a) goopg's startup sequence eagerly opens/scans every relation's main fork (unlike real PG, which only opens files lazily on first access, so a missing file is invisible until a query touches that relation) and blocks/retries indefinitely instead of erroring; (b) a WAL-replay or checkpoint-scan step at startup tries to fsync/verify every known RelFileNode and hangs the same way; (c) the removed file's absence triggers some retry-with-backoff loop mistaken for a transient error. | Resume point: reproduce manually (init a data dir, insert a toastable-column table, stop cleanly, `rm base/<db>/<relfilenode>`, start with verbose/debug logging and no test-harness timeout) and capture where startup actually blocks — grep `internal/initdb/open.go`'s relation-loading loop and `internal/storage`'s `Pool`/`Manager` open-time file-existence checks first, since those are the most likely place an eager per-relation open would live. Each of the 4 blocked tests already has its own removal-then-restart helper (`pgamcheck003_missingheap_test.go`, `pgamcheck003_missingfork_test.go`, `pgamcheck003_schemascoped_test.go`, `pgamcheck003_combined_test.go`) that can be reused verbatim as the repro once the root cause is found; do not build a new one. | Genuinely new discovery this loop, not a narrowing of a previously-known gap — the TOAST-OID fix this loop landed was the only thing standing between "test always skips at the pre-corruption baseline check" and "test reaches the restart step," so this blocker was structurally unreachable before today. |
| resolved | 2026-07-07 | M0119-0006 (AC-003 cluster, restart-after-file-removal timeout — the previous row's "genuinely new discovery" blocker) | Root-caused via CPU profiling (`pprof.StartCPUProfile` around a throwaway `initdb.Open` probe against the exact stop→remove-file→restart repro): `internal/wal/xlog_emit.go`'s `extractRecordBytes(stream, streamStart, segSize, wantBytes)` allocated `make([]byte, 0, len(stream))` — the entire REMAINING input (callers pass `stream[off:]`, so this is close to the full WAL segment size, e.g. 16 MB, for records near the start) — regardless of `wantBytes` (a single record's 24-byte header or padded body). `readAllPageAware` calls it twice per record, and `initdb.Open` runs several independent full-WAL replay passes (`replayEventTriggerDDLRecords`/`replayOperatorClassDDLRecords`/`replayCLogFromWAL`/`replayRangeTypeDDLRecords`/`replayViewRecords`/more via `wal.ReadAll`), so a handful of small DDL/heap records multiplied into 20-40s of allocation+`memclrNoHeapPointers` (70% of all CPU samples), blowing the test harness's 20s restart budget. Fixed to `make([]byte, 0, min(wantBytes, len(stream)))` — a strict reduction, safe because the loop's own `len(recordBytes) < wantBytes` guard means it can never append past `wantBytes`. Verified via the exact profiling repro: `initdb.Open` on the broken data dir dropped from 23.57s to 0.18s. New regression test `TestExtractRecordBytesCapBoundedByWantBytes` (`internal/wal/xlog_emit_test.go`), confirmed non-vacuous via `git stash` on `xlog_emit.go` alone (fails with `cap=16777216`). Result: all four previously-blocked AC-003 tests (`TestPort_PgAmcheck003CombinedCorruption`/`003MissingIndexFork`/`003MissingHeapFile`/`003SchemaScoped`) now PASS. Gates: `go build ./...` clean; `go test ./internal/wal/... ./internal/initdb/... ./internal/executor/... ./internal/amcheck/... ./internal/catalog/...` PASS; `go test -v -run '^TestPort_PgAmcheck003' ./internal/testport/` PASS (4/4); `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke scripts/ralph-precommit-test.sh` PASS (0 failed, all 3 workloads). Design: `docs/design/0110-0003-pg-amcheck-tap-port.md` new "Follow-up (2026-07-07): AC-003 restart-timeout blocker closed" section; `docs/design/README.md` row updated. | n/a — fully resolved, no resume point needed. | AC-003/AC-004 cluster (M0119-0006) is now fully unblocked: no open blockers remain in this cluster. |  |
| resolved | 2026-07-08 | M0122-0006 (index column order restart persistence) | Fixed the 3-part gap that lost `CREATE INDEX ... (col DESC/NULLS ...)` ordering across a restart: live `pg_index.indoption` (was hardcoded zero in the `Virtual` builder, `internal/catalog/catalog.go`), the heap-persisted shadow row's `indoption` (`buildUserPGIndexRow`/`DecodePGIndexPhysicalRow`), and — the actual crash-durability bug — `createBTreeIndex`'s CREATE INDEX WAL record was emitted before `execCreateIndex`'s post-call block set `idx.ColDescending`/`ColNullsFirst`, so a genuine uncheckpointed `SIGKILL` restart always replayed the index as plain ascending regardless of its declared ordering. Fixed by computing the ordering before the `createBTreeIndex` call and threading it in as new parameters. `wal.CreateIndexPayload` gained `ColDescending`/`ColNullsFirst` as two trailing append-only blocks. See `docs/design/0122-0006-index-column-order-restart-persistence.md`. | While diagnosing the WAL-emission-ordering bug, confirmed `wal.CreateIndexPayload` carries ONLY `OID/TableOID/Schema/Name/Method/Columns/Unique/Primary` (now plus ColDescending/ColNullsFirst) — it has never carried `HasPredicate`/`Predicate`/`IncludeColumns`/`ColOpClasses`/`ColCollations`/`Fillfactor`/`DeduplicateItems`, all of which `execCreateIndex`'s SAME post-call resync block (`internal/executor/operators_ddl.go`, ~line 5994 pre-fix) sets on `idx` and persists to the HEAP row via `resyncIndexClassHeapRow`/`resyncIndexHeapRow` (M0119-0004) but never to WAL. These round-trip correctly across a *checkpointed* restart (heap wins the `RegisterIndexDuringRecovery` dedup race) but — by the exact same mechanism just fixed for ColDescending/ColNullsFirst — would silently revert to defaults (no predicate, no INCLUDE columns, default opclass/collation, no fillfactor/dedup) across a genuine uncheckpointed crash restart. Not reproduced end-to-end (only reasoned from the WAL payload's field list and the shared code path), so treat as a confirmed-real-but-unverified-in-full risk, not a certainty for every field. | Resume point: extend `wal.CreateIndexPayload` (or introduce a dedicated `AlterIndexPropertiesPayload`) to carry these fields, mirroring the append-only trailing-block pattern this task established for ColDescending/ColNullsFirst (to preserve backward compatibility with existing on-disk WAL), and thread them through `createBTreeIndex`/`RegisterIndexDuringRecovery` the same way. Add a `TestCrashRecoveryReplaysWALAfterUncleanShutdown`-style true-crash test (not a plain `rt.Close()`, which masks the bug via its shutdown checkpoint) creating an index with a WHERE predicate / INCLUDE columns / non-default opclass or fillfactor, verify each survives an uncheckpointed SIGKILL. | Scoped out of M0122-0006 to keep the change bounded to the milestone's stated item (index column ordering); this is a materially larger, multi-field extension of the same mechanism, better done as its own task now that the pattern (and the true-crash test technique needed to catch it) is established. |
| resolved | 2026-07-08 | M0122-0006 (index column order restart persistence, follow-up: index properties beyond ordering) | Extended `wal.CreateIndexPayload` with `HasPredicate`/`PredicateString`/`IncludeColumns`/`ColOpClasses`/`ColCollations`/`Fillfactor`/`DeduplicateItems`/`NullsNotDistinct`, encoded as a new optional, self-describing trailing "extension" block (omitted entirely for a plain index, so old WAL/tests are unaffected) appended after the ColDescending/ColNullsFirst blocks. `createBTreeIndex` (`internal/executor/operators_ddl.go`) gained an optional `btreeIndexProps` struct parameter (replacing the old bare `predExpr ...planner.Expr` variadic) set on the freshly-created `idx` BEFORE its WAL emission, mirroring the ColDescending/ColNullsFirst fix exactly. Only the two call sites that actually declare these properties (`execCreateIndex`'s direct CREATE INDEX statement path, `createPartitionChildIndexes`) needed updating; the other 14 call sites (constraint-backed PK/UNIQUE/EXCLUDE indexes) are unaffected. `catalog.RegisterIndexDuringRecovery` + the `indexRegistryRecovery` interface gained the same 8 params; the pure-WAL replay driver (`replayIndexDDLRecords`) threads the decoded payload straight through. Verified live against the real `cmd/goopg` binary with an actual `kill -9`: `pg_get_indexdef`/`pg_index.indoption`/`indnullsnotdistinct`/`indclass`/`indcollation` all correct post-restart for `CREATE UNIQUE INDEX ... (a, b COLLATE "C" text_pattern_ops) INCLUDE (c) WITH (fillfactor=70, deduplicate_items=off) NULLS NOT DISTINCT WHERE (a > 0)`. See `docs/design/0122-0006-index-column-order-restart-persistence.md`'s "Follow-up (2026-07-08)" section. | The heap-recovery driver (`loadUserIndexesFromHeap`, `internal/initdb/open.go`) still cannot restore ANY of these 8 fields after a *checkpointed* restart (a plain graceful stop, or any restart where a checkpoint ran before the crash): `catalog.PGIndexRow`/`DecodePGIndexPhysicalRow` (`internal/catalog/codec.go`) never decode `indclass`/`indcollation`/`indexprs`/`indpred` content (the comment there explicitly says "their content isn't needed here" — written for the ColDescending/ColNullsFirst fix, now stale for this wider set), and there is no `indnullsnotdistinct` field at all. `buildUserPGIndexRow` (`internal/executor/pg18_user_catalog_rows.go`) symmetrically always writes `indcollation`/`indclass` as all-zero oidvectors and `indexprs`/`indpred` as NULL, regardless of what's on the in-memory `idx`. Fillfactor/DeduplicateItems ARE already restored on a checkpointed restart, but via the separate, pre-existing `ApplyIndexReloptions`/`pg_class.reloptions` mechanism (`internal/initdb/open.go`), not through pg_index at all. | Resume point: (1) write side — `buildUserPGIndexRow` needs to actually populate `indclass`/`indcollation`/`indexprs`/`indpred` from `idx.ColOpClasses`/`ColCollations`/`Predicate`(or `PredicateString`)/`IncludeColumns` instead of hardcoding zero/NULL (INCLUDE columns also need to extend `indkey`/`indnatts` vs `indnkeyatts`, which `buildUserPGIndexRow` already tracks separately per its existing comments); a new `indnullsnotdistinct` boolean column write. (2) read side — `DecodePGIndexPhysicalRow` needs to decode those same varlena columns (the "walks past" comment marks exactly where to hook in) and expose them on `PGIndexRow`; `loadUserIndexesFromHeap` (open.go, Pass 3) then threads them into its own `RegisterIndexDuringRecovery` call instead of the `false, "", nil, nil, nil, 0, nil, false` zero-value literal this loop left there. (3) `indexprs`/`indpred` are `pg_node_tree` in real PG — this codebase's established sibling pattern for WAL-persisted expr-as-SQL-text (`column_defaults_recovery.go`'s `parser.ParseExpr` round-trip) is the template, not a new serializer. Add a `TestCreateIndexExtendedPropertiesSurviveRestartViaWAL`-sibling test that uses a *graceful* `rt1.Close()` (checkpointed restart) instead of the true-crash pattern, to catch this specific gap once fixed. | Scoped out of this loop to keep the change bounded to the WAL/uncheckpointed-crash path explicitly named in the prior ledger row's resume point; the heap-decode side is a materially separate mechanism (pg_index heap row encode/decode, not WAL) with its own resume point now spelled out here. |
| resolved | 2026-07-08 | M0122-0006 (index column order restart persistence, follow-up 2 of 2: heap-recovery decode of predicate/INCLUDE/nulls-not-distinct) | Closed the checkpointed-restart half of the prior row's resume point for 3 of its 5 fields. `internal/executor/pg18_user_catalog_rows.go`'s `buildUserPGIndexRow` now writes real `indnatts`/`indnkeyatts` (previously always equal — INCLUDE columns were silently dropped from `indkey` entirely), appends INCLUDE-column attnums to `indkey`, and writes `indpred` as the predicate's SQL text (`idx.PredicateString`) when `idx.HasPredicate`, mirroring `column_defaults_recovery.go`'s expression-as-SQL-text pattern (`indexprs` stays NULL — goopg has no expression-index support, `Index.Columns` only ever holds plain column names, so no writer can ever populate it). `internal/catalog/codec.go`'s `PGIndexRow`/`DecodePGIndexPhysicalRow` gained `IndNKeyAtts`/`IndNullsNotDistinct`/`IndHasPred`/`IndPred`: the `indnullsnotdistinct` bool sits at the previously-unread fixed offset 13 (between `indisunique`@12 and `indisprimary`@14); `indpred`'s NULL-vs-present state is inferred from data length alone (no null-bitmap plumbing needed) since `indexprs` — the column immediately before it — is proven always NULL, and a NULL varlena column consumes zero bytes in goopg's physical tuple encoding (`encodeRowPG`), so any bytes remaining after `indoption` belong entirely to `indpred`. New `decodePGIndexVarlenaText` helper decodes both the 1-byte-short and 4-byte-long PG varlena text header forms. `internal/initdb/open.go`'s `loadUserIndexesFromHeap` Pass 2/3 now thread `IndNKeyAtts`/`IndNullsNotDistinct`/`IndHasPred`/`IndPred` through to `RegisterIndexDuringRecovery`, and split `indkey` into key-column names (first `indnkeyatts`) vs `IncludeColumns` (the rest) instead of treating every attnum as a key column. New test `TestCreateIndexPredicateAndIncludeColumnsSurviveCheckpointedRestart` (`internal/initdb/index_ddl_recovery_test.go`) uses a graceful `rt1.Close()` (checkpointed restart, not the crash-only WAL path the prior row's test already covers) and asserts `HasPredicate`/`PredicateString`/`IncludeColumns`/`NullsNotDistinct` all survive; confirmed non-vacuous via `git stash` on the 3 impl files (fails with exactly the pre-fix symptom: all 4 assertions report zero-value). Verified live against the real `cmd/goopg` binary with a graceful `stop`/`start` restart: `pg_get_indexdef` and `pg_index.indnatts`/`indnkeyatts`/`indkey`/`indnullsnotdistinct` all correct post-restart for `CREATE UNIQUE INDEX ext3_idx ON ext3 (a, b) INCLUDE (c) NULLS NOT DISTINCT WHERE (a > 0)`. Design: `docs/design/0122-0006-index-column-order-restart-persistence.md`'s new "Follow-up 2 (2026-07-08): checkpointed-restart heap decode" section; `docs/design/README.md` row updated. Gates: `go build ./...`/`go vet` clean; `go test ./internal/catalog/... ./internal/executor/... ./internal/initdb/... ./internal/wal/... ./internal/planner/... ./internal/analyzer/... ./internal/parser/... ./internal/server/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke scripts/ralph-precommit-test.sh` PASS (0 failed, all 3 workloads). | ColOpClasses/ColCollations (`indclass`/`indcollation`) remain entirely unresolved after a checkpointed restart: they still always decode/encode as all-zero OID vectors. Unlike predicate/INCLUDE/nulls-not-distinct, this isn't just a missing decode step — there is no name↔OID resolution registry for arbitrary index-column opclasses/collations anywhere in the codebase yet (`builtinOpclassOIDByName` only covers the small `builtinRangeSubtypeOpclasses` curated set used by range-type subtype defaults; the *live*, non-restart `pg_index` `VirtualRows` builder itself — `internal/catalog/catalog.go` ~line 7660 — only fills `indclass` via a hard-coded per-Go-type-name default-opclass switch and always zeroes `indcollation`, ignoring `idx.ColOpClasses`/`ColCollations` entirely even before any restart). So `pg_index.indclass`/`indcollation` numeric-OID accuracy is a pre-existing, separate, and materially larger gap than restart-persistence — it needs a real opclass-name→OID and collation-name→OID resolver (covering the full builtin opclass/collation universe, not just the range-type subset) plus the reverse OID→name lookup, wired into both the live `VirtualRows` builder and `buildUserPGIndexRow`/`DecodePGIndexPhysicalRow`. `pg_get_indexdef`/`\d` are unaffected either way, since they render `ColOpClasses`/`ColCollations` from the in-memory `Index` struct's name-string fields directly, never through `pg_index`'s numeric columns. | Scoped out of this loop: real opclass/collation OID resolution requires building a new, fairly large builtin-name registry (covering every standard PG opclass per type, e.g. `int4_ops`/`text_pattern_ops`/`numeric_ops`/etc.) plus bidirectional user-opclass/collation lookup, which is a materially separate task from the heap-decode plumbing this loop closed; attempting a partial/half-resolved OID mapping risked producing values that disagree with the already-established (if inaccurate) live-path rendering in a new, confusing way. |  |
| resolved | 2026-07-08 | M0097-0150 (`ALTER FUNCTION/PROCEDURE/ROUTINE OWNER TO`/`SET SCHEMA` enforcement) | Landed: `Routine.Owner`/`OwnerOrDefault()`, `Routines.SetSchema` (re-keys `byKey`/`byName`, mirroring `RenameRoutine`), `execAlterFunction` OWNER TO/SET SCHEMA branches (mirroring `execAlterAggregateOwner`/`execAlterCollation`'s shape and error codes), `pg_proc.proowner` now reads `r.OwnerOrDefault()` instead of a hardcoded `"10"`, WAL record kinds 121/122 + replay wiring. Also fixed a genuine pre-existing parser bug found while implementing: the ALTER FUNCTION attribute loop's SET-clause detection matched `TokenIdent` value `"set"`, but `SET` lexes as the real keyword `KwSet` — so it never matched and every `ALTER FUNCTION ... SET ...` form (not just SET SCHEMA) was a syntax error, not the documented no-op. Verified live against `cmd/goopg` (OWNER TO + SET SCHEMA changed real role/schema OIDs in `pg_proc`, function stayed callable under its new schema, both survived `goopg restart`). | The fix above only repaired the `SET SCHEMA` sub-branch inside that now-reachable block. The generic `SET config_param {TO|=} value` / `SET config_param FROM CURRENT` / `RESET` clauses on ALTER FUNCTION (legitimate, combinable-with-VOLATILE/etc PG grammar per `gram.y`'s `common_func_opt_item: FunctionSetResetClause`) remain broken for two more reasons even with the `KwSet` fix applied: (a) `p.acceptSymbol("=")` only matches `TokenSymbol`, but `=` lexes as `TokenOperator` in this lexer (confirmed via `internal/parser/lexer.go`'s operator-char branch) — so `SET name = value` still errors; (b) the `SET FROM CURRENT` branch checks for a literal `"from"` token immediately after `SET` (treating "from" as if it could be the config-param name), but real PG grammar is `SET var_name FROM CURRENT` — the config-param name always comes first — so this branch is checking for a form that doesn't exist and doesn't handle the form that does. | `internal/parser/ddl.go`, the `ALTER FUNCTION/PROCEDURE/ROUTINE` attribute-consuming loop's `SET` branch (~line 7270, right after the fixed `SET SCHEMA` sub-branch): (1) widen the `=`-acceptance check to `(p.cur().Kind == TokenSymbol || p.cur().Kind == TokenOperator) && p.cur().Value == "="`, matching the established idiom used everywhere else in this file (e.g. ddl.go:284/300/421/926/983); (2) restructure so the config-param name is parsed FIRST (one ident/keyword token), then check for `TO`/`=`/`FROM CURRENT` in that order, discarding the value as a no-op (goopg has no per-function GUC-override storage to write it into) — add a regression test asserting all three forms plus bare `RESET name`/`RESET ALL` parse without error. | Not a regression (this whole form was already a syntax error before this loop — the outer `KwSet` bug made the branch unreachable), and out of the M0097-0150 ledger entry's own named scope (OWNER TO/RENAME TO/SET SCHEMA only); recording as its own resume point since it's now a well-isolated, small parser fix rather than a symptom of the bigger bug this loop actually closed. |
| resolved | 2026-07-08 | M0097-0150 (`ALTER FUNCTION ... SET config_param {TO\|=} value` generic form, follow-up) | Landed the resume point from the row above: `internal/parser/ddl.go`'s ALTER FUNCTION `SET` branch now widens the `=`-acceptance check to accept `TokenOperator` (not just `TokenSymbol`), and restructures so the config-param name is always parsed BEFORE checking for `TO`/`=`/`FROM CURRENT`, matching real `gram.y` `var_name {TO\|=} var_value \| var_name FROM CURRENT` grammar order. New test `TestParseAlterFunctionGenericSetReset` (`internal/parser/alter_function_owner_schema_test.go`) covers `SET name TO value`, `SET name = value`, `SET name FROM CURRENT`, `SET name TO DEFAULT`, `RESET name`, `RESET ALL`, and a VOLATILE-combined form; confirmed non-vacuous via `git stash` on `ddl.go` alone (3/7 cases fail pre-fix with the exact predicted syntax errors). Verified live against `cmd/goopg`: `ALTER FUNCTION add_one(int) SET search_path TO app` / `SET search_path FROM CURRENT` / `SET search_path TO DEFAULT` / `RESET search_path` / `RESET ALL` all now return `ALTER FUNCTION` (previously syntax errors), function stayed callable throughout. | Discovered live (not in scope of the resume point above, which only asked for a single value token): `SET config_param = value1, value2` — a genuine PG grammar form for list-valued GUCs like `search_path`, `temp_tablespaces`, etc. (`var_list` in `gram.y`, comma-separated) — still errors, e.g. `ALTER FUNCTION add_one(int) SET search_path = app, public` fails with a syntax error at the comma. This is a narrower, pre-existing limitation carried over unchanged from the original no-op branch (which also only ever consumed one value token) — not introduced or widened by this loop's fix, but confirmed still-open by live testing rather than left as an unverified assumption. | `internal/parser/ddl.go`'s ALTER FUNCTION `SET` branch (the same block just fixed, ~line 7277): after consuming the first value token, loop while the next token is `,` (comma) and consume `, value` pairs, mirroring how `var_list` is defined in `gram.y` (comma-separated `var_value` items) — add a regression test for `SET search_path = app, public` and `SET search_path TO app, public, pg_catalog`. | Single-value-token consumption was already the pre-existing no-op behavior before this loop (not a regression); fixing the two named bugs (KwSet gate, `=`-as-TokenOperator, name-before-value ordering) closed the loop's actual named scope without needing to also handle comma lists, which is a separate, additive grammar extension rather than a broken-vs-working distinction. |
| resolved | 2026-07-08 | M0097-0150 (`ALTER FUNCTION ... SET config_param = value1, value2` var_list form, follow-up 2 of 2) | Landed the resume point from the row above: `internal/parser/ddl.go`'s ALTER FUNCTION `SET` branch now calls the same `p.parseSetValueAtoms()` helper the generic `SET` statement (`parser.go`'s `parseSet`/`parseSetValue`) already uses for comma-separated `var_list` values, discarding the result (still a no-op — goopg has no per-function GUC-override storage). New cases added to `TestParseAlterFunctionGenericSetReset` (`internal/parser/alter_function_owner_schema_test.go`): `SET search_path = app, public` and `SET search_path TO app, public, pg_catalog`; confirmed non-vacuous via `git stash` on `ddl.go` alone (both new cases fail pre-fix with the exact predicted syntax error at the comma). Verified live against `cmd/goopg` (real `postgres` superuser session): both forms now return `ALTER FUNCTION`, function stayed callable throughout. Gates: `go build ./...` clean; `go test ./internal/parser/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke scripts/ralph-precommit-test.sh` PASS (0 failed, all 3 workloads). **M0097-0150's ALTER FUNCTION/PROCEDURE/ROUTINE cluster (OWNER TO/RENAME TO/SET SCHEMA/generic SET-RESET incl. var_list) has no known open residuals.** | None — this closes the named resume point with no new narrower gap discovered in-scope. (A genuinely unrelated, severe bug was found incidentally while live-verifying this fix over a non-superuser role; recorded as its own new ledger row below rather than folded in here, since it has nothing to do with ALTER FUNCTION parsing.) |  |  |
| resolved | 2026-07-08 | M0119-0006-DATCONNLIMIT-DEFAULT (CRITICAL — `catalog.InMemory.DatabaseConnLimit` default (0) silently blocks every non-superuser connection to any database; found live while e2e-verifying the M0097-0150 SET var_list fix above, fixed in its own follow-up loop) | Fixed via the comma-ok map lookup: `DatabaseConnLimit` now returns `-1` (not the Go zero-value) when `name` has no explicit `SetDatabaseConnLimit` override. Updated the stale "0 (PG's no limit default)" comments at catalog.go's `databaseConnLimit` field doc and the function doc; the `pg_database` `VirtualRows` render path already called `c.DatabaseConnLimit(n)` so it picked up the corrected default with no separate change needed (only its comment was stale, now fixed too). Added `TestConnectNonSuperuserFirstConnectionUnlimitedDatabaseAccepted` (`internal/server/database_exists_test.go`) — confirmed non-vacuous by reverting catalog.go to its pre-fix HEAD content and re-running: it now fails with the exact predicted `53300 too many connections` FATAL (the test itself needed a fix first: reading only the first frame can't distinguish accept/reject since AuthenticationOk is always sent before the datconnlimit check runs — the test now reads until ErrorResponse or ReadyForQuery). Gates: `go build ./...` clean; `go test ./internal/server/... ./internal/catalog/... ./internal/activity/...` PASS (x3 repeat for flake-check); `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke scripts/ralph-precommit-test.sh` PASS (0 failed, all 3 workloads). | None — fully resolved this loop; no new narrower gap discovered in-scope. | Originally deferred out of the unrelated M0097-0150 loop that discovered it live (Ralph's one-task-per-loop discipline); picked up as the very next task per that row's own resume-point instruction and closed in full. |  |
| resolved | 2026-07-08 | M0122-0006 (index column order restart persistence, follow-up 3: opclass/collation name-OID registry, live + heap-write) | Built the missing name→OID registry row 583 called for: `internal/catalog/catalog.go` gained `builtinColumnOpclassOIDs` (btree-opclass name→OID, verified against a real PostgreSQL 18.3 instance's `pg_opclass` — only a handful have BKI-frozen OIDs in `pg_opclass.dat`, the rest are genbki-autonumbered but stable per build), `defaultColumnOpclassNameForType` (implicit per-type default, also oracle-verified via `CREATE INDEX`+`pg_index.indclass` round-trips), and two exported `*InMemory` methods — `ResolveIndexColumnOpclassOID`/`ResolveIndexColumnCollationOID` — that check user-created classes/collations (`CREATE OPERATOR CLASS`/`CREATE COLLATION`) before the builtin tables. Both were added to the `catalog.Catalog` interface and are now the SINGLE resolution path shared by (1) the live `pg_index` `VirtualRows` renderer (previously a hardcoded, PG-inaccurate per-type OID switch that ignored `ColOpClasses` entirely, e.g. `int2_ops` rendered as 1970 instead of the real 1979) and (2) `buildUserPGIndexRow` (`internal/executor/pg18_user_catalog_rows.go`, the heap-row writer used by `syncIndexToCatalogHeap`/`resyncIndexHeapRow` for restart-recoverable and PG-standby-visible catalog rows — previously always wrote all-zero `indclass`/`indcollation` regardless of `idx.ColOpClasses`/`ColCollations`), so the two renderers can no longer silently diverge. New test `TestPgIndexIndclassIndcollation` (`internal/catalog/catalog_test.go`); live-verified against the real `cmd/goopg` binary via `psql`: `CREATE INDEX ... (body text_pattern_ops)` → indclass=4217, `CREATE INDEX ... (note COLLATE "C" varchar_pattern_ops)` → indclass=4218/indcollation=950, plain `int4` column → indclass=1978 (default) — all match the oracle. | The heap-row WRITE side is now correct, but the READ/decode side is not: `catalog.PGIndexRow`/`DecodePGIndexPhysicalRow` (`internal/catalog/codec.go`) still do not decode `indclass`/`indcollation` bytes at all, so `internal/initdb/open.go`'s `loadUserIndexesFromHeap` has no way to reconstruct `idx.ColOpClasses`/`ColCollations` (the name strings, e.g. "text_pattern_ops") after a *checkpointed* restart — those two fields silently revert to empty/default on the in-memory `Index` struct even though the heap row itself now carries the correct OIDs. An uncheckpointed crash restart is unaffected (per row 582: `wal.CreateIndexPayload`'s extension block already carries `ColOpClasses`/`ColCollations` as name strings directly, replayed via `RegisterIndexDuringRecovery`, no OID round-trip involved) — this gap is specifically the checkpointed/heap-scan recovery path. | Resume point: decode `indclass`/`indcollation` in `DecodePGIndexPhysicalRow` (both are `oidvector`, same wire shape `pgOIDVectorBytes` already encodes — see `indoption`'s `int2vector` decode immediately adjacent for the pattern) and expose as new `PGIndexRow.IndClass`/`IndCollation []uint32` fields; then build the reverse OID→name lookup (`builtinColumnOpclassOIDs`/`builtinCollationOIDByName` inverted, plus a scan over `c.ListUserOperatorClasses()`/`c.ListUserCollations()` for user-created ones) and thread it into `loadUserIndexesFromHeap` (open.go, Pass 3) to populate `ColOpClasses`/`ColCollations` on the recovered `*Index` before `RegisterIndexDuringRecovery`. Add a checkpointed-restart test mirroring `TestCreateIndexPredicateAndIncludeColumnsSurviveCheckpointedRestart` (graceful `rt1.Close()`, not a crash) asserting `ColOpClasses`/`ColCollations` survive for a `text_pattern_ops`/`COLLATE "C"` index. | Scoped out of this loop to land the registry + both write-side renderers as a complete, independently-verifiable unit (matching row 583's own prediction that the registry would be "a materially separate task from the heap-decode plumbing"); the reverse OID→name lookup and `loadUserIndexesFromHeap` wiring is now a much smaller, well-defined follow-up now that the forward registry exists, but is still a distinct read-side change with its own test surface. |
| resolved | 2026-07-08 | M0122-0006 (index column order restart persistence, follow-up 4 of 4: heap-decode of indclass/indcollation, closes the opclass/collation restart-persistence cluster) | Closed the resume point row 591 left open: `internal/catalog/codec.go`'s `PGIndexRow` gained `IndCollation`/`IndClass []uint32`, and `DecodePGIndexPhysicalRow` now decodes both oidvector columns (new `decodePGIndexOIDVector` helper, mirroring the existing int2vector decode for `indkey`/`indoption`) instead of walking past them unread. `internal/catalog/catalog.go` gained the reverse-lookup siblings of row 591's forward resolvers — `ResolveIndexColumnOpclassName`/`ResolveIndexColumnCollationName` on `*InMemory` (added to the `catalog.Catalog` interface) — which turn a decoded OID back into a `ColOpClasses`/`ColCollations` name string, checking user-created classes/collations first then inverting the builtin `builtinColumnOpclassOIDs` map / a new `builtinCollationNameByOID` switch; an opclass OID matching the column type's own implicit default resolves to `""` (no explicit name), mirroring real PostgreSQL's own indclass-vs-default comparison in `ruleutils.c`'s `pg_get_indexdef_worker` so the round-trip is lossless in exactly the same sense PG's is. `internal/initdb/open.go`'s `loadUserIndexesFromHeap` Pass 3 now threads per-key-column `ColOpClasses`/`ColCollations` through to `RegisterIndexDuringRecovery` instead of the `nil, nil` this driver always passed before. New test `TestCreateIndexOpclassAndCollationSurviveCheckpointedRestart` (`internal/initdb/index_ddl_recovery_test.go`) uses a graceful `rt1.Close()` (checkpointed restart, the gap row 591 identified) and asserts a `text_pattern_ops`/`COLLATE "C" varchar_pattern_ops` index's `ColOpClasses`/`ColCollations` survive; confirmed non-vacuous via `git stash` on the 3 impl files (fails with exactly the predicted pre-fix symptom: both slices empty). Gates: `go build ./...`/`go vet ./...` clean; `go test ./internal/catalog/... ./internal/executor/... ./internal/initdb/... ./internal/planner/... ./internal/wal/... ./internal/parser/... ./internal/server/...` PASS; `scripts/tpch-spotcheck.sh` PASS (Q12=2/Q13=33); `RALPH_PRECOMMIT_SCOPE=smoke scripts/ralph-precommit-test.sh` PASS (0 failed, all 3 workloads, run twice). Design: `docs/design/0122-0006-index-column-order-restart-persistence.md`'s new "Follow-up 4 (2026-07-08): heap-decode of indclass/indcollation" section; `docs/design/README.md` row updated. | None — this closes row 591's own resume point in full; the M0122-0006 index-restart-persistence cluster (column ordering, predicate/INCLUDE/nulls-not-distinct, opclass/collation — both the WAL-crash and checkpointed-restart recovery paths) now has no known open residual. | n/a. | n/a — fully resolved, no resume point needed. |
| resolved | 2026-07-08 | M-NIGHTLY (AI-20260708-064334-001, pgbench/nightly reopen) | Found and fixed a real bug in `internal/amcheck` (not `internal/access/btree`): `VerifyBtreeItemOrder` (`internal/amcheck/verify_nbtree.go`) required strictly-ascending adjacent keys, but goopg's `btree.CompareKeys` — the only comparator the engine's own descent/insert/split path uses anywhere — has no heap-TID tiebreak, so goopg's btree never guarantees any particular order among same-key duplicates (confirmed empirically: a throwaway probe dumping `pgbench_branches_pkey` block 1 found 331 leaf items sharing key=1, heap TIDs in genuinely random order — expected under non-HOT-UPDATE churn on a 50-row hot table with no autovacuum in the test window). This made `bt_index_check`/`bt_index_parent_check` report false-positive "item order invariant violated" on nearly every block of any table with duplicate-key churn, including a physically-impossible ~40-block footprint on a 50-row index — which is what first proved it was a checker bug, not real corruption. First attempt matched upstream's heapkeyspace `_bt_compare` tiebreak model exactly (new `PageItemKeyTIDs`/`CompareItemPointers` helpers) but re-verification against the same on-disk data still flagged violations, proving TIDs really are unordered in this engine and the tiebreak model itself was wrong for goopg; replaced with the correct fix — relax to non-decreasing keys only (`cmp > 0` is the sole violation), matching upstream's own non-heapkeyspace fallback (`invariant_leq_offset`). Updated `TestVerifyBtreeItemOrder_DuplicateKeysViolation` (renamed `..._DuplicateKeysAllowed`, flipped assertion — its "goopg dedups equal keys into a posting item" premise was itself wrong, since dedup only runs from `BulkCreate` per the 2026-07-07 row below) and added `..._DecreaseAfterDuplicateViolation` for the still-real case. Post-fix, `pgbench_branches_pkey`/`pgbench_tellers_pkey` verify clean against the same captured data; `pgbench_accounts_pkey` (RelOid 16412 — the exact relation AI-20260708-064334-001 named) now surfaces a much more targeted, plausibly-genuine "high key invariant violated" on 2 of ~10000+ blocks instead of near-universal. | The ORIGINAL nightly symptom (pgbench transaction aborting mid-workload with a runtime "btree: empty internal page" error) was never reproduced this loop — only 2×25s repro windows were tried (the nightly stage uses T=180×3) and both completed with 0 failed transactions; whether it's already fixed by the two 2026-07-07 fixes (`510615b4`/`8ebb71cd`, both ancestors of the nightly commit `2e435e91`) or a distinct still-live bug is unresolved. Separately, the newly-surfaced `pgbench_accounts_pkey` high-key violation (2 internal/level=1 pages where only the LAST item exceeds the page's own high key, e.g. block 10158's last item `803a6aa9` > high key `80397cd3`, every earlier item properly ascending) is unexplained — consistent with a downlink misrouted to the wrong sibling during an internal-page split, but not yet traced to a mechanism. | Resume point 1 (confirm/refute original symptom): re-run the authoritative repro at the FULL T=180×3 scale (`bash ci/batch/stages/stage-pgbench.sh`) or loop the cheap scale=50/c=100/j=20/T=25 recipe several times back-to-back with `bt_index_check(..., true)` after each run, watching for the actual runtime abort (not just the post-hoc amcheck scan). Resume point 2 (accounts_pkey high-key violation): instrument `insertIntoBlock`'s internal-page-split path (`internal/access/btree/btree.go` ~1420-1660, under `bt.splitMu`) to catch the exact moment a downlink lands on the wrong side of a split — the high-key-violation symptom is deterministically reproducible (unlike the intermittent runtime abort) via the same `pgbench -i -s 50` once + `pgbench -c 100 -j 20 -T 25 -P 5` twice + `bt_index_check` recipe, so it is the cheaper lead to chase first. | The amcheck false-positive fix was a complete, independently-testable, high-value unit (it was actively producing misleading corruption reports for every future nightly diagnosis on any duplicate-key-churned table) — landed and verified in full this loop. The two remaining leads (original runtime symptom confirmation, accounts_pkey high-key violation root cause) are each open-ended investigation threads in the same vein as the 18-loop 2026-07-07 chase (see the M-NIGHTLY pgbench/nightly rows above) and warrant their own dedicated, non-rushed loops rather than a rushed attempt here. |
| resolved | 2026-07-08 | M-NIGHTLY (AI-20260708-064334-001, 3rd loop on the reopen) | Un-skipped `TestVerifyBtreeEngineSilentOnRealConcurrentContended` (2nd-loop repro) and confirmed it still reproduces (6-23 lost entries per run). Extended `buildRealTreeConcurrent` to return every successfully-inserted `(key, TID)` pair so the test can diff the real leaf walk against the expected set and log the EXACT missing entries (not just a count) — losses are single-item, scattered across the full key range and across writers/iterations (no clustering by page, writer, or iteration), ruling out a whole-page-skip or single-hot-writer theory. Added `pool.DebugValidateCleanEvictions` (`internal/storage/bufpool.go`, off by default/zero-cost, gated bool field + `debugValidateCleanEviction` helper called from `evictVictim`'s `!wasDirty` branch): when set, every "clean" (non-dirty) eviction re-reads its block from disk and byte-compares against the in-memory page before discarding it. This test now sets the flag and it FIRES on every run (1-2 times), e.g. `tag={...Block:127} firstDiffByte=12 lastDiffByte=3527 ndiffBytes=264 diskLSN=0 memLSN=0` — DEFINITIVE, direct proof that `evictVictim`'s `!wasDirty` fast path ("on-disk content already matches, safe to skip the flush") is being taken for slots whose in-memory content has dozens-to-hundreds of bytes of accumulated, unflushed writes (diffs start right after the 12-byte page header, spanning most of the item area — consistent with several inserts' worth of content, matching the 1-2 mismatch-events-per-run vs. 6-23-entries-per-run ratio). Ruled out three specific mechanisms this loop: (1) ABA via the 15-bit slot-generation counter (`claimVictim`'s `newGen := (curGen+1)&0x7FFF`) wrapping past 32768 and coincidentally matching a stale caller's cached gen — measured (temporary per-slot counter, since removed) a max of ~2500 claims per slot in a full run, nowhere near the 32768-wrap threshold; (2) stale recycled-block reuse via `pinNewOrRecycled`'s `popRecycledBlock`/zero-and-reuse path — dead code for this workload, since `recycleBlock` is only ever called from `btree_vacuum.go` and this test never vacuums/deletes; (3) a plain unsynchronized data race — `go test -race` on the exact same repro reports ZERO race warnings while still reproducing both the missing-entries symptom AND the clean-eviction-mismatch log lines, meaning every read/write of the relevant state IS properly synchronized via the existing atomics/mutexes, so the bug is a pure protocol/ordering-logic defect, not a torn/unsynchronized memory access. Also audited every `MarkDirty`/`MarkDirtyChangeRecord`/`MarkDirtyWithLSNLocked` call site in `internal/access/btree/btree.go` (`tryInsertNoSplit`, `insertIntoBlock`'s no-split/dedup-avoided-split/split branches, `createNewRoot`, `finishSplit`, `clearIncompleteSplit`, `clearRootFlag`) — every one correctly follows `insertItemSorted`/content-write and precedes `Unpin`, with the pin held continuously across the write+dirty-mark in every case (confirms MarkDirty is not simply being skipped by btree.go). Enumerated every dirty-bit-CLEARING site in bufpool.go (`flushBatch`'s post-flush clear loop — not invoked, this test never calls `FlushAll`/`FlushAllPaced`/checkpointer; `InvalidateRel`/`InvalidateBlock` — not called by this workload; `claimVictim`'s own full-state-overwrite CAS, which is the eviction decision itself and correctly reads dirty from a freshly-loaded `old` before replacing it) — none explain an already-correctly-set dirty bit reading back false later for the SAME (slot, tag) occupancy without an intervening eviction. Confirmed (via `storage.InvalidBlockNumber = 0xFFFFFFFF` in `page.go`) that the prior loop's "`tryInsertOnCachedRightmost` compares `op.Next` against literal `0`, dead code" finding is correct and not the cause. | The exact sequence that makes `claimVictim` observe `dirty=false` for a slot that a completed, properly-ordered `MarkDirty` call had already set `dirty=true` on is STILL not identified at the line level — this is now a very tightly bounded mystery (proven not ABA, not recycled-block reuse, not a data race, not a missing/misordered MarkDirty call in btree.go, not a known bufpool.go clearing site) but the actual mechanism remains open. | Resume point: add a monotonic (atomic-counter-stamped) per-slot ring-buffer event log inside bufpool.go recording every `MarkDirty`/`MarkDirtyChangeRecord`/`MarkDirtyWithLSNLocked` call (slot idx, tag, old/new state, sequence number) and every `claimVictim` CAS attempt (slot idx, tag, old/new state, sequence number, dirty-bit observed) — gate it behind a new bool flag mirroring `DebugValidateCleanEvictions`'s zero-cost-when-off pattern. Re-run `TestVerifyBtreeEngineSilentOnRealConcurrentContended` with both flags on, and when a `debugValidateCleanEviction` mismatch fires for tag T, dump T's full event history to find the specific MarkDirty call whose effect got lost and what happened to `s.state` immediately after it (a legitimate full-state-overwrite from a LATER, unrelated eviction of the same slot for a DIFFERENT tag is one still-unexplored possibility — i.e., does this specific tag ever get evicted+reloaded into a DIFFERENT slot mid-lifetime, and could two logically-sequential dirty-tracking windows for the "same" tag actually span TWO different physical slots in a way that loses the dirty signal at the handoff?). Also worth cross-checking against the real pgbench nightly repro once fixed, since that workload DOES run with vacuum/autovacuum enabled — the `pinNewOrRecycled` recycled-block path ruled out here is NOT dead code there, and deserves its own dedicated check once this insert-only bug is closed. | Landed a definitive, byte-level proof of the exact WHERE (the clean-eviction fast path) with a reusable, zero-cost-by-default instrumentation flag as the durable artifact, and closed off three plausible mechanisms with hard measurements (max-claims count, dead-code confirmation, `-race` clean run) rather than leaving them as open guesses for the next loop to re-litigate — but the precise WHY (which write's dirty-mark effect actually vanishes, and through what sequence) needs a live event-log trace to catch in the act, which is a distinct, well-scoped next step rather than something to rush in an already-long loop. |
| resolved | 2026-07-08 | M-NIGHTLY (AI-20260708-064334-001, 2nd loop on the reopen) | Built a fast (~1s), reliable in-process repro for a genuine lost-index-entry bug: `internal/amcheck/verify_nbtree_realtree_test.go`'s new `TestVerifyBtreeEngineSilentOnRealConcurrentContended` (skipped by default, `t.Skip` names this exact ledger row) drives 64 goroutines calling `bt.Insert` concurrently on a shared narrow key range (n=200000, keyRange=50000 — mirrors pgbench's uniformly-random-aid UPDATE churn on `pgbench_accounts_pkey`, goopg has no HOT update so every UPDATE re-inserts), then asserts the amcheck engine's leaf-level walk collects exactly n entries. Narrowed the root cause LOCUS (not yet the exact line/fix): built a debug `insertItemSorted` write-log hook (temporary, reverted) proving every lost item genuinely reached `insertItemSorted` (no logic bug in `internal/access/btree`'s split/dedup/redistribution code — `pageItems`/`parseItem`/`parsePostingRaw` all correctly copy key bytes, ruling out an aliasing theory) — the same insert workload with a small buffer pool (`storage.PoolConfig{Slots: 64}`, heavy eviction churn) loses 7-16 real leaf entries in every run; the IDENTICAL workload with a large pool (`Slots: 2048`, no eviction needed for this ~530-block working set) loses ZERO. This isolates the bug to `internal/storage/bufpool.go`'s eviction/flush path (`claimVictim`/`evictVictim`/`pinLoad`), not the btree package — a sibling of the already-fixed `evictVictim` bufmap.Delete-before-flush race (commit `510615b4`, M0056-followup-multiwriter-flake). Also noted but NOT fixed (harmless, out of scope this loop): `tryInsertOnCachedRightmost`'s rightmost-leaf fast-path cache (`btree.go` lines ~1338, ~2047) compares `op.Next` against literal `0` instead of `storage.InvalidBlockNumber` (0xFFFFFFFF) — since every real page construction site in the codebase sets `Next: storage.InvalidBlockNumber` for "no right sibling", this comparison is always false for a genuine rightmost leaf, so the cache never actually engages (dead optimization, confirmed NOT the data-loss cause since disabling it entirely — implicit, it never fires — didn't stop the loss). | The exact eviction-path bug (which function, which race window) is still unidentified — only the module-level locus (bufpool.go eviction/flush, not btree.go) and a load-bearing pool-size threshold (loss at Slots=64, none at Slots=2048) are confirmed. The original nightly runtime symptom ("empty internal page" abort) and the accounts_pkey high-key-invariant violation from the prior loop's row above are ALSO still unconfirmed/untraced — this loop did not re-attempt either. | Resume point: instrument `internal/storage/bufpool.go`'s `claimVictim`/`evictVictim`/`pinLoad` (and `flushSlot`) with a debug hook analogous to the one built (and reverted) this loop, re-run `TestVerifyBtreeEngineSilentOnRealConcurrentContended` (un-skip it first) at `Slots: 64`, and binary-search the pool size (try 128, 256, 512...) to find the exact threshold — a narrower threshold will narrow the eviction-frequency window the race needs. Once the exact race is found and fixed, un-skip the test (remove the `t.Skip` line) as its regression guard, and separately re-run the original pgbench-based repro (`ci/batch/stages/stage-pgbench.sh`, s=50 c=100 j=20 T=180) to check whether the runtime "empty internal page" abort is the SAME bug (lost item feeding a downstream structural read) or still a distinct one. | Root-caused enough to hand off a precise, cheap, reliable repro + a narrowed locus in one loop; going further today risked burning the loop budget on ad hoc bufpool instrumentation without landing anything reviewable — the repro test itself, even skipped, is the durable artifact that makes the next loop's job a targeted bufpool debugging session instead of a re-derivation from a 5-minute pgbench symptom. |
| resolved | 2026-07-08 | M-NIGHTLY (AI-20260708-064334-001, 4th loop on the reopen) | Built `pool.DebugTraceSlotEvents` (`internal/storage/bufpool.go`, off by default/zero-cost like `DebugValidateCleanEvictions`): a bounded per-slot ring log (`slotEventRing`, 64 entries) recording every `MarkDirty`/`MarkDirtyWithLSNLocked`/`MarkDirtyChangeRecord`/`claimVictim`/`evictVictim`/`pinLoad`-publish/`PinNew`-publish/`releaseVictimSlot` state transition (slot idx, tag, old/new packed state, global monotonic seq); wired to auto-dump (a) the victim slot's own history and (b) a cross-slot scan for any OTHER slot ever touching the same tag, whenever `debugValidateCleanEviction` fires a mismatch. Re-ran `TestVerifyBtreeEngineSilentOnRealConcurrentContended` (temporarily un-skipped locally, re-skipped before commit) and captured the exact sequence for one real mismatch: a slot loaded a page fresh from disk via `pinLoad` (genuine cache miss), was `Unpin`'d with **zero** intervening `MarkDirty` calls, then was re-claimed as a victim moments later — legitimately clean by every tracked transition — yet the disk re-read still disagreed with the just-read memory content. The cross-slot scan found no other slot's ring ever recorded that tag, ruling out double-ownership via bufmap. Audited (all clean, no bug found): `bufmap.go`'s `Insert`/`Delete`/`Lookup`/`compact` (mutex-serialized, no duplicate-tag window); `relFile.readBlock`/`writeBlock`/`extend` in `smgr.go` (all three share one per-relation `r.mu` and use `ReadAt`/`WriteAt`, not `Seek`+`Read`, so no torn-offset race); `Manager.relFile`'s single-relFile-instance-per-rel cache (mutex-guarded map lookup); `arena.slot`'s three-index, non-overlapping byte-range slicing. **Decisive finding**: ran the repro 3 more times back-to-back and recorded (mismatches, missing-entries) pairs of (1,12)/(0,20)/(0,13) — the run with the MOST missing entries (20) had ZERO `DebugValidateCleanEviction` mismatches. Missing-entry count and mismatch-firing are uncorrelated, so the "clean eviction discards a real unflushed write" mechanism proven in the 3rd loop is at most a minor/coincidental contributor, not the dominant (or possibly not even a real) cause of the lost B-tree entries. | The dominant loss mechanism is now believed to be a genuine lost `btree.Insert` via a structural race (concurrent split/redistribution corrupting or dropping an item on write) rather than an eviction/flush race in `internal/storage/bufpool.go` — but this is not yet proven, only the leading hypothesis after ruling out the eviction-path theories this loop and the prior three. | Resume point: reuse the 2nd loop's reverted `insertItemSorted` write-log hook (`internal/access/btree/btree.go`), but this time keep it live end-to-end (not revert it) and cross-reference its full write log against the final leaf walk to pinpoint the exact `(page, slot-offset)` a lost key was written to; then dump that page's `slotEvents` history (now available via `DebugTraceSlotEvents` without building new instrumentation) to see whether a split/redistribution call overwrote or dropped it. If the page-level trace shows nothing wrong on the storage side either, the bug is purely logical (pageItems/parseItem/redistribution arithmetic) inside `internal/access/btree`'s split code, not a storage-layer race at all — re-focus there next. | The eviction-path investigation (this loop's 4th consecutive) has now been run to ground: every mechanism reachable from `claimVictim`/`evictVictim`/`pinLoad`/`PinNew`/bufmap/relFile/arena has been either fixed (loop 13's Delete-after-flush ordering) or affirmatively ruled out with hard evidence (this loop's uncorrelated mismatch-vs-missing-count data), and continuing to chase the clean-eviction mechanism specifically would very likely be chasing a red herring; recording the pivot explicitly here so the next loop starts at the btree split/redistribution layer instead of re-deriving this loop's ruling-out work. |
| resolved | 2026-07-08 | M-NIGHTLY (AI-20260708-064334-001, 5th loop on the reopen) | Built `BTree.DebugTraceInserts` (`internal/access/btree/btree.go`, off by default/zero-cost like the sibling storage-layer flags): an unbounded, mutex-guarded global log of every `insertItemSorted` call across all 6 call sites (fast-path, cached-rightmost, split-left-refill, split-right-fill, dedup-recovery-rebuild, ApplyInsertRecord/createNewRoot untouched — those write downlinks, not leaf entries), recording (seq, block, 0-based line-pointer index, key, TID); exported `InsertLogRecordsForTID`/`InsertLogRecordsForBlockAfter` for cross-referencing. Paired with `Pool.DumpEventsForTag` (`internal/storage/bufpool.go`), the exported string-returning sibling of the existing `dumpCrossSlotEventsForTag`, so a caller outside package storage can pull a block's per-slot event history. Wired both into `TestVerifyBtreeEngineSilentOnRealConcurrentContended` (temporarily un-skipped to run, re-skipped before commit) via a per-missing-entry diagnostic block. **Result: proved, not merely hypothesized, that the loss happens inside the split/redistribution rewrite, not the storage/eviction layer.** For every missing entry across two fresh repros: (1) `insertItemSorted` was called EXACTLY ONCE for that exact (key,TID) — confirmed physically written, since `PageInsertItemRawAt` panics on failure and no panic occurred; (2) a global scan of the ENTIRE run's insert log for that TID found no second occurrence on ANY block — ruling out "carried to a new block by a later split" (a split's left/right redistribution loop re-invokes `insertItemSorted` for every surviving item, which would log a second record with the same Ptr); (3) the origin block went on to receive hundreds more successful `insertItemSorted` calls afterward (healthy ongoing activity, not an abandoned/evicted page); (4) reading the block's CURRENT on-disk bytes at test end confirms the TID is genuinely absent — in several cases with sibling entries sharing the identical (duplicate) key still present and intact, proving this is not a whole-page loss but a single-entry drop during a rewrite. Since a plain single-item `insertItemSorted` call only adds a line pointer (shifts others, never deletes existing tuple bytes), the only mechanism that can make a previously-written entry vanish without a trace is `insertIntoBlock`'s split branch: `resetPageItems` wipes the entire line-pointer array, then `pageItems`+`appendSorted`+`dedupConsolidate` recompute the survivor set from scratch and reinsert it split across left/right (btree.go ~1523-1574). | The exact statement inside that rebuild that drops the item is still unidentified — `pageItems`, `dedupConsolidate` (exact-(key,ptr)-duplicate filter, uses the standard safe in-place `items[:0]` idiom), and `appendSorted` (binary-search insert into a copy) were each re-read function-by-function this loop with no single-threaded logic flaw spotted by inspection alone. Also confirmed NOT the cause this loop (re-derived from code reading, not new instrumentation): goopg's postings mechanism (`marshalPosting`) is never invoked from the online `Insert` path at all (`dedupConsolidate`'s own comment: "we DON'T yet rebuild posting bytes here"), so posting-list expansion/parsing bugs are categorically ruled out for this repro; `bt.splitMu` fully serializes the entire split-path recursion (descendToLeaf + insertIntoBlock + createNewRoot), so a "two concurrent root lifts" race is unreachable in today's code (the `createNewRoot`/`finishSplit` defensive branches for that race are for a future splitMu-removal stage, not live now). | Resume point: instrument `insertIntoBlock`'s split branch directly (btree.go ~1523, gated by the same `DebugTraceInserts` flag or a sibling one) — log `PageLinePointerCount(slot.Page())` immediately before the split starts, `len(allItems)` right after `pageItems()`, and `len(allItems)` again after `dedupConsolidate`, for the specific block a lost entry's original insert targeted. Cross-reference against the already-available `bt.InsertLogRecordsForBlockAfter(blk, seq)` to find the FIRST split event after a lost entry's insert (identifiable as a tight burst of consecutive `insertItemSorted` calls with monotonically-increasing lineIdx starting from 0 — the left-refill loop's signature, distinct from steady-state single-item inserts) and check whether the lost item's key is present in the `pageItems()` read at that exact moment. If `pageItems()` already undercounts, the bug is in the page-read/decode; if `dedupConsolidate` receives the full correct set but emits fewer, the bug is in the dedup filter itself (re-audit for a comparison-direction or off-by-one edge case, e.g. three-or-more-way duplicate runs) despite this loop's inspection finding it structurally sound. | Root-caused to the split/redistribution mechanism with hard evidence (not inference) but the exact defective line is not yet found; recording as still-open rather than resolved because no fix has landed — the next loop should NOT re-derive the eviction-path ruling-out (loops 1-4) or the "was it ever written" proof (this loop), only continue from the split-branch instrumentation resume point above. |
| resolved | 2026-07-08 | M-NIGHTLY (AI-20260708-064334-001, 6th loop on the reopen) | Executed the 5th loop's exact resume point: built `BTree.RewriteLogEvent`/`traceRewrite` (`internal/access/btree/btree.go`) — a deep-copy snapshot of `allItems` taken right after `pageItems()`+`appendSorted()` and again right after `dedupConsolidate()`, for every `insertIntoBlock` rewrite (both the dedup-recovery no-split path and the real split path), plus `PageLinePointerCount` read just before. `insertLog` and the new `rewriteLog` now share one monotonic `logSeqNext` counter (previously each used its own `len()`-based sequence) so events from the two logs can be compared for true temporal order. Wired a per-missing-entry diagnostic into `TestVerifyBtreeEngineSilentOnRealConcurrentContended` (temporarily un-skipped to run, re-skipped before commit) that finds the first rewrite event on a lost entry's block at/after its insert and reports whether the (key,TID) was present in each snapshot. **Result: REFUTES the 5th loop's split/redistribution localization.** Every rewrite event on an affected block shows `presentAfterPageItems=false` with `postPageItemsCount` EXACTLY `preLineCount+1` (matching `PageLinePointerCount` with zero discrepancy) — `pageItems()` correctly decodes every physical line pointer that IS on the page; the page itself already lacks the lost entry's line pointer before this rewrite ever runs, so `dedupConsolidate` has nothing to wrongly drop. Several missing entries (e.g. writer 34 iters 2284/2601, blocks 285/311) show NO rewrite event at all after their insert — only plain single-item fast-path `insertItemSorted` calls (`tryInsertNoSplit` / `insertIntoBlock`'s own no-split branch / `tryInsertOnCachedRightmost`) ever touch those blocks afterward, yet the entry still vanishes. Also ran this exact contended-duplicate-key repro under `-race` for the first time ever (all 20+ prior -race-clean runs in this thread used the DISJOINT-key `TestMultiWriterStress_M0055_Phase_C` repro instead) with `GORACE=halt_on_error=0` to run to completion: exactly ONE report, inside `dumpCrossSlotEventsForTag`/`traceSlotEvent` (`internal/storage/bufpool.go`) — the `DebugTraceSlotEvents` ring-buffer debug tool's own cross-slot scan, whose doc comment already documents same-slot torn reads as an accepted best-effort tradeoff (the cross-slot case is a stricter variant of the same accepted tradeoff, not a new class of bug); it does not touch or explain the missing-entry mechanism. No other race fired across the full run. | The exact fast-path mechanism that makes a previously-written line pointer vanish during subsequent same-page `insertItemSorted` calls (with no rewrite/reset ever touching the page) is still unidentified — `PageInsertItemRawAt` (`internal/storage/heap.go`) was re-read this loop and its line-pointer-array shift arithmetic is internally consistent for a single isolated call (reads `Header(p)` once, computes `count`/`lower` from it, and unconditionally sets `Lower()` to `lower+itemIDSize` on success) — if a bug exists there it would need a second, concurrent mutator of the SAME page bypassing `pinW`'s exclusive lock, which the clean `-race` run this loop makes very unlikely. Not yet ruled out: `tryInsertOnCachedRightmost`'s dormant-cache path (confirmed dead code as of the 8th 2026-07-07 loop; not re-verified this loop) and a posting-list interaction (ruled out structurally in the 5th-loop row — `dedupConsolidate` never creates postings in the online Insert path — but not re-verified against this loop's new data). | Resume point: apply the SAME before/after `pageItems()`-snapshot technique built this loop, but to the fast-path single-item `insertItemSorted` call sites instead of the rewrite path — for the specific blocks known (post-hoc, from this loop's diagnostic) to lose an entry, snapshot `pageItems()` immediately before and immediately after each fast-path `insertItemSorted` call on that block (expensive per-call full-page decode, but this is already an opt-in instrumented test, not production code) to find the exact consecutive pair of fast-path inserts where a previously-present (key,TID) silently disappears — that pinpoints the exact call/statement responsible. Do NOT re-open the split/rewrite path (this loop's `RewriteLogEvent` instrumentation already conclusively clears it) or re-run `-race` on the disjoint-key repro (already exhaustively clean per loops 1-5's ~1180+ iterations). | Landed real, reusable, zero-cost-when-off instrumentation (`RewriteLogEvent`/`traceRewrite`, kept committed like every prior DebugTrace* aid in this thread) that definitively falsified the previous loop's leading hypothesis with hard per-snapshot evidence rather than more code reading, and closed off a THIRD independent data-race angle (this exact contended-duplicate-key repro, never before run under `-race`) — the correct amount of forward motion for one loop given this is the 6th consecutive loop on the same nightly item; going further into fast-path instrumentation this loop would risk an unreviewed, unfinished half-built trace at time-of-cutoff instead of a clean, verified stopping point. |
