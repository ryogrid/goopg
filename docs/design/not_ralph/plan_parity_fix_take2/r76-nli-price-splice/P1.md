# R76 P1 outcome: stamp works, election story intact on a real base

Binary: `/tmp/pp2/r76/goopg-r76stamp` (HEAD `0e203b1` + TEMP
env-gated `GOOPG_R76STAMP` splice, reverted after). Cluster: private
clone `/tmp/pp2/clone-tpch-r65` `:5533`. A/B on ONE binary (env
on/off) — the gate is the only delta.

## Q4 stamped (×2 identical)

`Sort 491312.45 → HashAggregate 491169.67 → NL Semi Join
0.38..490456.34 rows=44653` over scan 15570.66. Shape unchanged;
costs PG-scale (PG 192121.20..192222.42 — 2.56× = upstream
rows/width gaps, program items 1–2). pp: `Q4 SHAPE-DIFF
[aggregation-strategy,sort-strategy]` — the R72 election flip now
priced on a real base; no new category.

Rows side effect (recorded): semi rows 57066 → 44653
(rewrite-time `EstimateRows(j)` vs EXPLAIN-time estimate — the known
C-20a two-estimator asymmetry). Costs are the slice; the rows delta
rides along via the funnel and is adjudicated by gates at R77.

## Read-only family (Q21/Q22)

- Q22 ANTI stamps (jt=6, 279270.44): shape unchanged
  (Sort→HashAgg→Anti); pp same election family + InitPlan-parallel
  note. Display rows 16666 vs stamped 18200 — a second estimator
  seam (stamp applied to a node instance the renderer does not
  read?); R77 investigates only if gates complain.
- Q21 SEMI+ANTI both stamp, costs-only diffs (whole upper plan was
  seam-priced at 0.01 before).

## Non-target byte-identity: 19/22 identical

All 22 TPC-H EXPLAINs, stamped vs unstamped: only Q4/Q21/Q22 differ
(EXISTS family), all costs-only except Q4's rows note above. No
shape flips — admission moves nothing it does not price.

## Gates status

- pp Q4/Q22: done (above). Q13 untouched (byte-identical ✓).
- keeper subset 16 passed; optimizer builds clean.
- values 24/24 + sf025 sweep: DEFERRED to R77 (deviation from SCOPE
  P1 recorded here — TEMP binary; the full gates gate the
  production splice, where GUC-threading (caveat 2) also lands).

Temp fully reverted; tree clean.
