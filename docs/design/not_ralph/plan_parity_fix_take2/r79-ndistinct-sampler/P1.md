# R79 P1: nd-vs-target curves on l_orderkey (both engines)

## Curve data (foreground, pinned env)

goopg `:5556` (private clone-tpch-r79, HEAD binary, deterministic
seed `GOOPG_ANALYZE_SEED=20260905`), session
`default_statistics_target`, `ANALYZE lineitem` per point:

| target | n_distinct | ≈ rows |
|---|---|---|
| 1 | -1 (degenerate fallback) | — |
| 10 | -0.19984654 | 1,199,570 |
| 100 | -0.19558193 | 1,173,738 |
| 1000 | -0.20093048 | 1,205,960 |

Flat within ~3% for every usable target. (True distinct 1.5M.)

PG `:65432` (shared ref; per-column `SET STATISTICS`, restored
to -1 + re-ANALYZEd after; restore run within same order of
magnitude — sufficient for verdict (b) given the 1.2M-vs-0.34M
separation):

| target | n_distinct | ≈ rows |
|---|---|---|
| 10 | 343831 | 343,831 |
| 100 | 347537 → 324753 (restore run; 6.6% spread across two runs, n=2, confounded with set/restore cycle — mean ~336k labeled as such) | ~336k |
| 1000 | -0.20276237 | ~1,216,000 |

3.5× jump at 1000 (absolute→fraction form switch is by design).

## Reading (verdict input, not a fix)

- Sampler-effect CONFIRMED, target-handling cleared on the
  tested session-target path {10,100,1000} (sparse log-spaced
  points; intermediate targets, per-column-vs-session
  interaction beyond the found ALTER-persistence quirk, and
  the target=1 degenerate path unexamined). Curves are
  separate at 10/100 (1.2M vs 0.34M) and converge at 1000
  (1.206M vs 1.216M, ~0.8%) via PG's by-design
  absolute→fraction switch — i.e., PG's widening slice finally
  sees the full range, consistent with the P0 sampler-effect
  prediction; no crossing pattern indicative of a
  target-handling bug in the tested path.
- goopg closer to truth (1.5M) at 10/100 (1.2M vs 0.34M);
  tied at 1000 (1.206M vs 1.216M). Target=1 degenerate (-1)
  is an edge below any sane use.
- Contamination control: an early `ALTER ... SET STATISTICS
  10` DOES persist (`operators_ddl.go:10602`,
  `StatTarget=&v`) — the first 1000-point ran under column
  target 10. Caught by identical-to-8dp output; reset via
  `SET STATISTICS -1`, re-ran 1000 clean. Session
  `default_statistics_target` threads correctly
  (`dispatch.go:498` → `ectx.StatsTarget` →
  `analyzeRelationCtx`); target=1 response proves it.
  Deterministic seeded sampler is consistent with (single
  identical rerun; n=1) the restore run bit-identical to the
  pre-experiment value.
- goopg ANALYZE scope gap (CLAUDE.md "relation does not
  exist") did NOT reproduce on this clone path.

## Verdict: (b) keep superior stats, close Q4 elsewhere

Replicating PG-sampling = deliberately degrading every
ndistinct consumer corpus-wide to chase one derived fraction
(Q4's 0.23 needs nd2≈347k; goopg's floor is ~1.17M at every
tested usable target {10,100,1000} — 3.4× above the need,
unreachable within tested targets, not just unreached). The
nd2 input is exonerated as the lever. "Rows need another
input": nd2 ruled out as the lever on tested targets;
candidate replacement inputs are a non-nd2 semi-fraction
source to be owned by the follow-up program
(election/firing-rule per R71/R72, widths per R70-unblock);
named, not owned here. No estimator code changed (R80 owns
estimators).

Row bases: goopg 6,001,255 (`count(*)` live), PG 5,998,835
(`count(*)` live) — 0.04% apart.

## State left behind

- PG `:65432`: `l_orderkey` statistics target restored to
  default, re-ANALYZEd, verified same-order. Untouched
  otherwise.
- goopg `:5556`: re-ANALYZEd at default 100 (canonical
  -0.19558193); per-column override reset. Clone is private
  (`/tmp/pp2/clone-tpch-r79`); peer `:5533` untouched.
