# AGENT-REVIEW: take3 04 / 05 / 06 (goopg current-state docs)

Scope: `04-goopg-planning-pipeline.md`, `05-goopg-cost-model.md`,
`06-goopg-statistics-infrastructure.md` vs code at HEAD `b4e68c574`
(docs claim re-verification at `d5f8a6ff9`, 5 commits earlier).
Method: full read of all three; 35+ load-bearing claims spot-checked
read-only (`grep`/`read` in `internal/optimizer/`, `internal/catalog/`,
`internal/executor/`, `internal/postmaster/`; `git log -1` for cited
hashes). No servers, no tests. Serena symbol tools timed out, so
verification is by file search + targeted reads (same evidence, more lines).

## Verdict: APPROVE-WITH-CHANGES

The core technical narrative survives adversarial checking: every major
"landed" claim I tested is present in code (seam + constant propagation,
`uint32`/`maxSearchRels=32`, settings carrier + session fill, live
`DisabledNodes`, rescan/materialised-inner/bucket-walk/mergejoin-tuples
costing, unique clamp, conjunction pairing, nulltestsel, MCV pairing,
hypergeometric MCV, ndistinct two-form fix, override fix, decode fix,
VACUUM/TRUNCATE durability + cache invalidation/guards, reconcile
tripwire, joinorder.go deletion, GEQO wiring, all cited commits resolve
with matching subjects). No invented-landings found.
But the fidelity apparatus is structurally stale (take2 numbering carried
into take3), two file attributions are wrong, line citations drifted
systematically, and stale code comments at cited sites contradict the
docs. Fix M1–M9 before these docs anchor gap analysis.

## Findings

### M1 (MAJOR) — 05 §10: `"Item" cites doc 02 §10` dangles; whole column is take2 numbering
File+section: `05-goopg-cost-model.md` §10, header line 534.
Evidence: take3 `02-pg-cost-model.md` has §§1–9 only (checklist is §9,
53 numbered items, `02:599+`); there is no §10. Take2 `02` §10 had 93
items (`grep -c` = 93) — the source of 05's numbers. Spot misalignment
vs take3 `02` §9: `clamp_row_est`|5 → actually item 4; `get_parallel_divisor`|26
→ 18; `cost_qual_eval`|15–22 → 10–14; byte-size|6 → 5; `index_pages_fetched`|27
→ 19; `cost_seqscan`|25 → 17 (25 is bitmap-tree); EXPLAIN|93 → 53; items
56–92 have no target at all (`approx_tuple_count`|81, `hashjointuples`|80–81, etc.).
Fix: re-key the column to take3 `02` §9 numbers (or drop it and cite PG
symbols only). Until then the table is unauditable against its own oracle.

### M2 (MAJOR) — 05: `Section numbers mirror doc 02 one-to-one` is false for §2/§3/§4/§9/§10
File+section: `05` header lines 3–8.
Evidence: `02` §2 = {2.1 clamps, 2.2 parallel divisor, 2.3 qual_eval, 2.4 byte
helpers, 2.5 widths} vs `05` §2 = {2.1 clamps, 2.2 divisor, 2.3 qualEval,
2.4 rest-qual ABSENT, 2.5 bytes, 2.6 indexPagesFetched, 2.7 approx ABSENT,
2.8 width}; `02` §3.2 = `cost_index` vs `05` §3.2 = samplescan ABSENT (cost_index
is 05 §3.3); `02` §4.2–4.4 (sort/material/rescan/memoize/subplan/agg in two
subsections) vs `05` §4.1–4.13 split; `05` §9 = EXPLAIN surface vs `02` §9 =
checklist; `05` §10 vs nonexistent `02` §10 (see M1).
Fix: narrow the claim (top-level §§1,5,6,7,8 align) + add an explicit
section-mapping table for §2/§3/§4/§9/§10.

### M3 (MAJOR) — 04 §4.4: wrong file/symbol for `makeSpecialJoinInfo`
File+section: `04` §4.4 ("`makeSpecialJoinInfo` (`collapse.go:398`)").
Evidence: `collapse.go:398` is `deconstructFromItem`; `makeSpecialJoinInfo`
is `internal/optimizer/specialjoin.go:54` (method `collectSpecialJoinInfos`
at `specialjoin.go:150`, call site `collapse.go:416`, wired at
`planner.go:2547`). The blocker analysis (name resolution order) is
unaffected, but the citation sends a reader to the wrong function.
Fix: cite `specialjoin.go:54` (def) / `collapse.go:416` (call).

### M4 (MAJOR) — 06 §1.5: wrong file for `columnStatsForChildBase`
File+section: `06` §1.5 ("delegates to `columnStatsForChildBase`
(`selectivity.go:450-466`)").
Evidence: `selectivity.go:451-467` is the delegating wrapper
`columnStatsForChild`; the Base lives at
`internal/optimizer/cardinality.go:899`. The P1-26 claim itself verifies
(wrapper delegates, one arm list) — only the attribution is wrong.
Fix: cite `selectivity.go:451` (wrapper) → `cardinality.go:899` (Base).

### M5 (MAJOR) — 04 §12.2: `DefaultPlannerSettings()` "not a second copy" overstates
File+section: `04` §12.2 ("defined as what `defaultCostParams()` reads").
Evidence: `internal/optimizer/plannersettings.go:118-151` — only the seven
prices + ECS come from `cp := defaultCostParams()`; `WorkMem` comes from
`hashsize.DefaultMemLimitBytes` (deliberately, :144-149), enables from
process globals (`HashAggEnabled()`, `PresortedAggEnabled()`, `NLIEnabled()`,
`GeqoEnabled()`, `GeqoThreshold()`), `GeqoEffort: 5` and `GeqoSelectionBias:
2.0` are literals. `TestDefaultPlannerSettingsMatchTheHardWiredParams` pins
equality of output, not provenance — a drift in a literal would still be a
second copy. Fix: restrict "not a second copy" to the cost-number fields;
list the global/literal exceptions (they are the next P2-02c/P3-10 hazard).

### M6 (MAJOR) — 06 §13 `#` column is take2 numbering; does not resolve vs take3 03 §11
File+section: `06` §13 fidelity table (`#` to 113).
Evidence: take3 `03` §11 checklist has 42 items. `#` 43–113 dangle
(e.g. 105–108 FK/superkey → actually items 41; 109–113 invalidation/generic/
visibility → item 42). Low numbers misalign too (`#`6 TRUNCATE / `#`7
vacuum → item 1–3). Take2 `03` had a 13-section layout with the larger
checklist — the column was carried without re-keying and without a
`[carried]` mark. (Weaker than M1 only because 06 never states what `#`
cites.) Fix: re-key to take3 `03` §11 or drop `#`.

### M7 (MAJOR) — systematic line-citation drift; "re-verified at HEAD" no longer holds for lines
File+section: all three docs' `path:line` citations (HEAD moved
`d5f8a6ff9` → `b4e68c574`).
Evidence (claimed → actual): `PlanWithSettings` :101 → `planner.go:102`;
`planStmtWithSettings` :158 → :159; `planSelectWithSettings` :796 → :797;
`newResolveContext` :526 → :527; `joinPinned` :435 → `collapse.go:436`;
`rewriteJoinsToNLI` :88 → `nl_index_join.go:89`; `pathNCols`/`pathAvgVarBytes`
:347/:359 → `path.go:348/:360`; `addPath` :600 → :601; `pathRescanCost` :391 →
`joinpathsmemoize.go:392`; `estimateHashBucketSize` :478 → `joinselectivity.go:479`;
`mergeJoinScanSel` :532 → :533 (both are `searchCtx` methods — receivers
unmentioned); `clampRowEst` :490 → `relsize.go:494`; selectivity defaults :591
→ :593–594; `ResolvedNDistinct` `catalog.go:1912` → :1913; `sortPathFor`
:404 → `joinpathsmerge.go:423`; aggregate stage :7027 → `buildAggregateStage`
at `planner.go:7084`; `searchTupleFraction` :629 → def at
`joinsearchseam.go:653` (:629 is inside `chainCarriesLateral`); groupagg rules
:62/:44/:59 → :63/:45/:60; dispatch fill :1936–1941 → actually :1936–1951.
Call-site-vs-def cites: `planFromClause` :1129 is the call (`planner.go:1129`;
def :2494); `demoteDroppedMergeClauses` :233 is near call sites (:234/:251;
def :296); `joinrestrict.go:41` is a struct field (`buildRestrictInfos` :116,
`relidsOfExpr` :419). Fix: re-pin all lines at one HEAD or drop line numbers
and cite symbols only (symbols were stable everywhere I checked).

### M8 (MAJOR) — 04 §1 bypass predicate understates the code (and the code comment agrees)
File+section: `04` §1 table ("scan toggles **or** any of the nine cost GUCs").
Evidence: `plannerCostGUCsOverridden` (`dispatch.go:1979-2005`) fires on ~24
names: nine cost GUCs + `hash_mem_multiplier` + `enable_hashjoin/mergejoin/
nestloop/memoize/nestloop_index/hashagg/presorted_aggregate` + seven GEQO
knobs. A reader testing `SET enable_hashjoin=off` cache behaviour gets the
wrong answer from the doc. The code comment at :1975 ("The list is the nine
cost GUCs") shares the fiction. Fix doc table + fix the code comment.

### M9 (MAJOR) — re-verification missed stale code comments that contradict the docs at cited sites
Files+sections: 04 §12.2/§5/§0/§8, 05 §1, 06 §1.5 method claims.
Evidence (docs are right; code comments are stale and will mislead anyone
following the citations): `cost_funcs.go:77-79` ("per-session value does not
reach the planner yet"); `plannersettings.go:8-15` ("only source… reach
nothing"); `path.go:124-128` ("goopg has no enable_* GUCs, so it is always 0
today" — refuted by P2-05 writers at `path.go:796`, `pathgen.go:97/:136`,
`joinpathsmerge.go:389`); `joinsearch.go:34-36` + `path.go:26-29`
("`RelSet` is a uint16… 16 base relations" — type is `uint32` since P3-09);
`relsize.go:128-129` + `joinsearchseam.go:338` (cite deleted `bushy.go`);
`joinsearchseam.go:161` (cites deleted `reorderCommaFromByCardinality` /
`joinorder.go`); `joinkeyproof.go:120-125` ("`columnStatsForChild` still has
its own copy and is missing the `*IndexScan` arm" — refuted by P1-26,
`selectivity.go:451-467`). Fix: update the comments (or file follow-ups);
until then the "re-read at HEAD" method claim overpromises.

### m10 (MINOR) — 04 §10: `drivingScan` "only SeqScan/BitmapHeapScan" is stale
File+section: `04` §10 (P5-03 problem statement).
Evidence: `parallel.go:441-474` now also admits `*IndexOnlyScan` (M0134-0189)
plus `*Filter`/`*Project`/join-probe arms. The P5-03 point survives for plain
`*IndexScan`, but the parenthetical is false. Fix: "recognises everything
except plain IndexScan/NLI-probe shapes".

### m11 (MINOR) — 04 §12.3: unthreaded-sites enumeration is incomplete
File+section: `04` §12.3 (derived tables / set-op operands / scalar subqueries).
Evidence: CTE bodies also plan under defaults (`with.go:205/:264/:334/:395`
call defaulting `planSelect`), as do `copy.go:41`, `planner.go:58`,
`:915/:943/:1047` (set-op operands — named, ✓), `:10933`. The gap is bigger
than the three named families. Fix: enumerate all defaulting `planSelect`
call sites or scope the claim to `planSelectWithParent` + set-op operands.

### m12 (MINOR) — 04 §2 step 9: `unnest.go:424` is the post-pass, not the pre-DP branch
File+section: `04` §2.1 step 9.
Evidence: `unnest.go:424` is `func unnestSubqueriesInPlan` (post-pass); the
pre-DP switch is `unnestPreDPOn` (`unnest.go:42-45`); `predp.go:73` is
`runJoinSearchBelowPinned` def (plausible but likewise a def, not the branch
test). Fix: cite the flag + call sites.

### m13 (MINOR) — 06 §12 worked examples have no 03 counterpart (tail mirror drift)
File+section: `06` §12 vs `03` (§§1–11, checklist last).
Evidence: `03` has no worked-examples section; `06` §12 is new hand-derived
content (stated as such — low harm). Same class as M2 but explicitly labelled.
Fix: note "no 03 counterpart" in the §12 header.

### m14 (MINOR) — measurements cited as fact are unauditable read-only (flagged, not refuted)
Files+sections: 04 §12.4 (248.71s→403.27s, 17.6×), §5 (part_pk 497 pages),
05 §5.4/§5.7/§8.2 (258.28→240.73s, −1.2%, NBatch/NBuckets arithmetic),
06 §0.3/§0.4/§2.5 (288.10→257.75s, 5000→5, MCV counts). Spot arithmetic that
*is* checkable verifies (`EntryBytes` = 48n+24, `hashsize.go:46/51`;
512MiB×2 budget; bucket formula matches `cost_funcs.go:365-369`; descent
`(treeHeight+1)×50`, `costindex.go:42/226`). Gates named all exist
(`TestWalkersAgreeOnRowEstimate`, `TestPathTraceRecordsProducerAndVerdict`,
`TestPlannerSettingsReachSubqueryScan`, `TestDefaultPlannerSettingsMatchTheHardWiredParams`,
`TestPGStatisticRoundTripPreservesHistogram`, `TestEqjoinselInnerMCV*`,
`TestPgStatsRendersCorrelation`, `TestPlanCacheInvalidatingStmt`). Fix: mark
timings/sweeps "as measured, not re-verified" per the take3 method.

## Verified sample (no finding; checked against code)
04: seam shape + `inferEquivClassConstants` (`joinsearchseam.go:168/:319`);
`RelSet uint32` + `maxSearchRels=32`; collapse polarity (`collapse.go:152/:436`);
`planSelectWithParent` re-defaults (`planner.go:13808/:13828`, callers :4082/
:4096/:11615/:13581/:13595/:13675/:13701); `ctx.settings` stamped
(`planner.go:1151`); `ctxPlannerSettings` exists (`dispatch.go:1865`);
`generateScanPaths` test-only + `addPartialPath` unreachable in production;
11 `traceSeamDecline` sites; DPPATH tag (`pathtrace.go:32`); tail-check
`pathlistVerdict` (`path.go:612-617`); tripwire (`searchedtree.go:200`,
`createplanroot.go:137`); `joinorder.go` gone (only
`joinorder_determinism_test.go` remains); `createPlanNode` stamps
(`createplan.go:50`, `plancost.go:82/:116`); `MaybeAddGather` descends through
`Explain` (`parallel.go:97`); collapse limits 8/8 with `SET … =1` no-op
(`collapse.go:100-115`); `buildInitialRels` seeds one prebuilt costed
`costSeqscan(cp, estScanPages, rows, 0)` (`joinsearch.go:399`); `IndexRealPages`
+ `RelNBlocksFunc` (`catalog.go:78/85`); TRUNCATE (`operators_ddl.go:16262-270`)
+ VACUUM (`operators_vacuum.go:269/:279`) + `UpdateRelStats`
(`catalog.go:13073`); cache bypass + invalidation (`dispatch.go:1979/2011/3875`,
`dispatch_extended.go:123`); work_mem BootVal 512MB (`defaults.go:785`) +
`512<<20` (`hashsize.go:83`) + `HashMemLimit` (`hashsize.go:150`); GEQO
threshold 12 + session effort/pool/gens/seed/bias wiring (`geqo.go`,
`relfromjoinlist.go:374-381`); `sortInnerAndOuter`+`matchUnsortedOuterMerge`
closure signatures; merge-on-`mergeTuples` + residual-on-`mergeTuples`
(`joinpathsmerge.go:379-385`); `fullyBound` (`pathparamindex.go:360`);
`paramSourceRels()=0` (`joinpathsnli.go:44`); `ConsiderParamStartup` false
(`path.go:272`); `EXPLAIN (SETTINGS)` + `explainCostFields` (`:1896`).
05: `costParams`/`defaultCostParams`/`PlannerSettings` lines exact; GEQO fields
(`cost_funcs.go:82-95`); `GOOPG_INDEX_PROBE_MULT` path; `getCheapestFractionalPath`
(`tuplefraction.go:267`); unique clamp in *both* `btreeIndexAMCost` (:207-209)
and `parameterizedBaserelRows` (so the dual fidelity rows are legitimate);
`num_sa_scans` blocked with reason (`costindex.go:184`); registration-only
`enable_*` (`defaults.go:1216-1233`, no planner consumer).
06: `readVarlena`/`decodeTextArray` (`catalog/codec.go:1539/:1661`);
`ResolvedNDistinct` (`catalog.go:1913`); `columnRawRowsForChild` RAW-divisor
convention (`selectivity.go:444`); OpAnd→`conjunctionSelectivity`
(`selectivity.go:45`); resolved-ndistinct in `eqSelectivityForColumn` (:141);
date `numericValue` + 0.5 fallback (`selectivity.go:257-264/:313+`);
`defaultRangeIneqSel` (`rangequery.go:32`); `defaultUnkSel/defaultNotUnkSel`
(`selectivity.go:593-594`); override clears `NDistinctFrac`
(`operators_analyze.go:714`); `analyzeMCVList` (`:483`); `*CTEScan` arm
(`joinkeyproof.go:165-168`); DISTINCT sizing (`cardinality.go:1437/:1451`);
`eqjoinselInnerMCV` (`:1470`); writer slots incl. `eqOp=98`, `stainherit=false`,
NULL-when-zero correlation (`pg18_user_catalog_rows.go:1568/:1603/:1624-1627`);
`superkeyJoinSelectivity` + `keyImpliedRowsBound` (`joinrelsize.go:213/:284`).
All cited commits in the three "landed" tables resolve with matching subjects.

## Suggested fix order
1. M1+M6 (re-key fidelity columns — blocks any audit use), 2. M3+M4 (two-line
citation fixes), 3. M8+M9 (mechanism description + stale comments), 4. M5
(weaken the no-second-copy claim), 5. M2+M13 (scope the mirror claims),
6. M7 (re-pin lines mechanically at the merge HEAD), 7. minors m10–m14.
