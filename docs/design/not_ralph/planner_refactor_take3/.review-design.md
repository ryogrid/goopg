# Take3 synthesis review — 07 / 08 / 09 / TODO / README

Verdict: **APPROVE-WITH-CHANGES** (3 blockers, all fixable without re-deriving the evidence).

Method: read 07 (510 lines), 08 (507), 09 (308), TODO (627), README (78) fully;
skimmed 01/02/03 headers (§1 + preambles) and 04/05/06 preambles + landed-work
tables; read take2 TODO progress log end to end (P0–P7 + progress log + dropped
table, 1710 lines). Checked the six assigned axes. No code touched, nothing run.

What checked clean (axis 1): no finding of 07 claiming OPEN what 04/05/06 mark
landed with a commit. The landed census is consistent across 04 §0 table,
05 §1 + preamble list, 06 preamble + §0.3/§0.4, 07 §5, and TODO landed records
(P0-01/02/03/04b/04c/04d/11/12, P1-01/03/03b/05/07/08/11b/11c/12/13/14/15/19/20/
25/26/28, P2-01/02/02c/03/04/05/06/07/09-partial/11-half/12/13, c281b0830,
13d53603f, dd22e656c, P3-09/10/11/12, P4-01a; declined/blocked P2-08/P2-10,
P2-09-qual, P2-02b, P1-06/18/21, P6-03/04/05 all agree). Remaining findings are
omissions, duplicates, oversized items, dangling pointers, and unmeasurable
bars — not fabricated gaps.

## BLOCKER

### F1 — BLOCKER: TODO P6-06 bundles six flag retirements in one checkbox
- File+section: `TODO.md` Phase 6 P6-06 (retire GOOPG_PGSHAPED_DP,
  GOOPG_PGSHAPED_COLLAPSE, GOOPG_NLI_COSTGATE, GOOPG_INDEXKEY_HARVEST,
  GOOPG_HASH_OUTER_JOIN, GOOPG_INDEX_PROBE_MULT).
- Why wrong: directly contradicts the bundle's own commit discipline —
  `TODO.md` lines 11–12 ("One checkbox ≈ one commit"), 08 §1 P5, 08 §12
  ("Each plan-selection change gets one flag, default off, flipped in its own
  commit …, deleted one commit later"), and TODO's own sequencing note
  ("every flip its own commit"). Six flips with different blast radii
  (DP off-path unretirable per P6-03/04; HASH_OUTER_JOIN a wash per take2 TODO
  P6-06; COLLAPSE already retired by P3-05 — see F6) cannot share one
  acceptance arm. A single revert would re-couple six variables.
- Fix: split into six one-flag items (P6-06a…f), each with before/after parity
  roll-up + timing per 09 §9; keep DP last with its P6-03/04 dependency
  stated. Authoritative: 08 §12 + 08 §1 P5.

### F2 — BLOCKER: P2-02b's named blocker (settings propagation) has no TODO item
- File+section: `TODO.md` P2-02b ("ordered after P0-12 (landed) and P4-01 and
  settings propagation"); `08-target-design.md` §5.1, §10.1, §10.4;
  `07-gap-analysis.md` §3.3.
- Why wrong: the ordering constraints reference "propagation slices" that do
  not exist as checkboxes. TODO contains the word only inside P2-02b's prose
  and the sequencing notes — there is no P2-02x item to schedule, review, or
  close, so P2-02b is blocked on unscheduled work and §10.1/§10.4 constrain
  nothing. The mechanism is known (hand-thread one caller at a time from the
  wrong-scope reverted attempt; `planSelectWithParent` planner.go:13808 →
  `planSelect` :13828 re-defaults; take2 FINDING-planner-settings +
  take3 04 §12.3), but it has no owner.
- Fix: add P2-02d/e/… propagation items (one caller family per commit, after
  P4-01 per 08 §10.1), design pointer 07 §3.3 + 04 §12.3 + 08 §5.1, gate
  09 §5 P2 (GUC-effect + both-suites timing). Authoritative: 07 §3.3 + take2
  `impl/FINDING-planner-settings-not-propagated.md` Progress + 04 §12.3.

### F3 — BLOCKER: P4-01 is scheduled after the phase it must precede
- File+section: `08-target-design.md` §2.3 vs §§4–7 + `TODO.md` phase order
  (P4-01 in Phase 4, after Phase 3).
- Why wrong: §2.3 promotes "P4-01 ahead of the P1 tail and all of Phase 3 on
  the width evidence" and states the safety rule "wider search is not safe
  until the numbers ranking it are right" (Round-4 precedent: ANALYZE fixed Q5
  23× and broke five). But TODO executes Phase 3 (P3-02/03/04 widen the search
  to outer/semi/anti + full jointree) before P4-01 fixes the 14–39× width
  overcharge (07 §3.2, Q9 1098–3164 B vs PG 23–81 B). P3 would rank a wider
  candidate set with known-wrong costs — the exact failure §2.3 forbids. The
  "P4-01 first — it blocks P2-02b" note in TODO Phase 4 does not fix phase
  order.
- Fix: renumber P4-01 (+ F2 propagation slices) ahead of P3 (e.g. P2-17/P3-00)
  or add an explicit waiver in §10 accepting P3-before-width with a stated
  ranking-risk mitigation. Soften with take2's own caveat carried in TODO
  P4-01b (`FINDING-p401-alone-is-not-enough`: 128→64 batches, not 1; 48
  B/column co-dominant) — P4-01 is necessary but not sufficient, which is
  exactly why P3 must not run on pre-P4-01 numbers without saying so.
  Authoritative: 08 §2.3 + 07 §3.2 + take2 TODO P4-01 rev-5 note.

## MAJOR

### F4 — MAJOR: TODO P2-09 is two planner inputs in one checkbox
- File+section: `TODO.md` P2-09 ("build the ScalarArrayOp index path first …
  then land the batched remainder").
- Why wrong: new path generation (SAOP `= ANY` descents + `ceil(pages/3)`
  clamp, 02 §3.4) plus a cost-term batch (reverted per-tuple qual cost + rest
  of btcostestimate) violates one-variable-per-commit (08 §1 P5; TODO l.11).
  08 §5.2 itself sequences them ("Build the path first, then land the batched
  remainder") — two acceptances (path existence/shape vs aggregate TOTAL)
  sharing one gate. Repeats the M0126 two-inputs-one-env failure (07 §5).
- Fix: split P2-09a (SAOP path; gate PP shape on IN-list fixtures) and P2-09b
  (btcostestimate batch; gate TOTAL arm + attribution per R6). Authoritative:
  08 §1 P5 + 08 §5.2 + 09 §1 R6.

### F5 — MAJOR: TODO P2-16 bundles a whole flag family
- File+section: `TODO.md` P2-16 (port sort/material/agg-hashed-mixed/
  gather-merge setters + retire producer-skipping for scans + retire
  `enable_nestloop_index`).
- Why wrong: ≥3 independent behaviour changes (one per setter family, plus a
  producer-skip retirement that changes scan shapes, plus a flag retirement
  conditioned on NLI becoming ordinary per §6.4) in one checkbox. Each family
  needs its own GUC-effect test (09 §5 P2 row: "GUC-effect test per
  newly-live GUC") and its own revert.
- Fix: split per family (P2-16a…), each with its GUC-effect test; keep the
  `enable_nestloop_index` retirement conditioned on §6.4 NLI-ordinary-path
  with its own gate. Authoritative: 08 §1 P5 + 09 §5 P2 row.

### F6 — MAJOR: duplicate retirement of GOOPG_PGSHAPED_COLLAPSE
- File+section: `TODO.md` P3-05 vs P6-06; `08-target-design.md` §6.3 vs §9.
- Why wrong: P3-05 retires the flag "once P3-04 makes it the only jointree
  path"; P6-06 retires the same flag again among six. Two owners, two gates
  for one deletion — one will go stale or double-land. P0-13 (flip ON as
  control) makes this a three-touch lifecycle that must be sequenced once.
- Fix: keep the retirement solely in P3-05 (with `from/join_collapse_limit`
  upstream meaning incl. `=1`, 01 §12); remove COLLAPSE from the P6-06 list.
  Authoritative: TODO P3-05 + 08 §6.3 (over the generic §9 list).

### F7 — MAJOR: 08 §4 has no design for P1-18 / P1-21; P1-18 sits in the wrong phase
- File+section: `TODO.md` P1-18, P1-21 (both "*design: 08 §4*") vs
  `08-target-design.md` §4 (covers P1-11, P1-14b, P1-16, P1-22/23/24, P1-27,
  P1-10+P1-02 only).
- Why wrong: dangling design pointers — a reader opening 08 §4 finds nothing
  on the jointype switch or the `max(outer,inner)` keep-decision. Worse,
  P1-18 (BLOCKED on P3-04: "port the switch only after the search can see a
  non-inner join", 07 §3.1; take2 TODO P1-18 probe `nrels=1, pairs=0`) is
  scheduled in Phase 1 while its blocker P3-04 is Phase 3 — phase inversion.
  Executing phases in order attempts the item before its precondition exists.
- Fix: add P1-18/P1-21 paragraphs to 08 §4 (or repoint to §6.2 for P1-18 with
  the dead-code warning) and move P1-18 execution after P3-04 (defer or
  re-home to Phase 3). Authoritative: 07 §3.1 + 07 §3.5 P1-18/P1-21 rows +
  take2 TODO P1-18/P1-21.

### F8 — MAJOR: ranked gaps with no TODO owner
- File+section: `07-gap-analysis.md` §3.5 (endpoint probe + MCV widening,
  06 §4.3; text `bucketFraction` 0.5/no-clamp, 06 §5.3/§12.3) + §8 rank 11 vs
  `TODO.md` Phase 1; rank 12 (`RestrictInfo` caching) vs TODO Phase 6.
- Why wrong: §8 ranks work the checklist never schedules. No P-item exists
  for `get_variable_range` index-endpoint probe / MCV widening; none for text
  scalar (`convert_string_to_scalar`, network variants — take2 TODO P1-11b
  "still open" half); none in Phase 6 for `RestrictInfo` caching although
  TODO P1-12 explicitly files it "under Phase 6". Rank promises, TODO cannot
  deliver; deferral-ledger rule (08 §1 P6, 09 §4.3 C7) has no hook.
- Fix: add the three items (or explicitly defer each with a ledger row per
  08 P6/C7 and drop them from the rank table). Authoritative: 07 §3.5 + 06
  §4.3/§5.3 + TODO P1-12 note + 08 §1 P6.

### F9 — MAJOR: bar A4 invents a target for a STALE metric
- File+section: `09-verification-and-acceptance.md` §4.1 A4 (matched ≥ 40;
  "mismatches strictly below P0 re-measurement") vs §4.1 principle ("inventing
  targets for unmeasured metrics makes decoration") + 07 §2.2.
- Why wrong: 07 §2.2 rules the 46/32 figures STALE pending P0-04
  re-measurement (dedup exists, numbering diverges). A4 then sets matched ≥
  40 from the stale 32 without a baseline, violating its own section's rule
  (the rule A1/A2 obey by staying numberless until P0-07). "Strictly below
  re-measurement" is unmeasurable as written: below by 1? total or per
  category? over how many runs? — no delta, no instrument binding beyond "PP".
- Fix: make A4 numberless until the P0-04 re-measurement, then set
  `baseline + N` exactly like A1/A2 (09 §4.1, §5 P0 row). Authoritative:
  09 §4.1 A1/A2 form + 07 §2.2 + 09 §7.1.

### F10 — MAJOR: P3 exit criterion cites an undefined adjudication
- File+section: `08-target-design.md` §6 exit + `09-verification-and-acceptance.md`
  §5 P3 row ("`ENUM-TRACE` clause-6 adjudication").
- Why wrong: 09 never defines clauses 1–6 — §2.1's enumeration-trace row lists
  only OFFERED/DECLINED/NOT-ENUMERATED with no clause taxonomy, and no other
  section enumerates clauses. "Every PG-only spine OFFERED at its level or
  named reason" is therefore unjudgeable, as is "Q72-class queries one search
  problem" (class undefined beyond the Q72 witness).
- Fix: define the clause taxonomy in 09 §2.1/§5, or repoint the exit to the
  defined instruments (`DPPATH` OFFERED/ACCEPTED per 04 §6 + `estimate-audit
  --enum-trace`, 09 §2.1) and enumerate the Q72-class query list.
  Authoritative: 09 §2.1 + 04 §6.

### F11 — MAJOR: P4 exit names instruments that do not exist in 09
- File+section: `08-target-design.md` §7 exit + `09-verification-and-acceptance.md`
  §5 P4 row ("`NBatch` 2→1 on the witness, `DPPATH` hash total below merge
  total").
- Why wrong: no 09 instrument reports `NBatch` (plan-parity in §2.2 explicitly
  excludes widths/times to a separate column; instruments table §2.1 has no
  batch reporter). Witness query, relset, and budget regime are unnamed, and
  the "≈100, not PG's 6" threshold correction (declared NUMERIC keys, P4-A §8
  R2) lives in TODO prose, not in the gate. A single-witness batch gate also
  courts C2 single-query calibration (09 §4.3) without naming its both-suites
  binding.
- Fix: name the exact invocation (EXPLAIN `Batches:`? `tpch-runner` field?
  add to §2.1/§2.2), witness query + relset + regime (09 §6 header), and bind
  the values gate (`-digest` + `-diff`, R8) already required for projection
  work. Authoritative: 09 §2 + 09 §1 R8 + take2 `impl/P4-A-pathtarget.md` §8.

### F12 — MAJOR: pointer cluster misdirects implementers to wrong sections
- Files+sections: (a) 07 §3.3 "Sequencing: … (take2 TODO P2-02b note; 08 §9)"
  — 08 §9 is Phase 6 deletion, not sequencing (sequencing is §10; BootVal
  order is §5.1/§10.1). (b) 08 §5.1 "propagation (§7: `planSelectWithParent`
  …)" — 08 §7 is Phase 4 upper planner; propagation is 07 §3.3 + 04 §12.3.
  (c) 08 §10.4 cites 07 §3.2 for propagation order — propagation is §3.3
  (§3.2 is width). (d) 07 §2.1 "(… §7.5 below)" — 07 has no §7.5 (§7 is nine
  unnumbered reliability items; the split lives in §3.2 + take2
  `MEASUREMENT-p202b-width-vs-gather.md`).
- Why wrong: four load-bearing pointers resolve to the wrong phase or to
  nothing; (a) sends the BootVal reader to deletion, (b) to upper-planner.
- Fix: (a)→08 §10.1/§5.1; (b)→07 §3.3 + 04 §12.3; (c)→07 §3.2+§3.3;
  (d)→07 §3.2. Authoritative: 08 §10 (constraints) + 04 §12.3.

### F13 — MAJOR: planner paths without named executor consumers (P2-08/10 repeat)
- File+section: `08-target-design.md` §7 P4-05 (Incremental Sort node +
  `create_incremental_sort_path`, closes A3 MISSING-NODE) + §8 P5-06
  (parallel hash as `parallel_aware` path) vs 07 §6 + take2 precedent.
- Why wrong: take2 declined P2-08/P2-10 as premature precisely because no
  SubPlan/semi-anti consumer existed (05 §4.9/§5.1; 07 §3.4). P4-05/P5-06
  repeat the shape: executor sort speed / spill / skew buckets are out of
  scope per 07 §6, yet no section names the executor counterpart that will
  execute an Incremental-Sort or parallel-hash path, nor gates on its
  existence. Unconsumed paths are dead code that acceptance (PP shape) will
  read as failure.
- Fix: name each path's executor counterpart + gate, or mark blocked ("resume
  after executor support", ledgered like `take2-P2-08`/`take2-P2-10`) and
  remove from the A3-closure promise until then. Authoritative: 07 §6 + 07
  §3.4 P2-08/10 rows + 04 §10.

## MINOR

### F14 — MINOR: 09 §4 subsections out of order
- File+section: `09-verification-and-acceptance.md` §4 (4.1 Bar A, 4.2 Bar B,
  then 4.4 Permitted divergences, then 4.3 Bar C).
- Fix: renumber so Bar C precedes or follows divergences monotonically
  (references to "C1–C7" and "§4.4 case 1" in §§5/8 then resolve in order).
  Authoritative: 09 §4 itself.

### F15 — MINOR: TODO gates missing 09 pointers
- File+section: `TODO.md` P1-21 (gate "units" only), P1-22 ("full regress"
  with no 09 §), P1-24/P1-27 ("EA ratchet + PP" / "EA ratchet" with no 09 §),
  P4-00 ("scoped items filed, not code" — no 09 counterpart).
- Fix: add explicit pointers — P1-21→09 §5 P1 + §3 floor (units pins guard);
  P1-22→09 §3 item 7 + §5 P1; P1-24/P1-27→09 §4.1 A5 + §5 P1; P4-00→09 §9
  reporting. Authoritative: TODO l.23–25 (every item names gate) + 09 §5.

### F16 — MINOR: TODO P1-16 gate points at the wrong 09 subsection
- File+section: `TODO.md` P1-16 ("gate: EA final-joinrel bar on Q9 (09 §2.1)")
  vs 09 §2.1 (join-spine diff + slack/floor ratchet, no Q9 bar) and §2.2
  ("P1-16 re-baselines Q9's final joinrel bar").
- Fix: repoint to 09 §2.2. Authoritative: 09 §2.2.

### F17 — MINOR: P1-02/P1-10 batching + double-listed gap
- File+section: `TODO.md` P1-02 ("batch with P1-10") + P1-10 ("batch with
  P1-02") vs TODO l.11 ("One checkbox ≈ one commit"); `07-gap-analysis.md`
  §3.4 table + §3.5 both list P1-02 remainder.
- Fix: either merge into one checkbox (one commit, batched) or schedule two
  commits; list the gap once in 07 (keep §3.5, cross-ref from §3.4).
  Authoritative: TODO Ground Rules + 08 §1 P5.

### F18 — MINOR: naming drift + incomplete landed census; README nits
- File+section: 07 §5 ("ndistinct two-form fix", no number) vs TODO P1-29 vs
  06 §0.4 ("NEW"); 07 §5/04 §0 table omit P0-09 + verified-satisfied
  P1-04/P1-09/P1-17 that TODO records; `README.md` map lists REVIEW.md while
  this review lands as `.review-design.md`, and "implementation not started"
  elides the landed take2 record; README baseline caveat repeats 248.71
  without restating its different-binary caveat (take2 progress-log P0
  note) — pointer to 07 §2.1 exists, so clarity-only.
- Fix: unify the ndistinct name (recommend P1-29 everywhere); state the
  census exclusion rule (e.g. "verified-satisfied listed in 06, not §5") or
  complete it; rename the map entry to `.review-design.md` (or write REVIEW.md
  instead); qualify README Status ("take3 phases not started; take2 landings
  absorbed per TODO"). Authoritative: 06 §0.4 + TODO landed lists + take2
  progress log.

Counts: BLOCKER 3 · MAJOR 10 (F4–F13) · MINOR 5 (F14–F18). Total 18.
