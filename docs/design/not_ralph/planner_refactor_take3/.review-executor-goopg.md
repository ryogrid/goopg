# Review: executor bundle (11, 13, TODO_EXECUTOR, README_EXECUTOR)

**Verdict: REVISE** — one HIGH (stale hot-spot invalidates an EX3 item's
premise), three MEDIUM (unmeasurable bar, misattributed blocker,
commit-sizing violation), four LOW (citation/wording rot). No
query-specific forcing found; STALE discipline on Q14/Q3/Q10 is coherent;
README map is accurate. The bundle is close — fix F1–F4 and it is
executable as written.

**Method.** Read all four fully. Serena MCP timed out twice
(`initial_instructions`, -32001; same failure 11 §0 reports), so all
spot-checks are Grep/Read against the tree — ~30 claims verified.
Caution: docs pin HEAD `adf2d1e13`; tree tip is `7616ac964` (relocate
commits). Line pins below are checked against the pinned HEAD where it
matters (F1).

## Findings

### F1 [HIGH] 11 §11 + §17 row 8 are stale: the spill `runtime.Stack` tax already landed
- **Files/sections:** `11-executor-goopg-design.md` §11, §17 row 8;
  knock-on: `12-executor-gap-analysis.md` §14, `13` §5 EX3-01,
  `TODO_EXECUTOR.md` EX3-01.
- **Evidence:** At the doc's own pinned HEAD the constructor already
  caches the registry: `adf2d1e13:internal/executor/spill.go`
  ("Cache the registry reference once at construction time instead of
  calling LookupCurrentGoroutine (→ runtime.Stack) on every spilled
  row"). The per-row path (`writeFrame`) uses the cached handle; the
  only `LookupCurrentGoroutine` sites in `internal/executor`
  (`spill.go:81,181`) are constructors (once per file), not the write
  path the doc claims ("calls … on the write path (`spill.go:79-81`)
  and again at `:181`"). No per-row Stack walk remains in the package.
- **Fix:** Re-mark row 8 LANDED (residual TBD), delete "per-row stack
  walk on spill" from 12 §14, and re-scope EX3-01/13 §5 to
  verify-and-close (re-measure Q4/Q7/Q13-class; residual reader-path
  audit) instead of "land the round5 design". The 69–86% / 3.3–7.3×
  basis is gone — quoting it as live misprices EX3.

### F2 [MEDIUM] Bar E1 is unmeasurable beyond Q6
- **Files/sections:** `TODO_EXECUTOR.md` Acceptance E1; EX0-06.
- **Evidence:** E1: "no query slower than 1.2× its EX0-06 baseline".
  EX0-06 only re-baselines "the Q6 chain … serial 3.792 s / parallel
  0.838 s". No item captures per-query baselines for either suite, so
  E1 has no denominator for any non-Q6 query. Same gap bites the
  README Status claim that take6 landings (Q14/Q3/Q10 wins, 12 §11)
  are "the baseline EX0-06 re-measures against" — EX0-06 does not
  measure those queries.
- **Fix:** Extend EX0-06 to record per-query timing+alloc baselines on
  both suites (or narrow E1 to "Q6-chain + witness shapes", keeping
  suite TOTALs as the backstop).

### F3 [MEDIUM] EX4-04's blocker is misattributed "planner-side"
- **Files/sections:** `TODO_EXECUTOR.md` EX4-04; `13` §6, §8.5.
- **Evidence:** Blocker named: "planner-side `Gather` arm in `buildRec`
  (11 §12)". But `buildRec` is executor code
  (`internal/executor/executor.go:486`), switching on optimizer node
  types with a proven pattern (Join arm `:604-616`, default→legacy
  adapter `:618-626`; legacy `Build` already handles Gather,
  `executor.go:264-279`). The arm is delivered by executor item EX5-01,
  which 13 §8.5 itself names as the precondition. 13 §6's "joint work:
  executor provides the slab arm, planner provides the shape" splits
  one executor edit across two bundles.
- **Fix:** Name the blocker as EX5-01 (executor-owned); keep the
  plan-shape pin as the planner interface, not the work item.

### F4 [MEDIUM] EX2-02b violates one-seam-family-per-commit
- **Files/sections:** `TODO_EXECUTOR.md` EX2-02a/02b; `13` §4, §1 EX-P3.
- **Evidence:** 13 §4: "One seam family per commit (join seams, agg
  input, gather transfer)" — three families. TODO ships 02a (join) +
  02b (agg input **+ gather transfer**, incl. arena rules and serial
  arm). 02b moves two inputs (ownership discipline + cross-goroutine
  transfer) at once — exactly what EX-P3 forbids.
- **Fix:** Split 02b into 02b (agg input) + 02c (gather transfer).
  Watch item with the same smell: EX5-02 (barrier discipline +
  stall measurement + worker-count scaling reads as three commits).

### F5 [LOW] Wrong-file citation: `buildKeyOfRow`
- **File/section:** 11 §5 ("`buildKeyOfRow`
  (`operators_join_agg.go:725)`").
- **Evidence:** `rg` puts it at `internal/executor/join_batch.go:725`;
  sibling geometry helpers cited in the same paragraph verify clean
  (`buildGeometry :699`, `presizeLazyHash :743`, `buildLoopLeft :781`,
  `insertBuildRow :843`, `lazyHashInsertDatum :1152`,
  `demoteIntHash :1184` — all exact).
- **Fix:** Cite `join_batch.go:725`.

### F6 [LOW] EX3-07 / planner P4-05 wait on each other; no tiebreaker
- **Files/sections:** `TODO_EXECUTOR.md` EX3-07; `13` §8.7; 11 §9.
- **Evidence:** EX3-07 files "only if planner P4-05 activates", but 11
  §9 states the planner's incremental-sort path "is BLOCKED on
  executor support". Both sides waiting = deadlock by default; 13
  §8.7 ("do not silently absorb it") names the hazard without
  resolving who publishes the presorted-prefix contract first.
- **Fix:** One sentence in §8.7: executor publishes the input contract
  (as EX3-04-adjacent work or a spike) before/independently of P4-05,
  or P4-05 prototypes behind a flag. Either ordering works; "wait on
  each other" does not.

### F7 [LOW] Line pins already rotting; cite symbols first
- **Evidence:** `BuildFast` cited as `executor.go:466` (comment block;
  func at `:633`); `schemaIdx :529` vs `:534`; `evalFastExpr
  :283` vs `:288`; tree tip moved (`7616ac964` relocations) past the
  pinned `adf2d1e13`. Counter-model inside the same docs: §17 rows
  that cite symbols (`mergeSortedSource`, `hashBatchState`) survived
  the move; bare line ranges did not.
- **Fix:** Keep HEAD pin, prefer `file + symbol` with lines as
  parenthetical; refresh the drifted pins (F5 + this list).

### F8 [LOW] "Zero callers outside its own file" is literally false
- **File/section:** 11 §14 (AIO `ReadStream`); §18 item 2.
- **Evidence:** `NewReadStream` is called in
  `internal/storage/aio/read_stream_test.go` (7 sites). Production
  callers are indeed zero — the intended claim.
- **Fix:** "zero production callers (test-only otherwise)".

## Passes (explicitly checked, no finding)

- **No query-specific forcing.** EX-P2's letter ("any item whose gate
  names a single query is split or dropped") holds: Q6/Q9/Q4/Q7/Q13 /
  q16 / TOAST-DS mentions are witness-shape measurement citations
  with operator-slice gates (EX0-04/05); EX0-06's Q6 is a baseline
  re-measurement, not a fast path; EX3-01 explicitly refuses
  Q14/Q3/Q10 as witnesses.
- **STALE Q14/Q3/Q10 discipline coherent** across 11 row 16, 12 §13,
  12 §11 atomics-row caveat, TODO EX3-01 exclusion.
- **Phases executable in order** EX0→EX5; [!] items name real
  blockers (P4-05 → planner TODO:533; P2-11b → :375; P4-01 → :510;
  Gather arm → EX5-01). Wrinkle: EX4-04's precondition (EX5-01) lives
  in a later-numbered phase — mitigated by [!], consider numbering
  the Gather arm EX4-00b or moving EX5-01 earlier (see F3).
- **No duplicate IDs** (39 checkboxes, all unique); every open item
  carries a `design: 13 §` + `gate:` pointer; landed `[x]` items carry
  evidence pointers. Bars E2–E5 measurable; E6 correctly excluded
  from item gates.
- **README map accurate:** 10/11/12/13/TODO all exist with true role
  rows (G-EX1…G-EX8, §17+§18, EX0→EX5, E1–E6 verified);
  `REVIEW_EXECUTOR.md` is correctly marked "Planned, not yet written"
  — existence is **not** claimed. Numbers consistent (23.6× motivating
  figure matches 11 row 1; 12 §11 rows match TODO `[x]` items).
- **Spot-verified exact (Grep):** `Build :21`, `BuildWorker :32`,
  `BuildFast :633`, `Operator operator.go:34`, `opOpen/opClose/opNext
  506/616/679`, `filterOpNext/projectOpNext/limitOpNext 732/765/805`,
  `joinOp :39`, `Open :308` (Hash `:321`, NL `:323`), `Next :1381`,
  merge state `:127-131`, `openNestedLoop :100` / `nextNL :162`,
  `openLateral :98` / `nextLateral :135`, `mergeSortedSource :83`,
  `memoizeOp` (no `Open`; `openPrep :96`, `Next :194`, `Close :251`),
  `aggregateOp` (`Open :1966`, `openSorted :2455`, `setGroupKey :2290`,
  `Next :4082`), `sortOp` full set (`:760/826/905/923/957/1003/1074/
  1119/1171/1203/1236`), `gatherOp Open :210` / `runWorker :336`,
  `gatherMergeOp :46/81/193`, `parallelBuildEligible :339` /
  `parallelBuildLazyHashTable :402`, `Datum 48B :187`,
  `cloneRowOwned :493`, `acquireRow :42`, slot `Materialize :113-114`
  + arena fix `:181-188`, `materializeOp :216/249/295/308`,
  `decodeScanRow :1378`, `DetoastRow toast.go:514`,
  `filterOp operators.go:565` (still `evalExprSlot`), `tempFileRegistry
  :52/75/82/91`, `DatumBytes=48` + `DefaultMemLimitBytes=512<<20`,
  no-`Gather`-arm in `buildRec` (default arm `:618-626` +
  `executor.go:270` comment).

## Counts

8 findings: 1 HIGH (F1) · 3 MEDIUM (F2–F4) · 4 LOW (F5–F8).
~30 code claims spot-checked: 26 pass exact, 1 refuted (F1), 3 minor
citation drifts (F5, F7, F8). Checklist: forcing 0 · dup IDs 0 ·
missing design/gate pointers 0 · phantom blockers 0 · README map
errors 0 · unmeasurable bars 1 (F2).
