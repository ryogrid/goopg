# Upstream isolation Spec Coverage Classification

Generated by `go run ./cmd/gen-isolation-coverage --repo-root . --out docs/test-port/upstream-isolation-coverage.md`.
Generated at: 2026-08-24T19:54:26+09:00

Total discovered specs: **122**

Status policy for this phase:
- `not-tried`: spec has not yet been executed against goopg.
- `failed`: spec has been run but output does not match expected.
- `pass`: spec output matches the isolation test expected result.

| spec | status | rationale |
| ---- | ------ | --------- |
| `postgres/src/test/isolation/specs/aborted-keyrevoke.spec` | pass | TestPort_IsolationAbortedKeyrevoke PASS (strict) — all permutations byte-for-byte vs PG 18.3. A SAVEPOINT-issued key UPDATE (KEY REVOKE) rolled back via ROLLBACK TO must not leave a phantom conflicting lock; s2 FOR KEY SHARE stays compatible. M0118-0009 design 0118-0014: SAVEPOINT eagerly materialises the top-level XID before AllocateSubXid so the subxid->parent link is non-zero from birth (parent=0 previously made the subxact writes cross-session-visible and defeated the row-lock wait); sub-XID-scoped NEW-tuple xmin + HEAP_KEYS_UPDATED on the single-xid old-tuple xmax stamp; descendant-direction subxact self-visibility in isCurrentTxXID. (case `aborted-keyrevoke`) |
| `postgres/src/test/isolation/specs/alter-table-1.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationAlterTable1 (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `alter-table-1`) |
| `postgres/src/test/isolation/specs/alter-table-2.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationAlterTable2 (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `alter-table-2`) |
| `postgres/src/test/isolation/specs/alter-table-3.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationAlterTable3 (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `alter-table-3`) |
| `postgres/src/test/isolation/specs/alter-table-4.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationAlterTable4 (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `alter-table-4`) |
| `postgres/src/test/isolation/specs/async-notify.spec` | pass | LISTEN/NOTIFY/UNLISTEN + pg_notify + pg_notification_queue_usage; pinned strict by TestPort_IsolationAsyncNotify (M0118-0009; design 0118-0090). Savepoint-aware NOTIFY buffer + same-transaction de-dup + cross-backend delivery byte-identical to PG 18.3. (case `async-notify`) |
| `postgres/src/test/isolation/specs/classroom-scheduling.spec` | pass | Passes — TestPort_IsolationClassroomScheduling (internal/testport/isolation_port_test.go) matches PG 18.3 byte-for-byte across all generated permutations; verified stable 3/3 runs. A SERIALIZABLE double-booking write-skew — s1 reads room_reservation with an overlap predicate then INSERTs an overlapping booking; s2 reads with its own overlap predicate then UPDATEs the existing booking's start_time into the contested window — where any overlap must raise 40001. Zero-SSI-change promotion: the dangerous-structure detection already validated on the prior 24 specs produces the exact abort pattern because the inserted/updated rows fall within the concurrent readers' overlap predicates so the relation/tuple-grain SIREAD locks cross-cover identically to PG. Two global-setup blockers had to clear. First — btree key-type support: the PRIMARY KEY (room_id text; start_time timestamptz) rejected the timestamptz key (0A000 btree v0 only supports int4 / numeric keys) at global setup. Fix: timestamptz is now an accepted btree key type that reuses the int64-micros-since-epoch timestamp key path (EncodeTimestamp) since timestamp and timestamptz share the same on-disk form — encodeBTreeKeyForColumn plus both index-only key decoders — so probe and backfill keys are byte-identical. Second — seconds-less timestamptz literal parse: the spec books rooms on the half-hour with literals like TIMESTAMP WITH TIME ZONE '2010-04-01 10:00' (no seconds) which evalTypedStringLit rejected with 22007 until its timestamp/timestamptz layout list also accepted HH:MM and numeric-offset forms — verified by TestEvalTypedStringLitTimestampForms and a deterministic 5/5 TestPort_IsolationClassroomScheduling (previously 0/8 SKIP masked by a stale Go test cache). (case `classroom-scheduling`) |
| `postgres/src/test/isolation/specs/cluster-conflict-partition.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationClusterConflictPartition (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `cluster-conflict-partition`) |
| `postgres/src/test/isolation/specs/cluster-conflict.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationClusterConflict (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `cluster-conflict`) |
| `postgres/src/test/isolation/specs/create-trigger.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationCreateTrigger (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `create-trigger`) |
| `postgres/src/test/isolation/specs/deadlock-hard.spec` | pass | TestPort_IsolationDeadlockHard PASS — eight sessions each LOCK TABLE their own relation then attempt to LOCK the next in a ring (s1→a2 … s8→a1) forming an 8-way cycle. Each session sets deadlock_timeout=100s except s8 (10ms) so s8 waits the shortest and runs the detector first; the general timeout-driven wait-for-graph detector finds the multi-relation cycle and rolls back the discovering session s8 with 40P01. New engine work: deadlock_timeout is now a registered superuser-set GUC (config.defaults) plumbed per-session into lockmgr via the new AcquireWithTimeout used by executor.acquireRelLockTxn (Context.deadlockTimeout reads the effective GUC); the timer-driven check (runDeadlockCheckFor / checkDeadlockLockedFor) prefers the firing backend as victim when it is in the cycle mirroring PostgreSQL DeadLockCheck — CheckDeadlocksNow keeps youngest-in-cycle selection. The isolation runner now honours the (*) BlockerStar permutation marker (hasStarBlocker) rendering the annotated step in waiting/completed format even when goopg observed it complete immediately so s8a1(*) shows the <waiting ...> marker the spec expects. M0118-0004. (case `deadlock-hard`) |
| `postgres/src/test/isolation/specs/deadlock-parallel.spec` | failed | Became an in-scope porting target after goopg implemented the REPEATABLE READ and SERIALIZABLE isolation levels (M0104); previously out of scope while only READ COMMITTED was supported — targeted for pass-through by M0118. (case `deadlock-parallel`) |
| `postgres/src/test/isolation/specs/deadlock-simple.spec` | pass | TestPort_IsolationDeadlockSimple PASS — two sessions each take ACCESS SHARE on a1 then each attempts a lock upgrade to ACCESS EXCLUSIVE; the second upgrader closes a simple lock-upgrade deadlock cycle which PostgreSQL resolves synchronously (JoinWaitQueue early_deadlock) without waiting for deadlock_timeout so the victim errors immediately with no <waiting> marker. New engine work: lockmgr.Acquire now runs lockState.hasSimpleDeadlock before parking — when the requester already holds a lock here and would queue behind a conflicting waiter that itself conflicts with the requester's held locks the requester is the victim and gets lockmgr.ErrDeadlockDetected (mapped to SQLSTATE 40P01) immediately after releasing its other holdings (mirroring the timeout victim path) so the surviving upgrader proceeds. M0118-0003. (case `deadlock-simple`) |
| `postgres/src/test/isolation/specs/deadlock-soft-2.spec` | pass | TestPort_IsolationDeadlockSoft2 PASS — s1 (deadlock_timeout 10ms) blocks for SHARE UPDATE EXCLUSIVE on a2 behind two ACCESS EXCLUSIVE waiters s3 and s4 that are themselves hard-blocked on s2's ACCESS SHARE holder. s1's request does not conflict with the holder so the soft-deadlock resolver topologically reorders a2's multi-waiter queue (s1 ahead of s3 and s4) via topoSort and grants s1 immediately rather than aborting anyone. Shares the soft-edge engine added for deadlock-soft (checkDeadlockLockedFor → findLockCycle → deadLockCheck/testConfiguration → expandConstraints/topoSort → applyWaitOrders). M0118-0004. (case `deadlock-soft-2`) |
| `postgres/src/test/isolation/specs/deadlock-soft.spec` | pass | TestPort_IsolationDeadlockSoft PASS — four sessions form a 4-cycle with two hard edges (e1→d1 e2→d2: an ACCESS EXCLUSIVE waiter behind an ACCESS SHARE holder) and two soft edges (d2→e1 d1→e2: an ACCESS SHARE waiter queued behind a conflicting ACCESS EXCLUSIVE waiter). Because the cycle contains soft edges the detector resolves it by reordering a2's wait queue (moving d1 ahead of e2) so d1 is granted immediately and nobody is rolled back. New engine work: lockmgr.checkDeadlockLockedFor now runs a soft-aware search on the timer path mirroring deadlock.c — findLockCycle records soft (queue-order) edges; deadLockCheck/testConfiguration try reversing them; expandConstraints/topoSort rebuild the affected wait queues; applyWaitOrders rewrites the queue and wakes newly grantable waiters. CheckDeadlocksNow keeps the legacy hard-only youngest-victim path. M0118-0004. (case `deadlock-soft`) |
| `postgres/src/test/isolation/specs/delete-abort-savept-2.spec` | pass | TestPort_IsolationDeleteAbortSavept2 PASS (strict) — all permutations byte-for-byte vs PG 18.3. A SELF lock-only upgrade inside a SAVEPOINT (FOR KEY SHARE -> FOR NO KEY UPDATE under the sub-XID) rolled back via ROLLBACK TO must revert to the outer FOR KEY SHARE strength; a conflicting FOR UPDATE waiter keeps waiting on the restored lock. M0118-0009 design 0118-0015: the savepoint upgrade routes through stampMultiLock (gate widened by hasOuterSelfLockMember) so the outer top-level KEY SHARE survives as a multi member; ROLLBACK TO drops only the sub-XID member. (case `delete-abort-savept-2`) |
| `postgres/src/test/isolation/specs/delete-abort-savept.spec` | pass | TestPort_IsolationDeleteAbortSavept PASS — all 7 permutations. A DELETE inside a SAVEPOINT over a row the same xact already locked FOR KEY SHARE is a lock upgrade; ROLLBACK TO must restore the outer KEY SHARE lock. Three engine fixes (M0118-0009 design 0118-0013): heap DELETE/UPDATE now stamp the old tuple xmax (and its WAL record) under the effective writer xid (current sub-XID inside a savepoint) via effectiveWriterXID rather than the top-level XID so an aborted-subxact deletion is identifiable; the multixact producer preserves an outer-level self locker into {outer-keyshare + subxid-updater} so dropping the rolled-back subxid member leaves the KEY SHARE alive; lockRowsOp stampLockInner treats a subxact-aborted xmax (IsAborted) as a rolled-back updater (row live at original ptr) not a committed delete. s2 FOR UPDATE waits on the restored outer lock then proceeds after s1 commits. (case `delete-abort-savept`) |
| `postgres/src/test/isolation/specs/delete-using-epq.spec` | not-tried | Not yet attempted; execution parity deferred to staged porting. (case `delete-using-epq`) |
| `postgres/src/test/isolation/specs/detach-partition-concurrently-1.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationDetachPartitionConcurrently1 -- ALTER TABLE … DETACH PARTITION … CONCURRENTLY is two-phase keyed on a global mvcc partition-detach epoch (design 0118-0058 primitives wired in 0118-0059): phase 1 NextPartitionDetachEpoch + MarkPartitionDetachPending keeps the child registered (relpartbound stays set so s3i reads relpartbound IS NULL = f) while every snapshot taken afterwards captures the higher epoch and omits the child (READ COMMITTED gone now / REPEATABLE READ visible until commit); the wait is the snapshot-based WaitForOlderSlotsToCommit (a RR session that only PREPAREd a statement is still waited for); phase 2 unregisters the child and clears the bound so s3i flips f→t. SELECT expansion (collectAllPartitionLeaves) and INSERT routing (routeToPartitionDepth) both filter through catalog.VisiblePartitionChildren by the snapshot epoch (sibling paths) and the cross-session plan cache is bypassed while a detach is pending (partitionDetachPending) -- byte-identical to PG 18.3 across all 13 permutations. (case `detach-partition-concurrently-1`) |
| `postgres/src/test/isolation/specs/detach-partition-concurrently-2.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationDetachPartitionConcurrently2 (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `detach-partition-concurrently-2`) |
| `postgres/src/test/isolation/specs/detach-partition-concurrently-3.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationDetachPartitionConcurrently3 (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `detach-partition-concurrently-3`) |
| `postgres/src/test/isolation/specs/detach-partition-concurrently-4.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationDetachPartitionConcurrently4 (design 0118-0064) -- foreign keys vs concurrent DETACH PARTITION CONCURRENTLY of the referenced table: an UPDATE that sets an FK column now fires the RI_FKey_check parent-existence assertion just like an INSERT (updateOp gains childFKsToRecheck/recheckChildFKs gated on an FK column being in the SET list so blast radius is bounded to FK-key UPDATEs; wired into all three write paths Next/updateViaIndex/updateWithFrom) so update d4_fk set a=1 where current of f fails 23503 because value 1 lives only in the concurrently-detaching partition (invisible to the latest snapshot per 0118-0062); and DETACH re-validates RI_PartitionRemove_Check after the hybrid wait (fresh snapshot with PartitionDetachEpoch=0 so the still-pending child stays in the routing set) so a referencing row created/updated by the session the detacher waited on makes the DETACH fail removing partition d4_primary1 violates foreign key constraint d4_fk_a_fkey_1; cursor pinning at DECLARE + abort-releases-snapshot from 0118-0063 -- byte-identical to PG 18.3 across all 21 permutations. WHERE CURRENT OF positioned-DML is indistinguishable here (d4_fk has one row) and is a separate project-wide follow-up (ledger). (case `detach-partition-concurrently-4`) |
| `postgres/src/test/isolation/specs/drop-index-concurrently-1.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationDropIndexConcurrently1 (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `drop-index-concurrently-1`) |
| `postgres/src/test/isolation/specs/eval-plan-qual-trigger.spec` | pass | TestPort_IsolationEvalPlanQualTrigger PASS — Phase 1 inline EPQ + BEFORE trigger inline firing; ON CONFLICT trigger paths; bpchar output fix (M0100-0011) (case `eval-plan-qual-trigger`) |
| `postgres/src/test/isolation/specs/eval-plan-qual.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationEvalPlanQual (design 0118-0106) -- all 50 permutations byte-identical to PG 18.3. The last divergence was the EPQ-over-join selectresultforupdate case (SELECT ... FOR UPDATE OF jt over a join whose locked relation jt is the inner index scan): goopg folded the index key condition jt.id = y into the per-row EvalPlanQual recheck filter but y is another join input's column whose index lives in the join coordinate space -- misread against the 2-column jointest tuple as jt.id = jt.data so the post-update row was wrongly dropped (0 rows). Fixed by only folding a row-local (constant) index key into the recheck (exprRefsColumnOrOuter guard); join/correlated keys are excluded and the CTID-chain logic still catches key-column changes. Sibling fix: a lazy hash join now preserves build-side heap ctids (preserveCTIDRel/lazyHashCTID) so FOR UPDATE over the hash-join variant recovers the locked TID after the build scan is drained+closed. (case `eval-plan-qual`) |
| `postgres/src/test/isolation/specs/fk-contention.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationFkContention (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `fk-contention`) |
| `postgres/src/test/isolation/specs/fk-deadlock.spec` | pass | Promoted via runIsoSpecStrict TestPort_IsolationFkDeadlock (design 0118-0094) -- FK existence scan scanRelForFKMatch made FOR KEY SHARE-aware (waits only on a key-changing parent xmax; no-key UPDATE is a clean match); all 14 permutations byte-identical to PG 18.3. (case `fk-deadlock`) |
| `postgres/src/test/isolation/specs/fk-deadlock2.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationFkDeadlock2 (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `fk-deadlock2`) |
| `postgres/src/test/isolation/specs/fk-partitioned-1.spec` | pass | Promoted M0118-0005/0009 (designs 0118-0118/0119/0120): cloning a foreign key onto an ATTACHed partition validates existing rows referencing-side (Class A) and the referenced-side leaf-partition DELETE check names the ordinal-suffixed clone constraint (committed Class B); the deferred attach records its XID so a concurrent referenced-row DELETE waits for it to commit then re-evaluates (concurrent Class B). Strict via TestPort_IsolationFkPartitioned1. (case `fk-partitioned-1`) |
| `postgres/src/test/isolation/specs/fk-partitioned-2.spec` | pass | Promoted M0118-0005 (design 0118-0121) -- FK referencing a partitioned table: INSERT-side scanTableForMatchFKWait raises 40001 (not 23503) when the FOR KEY SHARE wait finds the parent key-changed by a committed concurrent xact under RR/SSI; DELETE FROM the partitioned parent routes to its leaf so the violation names the per-partition clone (pfk_a_fkey_1); fkDeleteAncestorPass honours DEFERRABLE INITIALLY DEFERRED. All six permutations byte-identical to PG 18.3; strict via TestPort_IsolationFkPartitioned2. Closes the M0118-0005 FK/referential-integrity group. (case `fk-partitioned-2`) |
| `postgres/src/test/isolation/specs/fk-snapshot.spec` | pass | TestPort_IsolationFkSnapshot PASS — all 7 permutations (M0100-0005w: parent DELETE waits for in-flight child INSERT; RR raises 40001). (case `fk-snapshot`) |
| `postgres/src/test/isolation/specs/freeze-the-dead.spec` | pass | Verified by TestPort_IsolationFreezeTheDead (M0118-0009 design 0118-0020). VACUUM now reclaims dead tuples through the HOT-chain-aware and multixact-aware PageVacuumPrune (the sibling of the opportunistic PagePruneOpt) instead of the naive xmax<horizon slot removal; dead HOT chain roots become ItemIDRedirect so index scans keep resolving the live tip and an updater-bearing multi xmax resolves its updater before the horizon compare. Both halves of the spec (index-scan find + final two-row select) match PG 18.3 byte-for-byte. (case `freeze-the-dead`) |
| `postgres/src/test/isolation/specs/horizons.spec` | pass | PROMOTED (M0118-0009 design 0118-0105): all 5 permutations match PG 18.3. Builds on the 0118-0104 temp-half enabler; the final permanent-table VACUUM-respects-older-snapshot permutation was unblocked by (1) pinning an explicit RR/SSI transaction's snapshot at the first snapshot-taking statement of a batched BEGIN message (registering its proc-array xmin so OldestXmin/VACUUM retain the rows) and (2) making the EvalPlanQual write paths' concurrent-update 40001 detection authoritative (epqXmaxSettled via the transaction manager) rather than relying on frozen-snapshot membership, fixing a latent lost-update bug. Test: TestPort_IsolationHorizons (strict). (case `horizons`) |
| `postgres/src/test/isolation/specs/index-only-bitmapscan.spec` | pass | Passes — TestPort_IsolationIndexOnlyBitmapscan (internal/testport/isolation_port_test.go) matches PG 18.3 byte-for-byte. M0118-0002 (design 0118-0122): a regression guard for an unsound index-only bitmap heap scan removed upstream. s1 opens a NO SCROLL cursor over SELECT row_number() OVER () FROM ios_bitmap WHERE a>0 OR b>0 (a BitmapOr over two indexes) then FETCHes one row to force the index-scan portion; s2 then VACUUMs after deleting nearly all rows; the post-FETCH FETCH ALL must return 0 rows (not stale all-visible rows). goopg returns 1 then 0 rows. The sole remaining blocker was that EXPLAIN (COSTS OFF) DECLARE foo ... CURSOR FOR <query> (step s1_explain) raised 0A000 because the planner rejected a DeclareCursorStmt inner — now unwrapped to plan the cursor query (PG ExplainOneUtility to ExplainOneQuery). The EXPLAIN plan body is stripped on both sides by the runner plan-strategy normalization policy so the FETCH row counts are the compared anomaly check; no execution-engine change. (case `index-only-bitmapscan`) |
| `postgres/src/test/isolation/specs/index-only-scan.spec` | pass | Passes — TestPort_IsolationIndexOnlyScan (internal/testport/isolation_port_test.go) matches PG 18.3 byte-for-byte across all six generated permutations. A SERIALIZABLE write skew across two all-visible tables (tabx / taby) where each transaction reads one table via SELECT min(id) — an index-only scan after VACUUM FREEZE ANALYZE — then DELETEs the matching row from the other table; every overlap forms a rw-dependency cycle so the second committer aborts with 40001 while the two serialized orderings commit cleanly. Zero-SSI-change promotion — the existing index-only-scan SIREAD granularity already records the cross-covering reads that close the dangerous structure. Three harness blockers cleared: parseSetValueAtoms now accepts floating-point GUC values (SET seq_page_cost = 0.1 was rejected as a TokenNumericLit); the isolation runner now runs each global setup block as its own submission (mirroring isolationtester.c PQexec-per-setupsqls) so VACUUM FREEZE ANALYZE runs standalone instead of aborting an implicit multi-statement transaction — previously multiple global setup blocks silently overwrote each other leaving only the last; and the real GUCs cpu_tuple_cost / cpu_index_tuple_cost / cpu_operator_cost are now registered so the session-setup SETs do not raise unrecognized configuration parameter. (case `index-only-scan`) |
| `postgres/src/test/isolation/specs/inherit-temp.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationInheritTemp (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `inherit-temp`) |
| `postgres/src/test/isolation/specs/inplace-inval.spec` | pass | Passes by construction via TestPort_IsolationInplaceInval (internal/testport/isolation_port_test.go). goopg's pg_class is a virtual relation whose relhasindex is derived live from the in-memory index set (len(c.byTable[oid])>0) on every read so the upstream inplace-update-revert hazard cannot occur — there is no heap tuple no catcache oldtup and no heap_inplace_update path to overwrite. Both permutations observe relhasindex=t. Design 0118-0019. (case `inplace-inval`) |
| `postgres/src/test/isolation/specs/insert-conflict-do-nothing-2.spec` | pass | TestPort_IsolationInsertConflictDoNothing2 PASS — multi-row INSERT ... ON CONFLICT DO NOTHING under REPEATABLE READ and SERIALIZABLE; the conflict arbiter skips the duplicate key without ever raising 40001 so goopg matches PG 18.3 byte-for-byte across all 8 permutations; zero engine change on the M0100-0002 ON CONFLICT DO NOTHING path layered on the M0104 RR/SER isolation levels. (case `insert-conflict-do-nothing-2`) |
| `postgres/src/test/isolation/specs/insert-conflict-do-nothing.spec` | pass | TestPort_IsolationInsertConflictDoNothing PASS (M0100-0002: eager XID materialisation + IsolationRunner global teardown). (case `insert-conflict-do-nothing`) |
| `postgres/src/test/isolation/specs/insert-conflict-do-update-2.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationInsertConflictDoUpdate2 (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `insert-conflict-do-update-2`) |
| `postgres/src/test/isolation/specs/insert-conflict-do-update-3.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationInsertConflictDoUpdate3 (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `insert-conflict-do-update-3`) |
| `postgres/src/test/isolation/specs/insert-conflict-do-update-4.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationInsertConflictDoUpdate4 (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `insert-conflict-do-update-4`) |
| `postgres/src/test/isolation/specs/insert-conflict-do-update.spec` | pass | TestPort_IsolationInsertConflictDoUpdate PASS (M0096-0005 + M0100-0002 cross-reference). (case `insert-conflict-do-update`) |
| `postgres/src/test/isolation/specs/insert-conflict-specconflict.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationInsertConflictSpecconflict (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `insert-conflict-specconflict`) |
| `postgres/src/test/isolation/specs/intra-grant-inplace-db.spec` | pass | PROMOTED M0118-0009 (design 0118-0098); TestPort_IsolationIntraGrantInplaceDb strict — a database-wide VACUUM now waits (mvcc.WaitForXID) on the writer XID of a concurrent uncommitted GRANT … ON DATABASE before advancing datfrozenxid in place; replays PG's heap_inplace_update_scan waiting on the pg_database tuple xmax (GRANT takes no heavyweight lock); 1 permutation byte-identical to PG 18.3. (case `intra-grant-inplace-db`) |
| `postgres/src/test/isolation/specs/intra-grant-inplace.spec` | pass | PROMOTED M0118-0009 (design 0118-0117); TestPort_IsolationIntraGrantInplace strict — all 10 permutations byte-identical to PG 18.3. pg_class-tuple-xmax serialization for GRANT/REVOKE ACL changes plus FOR UPDATE/SHARE rowmarks plus in-place ADD PRIMARY KEY plus a transaction-deferred DELETE FROM pg_class. Perm 10 capstone: the deferred catalog-tuple delete records a pg_class delete xmax (Catalog.SetTablePendingDropXID) that sfu3 FOR UPDATE waits on then returns 0 rows once committed; the FOR UPDATE retracts its rowmark on empty so revoke4 (blocked behind it via a poll-based mark-release wait) proceeds before ROLLBACK and finds the relation gone — raising cache lookup failed for relation OID caught by the DO block EXCEPTION handler (plpgsql TryBody wrapping plus SQLERRM binding plus RAISE WARNING severity fixes) and re-raised as a REDACTED WARNING. (case `intra-grant-inplace`) |
| `postgres/src/test/isolation/specs/lock-committed-keyupdate.spec` | pass | TestPort_IsolationLockCommittedKeyupdate PASS — FOR KEY SHARE on a tuple whose key was UPDATEd by a concurrent committed s1; the key-update conflicts with the locker so under READ COMMITTED the locker follows the CTID chain to the live successor while under REPEATABLE READ / SERIALIZABLE it raises 40001; the locker also blocks behind s1 in-progress key UPDATE until commit; passes on the M0118-0003 row-locking infrastructure — lock-only-xmax cross-statement conflict detection plus the blocking WaitForXID path in stampLockInner — with no new engine code. (case `lock-committed-keyupdate`) |
| `postgres/src/test/isolation/specs/lock-committed-update.spec` | pass | TestPort_IsolationLockCommittedUpdate PASS — all 24 permutations (M0100-0005e seqScanOp page-RLock per-tuple; M0100-0005f stampLock preserves real updater xmax). (case `lock-committed-update`) |
| `postgres/src/test/isolation/specs/lock-nowait.spec` | pass | TestPort_IsolationLockNowait PASS — s1 takes ACCESS EXCLUSIVE on a1 then s2 requests EXCLUSIVE and blocks behind it; while s2 waits s1 takes SHARE ROW EXCLUSIVE NOWAIT which is granted immediately because s1 already holds a stronger self-compatible lock and jumps ahead of the parked conflicting waiter (lockmgr JoinWaitQueue early-grant rule) rather than failing NOWAIT; after s1 COMMIT s2 is woken and completes. New engine work: LOCK TABLE now acquires a real transaction-scoped heavyweight lock on a dedicated executor.tableLockMgr keyed by a stable per-connection backend identity (connTxState.LockBackendID) so the lock survives dispatch.go per-statement ReleaseAll and is dropped only in connTxState.End() at COMMIT/ROLLBACK. The dedicated manager keeps blast radius off the nil production Context.LockMgr so ordinary scans/DML/DDL are unaffected. M0118-0003. (case `lock-nowait`) |
| `postgres/src/test/isolation/specs/lock-update-delete.spec` | pass | TestPort_IsolationLockUpdateDelete PASS — all 12 permutations. s2 holds a session advisory lock so s1l (SELECT FOR KEY SHARE) blocks at the advisory gate while s2 builds an update chain (no-key UPDATE then DELETE / key-UPDATE / no-key-UPDATE); on unblock the blocked-then-woken locker walks the chain forward (propagateLockForward = heap_lock_updated_tuple_rec) and WAITS on a conflicting in-flight DELETE / key-UPDATE until s2 commits or aborts. Committed DELETE -> chainLockDeleted so 0 rows; committed key-UPDATE -> chainLockUpdated then EvalPlanQual recheck of the index key=1 predicate fails so 0 rows; abort or the no-key-UPDATE blocker3 -> the original (1 1) row is returned. A goopg DELETE leaves t_ctid self-pointing (no HEAP_KEYS_UPDATED) so it is classified StatusUpdate to conflict with FOR KEY SHARE. EPQ recheck folds the index-scan key predicate so a relocated key is detected (M0118-0003). (case `lock-update-delete`) |
| `postgres/src/test/isolation/specs/lock-update-traversal.spec` | pass | TestPort_IsolationLockUpdateTraversal PASS — all 3 permutations. s1 takes FOR KEY SHARE on a row s2 updated in-flight (no-key); the lock is propagated forward onto the live successor (locker half) and the plain UPDATE/DELETE write path now waits on the still-active holder before stamping xmax (write-path half waitForConflictingRowLock): s2d1 DELETE and s2d2 key-UPDATE block until s1 COMMITs while s2d3 no-key UPDATE proceeds immediately (FOR KEY SHARE does not conflict with a no-key update) (M0118-0003). (case `lock-update-traversal`) |
| `postgres/src/test/isolation/specs/matview-write-skew.spec` | pass | Passes — TestPort_IsolationMatviewWriteSkew (internal/testport/isolation_port_test.go) matches PG 18.3 byte-for-byte across all eight permutations. Write skew between a SERIALIZABLE REFRESH MATERIALIZED VIEW CONCURRENTLY (reads the parent orders and rewrites the order_summary matview) and a concurrent SERIALIZABLE xact that reads the matview and writes the parent — every overlap is a dangerous structure so the second committer aborts with 40001. goopg already detected the four refresh-before-read permutations via read-side conflict-out but missed the four read-before-refresh permutations: the refresh rewrites the matview heap through the low-level writeHeapRow path which never runs the per-tuple ssiRecordTupleWrite hook so no rw-edge formed from the matview reader to the refresher. Fixed by adding Manager.CheckTableForSerializableConflictIn — a relation-wide all-granularity conflict-in mirroring upstream CheckTableForSerializableConflictIn (predicate.c) used for TRUNCATE/DROP — invoked from execRefreshMatView via the new ssiRecordTableWrite executor hook so the refresh forms an rw-conflict in from every transaction holding a predicate lock of any granularity on the matview. Date columns render MM-DD-YYYY (DateStyle Postgres MDY) and the runner replicates libpq PQprint content-based right-justification. Verified by the TestCheckTableForSerializableConflictIn mvcc unit suite plus the deterministic port test. (case `matview-write-skew`) |
| `postgres/src/test/isolation/specs/merge-delete.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationMergeDelete (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `merge-delete`) |
| `postgres/src/test/isolation/specs/merge-insert-update.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationMergeInsertUpdate (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `merge-insert-update`) |
| `postgres/src/test/isolation/specs/merge-join.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationMergeJoin (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `merge-join`) |
| `postgres/src/test/isolation/specs/merge-match-recheck.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationMergeMatchRecheck (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `merge-match-recheck`) |
| `postgres/src/test/isolation/specs/merge-update.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationMergeUpdate (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `merge-update`) |
| `postgres/src/test/isolation/specs/multiple-cic.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationMultipleCic (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `multiple-cic`) |
| `postgres/src/test/isolation/specs/multiple-row-versions.spec` | pass | Passes — TestPort_IsolationMultipleRowVersions (internal/testport/isolation_port_test.go) matches PG 18.3 byte-for-byte across all generated permutations; verified 3/3 runs at ~67s each with no skip. SSI behavior is correct via the M0118-0001 single-column full-key index-scan SIREAD granularity change (s1 reading id=1000000 v0 no longer phantom-covers s3 overwriting the intermediate s2 v1; only wz1 aborts where PG does). The prior intermittent setup blocker — the WAL-buffer ring race errWALBufferReservedOutOfRange on the 1000000-row single-transaction bulk INSERT — is FIXED by reserving the worst-case segment-crossing pad footprint (2*conservativeSize) in the buffered append paths (state.tryAppend + state.appendPGCompat); see docs/design/0107-0007aj. (case `multiple-row-versions`) |
| `postgres/src/test/isolation/specs/multixact-no-deadlock.spec` | pass | TestPort_IsolationMultixactNoDeadlock PASS — s1 takes FOR SHARE on the single row then s2 also takes FOR SHARE turning the tuple lock into a multixact; s3 then requests FOR UPDATE which conflicts with the SHARE multixact and waits. While s3 waits s1 re-requests FOR SHARE (s1lock2) — a lock it already holds — and is granted immediately rather than queuing behind the waiting s3 (which would deadlock) because s1 is already a member of the SHARE multixact. After s2 and s1 commit s3's FOR UPDATE is granted. No new engine work — goopg's row-lock acquisition path already lets a session re-acquire a tuple lock it already holds without parking behind a conflicting waiter so the no-deadlock invariant holds without the heavyweight-lock deadlock detector ever being consulted. M0118-0004. (case `multixact-no-deadlock`) |
| `postgres/src/test/isolation/specs/multixact-no-forget.spec` | pass | TestPort_IsolationMultixactNoForget PASS — all 9 permutations. s1 holds FOR KEY SHARE then s2 UPDATEs (forming a {s1-keyshare + s2-no-key-update} multixact); when s2 aborts the surviving s1 lock must not be forgotten and when s2 commits the lock must be carried forward onto the new tuple version. Two engine fixes (M0118-0009 design 0118-0016). Fix 1 — the keyConflict branch of lockRowsOp.stampLockInner now builds its multi-member wait set from conflictingLockHolders rather than activeLockHolders so a non-conflicting surviving locker is not waited on (s3 FOR NO KEY UPDATE no longer blocks on s1 FOR KEY SHARE after s2 aborts; only s3 FOR UPDATE still conflicts and waits); and stampAtPtr now preserves surviving lockers via stampMultiLock so the aborted-updater stamp keeps s1's lock alive. Fix 2 — the HOT UPDATE producer carries the old tuple's non-conflicting lockers forward onto the new tuple version (carryForwardLockersToNewTuple — the new-version half of heap_update compute_new_xmax_infomask — sharing survivingLockersForUpdate with the old-tuple stamp) so after s2 commits s3 FOR UPDATE waits on the inherited FOR KEY SHARE until s1 commits. (case `multixact-no-forget`) |
| `postgres/src/test/isolation/specs/nowait-2.spec` | pass | TestPort_IsolationNowait2 PASS — NOWAIT with multixact locks. Like skip-locked-2 but s2 upgrades with FOR UPDATE NOWAIT and must abort fast with 55P03 instead of skipping: the multixact SHARE member held by s1 is a conflicting active holder so stampLockInner's NOWAIT branch raises could-not-obtain-lock (relation-qualified). Rides the M0118-0003 multixact producer with no new engine code. (case `nowait-2`) |
| `postgres/src/test/isolation/specs/nowait-3.spec` | pass | TestPort_IsolationNowait3 PASS — a blocking FOR UPDATE on a row whose lock-only xmax is held by another active session now waits via WaitForXID and re-stamps on the holder's COMMIT while a concurrent FOR UPDATE NOWAIT still fails fast with 55P03; stampLockInner lock-only branch honours the wait policy instead of overwriting the holder (M0118-0003). (case `nowait-3`) |
| `postgres/src/test/isolation/specs/nowait-4.spec` | pass | TestPort_IsolationNowait4 PASS — NOWAIT on an updated tuple chain. s1 takes a snapshot at BEGIN then blocks at a pg_advisory_lock(0) gate while s2 commits a no-key UPDATE of id=1 and opens a second still-in-progress UPDATE of the same row; when the advisory lock releases s1 follows the ctid chain from its stale snapshot version to the version whose xmax is s2's in-progress real update and fails fast with 55P03 instead of waiting. Fix threads the SKIP-LOCKED/NOWAIT wait policy into the real-updater wait branch of stampLockInner (only the lock-only branch honoured it before) so EvalPlanQualFetch ereports rather than blocking on the in-progress updater. M0118-0003. (case `nowait-4`) |
| `postgres/src/test/isolation/specs/nowait-5.spec` | pass | TestPort_IsolationNowait5 PASS — NOWAIT on an updated tuple chain. sl1's prepared FOR UPDATE NOWAIT blocks at a pg_advisory_lock(0) gate while upd commits a no-key UPDATE of id=2 and lk1 takes FOR SHARE on the live successor; when the advisory lock releases sl1 follows the ctid chain to the updated id=2 version and fails fast with 55P03 because lk1 holds a conflicting FOR SHARE lock-only xmax on it. Rides the READ COMMITTED chain-follow path plus the lock-only NOWAIT fail-fast branch in stampLockInner (M0118-0003) with no new engine code. (case `nowait-5`) |
| `postgres/src/test/isolation/specs/nowait.spec` | pass | TestPort_IsolationNowait PASS — FOR UPDATE NOWAIT fails fast with 55P03 (relation-qualified) via lock-only-xmax cross-statement conflict detection in stampLockInner (M0118-0003); parser accepts trailing LIMIT after the locking clause. (case `nowait`) |
| `postgres/src/test/isolation/specs/partial-index.spec` | pass | Passes — TestPort_IsolationPartialIndex (internal/testport/isolation_port_test.go) matches PG 18.3 byte-for-byte across all generated permutations; verified stable 3/3 runs at ~23s each. An UPDATE that moves a row out of a partial index (CREATE INDEX test_idx ON test_t(id) WHERE val2 = 1) under SERIALIZABLE must still create the read/write dependency a full-table read would so any overlap raises 40001. Zero-engine-change SSI promotion: the predicate read SELECT * FROM test_t WHERE val2 = 1 records SIREAD locks that cross-cover the rows the concurrent UPDATEs move out of the partial index — closing the dangerous structure exactly as the M0118-0001 index-scan SIREAD granularity already does for full indexes. The sole blocker was parser support for a fully parenthesised query source — INSERT INTO test_t (SELECT generate_series(0; 10000); 'a'; 2) — now disambiguated from a column list in parseInsert via the new nextIsParenQuerySource peek (PostgreSQL insert_rest permits a parenthesised SelectStmt source). (case `partial-index`) |
| `postgres/src/test/isolation/specs/partition-concurrent-attach.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationPartitionConcurrentAttach (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `partition-concurrent-attach`) |
| `postgres/src/test/isolation/specs/partition-drop-index-locking.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationPartitionDropIndexLocking (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `partition-drop-index-locking`) |
| `postgres/src/test/isolation/specs/partition-key-update-1.spec` | pass | TestPort_IsolationPartitionKeyUpdate1 PASS (M0100-0005q: FK INSERT waits for in-flight xmax + moved-partition EPQ error). (case `partition-key-update-1`) |
| `postgres/src/test/isolation/specs/partition-key-update-2.spec` | pass | TestPort_IsolationPartitionKeyUpdate2 PASS (M0100-0005t: partition-aware upsert + per-leaf arbiter inheritance). (case `partition-key-update-2`) |
| `postgres/src/test/isolation/specs/partition-key-update-3.spec` | pass | TestPort_IsolationPartitionKeyUpdate3 PASS — all 8 permutations (M0100-0005x: upsert dirty-snapshot probe + RR/SER 40001 raise). (case `partition-key-update-3`) |
| `postgres/src/test/isolation/specs/partition-key-update-4.spec` | pass | TestPort_IsolationPartitionKeyUpdate4 PASS (M0100-0005aa: cross-partition UPDATE fires BEFORE DELETE on source partition). (case `partition-key-update-4`) |
| `postgres/src/test/isolation/specs/plpgsql-toast.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationPlpgsqlToast (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `plpgsql-toast`) |
| `postgres/src/test/isolation/specs/predicate-gin.spec` | pass | PROMOTED M0118-0002 (design 0118-0140): GIN per-key predicate locking reproduced via per-search-key page SIREAD on the catalog-only GIN index (ssiRecordGinKeyRead / ssiRecordGinIndexInsert) replacing the seq scan relation-grain lock; fastupdate=on uses a whole-index sentinel page (toggled by ALTER INDEX SET); strict TestPort_IsolationPredicateGin byte-identical to PG 18.3 across all permutations. (case `predicate-gin`) |
| `postgres/src/test/isolation/specs/predicate-gist.spec` | pass | PROMOTED M0118-0002 (design 0118-0137): GiST page-level predicate locking reproduced via per-matching-tuple grid-cell SIREAD on the catalog-only GiST index (ssiRecordGistGridRead / ssiRecordGistIndexInsert) replacing the seq scan relation-grain lock; strict TestPort_IsolationPredicateGist byte-identical to PG 18.3 across all 36 permutations. (case `predicate-gist`) |
| `postgres/src/test/isolation/specs/predicate-hash.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationPredicateHash (design 0118-0099). PAGE (bucket) level predicate locking in a hash index. A declared-hash index (catalog.Index.DeclaredHash — Method stays btree on goopg's B-tree substrate) now drives a bucket-grain SIREAD predicate lock keyed by the index OID + a stable hash of the encoded equality key (ssiRecordHashBucketRead on the index scan / index-only scan; ssiRecordHashIndexInsert on the INSERT path) in place of a seq scan's relation-grain lock. The per-tuple heap reads switch to conflict-out-only (ssiConflictOutTupleRead) so they do not coarsen to a heap-page lock that would re-introduce a different-bucket false positive. Result: same-bucket scan/insert interleavings still conflict (40001) while the 12 different-bucket interleavings goopg previously over-aborted now commit — byte-identical to PG 18.3 across all 40 permutations. Blast radius bounded to SERIALIZABLE equality scans on declared-hash indexes; the pass-required btree/seqscan SSI specs are unaffected. (case `predicate-hash`) |
| `postgres/src/test/isolation/specs/predicate-lock-hot-tuple.spec` | pass | Passes — TestPort_IsolationPredicateLockHotTuple (internal/testport/isolation_port_test.go) matches PG 18.3 byte-for-byte; verified stable across multiple -count runs at ~1.4s each. Two SERIALIZABLE xacts each SELECT i IN (5;7) then UPDATE one of the two rows; the cross-covering point reads form a write-skew dangerous structure so the later committer s2 aborts at c2 with 40001. Zero-engine-change promotion: the M0118-0001 single-column full-key index-scan SIREAD granularity records exact per-tuple SIREAD locks for each IN-list point lookup on the PRIMARY KEY index (i=5 and i=7) so w1 (update i=7) and w2 (update i=5) each install an rw-edge to the other reader closing the 2-cycle. goopg has no HOT update so the setup HOT-update is a plain update; the predicate-lock correctness the spec guards still holds. (case `predicate-lock-hot-tuple`) |
| `postgres/src/test/isolation/specs/prepared-transactions-cic.spec` | pass | Promoted M0118-0009 (design 0118-0111); TestPort_IsolationPreparedTransactionsCIC strict -- byte-identical to PG 18.3. Builds on same-backend 2PC (0118-0110); mvcc.WaitForSlotsToCommit now arms the session lock_timeout (lockwait.Timeout) so a CREATE INDEX CONCURRENTLY parked behind a still-running prepared txn's MVCC slot is cancelled with 55P03 lock timeout at the 10ms budget. (case `prepared-transactions-cic`) |
| `postgres/src/test/isolation/specs/prepared-transactions.spec` | defer | M-NIGHTLY (AI-20260719-094219-001) demoted pass->defer (strict->runIsoSpec). SSI correctness intact -- every permutation's query results/aborts still match PG 18.3 (PrepareCheckForSerializationFailure at PREPARE TRANSACTION time; a PREPARED-but-not-committed peer treated like a committed-first one). Demoted because this uniquely long spec (1500 permutations, ~60s) is the most exposed to the runner's timing-only blocking heuristic (framework/isolation_runner.go blockDetectWait=300ms): goopg has a real intermittent ~300ms server-side 2PC-commit stall on WSL2 (WAL 16MiB segment zero-fill / 2PC state-file I/O) that hits a random PREPARE/COMMIT PREPARED step once per run (3/3 standalone on a quiet host, at a moving permutation), so a non-blocking step is mislabeled <waiting ...>/<... completed>, shifting output by two lines. Re-promote once pg_isolation_test_session_is_blocked (pg_proc OID 3378, registered-but-unimplemented) is implemented and the runner polls it to confirm genuine lock-blocking before annotating slow steps (upstream isolationtester.c behavior). (case `prepared-transactions`) |
| `postgres/src/test/isolation/specs/project-manager.spec` | pass | Ported as TestPort_IsolationProjectManager (internal/testport/isolation_port_test.go). M0118-0001: PHANTOM predicate locking now closes the resource-assignment dangerous structure. A SERIALIZABLE seq scan takes a relation-level SIREAD (PredicateLockRelation analogue in seqScanOp.Open) so a concurrent INSERT of a matching row forms the rw-conflict; the scan also conflicts-out against physically-present-but-invisible concurrent inserts (HeapCheckForSerializableConflictOut !visible path) for the INSERT-before-READ ordering; committed readers' predicate locks are now RETAINED past commit (finished-walk in checkForSerializableConflictInLocked) so a write after the partner committed still finds the SIREAD holder; and the write path now aborts the pivot MID-STATEMENT with 40001 (CheckForSerializableConflictInReportingFailure) where PG does. All 21 generated permutations match expected byte-for-byte vs PG 18.3. (case `project-manager`) |
| `postgres/src/test/isolation/specs/propagate-lock-delete.spec` | pass | TestPort_IsolationPropagateLockDelete PASS — all 8 permutations. s1 and s2 each INSERT INTO child VALUES(1) which the FK RI check guards; s3 then UPDATEs parent (no-key SET c=lower(c) or key-update SET i=i with an optional aborted SAVEPOINT) and finally DELETEs parent. The DELETE blocks on the still-in-flight child INSERTs via fkChildWaitForInFlightInsert (M0100-0005w); once s1 and s2 COMMIT the now-visible child rows make assertNoChildRows raise 23503 update-or-delete-violates-foreign-key-constraint child_i_fkey. Rides the existing FK in-flight-child-insert wait path plus the M0118-0003 row-locking infrastructure with no new engine code. (case `propagate-lock-delete`) |
| `postgres/src/test/isolation/specs/read-only-anomaly-2.spec` | pass | Ported as TestPort_IsolationReadOnlyAnomaly2 (internal/testport/isolation_port_test.go). M0118-0001: both permutations now match PG 18.3 byte-for-byte. Fixed by narrowing the SERIALIZABLE index-scan SIREAD granularity so a full-key point lookup on a single-column index (SELECT WHERE id=Y) relies on the exact per-tuple SIREAD locks recorded in Next instead of a blanket relation-grain lock. The coarse lock phantom-covered X; s2 writing X then formed a spurious s1->s2 edge that closed a false 2-cycle and over-aborted s2wx. Empty probes plus composite/range scans keep the relation-grain gap lock so the read-write-unique family stays green. (case `read-only-anomaly-2`) |
| `postgres/src/test/isolation/specs/read-only-anomaly-3.spec` | pass | Passes — TestPort_IsolationReadOnlyAnomaly3 (internal/testport/isolation_port_test.go) matches PG 18.3 byte-for-byte. The O'Neil read-only anomaly where s3 is declared SERIALIZABLE READ ONLY DEFERRABLE. PostgreSQL avoids the anomaly without aborting anyone by deferring s3's snapshot (GetSafeSnapshot) until a safe snapshot is available; s3r blocks while the concurrent read-write s2 is active then completes once s2 commits and observes the final committed state (X=-11 Y=20). goopg now records the declared READ ONLY / DEFERRABLE modes on the SerializableXact via Manager.MarkSerializableModes and Manager.waitForSafeSnapshot blocks the deferrable reader's first snapshot until every concurrent declared-read-write SERIALIZABLE xact has finished — so no transaction is rolled back. Two blockers were fixed: the DEFERRABLE keyword token was never consumed by parseBeginModes (acceptIdentKeyword vs acceptKeyword) and the safe-snapshot drain-the-writers deferral did not exist. Verified by the TestSafeSnapshot* mvcc unit suite plus the deterministic port test. (case `read-only-anomaly-3`) |
| `postgres/src/test/isolation/specs/read-only-anomaly.spec` | pass | Ported as TestPort_IsolationReadOnlyAnomaly (internal/testport/isolation_port_test.go). M0118-0001: the classic read-only transaction anomaly under REPEATABLE READ — snapshot isolation ALLOWS the anomaly so no serialization failure occurs; the read-only s3 simply observes the inconsistent state (X=0 / Y=20) and the output matches PG 18.3 byte-for-byte with no SSI machinery involved. (case `read-only-anomaly`) |
| `postgres/src/test/isolation/specs/read-write-unique-2.spec` | pass | Ported as TestPort_IsolationReadWriteUnique2 (internal/testport/isolation_port_test.go). M0118-0001: both SERIALIZABLE sessions probe for i=42 then INSERT; the overlapping interleaving (r1 r2 w1 w2 c1 c2) now aborts one with 40001 and the serialized interleaving (r1 w1 c1 r2 w2 c2) raises a 23505 unique violation — both permutations match PG 18.3 byte-for-byte. (case `read-write-unique-2`) |
| `postgres/src/test/isolation/specs/read-write-unique-3.spec` | pass | Ported as TestPort_IsolationReadWriteUnique3 (internal/testport/isolation_port_test.go). M0118-0001: the bug-9301 insert-if-not-exists SQL function under SSI now aborts s2 with 40001 where PG 18.3 does; the single permutation matches byte-for-byte. (case `read-write-unique-3`) |
| `postgres/src/test/isolation/specs/read-write-unique-4.spec` | pass | Ported as TestPort_IsolationReadWriteUnique4 (internal/testport/isolation_port_test.go). M0118-0001: the gapless per-year invoice sequence now matches PG 18.3 byte-for-byte across all three permutations — the both-read interleaving (r1 r2 w1 w2 c1 c2) aborts the loser with 40001 while the single-reader interleavings (r1 w1 w2 c1 c2 and r2 w1 w2 c1 c2) raise a 23505 unique violation. Fixed by deferring the post-wait 40001-vs-23505 decision in uniqueCheckWithWait to the SSI conflict-in walk plus closing the SERIALIZABLE index-scan / index-only-scan predicate-lock gaps (relation-grain SIREAD for phantoms; invisible-tuple conflict-out for INSERT-before-READ). (case `read-write-unique-4`) |
| `postgres/src/test/isolation/specs/read-write-unique.spec` | pass | Ported as TestPort_IsolationReadWriteUnique (internal/testport/isolation_port_test.go). M0118-0001: two SERIALIZABLE sessions that read-then-INSERT i=42 now produce the upstream outcome — 40001 when the reads overlap and 23505 when serialized; all permutations match PG 18.3 byte-for-byte. (case `read-write-unique`) |
| `postgres/src/test/isolation/specs/receipt-report.spec` | pass | Passes — TestPort_IsolationReceiptReport (internal/testport/isolation_port_test.go) matches PG 18.3 byte-for-byte across all 210 permutations. The daily-receipts read-only anomaly under SERIALIZABLE — only the 6 permutations where s1 overlaps both s2 and s3 and s2 commits before s3's first SELECT are genuine serialization failures; the declared READ ONLY s3 must not produce the other 42 false positives. goopg previously over-aborted (48 vs 6) because it modeled every SERIALIZABLE xact as read-write. Fixed by adding de-facto READ ONLY SSI modeling mirroring CheckForSerializableConflictOut / OnConflict_CheckForSerializationFailure / PreCommit_CheckForSerializationFailure (predicate.c): a SnapshotSeqNo watermark captured at the first statement snapshot (the lastCommitBeforeSnapshot analogue) plus a committedBeforeSnapshot predicate; a READ ONLY reader reading a committed writer now records no rw-conflict unless the writer holds an out-conflict that committed before the reader's snapshot; and OnConflict Cases 2/3 plus the PreCommit far-conflict scan skip a READ ONLY participant that cannot sustain the structure. Verified by the new TestCommittedBeforeSnapshot and TestCheckForSerializableConflictOut_ReadOnly* mvcc unit tests plus the deterministic port test; serializable-parallel-2/3 and read-only-anomaly-3 re-verified for no regression. (case `receipt-report`) |
| `postgres/src/test/isolation/specs/referential-integrity.spec` | pass | Passes — TestPort_IsolationReferentialIntegrity (internal/testport/isolation_port_test.go) matches PG 18.3 byte-for-byte across all generated permutations; verified stable runs. A SERIALIZABLE write skew across two tables (a / b) standing in for an application-enforced foreign key — s1 reads a(i=1) then INSERTs b; s2 reads a(i=1) and the empty b(a_id=1) then DELETEs a(i=1) — where any overlap must raise 40001. The fix closed an index-only-scan SSI gap: a SELECT of only the indexed PK column (SELECT i FROM a WHERE i=1) plans as an index-only scan whose non-ALL_VISIBLE heap-fetch fallback recorded the relation-grain SIREAD and the invisible-tuple phantom edge but NOT the per-tuple conflict-out against a visible tuple's xmax — so a reader that saw a row an in-flight SERIALIZABLE peer had DELETEd never installed the reader->deleter rw-edge. Only the commit order where the pivot committed first (rx2 ry2 wx2 rx1 wy1 c1 c2) under-aborted; indexOnlyScanOp now calls ssiRecordTupleRead on the HOT-resolved live version (xmin AND xmax) mirroring the heap-fetching index-scan path so both rw-edges of the 2-cycle form and the second committer aborts where PG does. (case `referential-integrity`) |
| `postgres/src/test/isolation/specs/reindex-concurrently-toast.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationReindexConcurrentlyToast (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `reindex-concurrently-toast`) |
| `postgres/src/test/isolation/specs/reindex-concurrently.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationReindexConcurrently (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `reindex-concurrently`) |
| `postgres/src/test/isolation/specs/reindex-schema.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationReindexSchema (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `reindex-schema`) |
| `postgres/src/test/isolation/specs/ri-trigger.spec` | pass | PROMOTED M0118-0009 (design 0118-0097); TestPort_IsolationRiTrigger strict — trigger-based RI under SERIALIZABLE matches PG 18.3 byte-for-byte after three fixes: plpgsql trigger-body errors now abort the DML (fireTriggers propagates instead of swallowing) + PERFORM accepts a full query form (SELECT <query>) + FOUND is tracked per frame. (case `ri-trigger`) |
| `postgres/src/test/isolation/specs/sequence-ddl.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationSequenceDdl (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `sequence-ddl`) |
| `postgres/src/test/isolation/specs/serializable-parallel-2.spec` | pass | Passes — TestPort_IsolationSerializableParallel2 (internal/testport/isolation_port_test.go) matches PG 18.3 byte-for-byte. A SERIALIZABLE READ ONLY transaction repeatedly counts foo while a concurrent SERIALIZABLE writer commits; upstream forces a parallel index-only scan via ALTER TABLE foo SET (parallel_workers = 2) plus SET enable_seqscan = off and the parallel cost GUCs to drive SXACT_FLAG_RO_SAFE through a worker. goopg has no parallel executor so the parallel GUCs and the parallel_workers reloption are accepted but inert; the read-only transaction is never a pivot so every step commits and the COUNT(*) is a stable 100 — identical to the serial-equivalent output. Sole blocker was the unsupported ALTER TABLE ... SET (reloptions) clause which now parses and merges table-level storage parameters with validate-then-apply atomicity matching CREATE TABLE WITH — verified by the TestAlterTableSetReloptions* unit suite and a deterministic TestPort_IsolationSerializableParallel2. (case `serializable-parallel-2`) |
| `postgres/src/test/isolation/specs/serializable-parallel-3.spec` | pass | Passes — TestPort_IsolationSerializableParallel3 (internal/testport/isolation_port_test.go) matches PG 18.3 byte-for-byte. Like serializable-parallel-2 but two SERIALIZABLE READ ONLY transactions (s2 and s4) share the oldest xmin alongside two read-write SERIALIZABLE transactions (s1 and s3) stressing the SXACT_FLAG_RO_SAFE oldest-with-this-xmin path in a parallel query. goopg has no parallel executor so the parallel_workers reloption and parallel cost GUCs are inert; no read-only transaction becomes a pivot so every step commits and each SELECT returns the full 10-row table — matching the serial-equivalent output. Unblocked by the same ALTER TABLE ... SET (reloptions) support added for serializable-parallel-2. (case `serializable-parallel-3`) |
| `postgres/src/test/isolation/specs/serializable-parallel.spec` | pass | Passes — TestPort_IsolationSerializableParallel (internal/testport/isolation_port_test.go) matches PG 18.3 byte-for-byte across both permutations. The O'Neil read-only-anomaly under SERIALIZABLE — the identical SSI dangerous structure to read-only-anomaly-2 — where the read-only s3 observing s1's committed write closes the cycle that dooms s2wx with 40001. Upstream runs s3 in a parallel worker via SET debug_parallel_query = on; goopg has no parallel executor so the GUC is a no-op and the serial SSI outcome is identical. Sole blocker was that debug_parallel_query (renamed from force_parallel_mode) was unregistered so its session-setup SET raised an 'unrecognized configuration parameter' error; it is now a no-op developer enum (off/on/regress at PGC_USERSET) per guc_tables.c — verified by TestDebugParallelQueryGUC plus a deterministic TestPort_IsolationSerializableParallel. (case `serializable-parallel`) |
| `postgres/src/test/isolation/specs/simple-write-skew.spec` | pass | Ported as TestPort_IsolationSimpleWriteSkew (internal/testport/isolation_port_test.go). M0118-0001: the runner now auto-generates all step interleavings for specs that declare no explicit permutation lines (run_all_permutations parity in framework/isolation.go generateAllPermutations) and SSI 40001 surfaces the bare upstream errmsg with the reason code in DETAIL; all 6 generated permutations match expected byte-for-byte vs PG 18.3. (case `simple-write-skew`) |
| `postgres/src/test/isolation/specs/skip-locked-2.spec` | pass | TestPort_IsolationSkipLocked2 PASS — SKIP LOCKED with multixact locks. s1 and s2 each take FOR SHARE on the same row; the second FOR SHARE combines both holders into a MultiXactId carrying two SHARE members (stampMultiLock) instead of overwriting the first holder. s2 then SELECT FOR UPDATE SKIP LOCKED must skip the row because FOR UPDATE conflicts with s1's still-active SHARE member: activeLockHolders resolves the multixact members through the shared store and tupleLockConflicts compares the request against the strongest member status (multixact.HintBits). Rides the M0118-0003 multixact producer plus the SKIP-LOCKED wait-policy branch in stampLockInner with no new engine code. (case `skip-locked-2`) |
| `postgres/src/test/isolation/specs/skip-locked-3.spec` | pass | TestPort_IsolationSkipLocked3 PASS — SKIP LOCKED with tuple locks. s1 holds the row lock (FOR UPDATE) on id=1 and s2 queues behind it for the same tuple; s3 SELECT FOR UPDATE SKIP LOCKED skips the contended id=1 and claims id=2. Rides the lock-only-xmax cross-statement conflict detection plus the SKIP-LOCKED drop branch in stampLockInner (M0118-0003) with no new engine code. (case `skip-locked-3`) |
| `postgres/src/test/isolation/specs/skip-locked-4.spec` | pass | TestPort_IsolationSkipLocked4 PASS — SKIP LOCKED with an updated tuple chain. s1 holds a snapshot taken at BEGIN and blocks at a pg_advisory_lock(0) gate while s2 commits a no-key UPDATE of id=1 then opens a second in-progress UPDATE of it; on release s1 follows the ctid chain to the version held by s2's in-progress real update and skips that row (FOR UPDATE SKIP LOCKED LIMIT 1) returning id=2 instead of waiting. Shares the nowait-4 fix that threads the wait policy into the real-updater wait branch of stampLockInner so the chain-follow honours SKIP LOCKED at a successor version not only at the head. M0118-0003. (case `skip-locked-4`) |
| `postgres/src/test/isolation/specs/skip-locked.spec` | pass | TestPort_IsolationSkipLocked PASS — FOR UPDATE SKIP LOCKED LIMIT n now plans Limit→LockRows→Sort (planner lifts the LIMIT above LockRows and caps the drain at LIMIT successfully-locked rows) and stampLock/stampLockInner silently drop rows held by another active xact via the lock-only-xmax + lockmgr tuple-lock probes (M0118-0003); sortOp preserves the per-row TID side-channel through the in-memory sort. skip-locked-2 (multixact) skip-locked-3 (cross-statement tuple-lock wait queue) skip-locked-4 (EvalPlanQual advisory chain) now also pass. (case `skip-locked`) |
| `postgres/src/test/isolation/specs/stats.spec` | pass | PROMOTED M0118-0009 (2026-06-28 design 0118-0133); TestPort_IsolationStats strict (runIsoSpecStrict) — pg_stat_* function/relation/SLRU cumulative statistics match PG 18.3 byte-for-byte across all permutations. Built over rungs 0118-0123..0132 (force-flush/clear GUCs; function-stats counters plus getters; transactional DROP FUNCTION; stats_fetch_consistency snapshot/cache; cross-backend 2PC stat drops; relation tuple stats plus pg_stat_get_xact_*; SLRU stats). Final rung 0118-0133: isolation-runner per-session connection reuse (sessionConns opened once per spec in RunSpec and reused across every permutation) mirroring isolationtester.c so step-set GUCs like track_functions persist across permutations. (case `stats`) |
| `postgres/src/test/isolation/specs/subxid-overflow.spec` | pass | Verified by TestPort_IsolationSubxidOverflow (M0118-0009 design 0118-0021). The recursive gen_subxids PL/pgSQL function opens 100 nested subtransactions via per-frame EXCEPTION handlers overflowing the subxid cache while other sessions probe MVCC visibility and XactLockTableWait. Two PL/pgSQL parser gaps blocked it: a bare RETURN (upstream-legal in a VOID function) was rejected at parse time and the NULL no-op statement (the empty EXCEPTION handler body) was unsupported. With both lifted goopg's existing subxact visibility and lock machinery already match PG 18.3 byte-for-byte. (case `subxid-overflow`) |
| `postgres/src/test/isolation/specs/temp-schema-cleanup.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationTempSchemaCleanup (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across both permutations (DISCARD TEMP cascade + pg_terminate_backend self-termination with backend-exit temp cleanup ordered before advisory-lock release). M0118. (case `temp-schema-cleanup`) |
| `postgres/src/test/isolation/specs/temporal-range-integrity.spec` | pass | Passes — TestPort_IsolationTemporalRangeIntegrity (internal/testport/isolation_port_test.go) matches PG 18.3 byte-for-byte across all generated permutations; verified stable 3/3 runs at ~2.5s each. A SERIALIZABLE temporal write-skew across two tables — s1 reads statute with a date-range predicate then INSERTs offense; s2 reads offense with a date-range predicate then DELETEs statute — where any overlap must raise 40001. Zero-SSI-change promotion: the dangerous-structure detection already validated on the prior 23 specs produces the exact abort pattern because the deleted statute row and the inserted offense row both fall within the concurrent readers' predicates so the relation/tuple-grain SIREAD locks cross-cover identically to PG — including the empty-relation predicate reads. The sole blocker was btree key-type support: the statute PRIMARY KEY (statute_cite text; eff_date date) rejected the date key (0A000 btree v0 only supports int4 / numeric keys; got date) at global setup. Fix: date is now an accepted btree key type encoded order-preservingly via the int4-days-since-epoch path (encodeBTreeKeyForColumn plus both index-only key decoders) mirroring the codec date encoding so probe and backfill keys are byte-identical. (case `temporal-range-integrity`) |
| `postgres/src/test/isolation/specs/timeouts.spec` | pass | Passes — row-level half via TestPort_TimeoutsRowLevel and table-level half via TestPort_TimeoutsTableLevel (internal/testport/timeouts_rowlock_test.go); both 4/4 matching PG 18.3 cancel messages and ~300ms timing. Row-level half (design 0118-0017) added the lock_timeout GUC plus statement-timeout-aware lock-wait cancel messages (lock_timeout->lock timeout / statement_timeout->statement timeout / client cancel->user request) via leaf pkg internal/lockwait; epqWait now surfaces the timeout instead of swallowing it into a spurious 40001. Table-level half (design 0118-0018) makes a plain SELECT take a transaction-scoped ACCESS SHARE on tableLockMgr (Context.acquireScanReadLockTxn at the seqscan / index / index-only opens) so a later bare LOCK TABLE (ACCESS EXCLUSIVE) conflicts and is cancelled by the shorter timeout. Bounded blast radius — no-op outside an explicit txn and for system catalogs; idempotent re-acquire; ACCESS SHARE grants instantly absent a concurrent LOCK TABLE / DDL. (case `timeouts`) |
| `postgres/src/test/isolation/specs/total-cash.spec` | pass | Ported as TestPort_IsolationTotalCash (internal/testport/isolation_port_test.go). M0118-0001: the read-only-anomaly pivot now aborts the reader MID-STATEMENT with 40001 where PG does — the read-path SSI hook CheckForSerializableConflictOutReportingFailure surfaces the upstream 'Canceled on conflict out to pivot during read' ereport through the three scan operators (seqScanOp/indexScanOp/scanMatching) so a SELECT that closes a dangerous structure to an already-committed writer fails in place rather than at COMMIT. Also closed a sibling-path gap: the index-based UPDATE path (updateViaIndex) now records the write-path conflict-in edge like the seqscan path so a PRIMARY KEY UPDATE installs rw-edges from concurrent SIREAD holders. All 20 generated permutations match expected byte-for-byte vs PG 18.3. (case `total-cash`) |
| `postgres/src/test/isolation/specs/truncate-conflict.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationTruncateConflict (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `truncate-conflict`) |
| `postgres/src/test/isolation/specs/tuplelock-conflict.spec` | pass | TestPort_IsolationTuplelockConflict PASS — verifies the documented tuple-lock conflict table across all four strengths (KEY SHARE / SHARE / NO KEY UPDATE / UPDATE) in both the single-locker permutations and the savepoint (multixact) permutations where s1 takes a second lock under a SAVEPOINT on the tuple it already locked. s2's blocking lock waits or proceeds per multixact.StatusesConflict evaluated against the strongest lock mode held on the tuple. Rides the M0118-0003 row-lock conflict detection (tupleLockConflicts + activeLockHolders + the blocking WaitForXID path in stampLockInner) with no new engine code. (case `tuplelock-conflict`) |
| `postgres/src/test/isolation/specs/tuplelock-partition.spec` | pass | TestPort_IsolationTuplelockPartition PASS — INSERT ON CONFLICT (key) DO UPDATE on a LIST-partitioned table. A no-key UPDATE arm (SET col1/col2) does not conflict with a concurrent FOR KEY SHARE so s2 proceeds; a key UPDATE arm (SET key=1) blocks s2 until s1 commits even though the key value is unchanged. Two fixes in the upsert applyUpdate path: (1) onConflictUpdateTouchesKeyColumn stamps HEAP_KEYS_UPDATED on the conflicting tuple whenever the DO UPDATE SET list assigns a unique/primary-key column — mirroring PG ExecUpdateLockMode which keys off the updated-column set (ExecGetAllUpdatedCols) not value comparison so a FOR KEY SHARE waits; (2) stampOldCtid now links the old tuple to the new version so a waiting FOR KEY SHARE follows the ctid chain to the live successor instead of short-reading a self-pointing ctid. M0118-0003. (case `tuplelock-partition`) |
| `postgres/src/test/isolation/specs/tuplelock-update.spec` | pass | TestPort_IsolationTuplelockUpdate PASS — s1 builds an update chain via UPDATE pktab SET data = DEFAULT then takes FOR KEY SHARE on the live row while s2/s3/s4 each hold a pending no-key UPDATE gated behind one of three advisory locks s1 owns. Releasing each advisory lock in turn lets that updater follow the ctid chain and propagate the KEY SHARE lock forward onto the new version without conflicting (no-key update does not conflict with KEY SHARE). Two fixes landed: (1) planner defaultMarkerReplacement rewrites an explicit DEFAULT keyword on a SERIAL or identity column to nextval('<table>_<col>_seq') instead of NULL because goopg leaves the catalog DefaultExpr nil for serial columns so the bare DEFAULT previously collapsed to NULL and tripped the SERIAL NOT NULL constraint; (2) the isolation runner now honours BlockerStepComplete annotations such as s1_advunlock1(s2_update) by rendering the unlock step in waiting/completed format with its report gated behind the referenced pending blocker step. M0118-0003. (case `tuplelock-update`) |
| `postgres/src/test/isolation/specs/tuplelock-upgrade-no-deadlock.spec` | pass | TestPort_IsolationTuplelockUpgradeNoDeadlock RESOLVED (design 0021-0012 loop #91): row-lock waiter FIFO fairness fixed. acquireTupleLock/tryAcquireTupleLock (internal/executor/context.go) now route to the always-on tableLockMgr under the per-statement BackendID when c.LockMgr is nil in production instead of being total no-ops; the tag is acquired lazily only at the about-to-sleep sites (stampLockInner's lock-only/multixact/xid-wait branches plus the DML write-path's waitForConflictingRowLock) mirroring heap_acquire_tuplock's placement before XactLockTableWait/MultiXactIdWait; tupleLockHwMode ports PG's four-mode tupleLockExtraInfo hwlock matrix (AccessShareLock/RowShareLock/ExclusiveLock/AccessExclusiveLock) so FOR KEY SHARE never queues behind an unrelated no-key UPDATE holder; selfIsLockMember/selfRowLockMember skip the tag on an upgrade to avoid deadlocking behind our own held lock. Verified: 20x strict standalone loop zero flakes; full isolation suite (121 specs) clean except three confirmed pre-existing unrelated failures (insert-conflict-specconflict / detach-partition-concurrently-4 / partition-drop-index-locking — all fail identically without this change); race-gate and tpch-spotcheck (Q12=2/Q13=33) and the pgbench CI-parity smoke all green. Re-promoted to runIsoSpecStrict. M0118-0004 groundwork (docs/design/0118-0012) plus this loop's 0021-0012. (case `tuplelock-upgrade-no-deadlock`) |
| `postgres/src/test/isolation/specs/two-ids.spec` | pass | Ported as TestPort_IsolationTwoIds (internal/testport/isolation_port_test.go). M0118-0001: the read-only-anomaly pivot now raises 40001 where PG does because committed SERIALIZABLE xacts are RETAINED past commit (mvcc ssiState.finished) with their rw-edges intact and OnConflict_CheckForSerializationFailure runs at edge creation (Case 2 dooms the in-flight pivot); a new XidIsConcurrent gate on the read-path conflict-out suppresses the phantom edge when the reader already sees the writer's commit. All 90 generated permutations match expected byte-for-byte vs PG 18.3. (case `two-ids`) |
| `postgres/src/test/isolation/specs/update-conflict-out.spec` | pass | Ported as TestPort_IsolationUpdateConflictOut (internal/testport/isolation_port_test.go). M0118-0001: SSI conflict-out handling for heapam interacting with a concurrently UPDATEd-then-aborted tuple (bug db7b729d) — both permutations abort session bar with 40001 at bar_commit where PG does. Passes with the existing retained-committed-xact conflict graph plus the index-UPDATE write-path conflict-in edge with NO new engine change; all generated permutations match PG 18.3 byte-for-byte. (case `update-conflict-out`) |
| `postgres/src/test/isolation/specs/update-locked-tuple.spec` | pass | TestPort_IsolationUpdateLockedTuple PASS — s1 (REPEATABLE READ) repeatedly UPDATEs orders whose user_id FK references users taking a KEY SHARE lock on the parent row while s2 UPDATEs a non-key column of that same users row; KEY SHARE does not conflict with the no-key parent update and the FK key is unchanged so no blocking nor 40001 serialization failure occurs in any permutation (M0118-0003). (case `update-locked-tuple`) |
| `postgres/src/test/isolation/specs/vacuum-concurrent-drop.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationVacuumConcurrentDrop -- VACUUM/ANALYZE without SKIP_LOCKED take a blocking per-relation ShareUpdateExclusiveLock (acquireRelLockMaybeTransient) so they wait behind a concurrent LOCK ... IN SHARE MODE; a target dropped by the committing session is re-detected after the lock via relationStillExists (LookupTableByOID) and skipped -- with a relation-no-longer-exists WARNING for an explicitly named relation or silently for an expanded partition child -- byte-identical to PG 18.3 across all six permutations (design 0118-0035). (case `vacuum-concurrent-drop`) |
| `postgres/src/test/isolation/specs/vacuum-conflict.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationVacuumConflict (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `vacuum-conflict`) |
| `postgres/src/test/isolation/specs/vacuum-no-cleanup-lock.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationVacuumNoCleanupLock (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `vacuum-no-cleanup-lock`) |
| `postgres/src/test/isolation/specs/vacuum-skip-locked.spec` | pass | Promoted to pass-required via runIsoSpecStrict TestPort_IsolationVacuumSkipLocked (internal/testport/isolation_port_test.go); matches PG 18.3 byte-for-byte across all generated permutations. M0118. (case `vacuum-skip-locked`) |
