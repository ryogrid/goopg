# Upstream isolation Spec Coverage Classification

Generated by `go run ./cmd/gen-isolation-coverage --repo-root . --out docs/test-port/upstream-isolation-coverage.md`.
Generated at: 2026-05-15T11:45:37+09:00

Total discovered specs: **121**

Status policy for this phase:
- `defer`: scheduler/parity not pass-required yet.
- `port`: used after a spec is wired and validated end-to-end.

| spec | status | rationale |
| ---- | ------ | --------- |
| `postgres/src/test/isolation/specs/aborted-keyrevoke.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `aborted-keyrevoke`) |
| `postgres/src/test/isolation/specs/alter-table-1.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `alter-table-1`) |
| `postgres/src/test/isolation/specs/alter-table-2.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `alter-table-2`) |
| `postgres/src/test/isolation/specs/alter-table-3.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `alter-table-3`) |
| `postgres/src/test/isolation/specs/alter-table-4.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `alter-table-4`) |
| `postgres/src/test/isolation/specs/async-notify.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `async-notify`) |
| `postgres/src/test/isolation/specs/classroom-scheduling.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `classroom-scheduling`) |
| `postgres/src/test/isolation/specs/cluster-conflict-partition.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `cluster-conflict-partition`) |
| `postgres/src/test/isolation/specs/cluster-conflict.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `cluster-conflict`) |
| `postgres/src/test/isolation/specs/create-trigger.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `create-trigger`) |
| `postgres/src/test/isolation/specs/deadlock-hard.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `deadlock-hard`) |
| `postgres/src/test/isolation/specs/deadlock-parallel.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `deadlock-parallel`) |
| `postgres/src/test/isolation/specs/deadlock-simple.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `deadlock-simple`) |
| `postgres/src/test/isolation/specs/deadlock-soft-2.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `deadlock-soft-2`) |
| `postgres/src/test/isolation/specs/deadlock-soft.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `deadlock-soft`) |
| `postgres/src/test/isolation/specs/delete-abort-savept-2.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `delete-abort-savept-2`) |
| `postgres/src/test/isolation/specs/delete-abort-savept.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `delete-abort-savept`) |
| `postgres/src/test/isolation/specs/detach-partition-concurrently-1.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `detach-partition-concurrently-1`) |
| `postgres/src/test/isolation/specs/detach-partition-concurrently-2.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `detach-partition-concurrently-2`) |
| `postgres/src/test/isolation/specs/detach-partition-concurrently-3.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `detach-partition-concurrently-3`) |
| `postgres/src/test/isolation/specs/detach-partition-concurrently-4.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `detach-partition-concurrently-4`) |
| `postgres/src/test/isolation/specs/drop-index-concurrently-1.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `drop-index-concurrently-1`) |
| `postgres/src/test/isolation/specs/eval-plan-qual-trigger.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `eval-plan-qual-trigger`) |
| `postgres/src/test/isolation/specs/eval-plan-qual.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `eval-plan-qual`) |
| `postgres/src/test/isolation/specs/fk-contention.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `fk-contention`) |
| `postgres/src/test/isolation/specs/fk-deadlock.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `fk-deadlock`) |
| `postgres/src/test/isolation/specs/fk-deadlock2.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `fk-deadlock2`) |
| `postgres/src/test/isolation/specs/fk-partitioned-1.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `fk-partitioned-1`) |
| `postgres/src/test/isolation/specs/fk-partitioned-2.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `fk-partitioned-2`) |
| `postgres/src/test/isolation/specs/fk-snapshot.spec` | port | TestPort_IsolationFkSnapshot PASS — all 7 permutations (M0100-0005w: parent DELETE waits for in-flight child INSERT; RR raises 40001). (case `fk-snapshot`) |
| `postgres/src/test/isolation/specs/freeze-the-dead.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `freeze-the-dead`) |
| `postgres/src/test/isolation/specs/horizons.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `horizons`) |
| `postgres/src/test/isolation/specs/index-only-bitmapscan.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `index-only-bitmapscan`) |
| `postgres/src/test/isolation/specs/index-only-scan.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `index-only-scan`) |
| `postgres/src/test/isolation/specs/inherit-temp.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `inherit-temp`) |
| `postgres/src/test/isolation/specs/inplace-inval.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `inplace-inval`) |
| `postgres/src/test/isolation/specs/insert-conflict-do-nothing-2.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `insert-conflict-do-nothing-2`) |
| `postgres/src/test/isolation/specs/insert-conflict-do-nothing.spec` | port | TestPort_IsolationInsertConflictDoNothing PASS (M0100-0002: eager XID materialisation + IsolationRunner global teardown). (case `insert-conflict-do-nothing`) |
| `postgres/src/test/isolation/specs/insert-conflict-do-update-2.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `insert-conflict-do-update-2`) |
| `postgres/src/test/isolation/specs/insert-conflict-do-update-3.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `insert-conflict-do-update-3`) |
| `postgres/src/test/isolation/specs/insert-conflict-do-update-4.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `insert-conflict-do-update-4`) |
| `postgres/src/test/isolation/specs/insert-conflict-do-update.spec` | port | TestPort_IsolationInsertConflictDoUpdate PASS (M0096-0005 + M0100-0002 cross-reference). (case `insert-conflict-do-update`) |
| `postgres/src/test/isolation/specs/insert-conflict-specconflict.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `insert-conflict-specconflict`) |
| `postgres/src/test/isolation/specs/intra-grant-inplace-db.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `intra-grant-inplace-db`) |
| `postgres/src/test/isolation/specs/intra-grant-inplace.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `intra-grant-inplace`) |
| `postgres/src/test/isolation/specs/lock-committed-keyupdate.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `lock-committed-keyupdate`) |
| `postgres/src/test/isolation/specs/lock-committed-update.spec` | port | TestPort_IsolationLockCommittedUpdate PASS — all 24 permutations (M0100-0005e seqScanOp page-RLock per-tuple; M0100-0005f stampLock preserves real updater xmax). (case `lock-committed-update`) |
| `postgres/src/test/isolation/specs/lock-nowait.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `lock-nowait`) |
| `postgres/src/test/isolation/specs/lock-update-delete.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `lock-update-delete`) |
| `postgres/src/test/isolation/specs/lock-update-traversal.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `lock-update-traversal`) |
| `postgres/src/test/isolation/specs/matview-write-skew.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `matview-write-skew`) |
| `postgres/src/test/isolation/specs/merge-delete.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `merge-delete`) |
| `postgres/src/test/isolation/specs/merge-insert-update.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `merge-insert-update`) |
| `postgres/src/test/isolation/specs/merge-join.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `merge-join`) |
| `postgres/src/test/isolation/specs/merge-match-recheck.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `merge-match-recheck`) |
| `postgres/src/test/isolation/specs/merge-update.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `merge-update`) |
| `postgres/src/test/isolation/specs/multiple-cic.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `multiple-cic`) |
| `postgres/src/test/isolation/specs/multiple-row-versions.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `multiple-row-versions`) |
| `postgres/src/test/isolation/specs/multixact-no-deadlock.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `multixact-no-deadlock`) |
| `postgres/src/test/isolation/specs/multixact-no-forget.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `multixact-no-forget`) |
| `postgres/src/test/isolation/specs/nowait-2.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `nowait-2`) |
| `postgres/src/test/isolation/specs/nowait-3.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `nowait-3`) |
| `postgres/src/test/isolation/specs/nowait-4.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `nowait-4`) |
| `postgres/src/test/isolation/specs/nowait-5.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `nowait-5`) |
| `postgres/src/test/isolation/specs/nowait.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `nowait`) |
| `postgres/src/test/isolation/specs/partial-index.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `partial-index`) |
| `postgres/src/test/isolation/specs/partition-concurrent-attach.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `partition-concurrent-attach`) |
| `postgres/src/test/isolation/specs/partition-drop-index-locking.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `partition-drop-index-locking`) |
| `postgres/src/test/isolation/specs/partition-key-update-1.spec` | port | TestPort_IsolationPartitionKeyUpdate1 PASS (M0100-0005q: FK INSERT waits for in-flight xmax + moved-partition EPQ error). (case `partition-key-update-1`) |
| `postgres/src/test/isolation/specs/partition-key-update-2.spec` | port | TestPort_IsolationPartitionKeyUpdate2 PASS (M0100-0005t: partition-aware upsert + per-leaf arbiter inheritance). (case `partition-key-update-2`) |
| `postgres/src/test/isolation/specs/partition-key-update-3.spec` | port | TestPort_IsolationPartitionKeyUpdate3 PASS — all 8 permutations (M0100-0005x: upsert dirty-snapshot probe + RR/SER 40001 raise). (case `partition-key-update-3`) |
| `postgres/src/test/isolation/specs/partition-key-update-4.spec` | port | TestPort_IsolationPartitionKeyUpdate4 PASS (M0100-0005aa: cross-partition UPDATE fires BEFORE DELETE on source partition). (case `partition-key-update-4`) |
| `postgres/src/test/isolation/specs/plpgsql-toast.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `plpgsql-toast`) |
| `postgres/src/test/isolation/specs/predicate-gin.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `predicate-gin`) |
| `postgres/src/test/isolation/specs/predicate-gist.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `predicate-gist`) |
| `postgres/src/test/isolation/specs/predicate-hash.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `predicate-hash`) |
| `postgres/src/test/isolation/specs/predicate-lock-hot-tuple.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `predicate-lock-hot-tuple`) |
| `postgres/src/test/isolation/specs/prepared-transactions-cic.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `prepared-transactions-cic`) |
| `postgres/src/test/isolation/specs/prepared-transactions.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `prepared-transactions`) |
| `postgres/src/test/isolation/specs/project-manager.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `project-manager`) |
| `postgres/src/test/isolation/specs/propagate-lock-delete.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `propagate-lock-delete`) |
| `postgres/src/test/isolation/specs/read-only-anomaly-2.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `read-only-anomaly-2`) |
| `postgres/src/test/isolation/specs/read-only-anomaly-3.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `read-only-anomaly-3`) |
| `postgres/src/test/isolation/specs/read-only-anomaly.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `read-only-anomaly`) |
| `postgres/src/test/isolation/specs/read-write-unique-2.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `read-write-unique-2`) |
| `postgres/src/test/isolation/specs/read-write-unique-3.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `read-write-unique-3`) |
| `postgres/src/test/isolation/specs/read-write-unique-4.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `read-write-unique-4`) |
| `postgres/src/test/isolation/specs/read-write-unique.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `read-write-unique`) |
| `postgres/src/test/isolation/specs/receipt-report.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `receipt-report`) |
| `postgres/src/test/isolation/specs/referential-integrity.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `referential-integrity`) |
| `postgres/src/test/isolation/specs/reindex-concurrently-toast.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `reindex-concurrently-toast`) |
| `postgres/src/test/isolation/specs/reindex-concurrently.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `reindex-concurrently`) |
| `postgres/src/test/isolation/specs/reindex-schema.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `reindex-schema`) |
| `postgres/src/test/isolation/specs/ri-trigger.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `ri-trigger`) |
| `postgres/src/test/isolation/specs/sequence-ddl.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `sequence-ddl`) |
| `postgres/src/test/isolation/specs/serializable-parallel-2.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `serializable-parallel-2`) |
| `postgres/src/test/isolation/specs/serializable-parallel-3.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `serializable-parallel-3`) |
| `postgres/src/test/isolation/specs/serializable-parallel.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `serializable-parallel`) |
| `postgres/src/test/isolation/specs/simple-write-skew.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `simple-write-skew`) |
| `postgres/src/test/isolation/specs/skip-locked-2.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `skip-locked-2`) |
| `postgres/src/test/isolation/specs/skip-locked-3.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `skip-locked-3`) |
| `postgres/src/test/isolation/specs/skip-locked-4.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `skip-locked-4`) |
| `postgres/src/test/isolation/specs/skip-locked.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `skip-locked`) |
| `postgres/src/test/isolation/specs/stats.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `stats`) |
| `postgres/src/test/isolation/specs/subxid-overflow.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `subxid-overflow`) |
| `postgres/src/test/isolation/specs/temp-schema-cleanup.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `temp-schema-cleanup`) |
| `postgres/src/test/isolation/specs/temporal-range-integrity.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `temporal-range-integrity`) |
| `postgres/src/test/isolation/specs/timeouts.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `timeouts`) |
| `postgres/src/test/isolation/specs/total-cash.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `total-cash`) |
| `postgres/src/test/isolation/specs/truncate-conflict.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `truncate-conflict`) |
| `postgres/src/test/isolation/specs/tuplelock-conflict.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `tuplelock-conflict`) |
| `postgres/src/test/isolation/specs/tuplelock-partition.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `tuplelock-partition`) |
| `postgres/src/test/isolation/specs/tuplelock-update.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `tuplelock-update`) |
| `postgres/src/test/isolation/specs/tuplelock-upgrade-no-deadlock.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `tuplelock-upgrade-no-deadlock`) |
| `postgres/src/test/isolation/specs/two-ids.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `two-ids`) |
| `postgres/src/test/isolation/specs/update-conflict-out.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `update-conflict-out`) |
| `postgres/src/test/isolation/specs/update-locked-tuple.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `update-locked-tuple`) |
| `postgres/src/test/isolation/specs/vacuum-concurrent-drop.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `vacuum-concurrent-drop`) |
| `postgres/src/test/isolation/specs/vacuum-conflict.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `vacuum-conflict`) |
| `postgres/src/test/isolation/specs/vacuum-no-cleanup-lock.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `vacuum-no-cleanup-lock`) |
| `postgres/src/test/isolation/specs/vacuum-skip-locked.spec` | defer | Deterministic scheduler foundation available; execution parity deferred to staged migration. (case `vacuum-skip-locked`) |
