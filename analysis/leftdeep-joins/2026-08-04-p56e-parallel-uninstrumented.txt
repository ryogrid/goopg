=== Q2
  F d2  Hash Join (INNER)                        est=1          actual=?        (no ANALYZE)
    d3  Hash Join (INNER)                        est=22         actual=?        (no ANALYZE)
    d4  Hash Join (INNER)                        est=4443       actual=?        (no ANALYZE)
    d5  Hash Join (INNER)                        est=100000     actual=?        (no ANALYZE)
    d6  Hash Join (INNER)                        est=800000     actual=?        (no ANALYZE)
    d4  Hash Join (INNER)                        est=500        actual=?        (no ANALYZE)
    d5  Hash Join (INNER)                        est=100000     actual=?        (no ANALYZE)
    d6  Hash Join (INNER)                        est=800000     actual=?        (no ANALYZE)

=== Q3
  F d3  Hash Join (INNER, build=left)            est=94423722   actual=?        (no ANALYZE)
    d4  Hash Join (INNER)                        est=856611     actual=?        (no ANALYZE)

=== Q5
  F d3  Hash Join (INNER)                        est=2024192    actual=?        (no ANALYZE)
    d4  Hash Join (INNER)                        est=2024192    actual=?        (no ANALYZE)
    d5  Hash Join (INNER)                        est=404838407  actual=?        (no ANALYZE)
    d6  Hash Join (INNER)                        est=2004467    actual=?        (no ANALYZE)
    d7  Hash Join (INNER)                        est=150000     actual=?        (no ANALYZE)

=== Q7
  F d3  Hash Join (INNER)                        est=945000     actual=?        (no ANALYZE)
    d4  Hash Join (INNER, build=left)            est=945000     actual=?        (no ANALYZE)
    d5  Hash Join (INNER, build=left)            est=126        actual=?        (no ANALYZE)
    d4  Hash Join (INNER)                        est=334287     actual=?        (no ANALYZE)
    d5  Hash Join (INNER)                        est=2674297    actual=?        (no ANALYZE)

=== Q8
  F d3  Hash Join (INNER)                        est=1332       actual=?        (no ANALYZE)
    d4  Hash Join (INNER)                        est=266477     actual=?        (no ANALYZE)
    d5  Hash Join (INNER)                        est=5997241    actual=?        (no ANALYZE)
    d6  Hash Join (INNER, build=left)            est=5997241    actual=?        (no ANALYZE)
    d7  Hash Join (INNER, build=left)            est=5997241    actual=?        (no ANALYZE)
    d6  Hash Join (INNER)                        est=3617585    actual=?        (no ANALYZE)
    d7  Hash Join (INNER)                        est=150000     actual=?        (no ANALYZE)

=== Q9
  F d3  Hash Join (INNER)                        est=39597200   actual=?        (no ANALYZE)
    d4  Hash Join (INNER)                        est=39597200   actual=?        (no ANALYZE)
    d5  Hash Join (INNER)                        est=39597200   actual=?        (no ANALYZE)
    d6  Hash Join (INNER)                        est=791944     actual=?        (no ANALYZE)
    d7  Hash Join (INNER)                        est=6335559    actual=?        (no ANALYZE)

=== Q10
  F d3  Hash Join (INNER)                        est=104274420  actual=?        (no ANALYZE)
    d4  Hash Join (INNER)                        est=2115950    actual=?        (no ANALYZE)
    d5  Hash Join (INNER)                        est=150000     actual=?        (no ANALYZE)

=== Q11
    d6  Hash Join (INNER)                        est=4000       actual=?        (no ANALYZE)
    d7  Hash Join (INNER)                        est=800000     actual=?        (no ANALYZE)
  F d3  Hash Join (INNER)                        est=4000       actual=?        (no ANALYZE)
    d4  Hash Join (INNER)                        est=800000     actual=?        (no ANALYZE)

=== Q16
  F d3  Hash Join (ANTI NULL-AWARE)              est=3832       actual=?        (no ANALYZE)
    d4  Hash Join (INNER)                        est=288888     actual=?        (no ANALYZE)

=== Q18
  F d3  Hash Join (SEMI)                         est=1750763574 actual=?        (no ANALYZE)
    d4  Hash Join (INNER, build=left)            est=1750763574 actual=?        (no ANALYZE)
    d5  Hash Join (INNER)                        est=8668515    actual=?        (no ANALYZE)

=== Q20
  F d1  Hash Join (SEMI)                         est=68420      actual=76         ratio=900.3x over
    d2  Hash Join (INNER)                        est=400        actual=374        ratio=1.1x over
    d2  Hash Join (SEMI)                         est=6842096    actual=2568       ratio=2664.4x over
    d3  Hash Join (INNER)                        est=20526289   actual=236624     ratio=86.7x over

=== Q22
  F d3  Hash Join (ANTI)                         est=2500000    actual=?        (no ANALYZE)

=== SUMMARY (tripwire 1000x; final joinrel 1000x, per-query overrides [Q9<=100x])
  VIOLATION Q20: joinrel "Hash Join (SEMI)" est=6842096 actual=2568 ratio=2664.4x (overestimated, threshold 1000x)
