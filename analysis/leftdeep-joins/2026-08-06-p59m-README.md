# M0127-P5.9-m — the collapse-ON acceptance pass (2026-08-06)

Write-up: `docs/design/leftdeep-joins/09-verification-and-acceptance.md` §3.18.
Verdict: **every clause green, and the `GOOPG_PGSHAPED_COLLAPSE` flip is a
NO-GO** — the flag cannot move a plan on the measured corpus, because
`tryPGShapedJoinSearch` declines an explicit-JOIN chain before `ctx.joinlist` is
consulted (`extractScans`, `internal/planner/bushy.go:261`, descends
`JoinTypeCross` only).

One binary across every arm: `5ed79a1b78bab6a8`, HEAD `d867ae03`,
`GOOPG_PGSHAPED_DP=1` explicit on both sides, `COLLAPSE` the only variable.

| file | what it is |
|---|---|
| `2026-08-06-p59m-coll-off.txt` | TPC-H SF1 arm, `COLLAPSE=0` — 370.29 s |
| `2026-08-06-p59m-coll-on.txt` | TPC-H SF1 arm, `COLLAPSE=1` — 364.26 s, 24/24 MATCH vs the OFF arm on values |
| `p59m-collapse-probe.sh` | the enumeration-trace probe: starts the SF0.5 cluster once per regime with `GOOPG_PGSHAPED_DP_TRACE=1`, EXPLAINs the named queries, harvests `DPTRACE` from the server log |
| `2026-08-06-p59m-probe-collapse{0,1}.{plans.txt,server.log}` | Q72/Q75 under both regimes. Q72's eleven-way explicit-JOIN level: **no trace**. Q75: one traced problem, `nrels=2 rels=curr_yr,prev_yr` (its top-level CTE join) |
| `2026-08-06-p59m-probe-syn-collapse{0,1}.{plans.txt,server.log}` | the synthetic control: the same three relations written `JOIN … ON` (no trace, either regime) and written comma-FROM (full four-pair enumeration) |

Not in this directory (they belong to the gate's own results dir):

- `bench/tpcds/runtime_goopg/tpcds-results-sf05/sweep-20260806-035500.txt` —
  the DS05 sweep with `COLLAPSE=1`: `PASS=95 (57 ck-verified) MISMATCH=0
  CKMISMATCH=0 ERROR=0 TIMEOUT=0 SKIP=4`, `STATUS-DELTA verdict-changes=none
  runtime-moves=0`.
- `…/plans-20260806-035500.txt` (COLLAPSE=1) vs `…/plans-20260806-042316.txt`
  (COLLAPSE=0) — the **one-variable** plan A/B: `queries=99 same=99 changed=0`.
  The sweep report's own plan channel instead says `changed (36)`; its baseline
  predates `d867ae03`, so that count is §3.17's `Join Filter:` line.

Reproduce:

```bash
PGSHAPED=1 COLLAPSE=0 scripts/tpch-acceptance-arm.sh <label> <out>
PGSHAPED=1 COLLAPSE=1 NO_BUILD=1 scripts/tpch-acceptance-arm.sh <label> <out>
tmp/tpch-acceptance-runner -diff <off> <on>

GOOPG_PGSHAPED_COLLAPSE=1 scripts/tpcds-sf05-regression.sh sweep
GOOPG_PGSHAPED_COLLAPSE=0 SF05_PLANS_BASELINE=<the COLLAPSE=1 plans file> \
    scripts/tpcds-sf05-regression.sh plans          # the one-variable A/B

analysis/leftdeep-joins/p59m-collapse-probe.sh       # Q72/Q75 traces
TAG=-syn QDIR=<dir> QUERIES="901 902" analysis/leftdeep-joins/p59m-collapse-probe.sh
```

The corpus-eligibility numbers quoted in §3.18 (0 of 22 TPC-H, 2 of 99 TPC-DS)
come from `go test -run TestCollapse -v ./internal/planner/`, not from this
directory — they are computed by the production `deconstructJointree`, so they
are a test and not an artefact.
