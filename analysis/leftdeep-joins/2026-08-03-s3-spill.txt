# M0127-P3.5 — S3 exit evidence run (TPC-H SF1)
#
# Gate (09 §2, "S3 exit"):
#   Q21 completes at SF1 under the standard cgroup cap with `work_mem` at
#   default, AND a forced-spill run (work_mem lowered until nbatch >= 4 on Q3)
#   returns byte-identical results to the no-spill run.
#
# VERDICT: BOTH CLAUSES MET.
#   (a) Q21 rc=0 in 132 s, 405 rows, server peak VmHWM 16.7 GB inside a
#       20G/24G cgroup cap with GOMEMLIMIT=12GiB / GOGC=off. No OOM, no
#       timeout, no cap eviction. (The historical Q21 failure this clause
#       exists to retire was a host-level OOM at SF1.)
#   (b) Q3 at work_mem=512kB (nbatch 512 on the lineitem-side build, grown
#       from an original 256) is BYTE-IDENTICAL to Q3 at work_mem=6GB
#       (nbatch 1 on every join): 11,521 rows,
#       sha256 066af3df3c9ea2f766a7da43a41b018d8cf5e93552995f139bb9082adcd16dc8.
#       128x of forced partitioning changes nothing about the answer.
#
# S3 (the hybrid-hash spill stage, P3.1-P3.5) is therefore CLEARED. P4 next.
#
# ---------------------------------------------------------------------------
# Protocol
# ---------------------------------------------------------------------------
#   - ONE fresh capped server, R0's config verbatim (as tmp/m0127-s1-ab-arm.sh):
#     scope goopg-m0127-s3spill, GOOPG_MEM_HIGH=20G MEM_MAX=24G SWAP_MAX=0,
#     GOMEMLIMIT=12GiB, GOGC=off; TPC-H SF=1 on 127.0.0.1:65433, tpch@tpch
#   - binary tmp/goopg-m0127-p35-bin, built at the P3.5 working tree
#   - driver tmp/m0127-s3-spill.sh; queries tmp/c7-tpch-queries/
#   - each work_mem arm sets it as the FIRST statement of its own psql file, so
#     the setting is bound to the session that runs the query
#   - the nbatch numbers are read out of `EXPLAIN (ANALYZE, COSTS OFF, TIMING
#     OFF)` — i.e. out of the P3.5 line this slice added. The measurement and
#     the feature under test are the same thing, which is the point: before
#     P3.5 this gate could not be evaluated at all without a debugger.
#
# ---------------------------------------------------------------------------
# Finding worth carrying forward: goopg's 512 MB DEFAULT already spills Q3
# ---------------------------------------------------------------------------
# The [up] sweep below is the interesting half. At goopg's default work_mem of
# 512 MB — 128x PostgreSQL's 4 MB — Q3's lineitem-side build STILL reports
# `Batches: 8 (originally 4)  Memory Usage: 475137kB`, and it takes 6 GB before
# the build is single-batch (the 2 GB arm still lands at nbatch 2 with a 1,998
# MB peak). That is not a bug and not a regression: goopg holds a build row as
# `[]Datum` (48 bytes per column plus the slice header) where PG holds a packed
# MinimalTuple, so the SAME TPC-H build is roughly an order of magnitude larger
# here. It is the measured form of P3.4's Q13 regression (deferral ledger
# 2026-08-03 M0127-P3.4) and it is the number P5.7's nbatch-aware
# `hashJoinCost` has to price: at SF1 with default settings, a TPC-H hash join
# spills, so a cost model that assumes it does not will keep choosing hash.
#
# ===========================================================================
# Raw run
# ===========================================================================
# M0127-P3.5 — S3 exit evidence (TPC-H SF1)
# binary: tmp/goopg-m0127-p35-bin
# target: tpch@tpch  server started 2026-08-03T23:00:13+09:00
# cap: GOOPG_MEM_HIGH=20G MEM_MAX=24G SWAP_MAX=0 / GOMEMLIMIT=12GiB GOGC=off
# work_mem default: 512MB

=== (a) Q21 @ SF1, default work_mem, capped ===
Q21 rc=0 wall=132s rows(raw psql body lines)=406
 Supplier#000007950 |       2
(405 rows)


=== (b) forced-spill identity, Q3 ===
  [up]   work_mem=512MB -> max Batches=8
                   Buckets: 1048576 (originally 1048576)  Batches: 8 (originally 4)  Memory Usage: 475137kB
                     Buckets: 1024  Batches: 1  Memory Usage: 0kB
  [up]   work_mem=2GB -> max Batches=2
                   Buckets: 2097152 (originally 2097152)  Batches: 2 (originally 1)  Memory Usage: 1998849kB
                     Buckets: 1024  Batches: 1  Memory Usage: 0kB
  [up]   work_mem=6GB -> max Batches=1
                   Buckets: 2097152  Batches: 1  Memory Usage: 0kB
                     Buckets: 1024  Batches: 1  Memory Usage: 0kB
  [down] work_mem=16MB -> max Batches=256
                   Buckets: 32768 (originally 32768)  Batches: 256 (originally 128)  Memory Usage: 14849kB
                     Buckets: 1024  Batches: 1  Memory Usage: 0kB
  [down] work_mem=4MB -> max Batches=512
                   Buckets: 16384 (originally 16384)  Batches: 512 (originally 256)  Memory Usage: 7425kB
                     Buckets: 1024 (originally 1024)  Batches: 4 (originally 1)  Memory Usage: 4049kB
  [down] work_mem=1MB -> max Batches=512
                   Buckets: 16384 (originally 16384)  Batches: 512 (originally 256)  Memory Usage: 7425kB
                     Buckets: 1024 (originally 1024)  Batches: 32 (originally 1)  Memory Usage: 977kB
  [down] work_mem=512kB -> max Batches=512
                   Buckets: 16384 (originally 16384)  Batches: 512 (originally 256)  Memory Usage: 7425kB
                     Buckets: 1024 (originally 1024)  Batches: 64 (originally 1)  Memory Usage: 465kB
  no-spill arm work_mem = 6GB; spill arm work_mem = 512kB
  no-spill rc=0 lines=11521
  spill    rc=0 lines=11521
  IDENTITY: PASS — byte-identical
  sha256: 066af3df3c9ea2f766a7da43a41b018d8cf5e93552995f139bb9082adcd16dc8

# server-peak-VmHWM=16681360 kB
# finished 2026-08-03T23:08:48+09:00
