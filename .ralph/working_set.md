(idle — nothing in flight)

Loop #33 landed + committed: `ALTER OPERATOR name (left_type, right_type)
SET (RESTRICT=/JOIN=/COMMUTATOR=/NEGATOR=/MERGES/HASHES)` (M0119-0004, DU-002),
closing the loop #32 (slice 407) ledger follow-up. Previously ALTER OPERATOR
fell into parseAlter's generic ALTER VIEW/SCHEMA/COLLATION/.../OPERATOR/...
compat-stub loop and was swallowed as a total no-op (not even OWNER TO did
anything, which is fine/unchanged) — a user-written `ALTER OPERATOR
foo(int,int) SET (RESTRICT = ...)` silently did nothing.

Parser: new branch in parseAlter (internal/parser/ddl.go), checked BEFORE
the generic stub loop. ALTER OPERATOR CLASS|FAMILY and any non-`SET(...)`
tail (OWNER TO / SET SCHEMA / other) still fall back to the old
consume-and-succeed no-op — nothing that used to parse now errors. Only
`SET ( option = value, ... )` produces new `AlterOperatorSetStmt`
(ast.go), reusing parseOperatorRefName (COMMUTATOR/NEGATOR) + existing
bare/=value MERGES/HASHES scan. LEFTARG/RIGHTARG/FUNCTION/PROCEDURE inside
SET -> syntax error (immutable after CREATE).
Planner: AlterOperatorSetStmt added to planner.go's DDL-passthrough case
list (else 0A000 "unsupported statement type").
Executor: new execAlterOperatorSet (operators_ddl.go) mirrors PG's
AlterOperator (operatorcmds.c) exactly — RESTRICT/JOIN change freely incl.
clearing via NONE; COMMUTATOR/NEGATOR/MERGES/HASHES settable only once
(same-value restatement = no-op, different value = 42P13, self-negation
rejected); not-found = 42883. Extracted CREATE OPERATOR's inline
resolveFn/resolveOther closures into shared (*ddlOp).resolveOperatorSupportFunc
/ resolveOperatorRef methods (zero behavior change — full CREATE OPERATOR
suite passed unchanged) so CREATE and ALTER share one resolution path.
New builtins eqsel/eqjoinsel/neqjoinsel (OIDs 101/105/106) curated in
catalog.go's builtinProcsByName so RESTRICT=/JOIN= test fixtures resolve
to a real OID.

Gates run this loop: go build ./... clean; go vet parser/catalog/executor/
planner clean; internal/parser+internal/catalog+internal/executor+
internal/planner suites PASS; TestPort_PgDumpConnectionSetup PASS
(confirms zero pg_dump regression, since ALTER OPERATOR OWNER TO output is
generated by real pg_dump reading goopg's data, unaffected); gofmt -l
flags only the same pre-existing go1.25/1.26-drift files as loop #32
(verified via git stash); TPC-H spotcheck Q12=2/Q13=33 PASS; pgbench smoke
= pre-commit hook; make ralph-state-guard auto-repaired the same
recurring stale-progress-marker pattern as every prior loop. Design doc
0119-0004-create-operator-roundtrip.md ("Loop #33" section) + README
index updated in this commit. New deferral ledger row marked `resolved`
(closes the loop #32 row); notes remaining opens: no pg_dump fixture
needs this statement (pg_dump never emits it — nothing to promote in the
port-status CSVs); operator OWNER TO/SET SCHEMA still a no-op (not
enforced, consistent with every other operator DDL arm having no
per-session role identity); regoper/regoperator OID->name resolution
still open (unchanged, unrelated, tracked since slice 405/407).

Next candidates (backlog, mostly unchanged from loop #32):
(1) regoper/regoperator OID->name resolution — still no observable gap
(no column typed regoper yet); (2) regprocedure argument-type-list
disambiguation for overloaded functions; (3) M0119-0005 (pg_waldump
server tier), M0119-0006 (pg_amcheck server tier), M0119-0007
(pg_basebackup recvlogical, blocked on logical decoding); (4) M0119-0002
(CLOG store swap Part B) still open; (5) datacl (pg_database ACL) stays
permanently deferred (untestable under the connsetup harness); (6) CREATE
OPERATOR CLASS custom-operator pg_dump ordering fixture (op_class_custom
in 002_pg_dump.pl) — CREATE OPERATOR CLASS already exists separately; not
investigated whether it already passes; (7) if a future fixture needs
ALTER OPERATOR OWNER TO to actually mutate UserOperator.Owner, add that
as its own DDL arm (mirrors execAlterAggregateOwner's shape) — nothing
exercises it today so left alone.
