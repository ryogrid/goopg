(idle — nothing in flight)

Last loop (#47) closed M-NIGHTLY "pg_get_indexdef prints a Go struct for a
COLLATE expression" (`e8277c163`, also M0134-0167d). Remaining open
M-NIGHTLY item: "work_mem boots at 512MB" — an owner call (plan costs move),
do not self-select. Next per banner: pre-existing milestones (M0119 first).
