Task: M0142-0005 — recon: re-verify the B6/B8 premise before implementing an
executor Memoize slice. **DONE and committed this loop** (`460cae829`).
Measurement-only (one temporary debug print added and fully reverted before
commit), no production code changed.

Files: `.ralph/fix_plan.md` (M0142-0005 entry rewritten in place with the
corrected mechanism — still `[ ]`, re-scoped not closed).
`.ralph/deferral_ledger.md` (new `m0142-0005` row).
`docs/design/0100-0149/m0142-0005-recon-partial-memoize-refused-by-gather-eligibility.md`
(new). `docs/design/README.md` (indexed).

Key symbols: `internal/optimizer/gatherpaths.go` `partialPathDrivingKind`'s
`PathNestLoop` case (`:456-490`, the actual refusal site — NOT touched, just
read); `joinpathsnli.go` `addNLIPaths`/`addPartialNestLoopPaths`;
`joinpathsmemoize.go` `getMemoizePath`; `internal/executor/operators_memoize.go`
`memoizeOp`. Used the pre-existing `GOOPG_PGSHAPED_DP_TRACE=1` channel
(DPTRACE/DPPATH lines, `joinsearchtrace.go`/`pathtrace.go`) — no new
instrumentation needed for the main finding; one temporary print was added to
`pathparamindex.go` early in the investigation to rule out an empty
`CheapestParameterized` list, then reverted once DPPATH showed the real
mechanism.

Findings this loop: the milestone's own working-set baton (last loop)
flagged M0142-0005 as the strongest non-recon M0142 candidate. Before
implementing, re-verified its premise per `planner_verify_both_candidates_
generated` and it was **stale**: Memoize has existed as BOTH a real executor
operator and a cost-based DP-search path since `894485ba7` (2026-07-21) — "no
Memoize on the NL probe path" is not true. On a private TPC-DS SF0.25 clone
(port 5533, `tmp/m0142-0005/`, cleaned up before commit), traced Q34 and found
the DP search **already generates and correctly costs** the PG-matching
`Nested Loop`+`Memoize`+`Index Scan using date_dim_pkey` shape as a partial
path (`total=16463.24`/worker, cheapest survivor in its joinrel's
`PartialPathlist`, matches PG's own real per-worker cost `16125.21` almost
exactly) — join-order/cardinality is already PG-faithful for this join. The
candidate never reaches CheapestTotal because **no Gather path is ever built
from it**: `partialPathDrivingKind`'s `PathNestLoop` case unconditionally
refuses a Memoize-wrapped inner in both its branches
(`in.Kind == PathMemoize` / `in.Kind != PathIndexScan`), a deliberate
restriction because `memoizeOp` has no per-worker-partitioned cache. Every
sibling relset in the same query got a `producer=gather` DPPATH line;
`{date_dim+household_demographics+store_sales}` (the one whose sole partial
survivor is Memoize-wrapped) got none — confirmed by full-log grep, not
inference. Rewrote M0142-0005's fix_plan entry with this diagnosis and left
B8 (`indexProbeCostMultiplier=2.0`) explicitly UNSETTLED (not re-measured;
its 2026-09-05 calibration post-dates Memoize's 2026-07-21 landing, so its
continued need is unverified either way).

Next step: M0142-0005 is now a real, correctly-scoped, but LARGE
executor+planner task (per-worker Memoize cache + a `partialPathDrivingKind`
relaxation) — needs its own scoping/floor-measurement recon before
implementation, same discipline as M0142-0012's `-verify`/`0012a` split. A
future loop should either (a) scope THAT implementation (how big is a
per-worker Memoize cache change — likely touches `memoizeOp`'s executor
state plus the parallel worker dispatch path) before attempting it, or (b)
pick a different M0142/M0141 item per the banner. Other still-open M0142
items as of this loop: **M0142-0003c** (Q9+Q45 2-witness recon),
**M0142-0008a/0008b** (SEMI/ANTI decorrelation scoping / filterless-INNER
DP-search widening, filed 2026-09-16), **M0142-0016c** (Q33/Q54/Q56
PG-analogous-shape check). None mandated over the others by the banner
(still item 4, "M0141's remaining slices / M0142").

Gates run: no production code touched (temporary instrument fully reverted,
confirmed via `git status --porcelain -- internal/` clean before commit), so
the practice-card row-count gate suite was not required. `go build ./...`
clean. `make ralph-state-guard`: same pre-existing stale progress-marker
inconsistency as the last several loops (status=running vs a stale
progress=completed marker from a prior loop's clean exit), self-repaired to
in_progress, then passed clean. Pre-commit pgbench smoke gate: PASS (all
three phases — TPC-B 43.4 tps, simple-update 43.5 tps, select-only 148.7 tps,
0 failed transactions).

In-flight: none. Nightly CI batch (`ci/logs/action-items.md`, mtime
2026-09-15 00:39, unchanged from last loop's check): all 14 `## AI-` items
under its single `20260914-235643` run are already filed under M-NIGHTLY in
`.ralph/fix_plan.md` — nightly triage was a no-op again this loop. The next
loop should check whether action-items.md has been regenerated by a newer
run before selecting work.
