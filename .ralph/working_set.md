# Working set — M0134-0006 CLOSED (stale `failed`); next is M0134-0007

**Task:** M0134-0006 (`select_having.sql`) — regress-sql case filed `failed`
with a "possible regression, verify" rationale. Re-verified at HEAD FIRST per
the milestone's Per-task discipline §3: it **PASSES**. Closed as a stale status
with **no goopg code change**.

**Evidence (two independent sources).**
- `scripts/pg-regress-runner.sh --verbose select_having` → `1/1 PASS
  (100.0% parity, 0 skipped)`, 84/84 lines byte-identical to
  `postgres/src/test/regress/expected/select_having.out`, no diff file emitted
  under `tmp/regress-diffs/`. Fresh auto-started capped server on :15435,
  `test_setup.sql` run (no `--no-setup`), no 6543x cluster touched.
- `ci/logs/20260819-011823/testport/results.csv` (the same night's nightly)
  records `TestPort_RegressSuite/select_having,PASS`.

**Files:**
- `docs/test-port/postgres-oracle-target-inventory.csv:179` — `failed,no` →
  `pass,yes`, rationale carries the full evidence.
- 4 derived docs regenerated by `make regen-testport` (two only churn their
  generated-at timestamp — expected, not drift).
- `.ralph/fix_plan.md` — M0134-0006 ticked with the evidence + the lesson.
- `docs/milestones/0134-…-digestion.md` — table row → `pass`; §3 gained an
  "Outcome so far" note.

**Findings worth carrying.**
- **The four "possible regression, verify" rows share one cause:** a baseline
  capture that disagreed with the inventory's own status. `select_having` cost
  ONE gate run instead of an implementation slice. Treat the remaining three
  (`mvcc`, `reindex_catalog`, `select_implicit`) as *probably* stale — but
  re-run each; never flip on inference.
- `TestPort_RegressSuite` auto-discovers cases; flipping a CSV row needs no
  test-list edit. `docs/test-port/regress-diff-baseline.csv` exists only in the
  stale `.claude/worktrees/cte-left-cross-join-fix` tree, not in this one — do
  not go looking for it.
- No deferral row: nothing PG implements in this case is missing from goopg.

**Next step:** select **M0134-0007 (`select_implicit.sql`)** — also a
"verify" case. Run `scripts/pg-regress-runner.sh --verbose select_implicit` at
HEAD BEFORE any design work; on PASS repeat this loop's bookkeeping exactly.

**Gates run:** `scripts/pg-regress-runner.sh --verbose select_having` PASS;
`make check-testport-inventory` PASS; `make ralph-state-guard` PASS; pre-commit
pgbench smoke via the hook.

**Delegation:** `tmp/ralph-handoffs/M0134-0006a/` (tester, 1 round, DONE).
**In-flight:** none.
