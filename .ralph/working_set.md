# Working set — M0134-0007 CLOSED (stale `failed`); next is M0134-0008

**Task:** M0134-0007 (`select_implicit.sql`) — regress-sql case filed `failed`
with a "possible regression, verify" rationale. Re-verified at HEAD FIRST per
the milestone's Per-task discipline §3: it **PASSES**. Closed as a stale status
with **no goopg code change** — the second such case in a row.

**Evidence (two independent sources, in agreement).**
- `scripts/pg-regress-runner.sh --verbose select_implicit` → `1/1 PASS
  (100.0% parity, 0 skipped)`, 316 lines byte-identical to
  `postgres/src/test/regress/expected/select_implicit.out`, no diff file
  emitted under `tmp/regress-diffs/`. Runner auto-started its own capped
  throwaway server; `test_setup.sql` run (no `--no-setup`); no 6543x cluster
  touched.
- `ci/logs/20260819-011823/testport/results.csv:795` —
  `TestPort_RegressSuite/select_implicit,PASS,0.08`.

**Files:**
- `docs/test-port/postgres-oracle-target-inventory.csv:180` — `failed,no` →
  `pass,yes`, rationale carries the full evidence.
- **5** derived docs regenerated by `make regen-testport`: four under
  `docs/test-port/` **plus `analysis/postgres-oracle-compatibility-report.md`,
  which lives OUTSIDE `docs/`** and is easy to miss when staging by explicit
  pathspec. Stage all five.
- `.ralph/fix_plan.md` — M0134-0007 ticked; Current Priority banner advanced to
  M0134-0008 and its verify-case note generalised to the two remaining cases.
- `docs/milestones/0134-…-digestion.md` — table row → `pass`; §3 "Outcome so
  far" now records 2-of-4 confirmed stale.

**Findings worth carrying.**
- Two-for-two on the "possible regression, verify" rows, one shared cause: a
  baseline capture that disagreed with the inventory's own status. **Do not
  flip the last two on inference** — `mvcc` and `reindex_catalog` exercise far
  more engine surface than these two cheap `select_*` cases, so a genuine
  divergence there is materially likelier than the 2-for-2 start suggests.
- `TestPort_RegressSuite` auto-discovers cases; flipping a CSV row needs no
  test-list edit.
- No deferral row: nothing PG implements in this case is missing from goopg.

**Next step:** select **M0134-0008 (`select_parallel.sql`)** — a `not-tried`
case, NOT a verify case, so it needs a real run + likely an implementation
slice. Start by running `scripts/pg-regress-runner.sh --verbose select_parallel`
to size the divergence before designing.

**Gates run:** `scripts/pg-regress-runner.sh --verbose select_implicit` PASS;
`make check-testport-inventory` PASS; `make ralph-state-guard` PASS; pre-commit
pgbench smoke via the hook.

**Delegation:** `tmp/ralph-handoffs/M0134-0007a/` (tester, 1 round, DONE).
**In-flight:** none.
