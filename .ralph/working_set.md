Task: M0145-0005 — single-pass DP over the jointree (fix_plan). Slice 1 landed this loop.
Files: internal/optimizer/joinsearchseam.go (knob arm skips splitOuterSpine in tryPGShapedJoinSearch), internal/optimizer/joinsearch_m0145_test.go (3 pins), docs/design/0100-0149/m0145-0005-single-pass-dp-over-jointree.md (slice table = resume map), docs/design/README.md, .ralph/fix_plan.md (nested notes), .ralph/deferral_ledger.md.
Key symbols: tryPGShapedJoinSearch (seam entry), splitOuterSpine/innerPrefixBelowOuterSpine (retired on arm), joinPinned (pins FULL only since C-04a/b), pinnedUnsearchable, joinIsLegal/makeRelFromJoinlist/searchOneProblem (substrate, complete), extractSearchLeaves (next retirement target), runJoinSearchBelowPinned Phase A/B, integratePulledSublinks/semiAntiChainLink.
Hypothesis/Findings: DP substrate is COMPLETE — task is construction-path retirement, not capability. Certified non-empty spine was unreachable (only FULL pins remain; spineLinkSearchable never certifies FULL) — peel/splice were dead code on reachable inputs; slice 1 is plan-identical by construction (evidence: 99/99 identical plan shapes in SF0.25 sweep).
Next step: slice per doc table — (a) semi/anti as real leaf items (integratePulledSublinks already makes real entries; retire synthetic-leaf splice + remapWalkOrderFlatToSpans + admitSemiAnti), or (b) IR-direct leaf materialisation to retire extractSearchLeaves node-walk; Phase A/B + one-rel floor also open. Pick by smallest exercised corpus delta.
Gates run: units PASS; tpch-spotcheck PASS (Q12=2/Q13=33); tpcds-sf025 sweep PASS 96/96, plan shapes identical 99/99; tpch-acceptance-arm PASS 24/24 under GOOPG_JOINTREE_PIPELINE=1+PGSHAPED=1. All three stamps fresh vs staged index.
In-flight: none — committing now; if the commit hook rejects on stamp provenance, re-stage identical content (code_tree is content-addressed) and retry.
